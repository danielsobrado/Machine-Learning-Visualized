export const P1_GRPO_REASONING_SCENARIOS_BY_LESSON = Object.freeze({
  'grpo-reasoning': Object.freeze([
    Object.freeze({
      id: 'grpo-normalized-advantage-worked',
      level: 'calculation',
      relatedComparison: 'mean-centered-vs-standardized-group-advantage',
      scenario: 'Four completions for the same prompt receive rewards [0, 1, 1, 2]. For this exercise use A_i = (r_i - mean(r)) / std(r), where std is the population standard deviation and no epsilon term is needed.',
      prompt: 'What normalized advantage does the reward-2 completion receive?',
      choices: Object.freeze([
        'About 1.414 because mean=1, std=sqrt(0.5)≈0.707, so (2-1)/0.707≈1.414',
        '1.0 because standardization changes only the group mean, not the reward spread',
        '2.0 because the best completion always keeps its raw reward as its advantage',
      ]),
      answerIndex: 0,
      explanation: 'The group mean is 1. Squared deviations are 1, 0, 0, and 1, so population variance is 0.5 and standard deviation is about 0.707. Dividing the centered reward 1 by 0.707 gives about 1.414.',
      misconceptionTested: 'GRPO-style normalization is equivalent to mean centering only, regardless of group reward spread.',
    }),
    Object.freeze({
      id: 'grpo-same-prompt-grouping-bug',
      level: 'diagnosis',
      relatedComparison: 'within-prompt-relative-baseline-vs-cross-prompt-mixing',
      scenario: 'A batching optimization accidentally forms one GRPO group from completions belonging to four different prompts. Their raw rewards are then centered against one shared group mean.',
      prompt: 'Why is this grouping wrong for the intended group-relative signal?',
      choices: Object.freeze([
        'The baseline should compare sibling completions for the same prompt; mixing prompts confounds prompt difficulty with response quality',
        'GRPO requires every completion in the entire training batch to have identical token length',
        'Different prompts are valid as long as the average reward of the mixed group is positive',
      ]),
      answerIndex: 0,
      explanation: 'Group-relative advantage is intended to rank alternatives conditioned on the same task. If prompt difficulty differs, a shared mean can reward an easy-prompt answer and punish a hard-prompt answer for reasons unrelated to which completion is better for its prompt.',
      misconceptionTested: 'Any responses in the same optimizer batch can share one GRPO group baseline even when they answer different prompts.',
    }),
    Object.freeze({
      id: 'grpo-zero-variance-group-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'reward-contrast-vs-zero-variance-group',
      scenario: 'For one prompt, all eight sampled completions receive reward 1.0. A naive implementation computes (r_i - mean) / std without handling std=0 and produces NaNs.',
      prompt: 'What is the correct conceptual treatment of this group?',
      choices: Object.freeze([
        'It contains no within-group reward contrast, so advantages should be neutral/zero or the group should be filtered rather than divided by zero',
        'Every completion should get the maximum positive advantage because all are tied for best',
        'The group should be assigned random advantage signs to keep the optimizer moving',
      ]),
      answerIndex: 0,
      explanation: 'Identical rewards provide no information about which sibling is better. Production implementations need an epsilon/guard or filtering policy, but the learning signal itself should not invent preference where no reward contrast exists.',
      misconceptionTested: 'A zero-variance reward group still contains a strong relative preference signal.',
    }),
    Object.freeze({
      id: 'grpo-all-wrong-relative-winner-risk',
      level: 'diagnosis',
      relatedComparison: 'relative-winner-vs-actually-correct-sample',
      scenario: 'Six completions are all mathematically wrong, but a noisy judge scores them [0.10, 0.12, 0.09, 0.18, 0.11, 0.10]. The reward-0.18 answer therefore receives the strongest positive relative advantage.',
      prompt: 'What is the main risk if this pattern dominates training?',
      choices: Object.freeze([
        'GRPO can reinforce the least-bad or judge-favored wrong trace because relative advantage does not guarantee any sample is actually correct',
        'The highest-reward completion becomes correct automatically once its advantage is positive',
        'All-wrong groups always collapse to zero variance, so no update can occur',
      ]),
      answerIndex: 0,
      explanation: 'Relative ranking identifies a winner inside the sampled set; it does not certify that the winner satisfies the real task. Very hard prompts or weak verifiers can therefore push the policy toward a wrong trace that merely scores slightly better.',
      misconceptionTested: 'Positive group-relative advantage is equivalent to absolute correctness.',
    }),
    Object.freeze({
      id: 'grpo-prompt-difficulty-contrast-choice',
      level: 'decision',
      relatedComparison: 'all-correct-all-wrong-vs-frontier-mixed-groups',
      scenario: 'Three prompt buckets produce these group outcomes: Easy: 96% of groups are all-correct. Frontier: 68% contain both correct and incorrect completions. Extreme: 91% are all-wrong. Sampling budget is limited and the main goal is to improve correctness.',
      prompt: 'Which bucket should receive more sampling budget first, assuming reward quality is comparable?',
      choices: Object.freeze([
        'Frontier, because mixed-quality groups most often provide reliable within-prompt correctness contrast',
        'Easy, because all-correct groups always create the largest standardized advantages',
        'Extreme, because an all-wrong group guarantees the strongest positive learning signal',
      ]),
      answerIndex: 0,
      explanation: 'For correctness-focused GRPO, mixed groups expose both successful and failed traces for the same prompt. Easy groups often have no correctness contrast, while extreme groups risk reinforcing relative winners that are still wrong.',
      misconceptionTested: 'The hardest prompts or easiest prompts necessarily provide the strongest GRPO learning signal.',
    }),
    Object.freeze({
      id: 'grpo-format-reward-guardrail-failure',
      level: 'diagnosis',
      relatedComparison: 'reward-quality-vs-update-size-guardrails',
      scenario: 'A GRPO run gives a large reward for emitting required reasoning tags and a smaller correctness reward. Tagged but wrong answers gain probability. The run already uses ratio clipping and a KL penalty to a reference model.',
      prompt: 'What is the first issue that must be fixed?',
      choices: Object.freeze([
        'Repair the reward so correctness dominates the exploitable format proxy; clipping and KL limit update size/drift but do not make a bad reward aligned',
        'Remove correctness scoring because clipping and KL are sufficient to guarantee reasoning quality',
        'Increase the format reward further so the policy reaches the clipped ratio boundary faster',
      ]),
      answerIndex: 0,
      explanation: 'Clipping and KL are optimization guardrails, not reward validators. If the easiest way to score is formatting a wrong answer, bounded updates can still consistently move the policy in the wrong direction.',
      misconceptionTested: 'KL and clipping can compensate for a misspecified or gameable reward function.',
    }),
  ]),
});
