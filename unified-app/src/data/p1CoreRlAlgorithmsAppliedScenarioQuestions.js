export const P1_CORE_RL_ALGORITHMS_APPLIED_SCENARIOS_BY_LESSON = Object.freeze({
  'markov-chains': [
    {
      id: 'rlcore-markov-two-step-worked',
      level: 'calculation',
      relatedComparison: 'one-step-transition-vs-multi-step-state-probability',
      scenario: 'A two-state Markov chain has states Healthy (H) and Alert (A). From H, the next-state probabilities are P(H)=0.70 and P(A)=0.30. From A, they are P(H)=0.40 and P(A)=0.60. The process starts in H, and no actions influence the transition matrix.',
      prompt: 'What is the probability that the chain is in state A after exactly two transitions?',
      choices: [
        '0.39 because the two paths ending in A have probability 0.70*0.30 + 0.30*0.60',
        '0.18 because only the H→A→A path should be counted',
        '0.30 because a Markov chain always reuses the one-step probability from the starting state',
      ],
      answerIndex: 0,
      explanation: 'After two transitions, A can be reached by H→H→A with probability 0.70*0.30=0.21 or H→A→A with probability 0.30*0.60=0.18. Adding mutually exclusive paths gives 0.39. The Markov property says the next transition depends on the current state, not that multi-step probabilities equal one-step probabilities.',
      misconceptionTested: 'Multi-step state probabilities can be read directly from one row of the transition matrix without summing all paths through intermediate states.',
    },
  ],
  'mdp-formalism': [
    {
      id: 'rlcore-mdp-expected-return-worked',
      level: 'calculation',
      relatedComparison: 'deterministic-reward-vs-transition-weighted-bellman-return',
      scenario: 'In an MDP, action inspect leads to a safe state with probability 0.80 and immediate reward +5, or a failure state with probability 0.20 and immediate reward -10. The discount factor is gamma=0.90. The current value estimates are V(safe)=4 and V(failure)=0.',
      prompt: 'Using a one-step Bellman expectation backup, what expected return should be assigned to taking inspect now?',
      choices: [
        '4.88 because 0.80*(5+0.90*4) + 0.20*(-10+0.90*0) = 4.88',
        '8.60 because only the most likely safe transition should determine the action value',
        '-5.00 because rewards should be averaged before transition probabilities and future values are applied',
      ],
      answerIndex: 0,
      explanation: 'Each transition branch contributes its probability times immediate reward plus discounted next-state value. The safe branch contributes 0.80*8.6=6.88 and the failure branch contributes 0.20*(-10)=-2, producing 4.88. MDP action values are expectations over stochastic outcomes rather than values of only the modal transition.',
      misconceptionTested: 'A stochastic MDP can be evaluated by following only the most probable next state instead of probability-weighting all transition outcomes.',
    },
  ],
  'value-iteration': [
    {
      id: 'rlcore-value-bellman-worked',
      level: 'calculation',
      relatedComparison: 'bellman-optimality-max-vs-immediate-reward-greediness',
      scenario: 'During one value-iteration sweep, state s has two deterministic actions. Action A gives reward 1 then moves to a state whose current value is 5. Action B gives reward 3 then moves to a state whose current value is 2. The discount factor is gamma=0.90.',
      prompt: 'What new value V(s) should the Bellman optimality backup write for this sweep?',
      choices: [
        '5.5 because max(1+0.90*5, 3+0.90*2) = max(5.5, 4.8)',
        '4.8 because action B has the larger immediate reward and value iteration is reward-greedy',
        '10.3 because value iteration adds both action returns instead of selecting the maximum',
      ],
      answerIndex: 0,
      explanation: 'Value iteration computes the return for every action using the current value function, then takes the maximum. Action A has lower immediate reward but better continuation value, giving 5.5 versus 4.8 for B. The backup is therefore 5.5, illustrating why planning cannot be reduced to immediate-reward greediness.',
      misconceptionTested: 'Value iteration chooses whichever action has the largest immediate reward instead of maximizing reward plus discounted continuation value.',
    },
  ],
  'policy-iteration': [
    {
      id: 'rlcore-policy-improvement-worked',
      level: 'decision',
      relatedComparison: 'policy-evaluation-values-vs-greedy-policy-improvement',
      scenario: 'A policy-evaluation phase has produced V_pi(s)=5 and V_pi(t)=4. In state s, the current policy chooses action A, which gives reward 1 and returns to s. Alternative action B gives reward 3 and moves to t. Use gamma=0.90 and perform one greedy policy-improvement comparison while holding V_pi fixed.',
      prompt: 'Should the improved policy keep action A or switch to B in state s?',
      choices: [
        'Switch to B because Q_pi(A)=1+0.90*5=5.5 while Q_pi(B)=3+0.90*4=6.6',
        'Keep A because policy iteration is not allowed to change an action until V_pi becomes zero',
        'Keep A because the current policy action automatically has the highest Q-value after policy evaluation',
      ],
      answerIndex: 0,
      explanation: 'Policy evaluation estimates V under the current policy; policy improvement then acts greedily with respect to that evaluated value function. Using the fixed V_pi, B has action value 6.6 versus 5.5 for A, so the policy should switch. Evaluation does not guarantee that the action currently in the policy remains greedy afterward.',
      misconceptionTested: 'Policy evaluation makes the current action self-confirmingly optimal, so the improvement phase should never replace it using one-step lookahead.',
    },
  ],
  'policy-gradients': [
    {
      id: 'rlcore-reinforce-weight-worked',
      level: 'calculation',
      relatedComparison: 'log-policy-gradient-vs-return-weighted-policy-gradient',
      scenario: 'For one sampled action, a policy network has gradient of the log action probability ∇log pi(a|s)=[0.4,-0.4]. The Monte Carlo advantage used by a simplified REINFORCE update is +3. Ignore learning-rate scaling and all other samples in the batch.',
      prompt: 'What gradient contribution does this sample make to the policy-gradient ascent objective?',
      choices: [
        '[1.2,-1.2] because the score-function gradient is multiplied by the advantage +3',
        '[0.4,-0.4] because returns affect only the critic and never scale a policy gradient',
        '[-1.2,1.2] because positive advantage must always reverse the log-probability gradient direction',
      ],
      answerIndex: 0,
      explanation: 'The basic policy-gradient estimator weights ∇log pi(a|s) by return or advantage. Multiplying [0.4,-0.4] by +3 gives [1.2,-1.2]. A positive advantage increases the probability of the sampled action under gradient ascent; negative advantage would reverse that pressure.',
      misconceptionTested: 'The return or advantage is merely reported as a metric and does not scale the sampled action log-probability gradient in REINFORCE-style updates.',
    },
  ],
  'actor-critic': [
    {
      id: 'rlcore-actor-critic-td-worked',
      level: 'calculation',
      relatedComparison: 'monte-carlo-return-vs-one-step-td-advantage',
      scenario: 'An actor-critic agent observes transition s→s_prime with reward 2. The critic predicts V(s)=3 and V(s_prime)=4, and gamma=0.90. For this exercise, use the one-step TD error delta = r + gamma*V(s_prime) - V(s) as the actor advantage estimate.',
      prompt: 'What TD error should train the critic and weight the actor update on this transition?',
      choices: [
        '2.6 because 2 + 0.90*4 - 3 = 2.6',
        '3.0 because the actor advantage is always identical to the current state-value estimate',
        '5.6 because the baseline V(s) should be added rather than subtracted',
      ],
      answerIndex: 0,
      explanation: 'The bootstrapped target is 2 + 0.90*4 = 5.6. Subtracting the critic baseline V(s)=3 gives TD error 2.6. The critic can reduce this prediction error, while the actor uses its sign and magnitude as evidence that the sampled action performed better than the baseline expectation.',
      misconceptionTested: 'Actor-critic methods use the raw state value or raw reward directly as the policy advantage instead of a baseline-corrected temporal-difference signal.',
    },
  ],
  'ppo-clipped-policy-gradient': [
    {
      id: 'rlcore-ppo-clipping-worked',
      level: 'calculation',
      relatedComparison: 'unclipped-probability-ratio-vs-clipped-ppo-surrogate',
      scenario: 'A PPO minibatch sample had old-policy probability 0.20 and new-policy probability 0.26, so its probability ratio is 1.30. The estimated advantage is +2 and epsilon=0.20. Use the PPO clipped surrogate min(r*A, clip(r,0.8,1.2)*A) for this positive-advantage sample.',
      prompt: 'What surrogate contribution is used for this sample before averaging the minibatch?',
      choices: [
        '2.4 because min(1.30*2, 1.20*2) = min(2.6,2.4)',
        '2.6 because clipping is only applied when the advantage is negative',
        '1.2 because PPO discards the advantage magnitude after clipping the probability ratio',
      ],
      answerIndex: 0,
      explanation: 'The new policy increased the sampled action probability by 30%, beyond the allowed 20% clipping interval. With positive advantage, PPO caps the beneficial ratio at 1.2 in the surrogate, producing 2.4 instead of 2.6. The advantage remains part of the objective; clipping limits the policy-ratio incentive.',
      misconceptionTested: 'PPO either ignores clipping for positive advantages or clips away the advantage itself rather than bounding the policy probability ratio used in the surrogate.',
    },
  ],
  'reward-shaping': [
    {
      id: 'rlcore-potential-shaping-worked',
      level: 'calculation',
      relatedComparison: 'potential-based-shaping-vs-arbitrary-behavior-bonus',
      scenario: 'A transition has environment reward r=1, discount gamma=0.90, potential Phi(s)=2, and Phi(s_prime)=5. A designer uses potential-based shaping F(s,s_prime)=gamma*Phi(s_prime)-Phi(s), then adds F to the environment reward.',
      prompt: 'What shaped reward is observed on this transition, and why is this form safer than an arbitrary progress bonus?',
      choices: [
        '3.5 because F=0.90*5-2=2.5 and r+F=3.5; potential-based shaping preserves optimal-policy structure under its standard assumptions',
        '6.5 because both state potentials should simply be added to the original reward',
        '1.0 because any shaping term necessarily changes the optimal policy and therefore must be ignored during learning',
      ],
      answerIndex: 0,
      explanation: 'The shaping term is 4.5-2=2.5, so the learner sees reward 3.5. Potential-based shaping is special because the added terms telescope across trajectories in a way that preserves optimal policies under the standard discounted-MDP conditions. Arbitrary bonuses do not have that guarantee and can create reward-hacking shortcuts.',
      misconceptionTested: 'All dense reward bonuses are equivalent and can be added freely without changing which long-run policy the MDP makes optimal.',
    },
  ],
  pagerank: [
    {
      id: 'rlcore-pagerank-iteration-worked',
      level: 'calculation',
      relatedComparison: 'raw-incoming-link-count-vs-damped-rank-propagation',
      scenario: 'A three-page graph has A linking equally to B and C, B linking only to C, and C linking only to A. Current PageRank values are all 1/3. With damping d=0.85 and N=3, use PR_new(x)=(1-d)/N + d*sum(PR(source)/outdegree(source)).',
      prompt: 'What is the next PageRank value for page C after one synchronous iteration?',
      choices: [
        '0.475 because 0.05 + 0.85*((1/3)/2 + (1/3)/1) = 0.475',
        '0.333 because synchronous PageRank can never move away from a uniform initialization',
        '0.900 because C has two incoming links and PageRank is just normalized incoming-link count',
      ],
      answerIndex: 0,
      explanation: 'C receives half of A’s current rank and all of B’s current rank: 1/6 + 1/3 = 1/2. Multiplying by 0.85 gives 0.425 and adding teleport mass 0.05 gives 0.475. PageRank propagates rank through normalized outgoing links and damping; it is not simply indegree counting.',
      misconceptionTested: 'PageRank is equivalent to counting incoming edges or remains uniform forever when initialized uniformly, regardless of asymmetric link structure.',
    },
  ],
});
