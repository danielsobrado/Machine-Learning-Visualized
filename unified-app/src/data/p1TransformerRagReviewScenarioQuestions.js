function q(value) { return Object.freeze({ ...value, choices: Object.freeze([...value.choices]) }); }
function lesson(items) { return Object.freeze(items.map(q)); }

export const P1_TRANSFORMER_RAG_REVIEW_SCENARIOS_BY_LESSON = Object.freeze({
  'attention-masks': lesson([{
    id: 'review-mask-before-softmax-infinity', level: 'mechanism',
    relatedComparison: 'logit-masking-before-softmax-vs-post-softmax-zeroing',
    scenario: 'A causal attention implementation must prevent a query from assigning probability mass to future keys. The code can either set forbidden logits to a very large negative value before softmax or compute softmax first and then zero forbidden probabilities without renormalizing.',
    prompt: 'Why is masking the logits before softmax the standard correct construction?',
    choices: ['Forbidden positions receive effectively zero exponentiated mass while allowed positions are normalized to sum to one', 'Post-softmax zeroing is always identical because normalization is unaffected by removed probability mass', 'Masks must be applied to values rather than attention logits to control visibility'],
    answerIndex: 0,
    explanation: 'Adding negative infinity, or a sufficiently negative finite sentinel, to forbidden logits makes their softmax contribution zero and lets the remaining allowed logits renormalize correctly. Zeroing probabilities afterward without renormalization changes the sum and therefore changes the weighted-value scale.',
    misconceptionTested: 'Masking after softmax is automatically equivalent to masking logits before softmax even when the remaining probabilities are not renormalized.',
  }]),
  'self-attention': lesson([{
    id: 'review-self-attention-qkv-roles', level: 'mechanism',
    relatedComparison: 'query-key-routing-vs-value-content',
    scenario: 'A self-attention head computes scores from QK^T and then uses the normalized scores to combine V. An engineer says keys carry the final content while values are used only to decide which token matches the query.',
    prompt: 'Which role assignment is correct?',
    choices: ['Queries and keys determine routing scores, while values provide the content that is mixed according to those scores', 'Values determine routing and keys are discarded after projection', 'Queries alone determine both routing and output content'],
    answerIndex: 0,
    explanation: 'The query-key interaction produces compatibility logits that become attention weights. Those weights are then applied to the value vectors to form the output. Keys influence where attention goes; values determine what information is retrieved from those positions.',
    misconceptionTested: 'Keys and values play interchangeable roles in dot-product attention.',
  }]),
  'flash-attention': lesson([{
    id: 'review-flash-attention-exactness', level: 'diagnosis',
    relatedComparison: 'exact-tiled-attention-vs-approximate-sparse-attention',
    scenario: 'A team considers FlashAttention for a model whose quality is sensitive to attention approximation. They assume the speedup comes from dropping low-weight attention entries and therefore expect different mathematical outputs from standard dense attention.',
    prompt: 'What should they understand about the core FlashAttention idea?',
    choices: ['It reorganizes exact attention computation to reduce high-bandwidth-memory traffic, using tiling and online softmax rather than approximating away attention entries', 'It is a learned sparse-attention pattern that permanently removes most query-key pairs', 'It speeds inference only by reducing the number of model layers'],
    answerIndex: 0,
    explanation: 'FlashAttention is primarily an IO-aware algorithm for computing the same dense attention operation while avoiding materializing the full score/probability matrices in slow memory. Floating-point ordering can cause tiny numerical differences, but the method is not conceptually a sparsification approximation.',
    misconceptionTested: 'FlashAttention gets its efficiency by changing dense attention into an approximate sparse attention rule.',
  }]),
  'kv-cache': lesson([{
    id: 'review-kv-cache-prefill-decode-memory', level: 'comparison',
    relatedComparison: 'prefill-parallel-context-vs-token-by-token-decode-cache-growth',
    scenario: 'A decoder processes a 4,000-token prompt once, then generates 500 new tokens. During generation it reuses previously computed key/value tensors for all earlier positions instead of recomputing them each step.',
    prompt: 'What tradeoff does the KV cache create between prefill/decode compute and memory?',
    choices: ['It avoids repeated K/V computation for prior tokens during decode, but cache memory grows with stored context, layers, heads, and generated tokens', 'It removes the need to run attention during decode and therefore uses no additional memory', 'It helps only during prefill and is discarded before autoregressive generation'],
    answerIndex: 0,
    explanation: 'Prompt prefill can compute many positions in parallel. Decode then processes one or a few new tokens while attending to cached past K/V tensors. Reuse saves repeated projection work and is essential for efficient generation, but the stored tensors become a major memory bottleneck at long context or large batch sizes.',
    misconceptionTested: 'KV caching is a free compute optimization with no memory growth, or it is useful only during prompt prefill.',
  }]),
  'multi-head-latent-attention': lesson([{
    id: 'review-mla-latent-cache-bottleneck', level: 'mechanism',
    relatedComparison: 'full-per-head-kv-cache-vs-shared-latent-bottleneck',
    scenario: 'A standard attention layer stores large per-head K/V tensors for every past token. An MLA-style design instead caches a much smaller learned latent representation and derives the head-specific behavior from that compressed state.',
    prompt: 'What systems bottleneck is this architecture primarily trying to reduce?',
    choices: ['Decode-time KV-cache memory and bandwidth while retaining expressive head-specific attention behavior', 'Training-set label noise by averaging experts', 'Tokenizer vocabulary size by sharing subword merges'],
    answerIndex: 0,
    explanation: 'Long-context autoregressive decoding can become constrained by reading and storing K/V state rather than by raw arithmetic. Compressing cached information into a lower-dimensional latent reduces memory footprint and bandwidth pressure. The architectural challenge is preserving enough information to reconstruct useful attention behavior.',
    misconceptionTested: 'Latent KV compression is mainly a tokenizer or dataset-compression technique rather than an attention-state systems optimization.',
  }]),
  'native-sparse-attention': lesson([{
    id: 'review-sparse-attention-connectivity-risk', level: 'design',
    relatedComparison: 'dense-global-connectivity-vs-sparse-compute-budget',
    scenario: 'A long-context model reduces attention cost by letting each token read a local window plus selected global blocks. A task sometimes requires connecting facts separated by hundreds of thousands of tokens.',
    prompt: 'What must the sparse pattern preserve for the efficiency gain not to destroy task capability?',
    choices: ['A reliable path to relevant long-range information, through global summaries, selected blocks, or other connectivity that does not depend only on local windows', 'Every token must still attend densely to every other token, otherwise the method is not sparse', 'Only adjacent-token connectivity matters because deep layers cannot propagate long-range information'],
    answerIndex: 0,
    explanation: 'Sparse attention saves work by omitting many pairwise interactions, so its quality depends on whether important dependencies remain reachable. Local windows alone can make distant facts hard to connect. Practical sparse designs add selected blocks, global representations, or learned routing to preserve critical long-range access.',
    misconceptionTested: 'Any sparse pattern gives the same capability as dense attention as long as it reduces the number of score calculations.',
  }]),
  'llm-training-objectives': lesson([{
    id: 'review-objective-causal-vs-masked', level: 'comparison',
    relatedComparison: 'left-to-right-causal-prediction-vs-bidirectional-masked-reconstruction',
    scenario: 'Two models see the same sentence during pretraining. Model A predicts each next token using only tokens to its left. Model B masks selected tokens and predicts them using visible context on both sides.',
    prompt: 'What architectural or usage distinction is most directly encouraged by these objectives?',
    choices: ['Causal LM training naturally matches left-to-right generation, while masked reconstruction naturally supports bidirectional representation learning', 'Both objectives impose the same visibility mask and therefore train identical conditional distributions', 'Masked reconstruction prevents the model from using right context'],
    answerIndex: 0,
    explanation: 'A causal objective factorizes sequence probability left to right and aligns directly with autoregressive decoding. Masked language modeling exposes surrounding context on both sides of selected positions and is naturally suited to bidirectional encoders. Modern systems can adapt these patterns, but the training signals and visibility constraints are not identical.',
    misconceptionTested: 'Causal language modeling and masked language modeling differ only in which token IDs are hidden, not in conditional information structure.',
  }]),
  'sampling-strategies': lesson([{
    id: 'review-sampling-temperature-top-p-interaction', level: 'decision',
    relatedComparison: 'temperature-logit-reshaping-vs-nucleus-candidate-truncation',
    scenario: 'A generator first divides logits by temperature and then applies top-p sampling. The team raises temperature while keeping p fixed and expects the nucleus candidate set to remain identical.',
    prompt: 'Why can the candidate set itself change even though p did not?',
    choices: ['Temperature changes the probability distribution before cumulative mass is computed, so reaching the same p threshold may require a different number of tokens', 'Top-p is applied to raw token IDs and is independent of probabilities', 'Temperature affects only the selected token after the top-p set is fixed'],
    answerIndex: 0,
    explanation: 'Temperature reshapes relative probabilities. A flatter distribution often spreads cumulative mass across more tokens, while a sharper distribution can concentrate it in fewer. Since nucleus sampling defines its candidate set from cumulative probability after reshaping, the two controls interact rather than acting independently.',
    misconceptionTested: 'Temperature and top-p operate on completely separate stages and therefore cannot change each other\'s effective candidate set.',
  }]),
  'long-context-frontier-models': lesson([{
    id: 'review-long-context-capacity-vs-retrieval-quality', level: 'diagnosis',
    relatedComparison: 'maximum-context-window-vs-effective-information-use',
    scenario: 'A model accepts a one-million-token prompt, but accuracy drops when the single relevant paragraph is placed in the middle among large amounts of distractor text. Moving the same paragraph near the end restores performance.',
    prompt: 'What does this result demonstrate about advertised context length?',
    choices: ['Input capacity does not guarantee uniform effective use of every position; long-context retrieval and lost-in-the-middle behavior must be evaluated separately', 'The model cannot technically accept one million tokens because any error proves the context window is fake', 'Context position can never affect a Transformer once the sequence fits in memory'],
    answerIndex: 0,
    explanation: 'Maximum context length describes what the system can process, not a guarantee that all positions contribute equally to every task. Attention patterns, training distribution, distractors, and retrieval difficulty can create positional degradation. Long-context models therefore need task-specific retrieval and utilization evaluations.',
    misconceptionTested: 'A supported context-window size is equivalent to reliable reasoning over every token anywhere in that window.',
  }]),
  'efficient-llm-serving': lesson([{
    id: 'review-serving-continuous-batching', level: 'design',
    relatedComparison: 'static-request-batching-vs-continuous-token-level-batching',
    scenario: 'Requests arrive continuously and have very different generation lengths. Static batches wait for the slowest request to finish before all batch slots can be reused, leaving GPU capacity idle as shorter requests complete.',
    prompt: 'What serving technique directly addresses this utilization problem?',
    choices: ['Continuous batching, which admits new work as sequence slots become available instead of waiting for an entire static batch to finish', 'Disable KV caching so every request has equal compute cost', 'Force every response to generate the same number of tokens regardless of stopping conditions'],
    answerIndex: 0,
    explanation: 'Autoregressive workloads are ragged: sequences enter and leave at different times. Continuous or iteration-level batching repacks active requests over decode steps so freed capacity can serve newly arrived work. This improves throughput and utilization while still requiring careful latency and memory scheduling.',
    misconceptionTested: 'Static batching is sufficient for variable-length autoregressive workloads because completed sequences cannot be replaced until the whole batch ends.',
  }]),
  'efficient-inference-compression-track': lesson([{
    id: 'review-compression-method-boundaries', level: 'comparison',
    relatedComparison: 'quantization-vs-pruning-vs-distillation',
    scenario: 'A deployment team wants to reduce model cost and considers three changes: storing weights at lower numerical precision, removing selected weights/structures, and training a smaller student to imitate a larger teacher.',
    prompt: 'Which mapping correctly names the three techniques?',
    choices: ['Quantization lowers precision, pruning removes parameters or structures, and distillation transfers behavior into a smaller learned model', 'Pruning lowers precision, distillation removes weights, and quantization trains a student', 'All three are names for exactly the same compression operation'],
    answerIndex: 0,
    explanation: 'These methods can be combined but target different mechanisms. Quantization changes numeric representation, pruning changes network sparsity or structure, and knowledge distillation changes the training process so a smaller model approximates a teacher. Their hardware, quality, and retraining tradeoffs therefore differ.',
    misconceptionTested: 'Model-compression methods are interchangeable labels with identical mechanisms and deployment consequences.',
  }]),
  'eagle-3-1-speculative-decoding': lesson([{
    id: 'review-speculative-decoding-target-correctness', level: 'mechanism',
    relatedComparison: 'draft-proposal-speedup-vs-target-model-distribution',
    scenario: 'A speculative decoder uses a cheaper draft model to propose several tokens, then the target model verifies those proposals with an acceptance rule designed to preserve the target distribution.',
    prompt: 'Why can the method accelerate generation without simply replacing the target model with an approximation?',
    choices: ['The draft proposes candidates, but verification and acceptance/rejection are constructed so the final sampling distribution remains that of the target model', 'Every draft token is accepted unconditionally, so quality depends only on the draft model', 'The target model is used only for the first token and never checks later proposals'],
    answerIndex: 0,
    explanation: 'Speculative decoding gains speed when the target can validate multiple draft proposals in fewer expensive sequential steps. Correct algorithms compensate for mismatches through acceptance and residual sampling so the target distribution is preserved. A poor drafter reduces speedup because more proposals are rejected, but it need not redefine the target distribution.',
    misconceptionTested: 'Speculative decoding is necessarily an approximate generation method whose outputs follow the draft model whenever multiple tokens are proposed.',
  }]),
  'rag-retrieval-evaluation': lesson([{
    id: 'review-rag-good-retrieval-bad-answer', level: 'diagnosis',
    relatedComparison: 'retrieval-recall-vs-generation-grounding-quality',
    scenario: 'An evaluation shows recall@10 of 96%: the required evidence is almost always in the candidate set. Yet final answer accuracy is only 58%, and manual review shows the generator frequently ignores the relevant passage or combines it with unrelated context.',
    prompt: 'Which subsystem should be investigated next rather than treating retrieval recall as the only bottleneck?',
    choices: ['Context selection, grounding, and generation behavior, because high candidate recall does not guarantee the generator uses the evidence correctly', 'Only the ANN index, because answer errors always imply failed retrieval', 'Tokenizer vocabulary size, because retrieval recall already proves generation quality'],
    answerIndex: 0,
    explanation: 'Retrieval recall establishes that the evidence is available somewhere in the candidate set. It does not prove reranking selected it, context packing preserved it, or the generator used it faithfully. Once candidate recall is high, downstream evidence selection and generation diagnostics become the natural next step.',
    misconceptionTested: 'High retrieval recall is sufficient to guarantee high end-to-end RAG answer accuracy.',
  }]),
  'rag-failure-modes': lesson([{
    id: 'review-rag-grounded-but-stale-conflict', level: 'diagnosis',
    relatedComparison: 'citation-grounding-vs-source-freshness-and-authority',
    scenario: 'A RAG answer cites a retrieved policy document perfectly, but that document was superseded six months ago by a newer authoritative policy with the opposite rule. The old document remains highly ranked because many historical pages link to it.',
    prompt: 'Why is citation grounding alone insufficient to certify the answer as correct?',
    choices: ['Grounding checks whether the claim follows from retrieved evidence, but the evidence can still be stale, superseded, or lower-authority than conflicting sources', 'A cited answer is correct by definition even if the source is outdated', 'Retrieval systems cannot compare source dates or authority, so freshness should never be evaluated'],
    answerIndex: 0,
    explanation: 'RAG quality has multiple layers: evidence must be relevant, authoritative, current enough for the task, and then faithfully used. Entailment against stale evidence can be perfect while the real-world answer is wrong. Source versioning, freshness metadata, authority rules, and conflict handling are therefore separate from grounding.',
    misconceptionTested: 'If every generated claim is entailed by a cited retrieved passage, the answer is necessarily factually current and authoritative.',
  }]),
});
