export const P1_CLASSIFICATION_METRICS_SCENARIOS_BY_LESSON = Object.freeze({
  'classification-metrics': [
    {
      id: 'metrics-cost-weighted-errors-worked',
      level: 'calculation',
      relatedComparison: 'unweighted-metric-vs-cost-weighted-errors',
      scenario: 'Two classifiers are evaluated on the same population. Model A has FP = 120 and FN = 8. Model B has FP = 40 and FN = 14. Each false positive costs 1 unit, while each false negative costs 20 units.',
      prompt: 'Which model has the lower stated error cost?',
      choices: [
        'Model A: 120 + 8*20 = 280 units, versus Model B at 40 + 14*20 = 320 units',
        'Model B because it has fewer false positives, regardless of the much larger false-negative cost',
        'The models tie because every classification metric assigns equal value to false positives and false negatives',
      ],
      answerIndex: 0,
      explanation: 'The application supplies asymmetric costs, so the relevant comparison is the weighted error cost. Model A costs 120 + 160 = 280 units. Model B costs 40 + 280 = 320 units. A metric such as accuracy or F1 can still be reported, but it does not replace the stated decision cost.',
      misconceptionTested: 'The model with fewer errors of one type is automatically better even when the application assigns very different costs to false positives and false negatives.',
    },
    {
      id: 'metrics-imbalanced-accuracy-worked',
      level: 'calculation',
      relatedComparison: 'raw-accuracy-vs-positive-recall-and-balanced-accuracy',
      scenario: 'A test set contains 10,000 examples, of which 100 are positive and 9,900 are negative. A trivial classifier predicts every example as negative.',
      prompt: 'What do accuracy, positive-class recall, and balanced accuracy reveal?',
      choices: [
        'Accuracy is 99%, positive recall is 0%, and balanced accuracy is 50%, exposing the majority-class failure',
        'Accuracy is 99%, positive recall is 99%, and balanced accuracy is 99% because most predictions are correct',
        'Accuracy is 0%, positive recall is 0%, and balanced accuracy is undefined because the model predicts no positives',
      ],
      answerIndex: 0,
      explanation: 'The classifier gets all 9,900 negatives correct and all 100 positives wrong, so accuracy is 9,900/10,000 = 99%. Positive recall is 0/100 = 0%, negative recall is 100%, and balanced accuracy is (0% + 100%)/2 = 50%. High accuracy here is almost entirely the base rate.',
      misconceptionTested: 'Very high accuracy necessarily means a classifier performs well on a rare but important positive class.',
    },
    {
      id: 'metrics-macro-recall-minority-worked',
      level: 'calculation',
      relatedComparison: 'support-dominated-summary-vs-macro-class-recall',
      scenario: 'A three-class classifier has per-class recall of 95% for a common class, 80% for a medium class, and 0% for a rare class. The common class dominates the dataset.',
      prompt: 'What macro recall should be reported, and what does it surface?',
      choices: [
        'About 58.3%, because (95 + 80 + 0) / 3 = 58.3%; equal class weighting exposes the complete failure on the rare class',
        'About 95%, because the common class should determine macro recall when it has most of the examples',
        'Exactly 0%, because one class with zero recall forces every multiclass average to zero',
      ],
      answerIndex: 0,
      explanation: 'Macro averaging computes the metric per class and gives each class equal weight. Here the mean recall is 175/3 ≈ 58.3%. A support-weighted or micro-style summary may look much stronger when the common class dominates, so both the averaging rule and per-class results matter.',
      misconceptionTested: 'A multiclass aggregate dominated by common classes is enough to prove acceptable performance on rare classes.',
    },
    {
      id: 'metrics-hard-label-vs-calibration-decision',
      level: 'decision',
      relatedComparison: 'confusion-matrix-metrics-vs-probability-quality',
      scenario: 'Two models use threshold 0.5 and produce exactly the same hard labels on a validation set, so their accuracy, precision, recall, F1, and confusion matrix are identical. Model A usually assigns scores near 0.55 or 0.45, while Model B often assigns scores near 0.99 or 0.01. A downstream system will use predicted probabilities to estimate expected financial risk.',
      prompt: 'Can the identical hard-label classification metrics determine which model is better for that downstream use?',
      choices: [
        'No; evaluate probability quality separately with calibration or proper scoring metrics because identical thresholded labels can hide very different probability reliability',
        'Yes; identical confusion matrices prove the models have identical probability quality at every score value',
        'Yes; choose Model B automatically because more extreme probabilities are always better calibrated',
      ],
      answerIndex: 0,
      explanation: 'Hard-label metrics discard score magnitude after thresholding. Two models can make the same class decisions while expressing very different and differently calibrated probabilities. If the application consumes probabilities directly, reliability diagrams, Brier score, log loss, or another appropriate probability-quality measure must be evaluated.',
      misconceptionTested: 'A confusion matrix and its derived metrics fully characterize probability quality whenever two classifiers produce the same labels.',
    },
    {
      id: 'metrics-test-threshold-selection-leakage',
      level: 'diagnosis',
      relatedComparison: 'validation-threshold-selection-vs-final-test-reuse',
      scenario: 'A team evaluates 50 candidate probability thresholds on the final test set, chooses the threshold with the highest F1, and then reports that same test-set F1 as the unbiased production estimate.',
      prompt: 'What is wrong with this evaluation?',
      choices: [
        'The final test set influenced threshold selection, so the reported F1 is selection-biased; choose the threshold on validation data and evaluate the locked policy on untouched test data',
        'Nothing is wrong because threshold tuning does not change model weights and therefore cannot overfit evaluation data',
        'The only issue is that 50 thresholds are too few; testing more thresholds would make the final test estimate unbiased',
      ],
      answerIndex: 0,
      explanation: 'A threshold is part of the prediction policy. Once test labels are used to choose it, the test set participates in model selection even if the underlying model weights stay fixed. The same data cannot then provide an untouched estimate of that selected policy.',
      misconceptionTested: 'Only gradient-based parameter fitting can contaminate a test set; post-training threshold selection is harmless.',
    },
    {
      id: 'metrics-small-slice-uncertainty-decision',
      level: 'decision',
      relatedComparison: 'equal-point-estimates-vs-different-sample-uncertainty',
      scenario: 'Subgroup A recall is 800/1000 = 80%. Subgroup B recall is 4/5 = 80%. A report concludes that recall is equally established for both groups because the point estimates match exactly.',
      prompt: 'What should the report say instead?',
      choices: [
        'The point estimates match, but subgroup B has far greater sampling uncertainty and needs an interval, more data, or an explicit low-support caveat before claiming parity',
        'The two groups are equally precise because recall depends only on the percentage and not on the denominator',
        'Subgroup B is more reliable because fewer examples make the metric less affected by noise',
      ],
      answerIndex: 0,
      explanation: 'A metric is an estimate, not just a percentage. Four successes among five positives contains much less information than 800 among 1,000. Slice-level reporting should include denominators and uncertainty so tiny groups do not look as well established as large ones.',
      misconceptionTested: 'Equal subgroup metric point estimates imply equal evidence and equal precision regardless of sample size.',
    },
  ],
});
