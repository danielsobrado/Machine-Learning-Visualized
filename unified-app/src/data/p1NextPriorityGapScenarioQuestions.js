export const P1_NEXT_PRIORITY_GAP_SCENARIOS_BY_LESSON = Object.freeze({
  'hypothesis-testing-intuition': Object.freeze([
    Object.freeze({
      id: 'hypothesis-equivalence-tost-decision',
      level: 'decision',
      relatedComparison: 'failure-to-reject-difference-vs-positive-equivalence-evidence',
      scenario: 'A product team defines effects between -2 and +2 percentage points as practically equivalent. A properly constructed 90% confidence interval for the treatment difference is [-1.1, +0.8] points, and the pre-specified analysis uses the two one-sided tests (TOST) procedure at alpha = 0.05.',
      prompt: 'What conclusion is justified under the stated equivalence design?',
      choices: Object.freeze([
        'The data support equivalence within the pre-specified +/-2 point margin because the entire 90% interval lies inside that margin',
        'Equivalence cannot ever be concluded from confidence intervals; only failure to reject an ordinary zero-difference test can establish it',
        'The treatments are exactly identical because zero lies inside the interval, so the equivalence margin is unnecessary',
      ]),
      answerIndex: 0,
      explanation: 'Equivalence is a positive claim relative to a pre-specified practical margin. Under the usual TOST confidence-interval correspondence, a 90% interval entirely inside [-2, +2] supports equivalence at alpha = 0.05. Merely failing to reject a conventional difference test would not provide the same evidence, and equivalence within a margin does not mean the true effect is exactly zero.',
      misconceptionTested: 'Failing to find a statistically significant difference is automatically evidence that two treatments are practically equivalent or exactly identical.',
    }),
  ]),
  calibration: Object.freeze([
    Object.freeze({
      id: 'calibration-ece-binning-sensitivity-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'coarse-binned-ece-vs-local-reliability-errors',
      scenario: 'A classifier has one group of predictions around 0.55 that realizes positives 35% of the time and another around 0.75 that realizes positives 95% of the time. A coarse calibration report merges both groups into one large bin whose average predicted probability and observed frequency happen to be close, producing a small ECE.',
      prompt: 'What is the strongest interpretation of the small ECE?',
      choices: Object.freeze([
        'The model is well calibrated everywhere because a low ECE is invariant to bin definitions and cannot hide opposite local errors',
        'The summary can hide substantial local over- and under-confidence through coarse binning or cancellation, so inspect reliability structure and sensitivity to the calibration metric or bins',
        'ECE proves discrimination is poor because calibration error and ranking quality are the same quantity',
      ]),
      answerIndex: 1,
      explanation: 'Expected calibration error is a binned summary, so its value depends on how predictions are partitioned. Opposite local calibration errors can partially cancel after aggregation, especially in coarse bins. A small ECE therefore does not prove every probability region is reliable; reliability diagrams, alternative binning, and complementary proper scoring rules can expose structure the scalar summary misses.',
      misconceptionTested: 'A single low ECE value is a binning-invariant certificate of local calibration quality and cannot conceal offsetting calibration errors within a bin.',
    }),
  ]),
  'cross-validation': Object.freeze([
    Object.freeze({
      id: 'cv-preprocessing-fit-inside-folds-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'global-learned-preprocessing-vs-fold-local-pipeline-fitting',
      scenario: 'Before five-fold cross-validation, a team fits a median imputer, standardizer, and PCA transform once on the full dataset, then performs CV only for the downstream classifier. Labels were not used by those preprocessing steps, but every validation fold influenced the learned medians, means, variances, and PCA directions.',
      prompt: 'How should the evaluation pipeline be corrected?',
      choices: Object.freeze([
        'Keep the global preprocessing because leakage requires direct use of validation labels',
        'Fit preprocessing globally only for PCA, but refit the imputer and scaler inside each validation fold',
        'Fit every learned preprocessing step on each training fold and apply that fitted pipeline to its validation fold, so validation features do not influence training-time transformations',
      ]),
      answerIndex: 2,
      explanation: 'Cross-validation must reproduce the full model-fitting procedure, not only the final estimator fit. Imputation statistics, scaling parameters, and PCA directions are learned from data, so estimating them with validation rows lets held-out feature information influence the trained pipeline. A fold-local pipeline fits all learned transforms using only the training partition before transforming the validation partition.',
      misconceptionTested: 'Unsupervised preprocessing cannot leak validation information because leakage exists only when held-out labels directly participate in fitting the model.',
    }),
  ]),
  'probability-distributions': Object.freeze([
    Object.freeze({
      id: 'probability-zero-inflation-count-model-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'single-poisson-process-vs-structural-zero-mixture',
      scenario: 'A daily incident count has mean about 3, yet roughly 70% of days contain zero incidents. A Poisson model with mean 3 would assign only about exp(-3), or 5%, probability to zero. Domain review suggests many locations are inactive on some days and cannot generate an incident at all.',
      prompt: 'What modeling issue should be investigated first?',
      choices: Object.freeze([
        'The counts are necessarily Gaussian because the empirical zero rate is larger than the mean',
        'A zero-inflated or hurdle-style process may be more appropriate because structural inactive days create far more zeros than a single Poisson process with the same mean predicts',
        'The Poisson model is validated because matching the mean guarantees the full count distribution, including its zero probability, is correct',
      ]),
      answerIndex: 1,
      explanation: 'For a Poisson mean of 3, P(Y=0)=e^-3 is about 0.05, far below the observed 0.70 zero fraction. The inactive-versus-active mechanism also provides a plausible second process that can generate structural zeros. This does not automatically prove one specific zero-inflated model is correct, but it is strong evidence that a single equidispersed Poisson mechanism is inadequate.',
      misconceptionTested: 'A count model that matches the sample mean is automatically distributionally adequate even when the observed zero frequency is radically different from what the model predicts.',
    }),
  ]),
  'classification-metrics': Object.freeze([
    Object.freeze({
      id: 'metrics-prevalence-shift-ppv-worked',
      level: 'calculation',
      relatedComparison: 'stable-sensitivity-specificity-vs-prevalence-dependent-precision',
      scenario: 'A deployed binary classifier keeps sensitivity at 90% and false-positive rate at 5%. In a new population the positive prevalence is only 1%. Consider 10,000 cases: about 100 are positive and 9,900 are negative.',
      prompt: 'Approximately what precision (positive predictive value) should the team expect in this new population?',
      choices: Object.freeze([
        'About 90%, because precision is identical to sensitivity when the classifier threshold is unchanged',
        'About 95%, because one minus the false-positive rate is the positive predictive value',
        'About 15%, because there are about 90 true positives and 495 false positives, giving 90/(90+495)',
      ]),
      answerIndex: 2,
      explanation: 'At 1% prevalence there are about 100 positives, of which 90 are detected, and 9,900 negatives, of which about 495 become false positives. Precision is therefore 90 / 585, about 15.4%. Sensitivity and false-positive rate can stay approximately stable while predictive values change sharply with prevalence.',
      misconceptionTested: 'Precision is an intrinsic threshold metric like sensitivity and therefore remains unchanged when class prevalence shifts but conditional error rates stay fixed.',
    }),
  ]),
  'propensity-scores': Object.freeze([
    Object.freeze({
      id: 'propensity-doubly-robust-estimator-decision',
      level: 'decision',
      relatedComparison: 'single-model-adjustment-vs-doubly-robust-combination',
      scenario: 'An observational study has a propensity model for treatment assignment and a separate outcome regression. The team considers an augmented inverse-probability weighted estimator under the usual exchangeability, consistency, and positivity assumptions. Reviewers ask what the phrase doubly robust actually guarantees about nuisance-model misspecification.',
      prompt: 'Which statement correctly describes the robustness property?',
      choices: Object.freeze([
        'The estimator is consistent only when both the propensity model and the outcome model are perfectly specified, so using two models adds no robustness',
        'The estimator is guaranteed unbiased even if both nuisance models are arbitrarily wrong and positivity fails',
        'Under the standard assumptions, consistency can be retained if either the propensity model or the outcome model is correctly specified, but not as a license for both models to be wrong or for causal assumptions to fail',
      ]),
      answerIndex: 2,
      explanation: 'Doubly robust estimators combine treatment and outcome nuisance models so that, under the underlying causal identification assumptions and regularity conditions, correct specification of either nuisance component can be sufficient for consistency. The property does not rescue simultaneous serious misspecification, unmeasured confounding, interference, or lack of overlap.',
      misconceptionTested: 'Doubly robust means the estimator remains causally valid regardless of whether both nuisance models are wrong or core identification assumptions such as positivity fail.',
    }),
  ]),
  optimization: Object.freeze([
    Object.freeze({
      id: 'optimization-objective-vs-update-rule-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'optimization-problem-geometry-vs-optimizer-update-rule',
      scenario: 'Two runs minimize the same badly conditioned objective L(w1,w2)=100*w1^2+w2^2 from the same starting point. Plain SGD zigzags across the steep direction, while an adaptive optimizer follows a smoother path because its coordinate-wise update scales differ. An engineer concludes that the two runs must therefore be optimizing different objectives.',
      prompt: 'What distinction should the team make before changing the loss function?',
      choices: Object.freeze([
        'Different parameter trajectories prove that the mathematical objective changed, so optimizer state can be ignored during diagnosis',
        'The optimization problem defines the objective and its geometry, while the optimizer defines how updates traverse that geometry; different trajectories can arise on the same objective without changing the loss',
        'Optimizer choice matters only for wall-clock speed and cannot change the path through parameter space when the starting point is fixed',
      ]),
      answerIndex: 1,
      explanation: 'Optimization describes the problem being solved, including the objective, constraints, conditioning, and geometry. An optimizer such as SGD, momentum, or Adam is an algorithm for choosing parameter updates on that problem. Different update rules can take very different paths, oscillate differently, or converge at different rates while minimizing exactly the same loss.',
      misconceptionTested: 'If two optimizers follow different parameter trajectories, they must be minimizing different objective functions rather than traversing the same geometry differently.',
    }),
  ]),
  transformer: Object.freeze([
    Object.freeze({
      id: 'transformer-block-parameter-ledger-worked',
      level: 'calculation',
      relatedComparison: 'attention-projection-parameters-vs-feedforward-parameters',
      scenario: 'A simplified Transformer block has d_model=512 and feed-forward width d_ff=2048. Ignore biases and normalization parameters. Self-attention uses four dense 512x512 matrices for Q, K, V, and output projection. The feed-forward network uses one 512x2048 matrix and one 2048x512 matrix.',
      prompt: 'How many trainable matrix parameters are in this block, and which sublayer contributes more?',
      choices: Object.freeze([
        'About 1.05 million total, because only the four attention projection matrices count as trainable Transformer parameters',
        'About 2.10 million total, because the feed-forward matrices replace rather than accompany the attention projections',
        '3,145,728 total: attention contributes 1,048,576 and the feed-forward network contributes 2,097,152, so the feed-forward sublayer is larger',
      ]),
      answerIndex: 2,
      explanation: 'Each 512x512 attention matrix has 262,144 parameters, so Q, K, V, and output projection contribute 1,048,576. Each feed-forward matrix has 512*2048=1,048,576 parameters, and there are two of them, giving 2,097,152. Together the block has 3,145,728 matrix parameters, with the feed-forward sublayer contributing twice the attention projection count in this configuration.',
      misconceptionTested: 'Transformer parameter count is dominated automatically by the attention score matrix, or the feed-forward matrices are not separate trainable parameters from Q/K/V/O projections.',
    }),
  ]),
  'fine-tuning': Object.freeze([
    Object.freeze({
      id: 'fine-tuning-evaluation-contamination-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'independent-evaluation-holdout-vs-training-data-contamination',
      scenario: 'A team fine-tunes an instruction model and reports a large gain on a public benchmark. A later data audit finds that many benchmark questions, paraphrases, and worked answers were included in the supervised fine-tuning corpus. The benchmark was not used to choose hyperparameters explicitly, but its content influenced parameter updates during training.',
      prompt: 'What should the team conclude about the reported benchmark improvement?',
      choices: Object.freeze([
        'The benchmark is no longer independent evidence for generalization; deduplicate or replace the evaluation set with a clean holdout and re-evaluate before attributing the gain to transferable capability',
        'The result remains a clean generalization estimate because contamination matters only when benchmark labels are read during the final evaluation call',
        'The contamination can be corrected by subtracting the percentage of duplicated examples from the benchmark score without rerunning evaluation',
      ]),
      answerIndex: 0,
      explanation: 'Evaluation is trustworthy only when its examples did not participate in fitting or selection in a way that lets the model memorize or specialize to them. Training on benchmark questions, answers, or close paraphrases violates that independence even if the final evaluation script is separate. A clean holdout or newly authored benchmark is needed to estimate transfer to unseen data.',
      misconceptionTested: 'A benchmark remains a valid independent generalization test as long as it was not explicitly passed to the hyperparameter-selection code, even when its examples appeared in training data.',
    }),
  ]),
  'rag-vector-indexing': Object.freeze([
    Object.freeze({
      id: 'rag-index-embedding-version-migration-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'single-embedding-space-vs-mixed-version-vector-index',
      scenario: 'A RAG service upgrades its embedding model from version A to version B. Both models emit 768-dimensional vectors, so the team switches query embedding to version B without rebuilding the document index, which still contains version-A vectors. Retrieval relevance collapses although indexing, filters, and HNSW health metrics look normal.',
      prompt: 'What migration error should be corrected first?',
      choices: Object.freeze([
        'Increase HNSW search breadth because matching dimensionality guarantees version-A and version-B embeddings share the same semantic coordinate system',
        'Keep corpus and query vectors in a compatible embedding space: re-embed and rebuild the corpus for version B, or keep using the matching version-A query encoder until the migration is complete',
        'Lower generation temperature because the vector-space mismatch occurs only after documents have already been retrieved',
      ]),
      answerIndex: 1,
      explanation: 'Equal vector dimensionality does not mean two embedding models define the same coordinate system. Similarity between a version-B query vector and version-A corpus vectors can therefore be meaningless even though the ANN index is mechanically healthy. Migration must keep query and corpus embeddings compatible, usually by versioning and rebuilding the corpus index or temporarily retaining the old query encoder.',
      misconceptionTested: 'Embedding models with the same output dimension can be mixed freely inside one retrieval space because vector dimension alone defines semantic compatibility.',
    }),
  ]),
  'attention-mechanism': Object.freeze([
    Object.freeze({
      id: 'attention-scaled-qk-score-worked',
      level: 'calculation',
      relatedComparison: 'raw-dot-product-scores-vs-scaled-attention-logits',
      scenario: 'For one attention query q=[2,0], two keys are k1=[2,0] and k2=[0,2]. The key dimension is d_k=2. Before softmax, scaled dot-product attention computes each logit as q dot k divided by sqrt(d_k). No mask or additional bias is applied in this example.',
      prompt: 'What logits enter the softmax, and which key receives the larger attention probability?',
      choices: Object.freeze([
        '[4,0], so k1 wins, because attention never applies the square-root scaling when d_k is smaller than the model width',
        '[2,2], so both keys tie, because only vector norms matter and the dot-product direction is discarded',
        'Approximately [2.83,0], so k1 receives the larger probability because 4/sqrt(2) is about 2.83 while the second dot product is zero',
      ]),
      answerIndex: 2,
      explanation: 'The raw dot products are q dot k1=4 and q dot k2=0. Scaled dot-product attention divides both by sqrt(2), giving logits about 2.83 and 0. Softmax preserves their ordering, so k1 receives most of the attention mass. The scaling controls logit magnitude as key dimension grows; it does not remove directional similarity.',
      misconceptionTested: 'Attention probabilities depend only on key norms, or the 1/sqrt(d_k) factor can be ignored when computing the logits that are passed to softmax.',
    }),
  ]),
  'self-attention': Object.freeze([
    Object.freeze({
      id: 'self-attention-causal-vs-bidirectional-decision',
      level: 'decision',
      relatedComparison: 'causal-left-context-vs-bidirectional-context-access',
      scenario: 'A team is comparing two self-attention masks. Model A is trained for left-to-right next-token generation, where token position t must not use tokens t+1 and beyond. Model B is trained to encode a complete observed sentence for masked-token understanding, where information from both sides of a position is available during encoding.',
      prompt: 'Which masking choice matches these two objectives?',
      choices: Object.freeze([
        'Use causal self-attention for Model A and bidirectional self-attention for Model B, because future-token access would leak targets in autoregressive generation while full observed context is useful for bidirectional encoding',
        'Use bidirectional self-attention for both models because self-attention is defined by attending to every token regardless of the training objective',
        'Use causal self-attention for both models because allowing any token to attend rightward is always data leakage, even when the entire input sentence is observed by design',
      ]),
      answerIndex: 0,
      explanation: 'The mask must match the prediction objective. An autoregressive decoder would leak future targets if position t could inspect later tokens, so causal masking is required. A bidirectional encoder such as a masked-token model can intentionally use both left and right observed context because it is not defining the same left-to-right conditional generation factorization.',
      misconceptionTested: 'Self-attention has one universally correct mask independent of whether the model is autoregressive or intentionally encodes a fully observed bidirectional context.',
    }),
  ]),
  'layer-normalization': Object.freeze([
    Object.freeze({
      id: 'layernorm-rmsnorm-comparison-worked',
      level: 'calculation',
      relatedComparison: 'mean-centered-layernorm-vs-rms-only-normalization',
      scenario: 'Ignore epsilon and learned affine scale/shift. For one token with feature vector x=[1,3], LayerNorm uses the feature mean 2 and population standard deviation 1. RMSNorm instead divides the original features by RMS=sqrt((1^2+3^2)/2)=sqrt(5) without subtracting the mean.',
      prompt: 'Which outputs correctly show the conceptual difference between LayerNorm and RMSNorm on this token?',
      choices: Object.freeze([
        'Both produce [-1,1] because every normalization layer must subtract the feature mean before applying any scale',
        'LayerNorm produces [-1,1], while RMSNorm produces approximately [0.447,1.342]; RMSNorm rescales by root-mean-square magnitude without mean-centering',
        'LayerNorm produces [0.447,1.342], while RMSNorm produces [-1,1] because RMSNorm performs the stronger centering operation',
      ]),
      answerIndex: 1,
      explanation: 'LayerNorm first centers x around its mean, giving [-1,1], then divides by standard deviation 1. RMSNorm does not subtract the mean: it divides [1,3] by sqrt(5), giving about [0.447,1.342]. Both can stabilize scale, but they are not the same transformation, and RMSNorm specifically omits mean-centering.',
      misconceptionTested: 'RMSNorm is merely another name for LayerNorm and therefore performs the same mean-centering step before rescaling token features.',
    }),
  ]),
  'policy-gradients': Object.freeze([
    Object.freeze({
      id: 'policy-gradient-baseline-variance-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'raw-return-reinforce-vs-action-independent-baseline',
      scenario: 'A REINFORCE agent has unbiased but extremely noisy episode returns, causing policy-gradient estimates to vary wildly between batches. The team proposes subtracting a learned state-value baseline b(s) from each return before weighting the sampled action log-probability gradient. The baseline depends on state but not on which action was sampled.',
      prompt: 'What is the main reason to use this baseline?',
      choices: Object.freeze([
        'An action-independent state baseline can reduce gradient-estimator variance while preserving the expected policy-gradient direction, making updates less noisy without redefining the environment reward',
        'The baseline makes REINFORCE off-policy by replacing sampled actions with whichever action the value network prefers',
        'Subtracting any baseline guarantees zero variance and removes the need to sample multiple trajectories or tune optimization',
      ]),
      answerIndex: 0,
      explanation: 'REINFORCE multiplies score-function gradients by noisy return estimates. Subtracting an action-independent baseline changes their variance but, under the standard policy-gradient argument, does not change the expected gradient because the baseline term has zero expectation over sampled actions. A learned value baseline can therefore stabilize training, though it does not eliminate all sampling variance.',
      misconceptionTested: 'A state-value baseline changes the underlying reward objective or necessarily biases the expected policy gradient simply because it is subtracted from sampled returns.',
    }),
  ]),
  'actor-critic': Object.freeze([
    Object.freeze({
      id: 'actor-critic-critic-error-propagation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'accurate-advantage-baseline-vs-biased-critic-signal',
      scenario: 'An actor-critic agent repeatedly chooses a poor action in one region of state space. Logged Monte Carlo returns for those transitions are low, but the critic systematically overestimates the successor states, making the one-step TD advantages for the poor action positive. The actor therefore keeps increasing that action probability.',
      prompt: 'What failure should be investigated before only reducing the actor learning rate?',
      choices: Object.freeze([
        'The actor must be ignoring all advantages because critic errors can affect value loss but can never influence policy updates',
        'The environment reward should be increased for the poor action until the critic prediction becomes self-consistent',
        'The biased critic is corrupting the actor advantage signal; compare critic targets and returns, inspect advantage estimation, and repair critic bias or bootstrapping before trusting the policy update',
      ]),
      answerIndex: 2,
      explanation: 'Actor-critic couples the policy update to a critic-derived advantage estimate. If the critic is systematically wrong, bootstrapped TD targets can give the actor the wrong sign or magnitude of learning signal. Lowering the actor learning rate may slow the damage but does not correct the bad advantage. The critic and return/advantage pipeline must be validated directly.',
      misconceptionTested: 'Critic prediction error is isolated to value-function training and cannot systematically push the actor toward worse actions through biased advantage estimates.',
    }),
  ]),
  'rl-exploration': Object.freeze([
    Object.freeze({
      id: 'exploration-nonstationary-environment-decision',
      level: 'decision',
      relatedComparison: 'near-zero-fixed-exploration-vs-adaptive-nonstationary-exploration',
      scenario: 'An epsilon-greedy agent succeeds in a bandit environment and decays epsilon from 0.20 to 0.005. Later the reward distribution changes and a previously weak action becomes the best action. Because the policy now almost always exploits its stale estimate, it rarely samples the newly optimal action and performance remains poor.',
      prompt: 'Which exploration strategy best addresses the failure mechanism?',
      choices: Object.freeze([
        'Set epsilon permanently to zero so the agent can exploit the old estimate more consistently after the environment changes',
        'Maintain or adapt some exploration when the environment can be nonstationary, or use a strategy that responds to uncertainty/change, so stale value estimates can be challenged by new evidence',
        'Increase the discount factor only, because discounting guarantees every action will eventually be sampled even when exploration probability is nearly zero',
      ]),
      answerIndex: 1,
      explanation: 'Aggressively decaying exploration assumes the environment and value ordering will remain stable enough that old evidence stays useful. In a nonstationary setting, near-zero exploration can lock the agent into stale beliefs. Retaining an exploration floor, resetting exploration after detected change, or using uncertainty-aware exploration gives the agent a mechanism to discover that the reward landscape moved.',
      misconceptionTested: 'Once an action has been identified as best, exploration can safely decay to essentially zero even when the reward process may change over time.',
    }),
  ]),
});

export function getP1NextPriorityGapScenariosForLesson(lessonId) {
  return P1_NEXT_PRIORITY_GAP_SCENARIOS_BY_LESSON[lessonId] || [];
}
