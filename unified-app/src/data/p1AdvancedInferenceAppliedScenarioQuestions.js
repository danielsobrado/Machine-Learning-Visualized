export const P1_ADVANCED_INFERENCE_APPLIED_SCENARIOS_BY_LESSON = Object.freeze({
  'multi-head-latent-attention': Object.freeze([
    Object.freeze({
      id: 'mla-cache-latent-ratio',
      level: 'calculation',
      scenario: 'A decoder layer using standard multi-head attention has 16 KV heads with head dimension 128. Caching both keys and values therefore stores 16 × 128 × 2 = 4,096 scalars per token per layer. An MLA variant instead caches a 512-dimensional compressed KV latent plus a separate 64-dimensional RoPE key component, for 576 scalars per token per layer. Assume the same numeric precision for both designs.',
      prompt: 'How much smaller is the MLA cache state per token per layer in this configuration?',
      choices: Object.freeze([
        'About 7.11× smaller, because 4,096 / 576 ≈ 7.11',
        'Exactly 2× smaller because MLA compresses keys but must leave values uncompressed',
        'About 1.13× smaller because the 64-dimensional RoPE component cancels most of the latent compression',
      ]),
      answerIndex: 0,
      explanation: 'The standard cache stores 4,096 scalars per token per layer, while the MLA layout stores 576. Their ratio is 4,096 / 576 ≈ 7.11, so the compressed cache needs about one seventh as many scalars. The exact byte saving depends on datatype, but the ratio remains the same when both use the same precision.',
      misconceptionTested: 'MLA can only save roughly a factor of two because key and value tensors must still be stored separately at their full per-head dimensions.',
      relatedComparison: 'Expanded per-head K/V caching versus caching a shared low-rank KV latent plus a small decoupled positional key component.',
    }),
    Object.freeze({
      id: 'mla-decoupled-rope-cache-design',
      level: 'design',
      scenario: 'An MLA implementation reconstructs content keys and values from a 512-dimensional cached latent c_KV. Positional information is handled by a separate 64-dimensional RoPE key component k_R. A serving engineer proposes caching only c_KV and deleting k_R, arguing that all key information should be recoverable from the compressed content latent. The model equations explicitly apply position-dependent RoPE to k_R outside that content reconstruction path.',
      prompt: 'Which cache design preserves the intended MLA attention computation without falling back to expanded full K/V storage?',
      choices: Object.freeze([
        'Cache both c_KV and the small decoupled k_R component, reconstructing content K/V while retaining the separate positional key state',
        'Cache only c_KV and discard k_R because positional information can always be reconstructed from a position-independent content latent',
        'Cache the fully expanded 16-head keys and values plus c_KV and k_R, because compression is useful only during training',
      ]),
      answerIndex: 0,
      explanation: 'The compressed latent carries the reconstructable content path, while the decoupled RoPE key carries position-dependent information that the stated equations do not reconstruct from c_KV alone. The efficient cache therefore keeps c_KV plus the small k_R term. Expanding and storing all heads would erase most of MLA’s cache-memory advantage.',
      misconceptionTested: 'Once a low-rank KV latent exists, every part of the attention key—including a separately computed position-dependent RoPE component—can be discarded and recovered from that latent.',
      relatedComparison: 'MLA compressed content caching with decoupled RoPE versus either under-caching positional state or reverting to full expanded K/V caching.',
    }),
  ]),
  'eagle-3-1-speculative-decoding': Object.freeze([
    Object.freeze({
      id: 'eagle-accepted-prefix-efficiency',
      level: 'calculation',
      scenario: 'A speculative decoder runs 100 target-model verification rounds. In each round the EAGLE-style drafter proposes 4 future tokens, so 400 draft tokens are proposed in total. Across those rounds, 280 draft tokens belong to accepted prefixes and are committed. Ignore fallback tokens for this calculation and compare only accepted draft progress with the 100 expensive target verification passes.',
      prompt: 'What are the draft-token acceptance rate and accepted draft tokens amortized per target verification pass?',
      choices: Object.freeze([
        '70% acceptance and 2.8 accepted draft tokens per target verification pass',
        '28% acceptance and 4.0 accepted draft tokens per target verification pass',
        '70% acceptance and exactly 1 accepted draft token per target verification pass because the target remains autoregressive',
      ]),
      answerIndex: 0,
      explanation: 'The acceptance rate is 280 / 400 = 70%. Those 280 accepted draft tokens were obtained across 100 target verification rounds, so the verifier amortizes 2.8 accepted draft tokens per expensive pass. Real end-to-end speedup also depends on drafter cost, fallback behavior, batching, and target verification latency.',
      misconceptionTested: 'Speculative decoding cannot advance by more than one accepted token per target-model pass because the target model itself is autoregressive.',
      relatedComparison: 'One-token-at-a-time target decoding versus block proposal followed by target verification of an accepted draft prefix.',
    }),
    Object.freeze({
      id: 'eagle-end-to-end-speedup',
      level: 'calculation',
      scenario: 'A target-only decoder takes 18 ms per generated token. A measured speculative configuration spends 6 ms on drafting and 24 ms on one batched target verification round, for 30 ms total round latency. Production traces show that each round commits an average of 3.6 output tokens after accepted-prefix and fallback handling. Ignore unrelated scheduler overhead.',
      prompt: 'What effective latency per committed token and speedup over target-only decoding does this measured speculative configuration deliver?',
      choices: Object.freeze([
        'About 8.33 ms/token and 2.16× speedup, because 30 / 3.6 ≈ 8.33 and 18 / 8.33 ≈ 2.16',
        '30 ms/token and 0.60× speedup because every speculative round must be charged entirely to one output token',
        '5 ms/token and 3.6× speedup because the average committed-token count can be used as the speedup directly',
      ]),
      answerIndex: 0,
      explanation: 'A round costs 30 ms and advances generation by 3.6 committed tokens on average, so effective latency is 30 / 3.6 ≈ 8.33 ms per token. Relative to the 18 ms target-only baseline, speedup is about 18 / 8.33 ≈ 2.16×. Acceptance length alone is not the speedup because drafting and verification still consume time.',
      misconceptionTested: 'Average accepted or committed tokens per speculative round is itself the end-to-end speedup, regardless of drafter and verifier latency.',
      relatedComparison: 'Acceptance-length metrics versus measured end-to-end token latency for speculative decoding.',
    }),
  ]),
  'spec-sparse-attention': Object.freeze([
    Object.freeze({
      id: 'specsa-union-block-reuse',
      level: 'calculation',
      scenario: 'Four speculative verifier rows select sparse KV blocks {1,2,3}, {2,3,4}, {2,4,5}, and {1,4,5}. A naive sparse kernel loads each row selected blocks independently. An exact merged SpecSA schedule instead loads the union of selected blocks once for the group and applies per-query masks so each verifier row still attends only to its original selection.',
      prompt: 'How many block loads are required by the naive rows versus the merged union, and what fraction of naive block loads are eliminated?',
      choices: Object.freeze([
        '12 naive loads versus 5 union loads, eliminating 7/12 ≈ 58.3% of the repeated block loads',
        '12 naive loads versus 12 merged loads, because exact semantics prevent any KV-block reuse across verifier rows',
        '5 naive loads versus 3 merged loads, because only blocks shared by every row belong in the exact union',
      ]),
      answerIndex: 0,
      explanation: 'Each of four rows selects three blocks, giving 12 row-local block loads. Their union is {1,2,3,4,5}, so an exact merged schedule can load five distinct blocks once and preserve row semantics with masks. It removes seven of twelve loads, or about 58.3%, without forcing every row to attend to every union block.',
      misconceptionTested: 'Exact sparse verification cannot reuse KV blocks across queries because preserving per-query sparse semantics requires loading each query selected blocks independently.',
      relatedComparison: 'Naive query-by-query sparse loads versus exact merged union loading with per-query masks.',
    }),
    Object.freeze({
      id: 'specsa-strict-exact-merge-choice',
      level: 'decision',
      scenario: 'A safety-critical deployment groups 8 speculative verifier rows. Profiling shows substantial overlap among their sparse layouts, but one row uniquely selects a context block containing a constraint that materially changes its verified next-token distribution. Shared-index scheduling would copy a representative layout to all rows and omit that unique block. Exact merged scheduling can load the union once while retaining per-query masks.',
      prompt: 'Which scheduling choice best matches a strict precision requirement while still exploiting cross-query KV reuse?',
      choices: Object.freeze([
        'Use exact merged scheduling: load the union for reuse and preserve each row original sparse selection with per-query masks',
        'Use shared-index scheduling because any representative sparse layout is equivalent once the target model performs verification',
        'Disable verification and accept the sparse drafter output directly so the unique block cannot create a scheduling mismatch',
      ]),
      answerIndex: 0,
      explanation: 'The deployment explicitly requires strict sparse semantics, and one query has a critical unique block. Exact merged scheduling still deduplicates common block traffic but masks the union per row, so that unique selection is preserved. Shared-index mode intentionally approximates layouts and is therefore the wrong precision class for this case.',
      misconceptionTested: 'Target-model verification makes query-specific sparse layouts irrelevant, so a shared representative index is always safe even when it omits a uniquely important block.',
      relatedComparison: 'Exact merged sparse verification versus approximate shared-index scheduling under strict quality constraints.',
    }),
  ]),
  turboquant: Object.freeze([
    Object.freeze({
      id: 'turboquant-effective-bit-memory',
      level: 'calculation',
      scenario: 'A serving workload would use 12 GiB of KV-cache memory if every cached key and value scalar were stored in FP16 at 16 bits. A TurboQuant configuration has an effective storage cost of 5 bits per scalar after including its low-bit main code, residual sketch, and protected outlier-channel overhead. The cache tensor geometry is otherwise unchanged.',
      prompt: 'What compressed KV-cache size and compression ratio should you expect from the effective-bit budget alone?',
      choices: Object.freeze([
        'About 3.75 GiB and 3.2× compression, because 12 × 5/16 = 3.75 and 16/5 = 3.2',
        'About 2.4 GiB and 5× compression because an effective 5-bit cache always means a fivefold reduction from FP16',
        'About 7.5 GiB and 1.6× compression because keys and values must each be counted a second time after quantization',
      ]),
      answerIndex: 0,
      explanation: 'With unchanged tensor geometry, memory scales directly with effective bits per scalar. Five bits instead of sixteen uses 5/16 of the FP16 memory: 12 × 5/16 = 3.75 GiB. The corresponding compression ratio is 16/5 = 3.2×. Effective bits already include the stated residual and outlier overhead.',
      misconceptionTested: 'A quantizer described as using roughly five effective bits must provide fivefold compression relative to FP16, ignoring that the baseline stores sixteen bits per scalar.',
      relatedComparison: 'Nominal or effective quantization bit width versus actual memory ratio relative to a 16-bit KV-cache baseline.',
    }),
    Object.freeze({
      id: 'turboquant-ranking-over-mse-choice',
      level: 'decision',
      scenario: 'Two candidate key-cache quantizers fit the same memory budget. Quantizer A has reconstruction MSE 0.0020, attention top-1 key agreement 89%, and score-rank correlation 0.94. Quantizer B has slightly worse reconstruction MSE 0.0035, but top-1 agreement 98% and rank correlation 0.99. The production failure mode is wrong attention routing, not visual reconstruction quality of the key vectors.',
      prompt: 'Which quantizer should be preferred for the key cache given the stated production objective, and why?',
      choices: Object.freeze([
        'Prefer B because key-cache quality is governed by preserving query-key score ordering and attention routing, not by minimizing coordinate MSE alone',
        'Prefer A because lower vector MSE mathematically guarantees better attention ranking for every possible query',
        'Treat them as equivalent because quantized keys affect only memory footprint; attention scores are computed from uncompressed values',
      ]),
      answerIndex: 0,
      explanation: 'Keys influence attention through query-key inner products and their relative ranking. Quantizer B better preserves both top-1 selection and rank correlation, directly matching the stated production objective, even though its coordinate reconstruction MSE is slightly worse. MSE is useful but not sufficient when score geometry is the behavior that matters.',
      misconceptionTested: 'The lowest reconstruction MSE is always the best KV-key quantizer because coordinate fidelity guarantees preservation of attention-score rankings.',
      relatedComparison: 'Vector reconstruction error versus attention-specific dot-product and ranking metrics for quantized keys.',
    }),
  ]),
});
