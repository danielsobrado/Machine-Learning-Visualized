export const P1_RAG_FAILURE_EVALUATION_SCENARIOS_BY_LESSON = Object.freeze({
  'rag-failure-modes': Object.freeze([
    Object.freeze({
      id: 'rag-failure-stage-attribution-trace',
      level: 'diagnosis',
      scenario: 'For one failed answer, the gold source is present in the initial top-20 candidate set, removed by reranking, absent from packed context, and therefore never visible to the generator.',
      prompt: 'Which stage should receive the first actionable failure attribution?',
      choices: Object.freeze([
        'Reranking or candidate-selection, because retrieval succeeded but the evidence was dropped before packing',
        'First-pass retrieval, because any failed answer must begin with retrieval',
        'Generation, because the generator produced the user-visible error',
      ]),
      answerIndex: 0,
      explanation: 'Failure attribution should follow the first pipeline boundary that loses a required condition. The gold source was retrieved, so first-pass recall succeeded. It disappeared during reranking or candidate selection before generation could use it.',
      misconceptionTested: 'Every incorrect RAG answer should be assigned to either retrieval or generation without tracing intermediate stages.',
      relatedComparison: 'Retrieval miss versus reranking loss versus generation failure.',
    }),
    Object.freeze({
      id: 'rag-failure-stale-conflict-resolution',
      level: 'diagnosis',
      scenario: 'Two authoritative policy chunks are retrieved. One says refunds are allowed for 30 days and is dated 2024. The second says 14 days and is dated 2026. The answer confidently states 30 days and cites only the older source.',
      prompt: 'What is the primary failure and correct response?',
      choices: Object.freeze([
        'Stale/conflicting evidence was not resolved; prefer the valid current authority or surface the conflict if authority is unclear',
        'The answer is grounded because it cites an authoritative source, so no further check is needed',
        'Increase top-k until enough copies of the 2024 policy dominate the context',
      ]),
      answerIndex: 0,
      explanation: 'Semantic relevance is not sufficient when policies change. The system must account for version/recency and conflicts among valid sources before treating a claim as grounded.',
      misconceptionTested: 'Any authoritative citation is valid support even when newer authoritative evidence contradicts it.',
      relatedComparison: 'Relevant evidence versus current, conflict-resolved evidence.',
    }),
    Object.freeze({
      id: 'rag-failure-context-dilution-operating-point',
      level: 'decision',
      scenario: 'A RAG benchmark shows top-k=8 gives evidence recall 0.91 and answer accuracy 0.88. Top-k=20 raises recall to 0.95 but answer accuracy falls to 0.81 because many distractors enter the prompt. Top-k=40 gives recall 0.96 and accuracy 0.73.',
      prompt: 'What should the team conclude?',
      choices: Object.freeze([
        'Retrieval recall and generation quality have different operating points; more context is causing dilution and should not be maximized blindly',
        'Top-k=40 is automatically best because retrieval recall is highest',
        'The generator must be broken because increasing evidence can never reduce answer quality',
      ]),
      answerIndex: 0,
      explanation: 'Higher candidate recall can be offset by noisy packed context. The operating point should optimize end-to-end quality under evidence and latency constraints, potentially with reranking or diversity-aware packing.',
      misconceptionTested: 'More retrieved context is always better for the generator.',
      relatedComparison: 'Candidate recall versus context dilution and answer accuracy.',
    }),
    Object.freeze({
      id: 'rag-failure-slice-regression',
      level: 'diagnosis',
      scenario: 'Overall grounded-answer accuracy rises from 84% to 86% after a retriever update. On Spanish support queries, however, accuracy falls from 78% to 61%, while English queries improve enough to hide the regression in the aggregate.',
      prompt: 'What is the correct evaluation response?',
      choices: Object.freeze([
        'Treat the Spanish slice regression as a release blocker or explicit risk; aggregate improvement does not erase a critical subgroup failure',
        'Ship automatically because the overall average improved',
        'Remove language labels from evaluation so the aggregate metric is easier to interpret',
      ]),
      answerIndex: 0,
      explanation: 'Aggregate metrics can hide concentrated retrieval or grounding failures. Important slices should have explicit acceptance thresholds and regression monitoring.',
      misconceptionTested: 'A higher global RAG metric proves every important query slice improved.',
      relatedComparison: 'Aggregate quality versus slice-specific regressions.',
    }),
    Object.freeze({
      id: 'rag-failure-abstain-without-valid-support',
      level: 'decision',
      scenario: 'A user asks for the current cancellation fee. Retrieval returns one unrelated billing FAQ and one outdated policy whose validity ended last year. No current approved source supports the requested fee.',
      prompt: 'What should a grounded system do?',
      choices: Object.freeze([
        'Abstain or state that current support is unavailable, rather than converting weak or stale evidence into a confident answer',
        'Use the outdated fee because some citation is better than no citation',
        'Lower grounding strictness until one of the retrieved chunks passes',
      ]),
      answerIndex: 0,
      explanation: 'When no valid evidence supports the claim, the grounding contract should allow a no-answer outcome. Lowering the bar to force coverage turns missing support into fabricated certainty.',
      misconceptionTested: 'A RAG system must always answer even when no valid evidence survives grounding.',
      relatedComparison: 'Grounded abstention versus forced unsupported coverage.',
    }),
  ]),
  'rag-retrieval-evaluation': Object.freeze([
    Object.freeze({
      id: 'rag-eval-recall-at-k-multirelevant-worked',
      level: 'calculation',
      scenario: 'A query has four labeled relevant chunks in the corpus. The top-5 retrieved set contains two of those four relevant chunks.',
      prompt: 'What is Recall@5 for this query?',
      choices: Object.freeze([
        '0.50',
        '0.40',
        '2.00',
      ]),
      answerIndex: 0,
      explanation: 'Recall@5 is relevant chunks recovered in the top 5 divided by all labeled relevant chunks: 2 / 4 = 0.50. Precision@5 would instead be 2 / 5 = 0.40.',
      misconceptionTested: 'Recall@k divides by k rather than by the total number of relevant items.',
      relatedComparison: 'Recall@k versus Precision@k.',
    }),
    Object.freeze({
      id: 'rag-eval-ndcg-ordering-worked',
      level: 'calculation',
      scenario: 'For one query, three returned chunks have graded relevance [3, 0, 2]. Another system returns the same chunks ordered [3, 2, 0].',
      prompt: 'Which ranking should have the higher nDCG, assuming the same relevance labels and cutoff?',
      choices: Object.freeze([
        '[3, 2, 0], because the useful grade-2 chunk receives less rank discount when moved upward',
        '[3, 0, 2], because nDCG ignores ordering once the same chunks are retrieved',
        'They must be identical because nDCG only uses binary relevance',
      ]),
      answerIndex: 0,
      explanation: 'nDCG rewards higher graded relevance near the top. Both systems retrieve the same items, but placing the grade-2 result earlier increases discounted gain.',
      misconceptionTested: 'nDCG measures only set membership and ignores graded ordering.',
      relatedComparison: 'Set recall versus rank-sensitive graded relevance.',
    }),
    Object.freeze({
      id: 'rag-eval-label-quality-trap',
      level: 'diagnosis',
      scenario: 'A retriever appears to have poor Recall@10 on current-policy questions. Manual review finds the evaluation labels still mark superseded 2024 policy chunks as the only relevant answers, while the retriever correctly returns the replacement 2026 policy.',
      prompt: 'What should be fixed first?',
      choices: Object.freeze([
        'The relevance ground truth and freshness policy, because incorrect labels make the retrieval metric misleading',
        'The retriever, because a low metric is always proof the search model is wrong',
        'The generator temperature, because evaluation labels are unrelated to retrieval metrics',
      ]),
      answerIndex: 0,
      explanation: 'Retrieval metrics are only as trustworthy as the relevance judgments. Stale or incomplete labels can penalize correct retrieval behavior and drive optimization in the wrong direction.',
      misconceptionTested: 'Offline retrieval metrics are objective even when relevance labels are stale or incorrect.',
      relatedComparison: 'Retriever error versus evaluation-label error.',
    }),
    Object.freeze({
      id: 'rag-eval-retrieval-vs-generation-boundary',
      level: 'diagnosis',
      scenario: 'The gold evidence is retrieved at rank 1 and survives into the packed prompt. The final answer nevertheless states the opposite of the source and cites the gold chunk.',
      prompt: 'Which subsystem should be investigated first?',
      choices: Object.freeze([
        'Generation or grounding/evidence-use, because retrieval and packing successfully delivered the required evidence',
        'Retriever Recall@k, because any wrong answer implies the gold evidence was not retrieved',
        'Vector index freshness, because rank 1 evidence cannot be used incorrectly by generation',
      ]),
      answerIndex: 0,
      explanation: 'The evidence path succeeded through retrieval and packing. The first broken condition is downstream: the answer contradicts visible evidence, so generation or claim-grounding behavior is the primary target.',
      misconceptionTested: 'End-to-end answer errors should always be optimized through retrieval metrics.',
      relatedComparison: 'Retrieval availability versus downstream evidence use.',
    }),
    Object.freeze({
      id: 'rag-eval-slice-gate-design',
      level: 'design',
      scenario: 'A support RAG system serves policy lookup, troubleshooting, billing, and multilingual queries. The team currently reports one global Recall@10 and one global nDCG.',
      prompt: 'What evaluation design is stronger for release decisions?',
      choices: Object.freeze([
        'Keep global metrics but add representative query slices with per-slice retrieval thresholds and enough labeled examples to detect regressions',
        'Use only the global average because slicing can reveal inconvenient variance',
        'Evaluate one hand-picked easy query from each category and skip aggregate metrics',
      ]),
      answerIndex: 0,
      explanation: 'Global metrics summarize the system but can hide important failure pockets. Representative slices with explicit thresholds make retrieval regressions observable without discarding the overall view.',
      misconceptionTested: 'One aggregate retrieval metric is sufficient for heterogeneous production traffic.',
      relatedComparison: 'Global retrieval metrics versus slice-level acceptance gates.',
    }),
  ]),
});
