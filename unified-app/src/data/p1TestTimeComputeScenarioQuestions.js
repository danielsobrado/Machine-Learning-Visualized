export const P1_TEST_TIME_COMPUTE_SCENARIOS_BY_LESSON = Object.freeze({
  'test-time-compute-thinking-budgets': Object.freeze([
    Object.freeze({
      id: 'ttc-best-of-n-total-compute-worked',
      level: 'calculation',
      relatedComparison: 'parallel-wall-clock-vs-total-generation-work',
      scenario: 'A Best-of-N service generates N = 8 candidates for one prompt. Each candidate averages 300 generated tokens. The serving stack batches the eight candidates efficiently, so wall-clock latency is only about 1.7 times the latency of one candidate, but every candidate still has to be decoded and scored.',
      prompt: 'How much generated-token work does the request perform before selection, and what does the batching result mean?',
      choices: Object.freeze([
        'About 2,400 generated tokens of work; batching reduces elapsed latency but does not make the other seven candidates computationally free',
        'About 510 generated tokens of work because total compute should be multiplied by the 1.7x wall-clock factor instead of by N',
        'Exactly 300 generated tokens of work because all eight candidates share one batched decoder pass and therefore have zero incremental generation cost',
      ]),
      answerIndex: 0,
      explanation: 'Eight candidates at 300 generated tokens each require about 8 × 300 = 2,400 generated-token steps before selection. Parallel batching can improve throughput and elapsed time, but total model work, memory traffic, and verifier work still scale with the number and length of candidates.',
      misconceptionTested: 'If Best-of-N candidates are batched in parallel, their total inference compute is approximately the same as generating one candidate.',
    }),
    Object.freeze({
      id: 'ttc-adaptive-routing-expected-cost-worked',
      level: 'calculation',
      relatedComparison: 'fixed-max-budget-vs-difficulty-routed-average-cost',
      scenario: 'A workload is 80% easy requests and 20% hard requests. A fixed policy gives every request a 1,600-token thinking budget. An adaptive router gives easy requests 200 tokens and hard requests 1,600 tokens. Assume requests consume their assigned budgets for this simplified calculation.',
      prompt: 'What average thinking-token budget does the adaptive policy use, and how much lower is it than the fixed 1,600-token policy?',
      choices: Object.freeze([
        '480 tokens on average, which is 70% lower than 1,600 because 0.8 × 200 + 0.2 × 1,600 = 480',
        '900 tokens on average, which is 43.75% lower because the easy and hard budgets should be averaged without workload weights',
        '1,440 tokens on average, which is only 10% lower because hard requests dominate the compute budget even when they are 20% of traffic',
      ]),
      answerIndex: 0,
      explanation: 'The weighted average is 160 + 320 = 480 tokens. Relative to 1,600, the saving is 1,120 tokens, or 70%. This is why routing can materially reduce average serving cost when most traffic is easy while preserving enough compute for hard cases.',
      misconceptionTested: 'Adaptive budget savings should be estimated by averaging budget tiers without weighting by the actual workload mix.',
    }),
    Object.freeze({
      id: 'ttc-hard-cap-misrouting-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'global-budget-limit-vs-routing-and-escalation-failure',
      scenario: 'A production router assigns a 256-token budget to requests predicted to be easy and 2,048 tokens to requests predicted to be hard. Easy-query accuracy is stable, but a hidden slice of multi-step proofs is frequently labeled easy, ends mid-derivation at exactly 256 tokens, and recovers when manually rerun with the hard budget.',
      prompt: 'What should be fixed first?',
      choices: Object.freeze([
        'The difficulty routing or escalation rule, because the failure is systematic under-budgeting of hard requests rather than evidence that every request needs the maximum budget',
        'Increase every request to the maximum budget permanently, because any truncation proves adaptive routing cannot work safely',
        'Reduce the hard budget to 256 tokens as well, because consistent caps matter more than completing the reasoning path',
      ]),
      answerIndex: 0,
      explanation: 'The exact-cap truncation and successful manual reruns identify a routing failure. A better classifier, uncertainty-aware escalation, or continuation rule can repair the hard slice while retaining savings on genuinely easy traffic.',
      misconceptionTested: 'Any hard-budget truncation in an adaptive system means the only safe solution is to give the maximum budget to every request.',
    }),
    Object.freeze({
      id: 'ttc-verifier-selection-gap-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'more-candidates-vs-better-selection-signal',
      scenario: 'A Best-of-N solver often generates both a terse correct solution and a polished incorrect solution. The selector gives the polished wrong answer score 0.93 and the correct answer 0.71. Increasing N from 8 to 32 makes this failure more frequent because more polished distractors are available.',
      prompt: 'Which bottleneck should be addressed before spending more compute on additional candidates?',
      choices: Object.freeze([
        'Verifier or selector quality, because more samples amplify the value of a biased selection signal instead of guaranteeing the correct candidate is chosen',
        'Candidate count only, because increasing N eventually forces every verifier to select the true answer regardless of its scoring behavior',
        'Thinking-token length only, because verifier ranking cannot affect Best-of-N once at least one correct answer was sampled',
      ]),
      answerIndex: 0,
      explanation: 'Best-of-N needs both useful candidate diversity and a selector aligned with true correctness. Once correct candidates already exist, a biased verifier can turn extra sampling into more opportunities to select convincing failures.',
      misconceptionTested: 'Increasing Best-of-N sample count can compensate for a verifier that systematically ranks wrong candidates above correct ones.',
    }),
    Object.freeze({
      id: 'ttc-prm-pruning-failure-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'aggressive-intermediate-pruning-vs-search-recall',
      scenario: 'A proof-search system keeps only the top two partial branches at each depth using a process reward model. Offline replay shows that the eventually correct proof branch repeatedly receives mediocre early PRM scores and is pruned at depth 3, while fluent but invalid branches score highly until they fail near the end.',
      prompt: 'What search failure is occurring, and what class of change should be evaluated?',
      choices: Object.freeze([
        'The PRM is misranking partial progress and pruning away future winners; evaluate PRM calibration and less aggressive or more diverse branch retention',
        'The beam is too wide, because keeping fewer branches always increases the probability that the correct proof survives',
        'The outcome verifier is too accurate, because correct final checking causes promising partial branches to disappear earlier',
      ]),
      answerIndex: 0,
      explanation: 'Tree search depends on intermediate scores having enough recall for promising paths. A biased PRM plus narrow pruning can permanently remove the only branch that would solve the task, so search depth alone will not repair the issue.',
      misconceptionTested: 'A process reward model can safely prune aggressively even when its early scores are poorly correlated with eventual solution success.',
    }),
    Object.freeze({
      id: 'ttc-tail-latency-budget-decision',
      level: 'decision',
      relatedComparison: 'mean-quality-vs-p99-serving-constraint',
      scenario: 'A reasoning endpoint has a 2-second P99 latency SLA. A model-controlled stopping policy improves average benchmark accuracy by 1.5 points, but 4% of requests enter long self-critique loops and push P99 latency to 5.1 seconds. The product supports an explicit slower expert tier for rare difficult cases.',
      prompt: 'Which deployment design best preserves useful test-time compute without violating the fast-tier contract?',
      choices: Object.freeze([
        'Keep a hard fast-tier budget or stopping guardrail and route or escalate difficult requests to the slower expert tier instead of allowing unbounded thinking on the SLA path',
        'Keep the unbounded stopping policy on the fast tier because average accuracy should override a documented P99 latency requirement',
        'Disable deeper reasoning everywhere, including the expert tier, because any tail-latency violation proves test-time compute has no production value',
      ]),
      answerIndex: 0,
      explanation: 'The fast tier needs predictable tail behavior, while the separate expert tier provides a place to spend extra compute when justified. This keeps the system on a product-specific cost, latency, and quality frontier rather than optimizing only average accuracy.',
      misconceptionTested: 'Average quality gains justify unbounded model-controlled thinking even when a production endpoint has a strict tail-latency SLA.',
    }),
  ]),
});
