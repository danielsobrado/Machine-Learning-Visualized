export const P1_RECOMMENDER_SYSTEMS_RANKING_SCENARIOS_BY_LESSON = Object.freeze({
  'recommender-systems-ranking-track': [
    {
      id: 'rec-candidate-recall-bottleneck-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'candidate-generation-recall-vs-reranker-quality',
      scenario: 'A two-stage recommender has an excellent reranker. Offline analysis shows that only 55% of the items users later engage with are present anywhere in the 500-item candidate sets passed to that reranker. The reranker cannot score items outside those candidate sets.',
      prompt: 'Where is the primary bottleneck?',
      choices: [
        'Candidate generation, because a reranker cannot recover relevant items that were never retrieved',
        'The reranker only, because better ranking loss can place items that are absent from the candidate set',
        'The final top-k metric only, because candidate recall cannot constrain end-to-end recommendation quality',
      ],
      answerIndex: 0,
      explanation: 'Two-stage systems have an upper bound set by retrieval. If a relevant item never enters the candidate set, no downstream scoring model can place it in the final list. Candidate recall therefore needs its own evaluation before reranker tuning can solve the end-to-end problem.',
      misconceptionTested: 'A sufficiently strong reranker can compensate for poor candidate generation even when relevant items are completely absent from the retrieved set.',
    },
    {
      id: 'rec-unexposed-is-not-negative-decision',
      level: 'decision',
      relatedComparison: 'unobserved-pair-vs-exposed-noninteraction',
      scenario: 'For one user, item A was shown near the top and not clicked. Item B was never retrieved or displayed. A training pipeline currently labels both A and B as equally strong negatives simply because neither has a positive interaction.',
      prompt: 'What should be changed first?',
      choices: [
        'Preserve exposure information and avoid treating every unobserved user-item pair as an equally certain negative',
        'Keep the labels because an unshown item is stronger evidence of dislike than an exposed non-click',
        'Delete exposure logs because recommendation training should use only positive interactions and item IDs',
      ],
      answerIndex: 0,
      explanation: 'Missing interaction is ambiguous. An unexposed item may never have had a chance to receive feedback, while an exposed non-click is still noisy but contains more behavioral context. Negative sampling and weighting should reflect the logging policy rather than silently turning all missing pairs into known dislikes.',
      misconceptionTested: 'Every missing user-item interaction is a true negative preference regardless of whether the item was ever exposed.',
    },
    {
      id: 'rec-cold-item-launch-strategy-decision',
      level: 'design',
      relatedComparison: 'interaction-only-collaborative-filtering-vs-content-exploration',
      scenario: 'A marketplace launches a new item with rich category, price, creator, and text metadata but zero interactions. The current collaborative model gives items with no learned interaction embedding almost no chance to be retrieved, so the item cannot collect clicks that would improve its embedding.',
      prompt: 'Which launch strategy best breaks this cold-start loop?',
      choices: [
        'Use content or hybrid retrieval plus controlled exploration so the item can receive initial exposure while interaction evidence accumulates',
        'Wait until the item has enough clicks before exposing it because collaborative filtering should never use side information',
        'Treat zero interactions as proof of low relevance and permanently suppress the item from candidate generation',
      ],
      answerIndex: 0,
      explanation: 'A new item cannot generate collaborative evidence without exposure. Content features, priors, and bounded exploration create a path into candidate sets so the system can learn from real feedback. This is a core reason hybrid recommenders are useful.',
      misconceptionTested: 'Interaction-only collaborative filtering can solve item cold start without any mechanism that gives new items an initial opportunity for exposure.',
    },
    {
      id: 'rec-temporal-interaction-leakage-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'cutoff-available-history-vs-future-interaction-features',
      scenario: 'For a recommendation generated on March 1, an offline feature table includes each user’s total category clicks computed from the entire month of March. The held-out label is whether the user engaged with a recommended item after March 1.',
      prompt: 'What is wrong with this evaluation?',
      choices: [
        'The feature includes interactions that happened after the recommendation cutoff, leaking future behavior into the model input',
        'Nothing, because aggregation makes future interactions safe as long as individual timestamps are removed',
        'Only the ranking metric is wrong; future interaction features are valid whenever the evaluation uses nDCG',
      ],
      answerIndex: 0,
      explanation: 'Every recommendation feature must be reconstructed from information available when the list would have been generated. A full-March aggregate for a March 1 decision includes future behavior and can make offline ranking results unrealistically strong.',
      misconceptionTested: 'Aggregating interactions over a larger window removes temporal leakage even when part of that window occurs after the recommendation timestamp.',
    },
  ],
});
