export const P1_NLP_TRANSFORMER_APPLIED_REPRESENTATION_SCENARIOS_BY_LESSON = Object.freeze({
  'tokenization': [
    {
      id: 'tokenization-budget-worked',
      level: 'calculation',
      relatedComparison: 'token-efficiency-vs-context-budget',
      scenario: 'A support prompt contains 12,000 characters of equivalent meaning. The tokenizer averages 4.0 characters per token for English but 1.5 characters per token for another language, and the deployed model has an 8,192-token context window before generation.',
      prompt: 'Which estimate best explains the multilingual context risk?',
      choices: ['English uses about 3,000 tokens while the other language uses about 8,000, leaving almost no room for generated output in the second case', 'Both languages use about 3,000 tokens because semantic meaning determines token count independently of the tokenizer', 'The other language uses about 18,000 tokens because tokens are always longer than characters'],
      answerIndex: 0,
      explanation: 'Token count is approximately characters divided by characters per token: 12,000/4.0 = 3,000 versus 12,000/1.5 = 8,000. The second prompt nearly consumes the whole 8,192-token budget before any response tokens are generated.',
      misconceptionTested: 'Equivalent semantic content does not imply equivalent token count, context usage, latency, or serving cost across languages.',
    },
  ],
  'embeddings': [
    {
      id: 'embedding-anisotropy-ablation-decision',
      level: 'decision',
      relatedComparison: 'raw-cosine-vs-centered-embedding-geometry',
      scenario: 'A sentence encoder gives unrelated document pairs cosine scores clustered between 0.78 and 0.91. Retrieval errors are common. On a held-out calibration corpus, subtracting the corpus mean direction spreads unrelated scores while preserving known semantic neighbors.',
      prompt: 'What is the strongest next experiment before changing the encoder?',
      choices: ['Run a controlled retrieval evaluation comparing the raw embeddings with the centered representation on the same queries and relevance labels', 'Assume every cosine above 0.78 is relevant because the encoder produced a high score', 'Increase the embedding dimension without measuring whether the common-direction problem changes'],
      answerIndex: 0,
      explanation: 'The observed geometry suggests a dominant common direction. A controlled ablation on the same labeled retrieval set tests whether centering improves discrimination without confounding the result with a different encoder, index, or dataset.',
      misconceptionTested: 'High absolute cosine similarity is not automatically semantic evidence when the embedding space itself is strongly anisotropic.',
    },
  ],
  'cosine-similarity': [
    {
      id: 'cosine-ranking-worked',
      level: 'calculation',
      relatedComparison: 'direction-vs-magnitude-ranking',
      scenario: 'A query vector is q = (3, 4). Candidate A is a = (6, 8), and candidate B is b = (5, 0). The retriever ranks candidates using cosine similarity only, not Euclidean distance or vector magnitude.',
      prompt: 'Which candidate should rank first, and why?',
      choices: ['A should rank first because cos(q,a) = 1 while cos(q,b) = 0.6, so A has the same direction as the query despite having a larger norm', 'B should rank first because its first coordinate is closer to the query first coordinate', 'A and B must tie because cosine ignores all coordinate values'],
      answerIndex: 0,
      explanation: 'q has norm 5, a has norm 10, and q dot a is 50, giving cosine 50/(5*10) = 1. For b, q dot b is 15 and the norms multiply to 25, giving 0.6. Cosine ranks angular alignment rather than raw magnitude.',
      misconceptionTested: 'Cosine similarity does not rank vectors by size or by one coordinate; it measures normalized directional alignment.',
    },
  ],
  'attention-mechanism': [
    {
      id: 'attention-weighted-value-worked',
      level: 'calculation',
      relatedComparison: 'attention-score-vs-value-contribution',
      scenario: 'For one query, the scaled attention logits for two source tokens are 0 and ln(3). Their value vectors are v1 = (2, 0) and v2 = (0, 4). Softmax therefore assigns weights 0.25 and 0.75 before the values are mixed.',
      prompt: 'What attention output should this query produce?',
      choices: ['(0.5, 3.0), because the output is 0.25*v1 + 0.75*v2', '(1.0, 2.0), because attention always averages value vectors equally', '(0.0, 4.0), because only the highest-scoring value survives ordinary softmax'],
      answerIndex: 0,
      explanation: 'Softmax converts the logits into weights that sum to one. The weighted value sum is 0.25*(2,0) + 0.75*(0,4) = (0.5,3.0). Scores decide weighting, while the value vectors provide the information actually combined.',
      misconceptionTested: 'Attention does not simply select the maximum score or average values equally; normalized query-key scores weight the value mixture.',
    },
  ],
  'self-attention': [
    {
      id: 'self-attention-length-memory-decision',
      level: 'decision',
      relatedComparison: 'context-length-vs-quadratic-attention-cost',
      scenario: 'A dense-attention training run is stable at sequence length 4,096 with batch size 8. The team doubles sequence length to 8,192 while keeping model width, batch size, and activation precision unchanged, then immediately hits attention-memory pressure.',
      prompt: 'Which scaling fact should guide the first mitigation?',
      choices: ['The dense attention score area grows about 4x when sequence length doubles, so batch size, checkpointing, kernels, or attention structure may need adjustment', 'Attention memory should only double because every transformer operation is linear in sequence length', 'The parameter count doubled automatically, so reducing vocabulary size is the direct fix'],
      answerIndex: 0,
      explanation: 'Dense self-attention forms interactions across pairs of positions, so its score matrix scales with n squared. Doubling n from 4,096 to 8,192 makes that component roughly four times larger even though parameter count is unchanged.',
      misconceptionTested: 'Longer context does not make every transformer memory component grow linearly; dense attention pair storage has quadratic scaling.',
    },
  ],
  'attention-masks': [
    {
      id: 'attention-mask-padding-leak-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'causal-mask-vs-padding-mask',
      scenario: 'A decoder batch uses right padding. Causal masking is enabled, but changing only the embedding values stored in padded positions changes logits for earlier valid tokens in the same sequence. Sequence lengths and all real tokens are unchanged.',
      prompt: 'What implementation bug is most consistent with this evidence?',
      choices: ['The padding positions are not fully excluded from attention, so the causal mask is present but the padding mask is missing or combined incorrectly', 'The causal mask must be removed because causal attention should read every padded token', 'The tokenizer vocabulary is too small, which makes padding embeddings leak into attention by definition'],
      answerIndex: 0,
      explanation: 'A causal mask blocks future positions by order, but it does not inherently mark padding as nonexistent. If changing padded embeddings changes valid-token logits, those padded positions are still influencing attention and need a correct padding mask.',
      misconceptionTested: 'Causal masking and padding masking solve different problems; enabling one does not automatically enforce the other.',
    },
  ],
  'positional-encoding': [
    {
      id: 'position-shift-generalization-design',
      level: 'design',
      relatedComparison: 'absolute-position-vs-relative-position-generalization',
      scenario: 'A sequence classifier is trained on patterns that always begin near position 0. At evaluation, the same local token relationships are shifted 200 positions later and accuracy drops sharply, even though token order and pairwise distances inside each pattern are unchanged.',
      prompt: 'Which controlled architecture experiment best tests position overfitting?',
      choices: ['Compare the current absolute-position setup with a relative-position method while holding model size, data, optimizer, and evaluation shifts fixed', 'Randomly change the labels so the model cannot memorize absolute positions', 'Remove all positional information and assume word order will still be represented exactly'],
      answerIndex: 0,
      explanation: 'The failure specifically concerns sensitivity to absolute location despite preserved relative structure. A controlled comparison with relative-position encoding isolates whether the positional representation improves shift generalization without changing unrelated factors.',
      misconceptionTested: 'Position encodings are not interchangeable; absolute location signals can create different generalization behavior from relative relationships.',
    },
  ],
  'rope': [
    {
      id: 'rope-position-interpolation-worked',
      level: 'calculation',
      relatedComparison: 'unscaled-rope-vs-position-interpolation',
      scenario: 'A model was trained with RoPE positions up to 8,192. A simple position-interpolation experiment targets a 32,768-token window by scaling inference position p to p/4 before applying the original rotary frequencies.',
      prompt: 'Where is inference token position 24,576 mapped in the original positional range?',
      choices: ['Position 6,144, because 24,576 divided by the 4x context-extension factor is 6,144', 'Position 24,576, because interpolation leaves every rotary position unchanged', 'Position 98,304, because context extension multiplies the rotary position by four'],
      answerIndex: 0,
      explanation: 'With a four-times target context, simple position interpolation maps p to p/4. Therefore 24,576 becomes 6,144, placing the rotary phase input inside the original 0-8,192 training range. This does not by itself guarantee long-context quality.',
      misconceptionTested: 'Position interpolation rescales positional inputs; it is not the same as running unmodified RoPE at arbitrarily larger positions.',
    },
  ],
  'residual-stream': [
    {
      id: 'residual-stream-additive-update-worked',
      level: 'calculation',
      relatedComparison: 'residual-addition-vs-replacement',
      scenario: 'At one transformer location the residual vector is x = (1.0, 2.0, -1.0). An attention sublayer produces an update a = (0.5, -1.0, 2.0), and the block uses the residual rule x_next = x + a.',
      prompt: 'What vector should enter the next residual stage?',
      choices: ['(1.5, 1.0, 1.0), because the attention result is added component-wise to the existing residual representation', '(0.5, -1.0, 2.0), because every transformer sublayer replaces the residual stream completely', '(1.5, -2.0, -2.0), because residual connections multiply the old and new representations'],
      answerIndex: 0,
      explanation: 'Residual connections preserve the current representation and add a learned update: (1.0+0.5, 2.0-1.0, -1.0+2.0) = (1.5,1.0,1.0). Replacing x with a would remove the identity path the residual design is meant to preserve.',
      misconceptionTested: 'A transformer sublayer normally contributes an additive update to the residual stream rather than overwriting the whole representation.',
    },
  ],
});
