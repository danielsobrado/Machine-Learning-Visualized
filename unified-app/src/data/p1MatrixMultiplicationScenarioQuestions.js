export const P1_MATRIX_MULTIPLICATION_SCENARIOS_BY_LESSON = Object.freeze({
  'matrix-multiplication': [
    {
      id: 'matmul-chain-association-cost-worked',
      level: 'calculation',
      relatedComparison: 'left-associated-vs-cost-aware-chain-order',
      scenario: 'A has shape 2000 x 20, B has shape 20 x 2000, and C has shape 2000 x 10. Both (AB)C and A(BC) are mathematically valid and equal. Count one scalar multiplication for each inner-dimension product.',
      prompt: 'How does the multiplication cost compare between the two valid association orders?',
      choices: [
        '(AB)C needs about 120 million multiplications, while A(BC) needs about 0.8 million, so choosing the cheaper association is roughly 150x less work',
        'Both orders require exactly 120 million multiplications because associativity guarantees identical computational cost',
        'A(BC) is invalid because B and C cannot be multiplied before A is applied',
      ],
      answerIndex: 0,
      explanation: '(AB) costs 2000 x 20 x 2000 = 80 million multiplications and multiplying that 2000 x 2000 result by C costs another 40 million, for 120 million total. BC costs 20 x 2000 x 10 = 400,000, then A(BC) costs 2000 x 20 x 10 = 400,000, for 800,000 total. Associativity preserves the result, not the cost of intermediate matrices.',
      misconceptionTested: 'Associative matrix products have the same computational cost regardless of how the chain is parenthesized.',
    },
    {
      id: 'matmul-batch-axis-layout-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'batch-sequence-axes-vs-feature-contraction-axis',
      scenario: 'Transformer activations have shape [64 batches, 128 tokens, 768 features]. A projection weight maps 768 features to 3072 outputs. A refactor treats the batch axis as the contraction dimension and starts mixing examples from different requests together.',
      prompt: 'What tensor-layout rule should the implementation restore?',
      choices: [
        'Preserve batch and token axes as independent examples and contract the 768 feature axis with the weight feature axis, producing [64, 128, 3072]',
        'Contract over the batch axis because the first dimension is always the matrix inner dimension',
        'Flatten all three activation axes into one vector because batching removes the need to preserve token identity',
      ],
      answerIndex: 0,
      explanation: 'Batch dimensions describe independent groups of vectors; they are not automatically dimensions to multiply away. The projection should apply the same 768-to-3072 linear map to every batch/token position, leaving the leading batch and token axes intact.',
      misconceptionTested: 'Any leading tensor axis can be treated as a matrix inner dimension without changing the semantics of a batched linear layer.',
    },
    {
      id: 'matmul-elementwise-broadcast-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'broadcasted-elementwise-product-vs-feature-dot-product',
      scenario: 'A dense layer should compute XW for X with shape 32 x 128 and W with shape 128 x 64. A developer instead reshapes X to 32 x 128 x 1 and W to 1 x 128 x 64, then uses elementwise multiplication. Broadcasting returns a 32 x 128 x 64 tensor, so the code runs without a shape error.',
      prompt: 'Why is this still not the intended dense-layer computation?',
      choices: [
        'The broadcasted product keeps the 128 feature terms separate; matrix multiplication must also sum over that shared feature dimension to produce a 32 x 64 output',
        'Broadcasting is equivalent to matrix multiplication whenever the output has more dimensions',
        'The dense layer should preserve all 128 input features as a separate output axis, so 32 x 128 x 64 is the correct result',
      ],
      answerIndex: 0,
      explanation: 'Matrix multiplication is not just pairwise multiplication. Each output feature is a dot product, so the 128 paired feature products must be accumulated. Broadcasting can make an incorrect implementation look shape-compatible while silently omitting the reduction that defines the matrix product.',
      misconceptionTested: 'If broadcasting makes elementwise multiplication run successfully, it is a valid substitute for a matrix product.',
    },
    {
      id: 'matmul-transpose-product-order-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'transpose-each-factor-vs-reverse-factor-order',
      scenario: 'A verification routine needs the transpose of AB. Because A and B are both square, a developer implements A^T B^T. The code passes shape checks but disagrees with a trusted reference on non-commuting test matrices.',
      prompt: 'What algebraic bug explains the disagreement?',
      choices: [
        'Transposing a product reverses factor order: (AB)^T = B^T A^T, so A^T B^T generally computes a different matrix',
        'Square matrices always commute, so the trusted reference must be wrong',
        'A transpose may be applied to only one factor because transposing both factors cancels out',
      ],
      answerIndex: 0,
      explanation: 'The transpose operation reverses composition order. Equal square shapes can hide this bug because both expressions are dimensionally valid, yet matrix multiplication is generally non-commutative, so A^T B^T need not equal B^T A^T.',
      misconceptionTested: 'Shape compatibility is enough to validate a transpose-of-product implementation, or factor order can be preserved when transposing a product.',
    },
    {
      id: 'matmul-floating-reduction-order-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'real-arithmetic-associativity-vs-floating-reduction-order',
      scenario: 'One output dot product contains the three terms [100000000, 1, -100000000]. In float32, one kernel accumulates them left-to-right and returns 0 because the +1 is rounded away next to 100000000. Another reduction first cancels +100000000 with -100000000 and then adds 1, returning 1.',
      prompt: 'What should the team conclude from this discrepancy?',
      choices: [
        'Different valid reduction orders can produce different floating-point results because finite-precision addition is not exactly associative',
        'One implementation must have violated matrix shape rules because mathematically equivalent reduction orders are bit-identical in float32',
        'Matrix multiplication cannot contain negative terms, so the input itself is invalid',
      ],
      answerIndex: 0,
      explanation: 'Real-number addition is associative, but floating-point addition rounds after intermediate operations. Large cancellation and very different magnitudes can make reduction order visible in the final bits. Production tests should use numerically appropriate tolerances rather than assume bitwise equality across kernels.',
      misconceptionTested: 'Mathematically equivalent matrix-multiplication kernels must produce bit-identical floating-point outputs regardless of accumulation order.',
    },
  ],
});
