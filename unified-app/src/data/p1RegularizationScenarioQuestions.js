export const P1_REGULARIZATION_SCENARIOS_BY_LESSON = Object.freeze({
  regularization: [
    {
      id: 'regularization-l1-l2-penalty-worked',
      level: 'calculation',
      relatedComparison: 'l1-absolute-vs-l2-squared-penalty',
      scenario: 'A linear model has two penalized coefficients w = [3, -4]. Ignore the intercept. For this exercise, define L1 penalty as sum |w_i| and L2 penalty as sum w_i^2 before multiplying by lambda.',
      prompt: 'What are the unscaled L1 and L2 penalty values?',
      choices: [
        'L1 = 7 and L2 = 25',
        'L1 = 25 and L2 = 7',
        'L1 = 1 and L2 = 5 because opposite signs cancel in regularization',
      ],
      answerIndex: 0,
      explanation: 'L1 uses absolute values: |3| + |-4| = 7. L2 uses squared magnitudes: 3^2 + (-4)^2 = 9 + 16 = 25. Coefficient signs do not cancel either penalty.',
      misconceptionTested: 'Positive and negative coefficients cancel inside L1 or L2 penalties, or the two norm penalties use the same arithmetic.',
    },
    {
      id: 'regularization-feature-scale-penalty-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'raw-unit-coefficients-vs-standardized-penalty',
      scenario: 'A regularized linear model uses annual_income measured in dollars and account_age measured in years. Income therefore needs a numerically tiny coefficient to represent a meaningful effect, while account age needs a much larger coefficient. The same L2 penalty is applied directly to both raw coefficients.',
      prompt: 'What problem should be fixed before interpreting the penalty as treating features comparably?',
      choices: [
        'Standardize or otherwise put continuous features on defensible comparable scales because coefficient magnitude depends on feature units',
        'Nothing; L1 and L2 penalties are automatically invariant to arbitrary feature units',
        'Rescale only the target labels because feature units cannot affect coefficient penalties',
      ],
      answerIndex: 0,
      explanation: 'A coefficient magnitude is partly determined by the units of its feature. Penalizing raw coefficient sizes can therefore constrain features unevenly for purely unit-based reasons. Standardization makes a common penalty strength more comparable across continuous predictors.',
      misconceptionTested: 'Weight penalties treat features fairly regardless of feature scale or measurement units.',
    },
    {
      id: 'regularization-adamw-decoupled-weight-decay',
      level: 'comparison',
      relatedComparison: 'coupled-l2-gradient-vs-decoupled-weight-decay',
      scenario: 'Two neural-network training runs use Adam. Run A adds lambda * ||w||^2 to the loss, so the penalty gradient enters Adam’s adaptive gradient machinery. Run B uses AdamW-style decoupled weight decay, applying shrinkage separately from the loss gradient update.',
      prompt: 'Should these two procedures be treated as automatically equivalent?',
      choices: [
        'No; adaptive preconditioning changes a coupled L2 gradient, while decoupled weight decay applies shrinkage separately, so the optimization behavior can differ',
        'Yes; L2 regularization and weight decay are mathematically identical for every optimizer and learning-rate schedule',
        'Yes; Adam ignores all regularization gradients, so only the parameter names differ',
      ],
      answerIndex: 0,
      explanation: 'For simple SGD, L2 regularization and multiplicative weight decay can be equivalent under matching conventions. With adaptive optimizers such as Adam, putting L2 inside the gradient means the adaptive scaling also acts on the penalty. AdamW separates decay from that preconditioning, so the two are not generally interchangeable.',
      misconceptionTested: 'L2 regularization and weight decay are universally identical implementation choices even under adaptive optimizers.',
    },
    {
      id: 'regularization-correlated-l1-instability-decision',
      level: 'decision',
      relatedComparison: 'l1-single-feature-selection-vs-elastic-net-group-stability',
      scenario: 'Five predictors are almost duplicates of the same sensor signal. Across resampled training sets, pure L1 keeps a different one of the five each time while predictive performance stays similar. Stakeholders want stable feature-level interpretation as well as sparsity.',
      prompt: 'Which experiment is most defensible?',
      choices: [
        'Compare elastic net or another grouped/stability-aware approach because pure L1 can choose arbitrarily among strongly correlated substitutes',
        'Increase pure L1 until exactly one coefficient survives and treat that surviving feature as the uniquely causal sensor',
        'Remove regularization entirely because correlated predictors cannot be stabilized by any regularization strategy',
      ],
      answerIndex: 0,
      explanation: 'Strongly correlated predictors can substitute for one another. Pure L1 often selects one representative, but which one survives can be unstable across samples. Elastic net combines sparsity with L2-style grouping stability and is a reasonable comparison when stable interpretation matters.',
      misconceptionTested: 'The one feature retained by L1 from a correlated group is automatically the uniquely important or causal predictor.',
    },
  ],
});
