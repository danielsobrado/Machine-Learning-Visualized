export const P1_FINE_TUNING_RAG_CHUNKING_SCENARIOS_BY_LESSON = Object.freeze({
  'fine-tuning': Object.freeze([
    Object.freeze({
      id: 'fine-tuning-small-data-forgetting-diagnosis',
      level: 'diagnosis',
      scenario: 'A pretrained model is fully fine-tuned on 900 narrow customer-support examples with a relatively large learning rate. Training accuracy reaches 99%, but a held-out general-language benchmark drops sharply while the support validation set improves.',
      prompt: 'What is the most likely diagnosis and first mitigation direction?',
      choices: Object.freeze([
        'Overfitting plus catastrophic forgetting; reduce update aggressiveness and consider freezing more of the base or using adapters',
        'The model needs a larger decoding temperature because catastrophic forgetting is an inference-time sampling problem',
        'The tokenizer should be rebuilt because any validation improvement proves the fine-tuning itself is safe',
      ]),
      answerIndex: 0,
      explanation: 'Small, narrow data can over-specialize a pretrained model. Large updates across all weights can improve the narrow task while damaging useful pretrained behavior. Lower learning rates, parameter-efficient tuning, stronger regularization, earlier stopping, and broader held-out evaluation are appropriate mitigations.',
      misconceptionTested: 'If task validation improves, fine-tuning cannot be damaging general pretrained capability.',
      relatedComparison: 'Narrow-task adaptation versus catastrophic forgetting of general capability.',
    }),
    Object.freeze({
      id: 'fine-tuning-layer-scope-learning-rate-choice',
      level: 'decision',
      scenario: 'A pretrained encoder already produces good generic features for a new classification task. The new dataset is small, and the randomly initialized classification head learns slowly while lower pretrained layers begin to drift.',
      prompt: 'Which update policy is the safer starting point?',
      choices: Object.freeze([
        'Train the new head aggressively while freezing or using a smaller learning rate for pretrained layers, then unfreeze selectively if validation justifies it',
        'Use the same large learning rate for every layer so all pretrained representations change as quickly as the new head',
        'Freeze the new head and update only the lowest pretrained layers because new output weights do not need training',
      ]),
      answerIndex: 0,
      explanation: 'Different parameter groups can have different adaptation needs. A new head may need larger updates, while useful pretrained layers often benefit from smaller learning rates or staged unfreezing, especially with limited data.',
      misconceptionTested: 'Fine-tuning requires one identical learning rate and update scope for every model parameter.',
      relatedComparison: 'Task-head adaptation versus cautious pretrained-layer adaptation.',
    }),
    Object.freeze({
      id: 'fine-tuning-full-vs-lora-qlora-choice',
      level: 'decision',
      scenario: 'A team needs to adapt a 13B model to a company writing style on one 24 GB GPU. They have 20,000 curated prompt-response examples and do not need to rewrite the model architecture.',
      prompt: 'Which method is the most practical starting point?',
      choices: Object.freeze([
        'LoRA or QLoRA, because the task needs behavioral adaptation under a tight training-memory budget',
        'Full fine-tuning of every base weight because parameter-efficient methods always require more optimizer memory',
        'Only top-p sampling, because decoding controls permanently teach the model the company style',
      ]),
      answerIndex: 0,
      explanation: 'LoRA reduces trainable parameter and optimizer-state memory, while QLoRA also lowers frozen-base memory through quantization. Full fine-tuning may be useful when broad capacity is required and hardware permits it, but it is not the default under this memory constraint.',
      misconceptionTested: 'Full fine-tuning is always the best adaptation method regardless of hardware or scope.',
      relatedComparison: 'Full fine-tuning versus LoRA and QLoRA under memory constraints.',
    }),
    Object.freeze({
      id: 'fine-tuning-domain-shift-validation',
      level: 'diagnosis',
      scenario: 'A medical-text fine-tune is trained and validated on notes from one hospital. Validation improves substantially, but deployment on notes from a second hospital with different abbreviations and documentation style performs poorly.',
      prompt: 'What evaluation/design problem should be fixed first?',
      choices: Object.freeze([
        'The held-out evaluation did not represent the deployment domain shift; add target-domain or cross-site validation and adapt only if evidence supports it',
        'The model must be overfitting because any distribution shift can be solved by increasing adapter rank',
        'Use the original hospital validation set more often because repeated evaluation makes it representative of the second hospital',
      ]),
      answerIndex: 0,
      explanation: 'A same-source validation split can miss deployment shift. Evaluation must represent the target distribution or explicitly measure cross-domain robustness before choosing more tuning, data collection, or retrieval.',
      misconceptionTested: 'A random held-out split from the training source guarantees performance under a different deployment distribution.',
      relatedComparison: 'In-domain validation versus cross-domain deployment validation.',
    }),
    Object.freeze({
      id: 'fine-tuning-freeze-unfreeze-debugging',
      level: 'diagnosis',
      scenario: 'During staged fine-tuning, the task head loss stops improving after the head converges. Inspection shows the upper pretrained layers are frozen, while the task requires domain-specific semantic distinctions that the existing representation does not separate well.',
      prompt: 'What is the next controlled experiment?',
      choices: Object.freeze([
        'Unfreeze a small upper block with a lower learning rate and compare held-out task gains against regressions',
        'Immediately unfreeze every model layer with the same large learning rate and train until training loss is zero',
        'Keep all pretrained layers frozen permanently because unfreezing can never improve representation quality',
      ]),
      answerIndex: 0,
      explanation: 'Staged unfreezing lets the model add task-specific representation capacity while controlling forgetting risk. The decision should be validated against both task performance and retained capabilities.',
      misconceptionTested: 'Freeze/unfreeze decisions are fixed rules rather than capacity-versus-stability choices validated experimentally.',
      relatedComparison: 'Frozen-feature transfer versus selective upper-layer adaptation.',
    }),
  ]),
  'rag-chunking-context': Object.freeze([
    Object.freeze({
      id: 'rag-chunk-boundary-condition-recovery',
      level: 'diagnosis',
      scenario: 'A policy says "Refunds are allowed within 30 days" at the end of one paragraph and "only if the device is unopened" at the start of the next. Fixed-size chunking splits the two clauses, and retrieval often returns only the first clause.',
      prompt: 'Which change most directly targets this failure?',
      choices: Object.freeze([
        'Use overlap or structure-aware chunking so the condition and rule can coexist in a retrievable unit',
        'Increase answer temperature so the generator invents the missing condition more confidently',
        'Decrease top-k to one so only the incomplete refund clause reaches the generator',
      ]),
      answerIndex: 0,
      explanation: 'This is a split-boundary failure. Overlap or section-aware chunking can keep the rule and its qualifier together, improving the chance that retrieval supplies complete evidence.',
      misconceptionTested: 'Generation settings can repair evidence that chunking removed from the retrieved context.',
      relatedComparison: 'Boundary-aware chunking versus inference-time decoding changes.',
    }),
    Object.freeze({
      id: 'rag-chunk-duplicate-budget-crowding',
      level: 'diagnosis',
      scenario: 'The top 8 retrieval results contain six near-identical overlapping chunks from the same paragraph. All eight are packed, leaving no room for a separate eligibility table required to answer the question.',
      prompt: 'What should the pipeline change first?',
      choices: Object.freeze([
        'Deduplicate or diversify candidates before packing so unique complementary evidence can use the context budget',
        'Increase overlap further so even more copies of the same paragraph occupy top-k',
        'Remove source identifiers because provenance is the cause of duplicate semantic content',
      ]),
      answerIndex: 0,
      explanation: 'Overlap can improve boundary recall but also create redundant retrieval results. Deduplication or diversity-aware selection such as MMR can preserve relevant but non-duplicative evidence under a finite prompt budget.',
      misconceptionTested: 'More repeated high-scoring chunks always improve grounded generation.',
      relatedComparison: 'Raw relevance ranking versus relevance-plus-diversity packing.',
    }),
    Object.freeze({
      id: 'rag-chunk-multihop-packing',
      level: 'decision',
      scenario: 'A question asks whether a customer qualifies for a benefit. One chunk contains the age rule, another contains the residency rule, and neither is sufficient alone. Vector search ranks four near-duplicates of the age rule above the residency chunk.',
      prompt: 'What packing strategy best supports the answer?',
      choices: Object.freeze([
        'Select complementary evidence so at least one strong age chunk and the residency chunk both reach the generator',
        'Pack only the highest-scoring age chunk because the single top score must contain every required fact',
        'Increase chunk size until the entire corpus becomes one retrieval unit regardless of token budget',
      ]),
      answerIndex: 0,
      explanation: 'Multi-hop questions require evidence coverage across multiple facts. Candidate selection should optimize complementary coverage, not only independent similarity scores.',
      misconceptionTested: 'The highest-scoring single chunk is sufficient for every RAG question.',
      relatedComparison: 'Single-fact retrieval versus multi-hop complementary evidence packing.',
    }),
    Object.freeze({
      id: 'rag-chunk-context-budget-calculation',
      level: 'calculation',
      scenario: 'A model has a 16,000-token context window. System instructions plus the user query consume 2,500 tokens, and the application reserves 2,000 tokens for the generated answer.',
      prompt: 'What is the maximum evidence budget before any additional safety margin?',
      choices: Object.freeze([
        '11,500 tokens',
        '13,500 tokens',
        '14,000 tokens',
      ]),
      answerIndex: 0,
      explanation: 'The evidence budget is 16,000 - 2,500 - 2,000 = 11,500 tokens. Retrieval top-k does not increase the model context window; the packer must fit selected evidence inside the remaining budget.',
      misconceptionTested: 'The full context window can be used for retrieved chunks without reserving space for instructions, the query, and model output.',
      relatedComparison: 'Retrieved candidate count versus actual token budget available for packed evidence.',
    }),
    Object.freeze({
      id: 'rag-chunk-retrieved-but-not-packed',
      level: 'diagnosis',
      scenario: 'Offline inspection shows the exact answer chunk is retrieved at rank 7 with top-k=10. The generator still cannot answer. The packed prompt contains only ranks 1-5 because those chunks consumed the entire evidence budget.',
      prompt: 'Where is the primary failure in this trace?',
      choices: Object.freeze([
        'Context packing or reranking after retrieval, because the required chunk was retrieved but never reached the model',
        'Initial retrieval recall, because rank 7 means the retriever failed to retrieve the chunk',
        'Fine-tuning, because any missing RAG answer requires updating model weights',
      ]),
      answerIndex: 0,
      explanation: 'Retrieval succeeded: the evidence exists in the candidate set. The failure occurs later when ranking or packing excludes it from the actual prompt. Debugging should compare retrieved candidates with packed context before changing the retriever.',
      misconceptionTested: 'Any grounded-answer failure should be blamed on retrieval even when the required evidence was retrieved successfully.',
      relatedComparison: 'Candidate retrieval recall versus final packed-context evidence coverage.',
    }),
  ]),
});
