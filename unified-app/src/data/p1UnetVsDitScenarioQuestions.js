export const P1_UNET_VS_DIT_SCENARIOS_BY_LESSON = Object.freeze({
  'unet-vs-dit': Object.freeze([
    Object.freeze({
      id: 'unet-skip-detail-loss-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'unet-multiscale-bottleneck-vs-skip-detail',
      scenario: 'A diffusion U-Net keeps its downsampling and upsampling path, but an experiment removes every encoder-to-decoder skip connection. Global composition remains recognizable, while small text strokes and object boundaries become noticeably worse.',
      prompt: 'Which mechanism best explains this regression?',
      choices: Object.freeze([
        'Removing skips forces fine high-resolution detail to survive only through the compressed bottleneck instead of being carried directly to matching decoder scales',
        'Removing skips changes self-attention from quadratic to linear complexity',
        'Removing skips prevents the model from receiving the diffusion timestep',
      ]),
      answerIndex: 0,
      explanation: 'U-Net skips preserve high-resolution spatial features across the encoder-decoder bottleneck. Without them, the decoder must reconstruct fine detail from more compressed representations, so boundaries and small structures can degrade even when coarse layout survives.',
      misconceptionTested: 'U-Net skip connections only make optimization easier and do not materially preserve spatial detail.',
    }),
    Object.freeze({
      id: 'dit-global-mixing-architecture-choice',
      level: 'decision',
      relatedComparison: 'local-convolutional-bias-vs-global-token-mixing',
      scenario: 'A large-scale latent diffusion project repeatedly fails on prompts where distant parts of an image must obey one global relation, such as matching the color of an object on the far left with a symbol on the far right. Compute and training data are both plentiful.',
      prompt: 'Which architectural property makes a DiT-style backbone an especially relevant experiment?',
      choices: Object.freeze([
        'Self-attention gives distant latent patch tokens a direct global mixing path instead of relying only on progressively enlarged convolutional receptive fields',
        'DiT eliminates the need for timestep conditioning during diffusion',
        'DiT guarantees lower compute than a U-Net at every spatial resolution',
      ]),
      answerIndex: 0,
      explanation: 'A DiT can directly mix distant patch-token information through self-attention. That does not make it universally better or cheaper, but it is a meaningful architectural hypothesis when long-range consistency is a recurring failure and scale is available.',
      misconceptionTested: 'DiT is preferable only because it is newer, rather than because its token-attention structure changes information flow.',
    }),
    Object.freeze({
      id: 'unet-dit-data-scale-choice',
      level: 'decision',
      relatedComparison: 'image-inductive-bias-vs-data-driven-scaling',
      scenario: 'Two teams must choose a denoiser backbone. Team A has a modest domain-specific image dataset and a tight training budget. Team B has a very large diverse dataset and substantial accelerator capacity. Both can run the same diffusion objective and sampler.',
      prompt: 'Which starting hypothesis best matches the architecture trade-off?',
      choices: Object.freeze([
        'Team A should seriously consider U-Net locality/multiscale bias for sample efficiency, while Team B can more plausibly benefit from DiT scale and globally learned token interactions',
        'Both teams should always choose DiT because transformer architectures dominate regardless of data and compute regime',
        'Both teams should always choose U-Net because convolution makes global structure impossible for transformers',
      ]),
      answerIndex: 0,
      explanation: 'Convolutional locality is a useful image-specific inductive bias when data and compute are constrained. Transformer-style backbones can become highly competitive at larger scale, where more structure can be learned from data. This is a starting hypothesis to validate, not a universal winner rule.',
      misconceptionTested: 'Backbone choice is independent of data scale and compute budget.',
    }),
    Object.freeze({
      id: 'unet-dit-output-contract-debugging',
      level: 'diagnosis',
      relatedComparison: 'internal-backbone-representation-vs-diffusion-output-contract',
      scenario: 'A refactor replaces a latent U-Net with a DiT. The noisy latent has shape [B, 4, 64, 64], but after transformer processing the implementation returns only a pooled [B, d_model] vector to the sampler. Training immediately fails because the denoising target is spatial.',
      prompt: 'What contract was violated?',
      choices: Object.freeze([
        'Regardless of backbone internals, the model must project/unpatchify its representation into a denoising tensor compatible with the noisy latent target shape',
        'A DiT sampler requires a single pooled classification vector instead of a spatial denoising prediction',
        'Changing from U-Net to DiT means the diffusion timestep should be removed from the model inputs',
      ]),
      answerIndex: 0,
      explanation: 'U-Net feature maps and DiT patch tokens are different internal representations, but both ultimately serve the same diffusion denoising interface. The sampler/training objective needs a noise, velocity, or related prediction aligned with the latent state dimensions.',
      misconceptionTested: 'Changing the denoiser backbone also changes the fundamental spatial output contract expected by diffusion training and sampling.',
    }),
    Object.freeze({
      id: 'unet-dit-fair-ablation-design',
      level: 'design',
      relatedComparison: 'architecture-effect-vs-confounded-system-comparison',
      scenario: 'A benchmark claims DiT is better than U-Net. The DiT run used twice the training data, a newer VAE, a different sampler, higher image resolution, and four times the training compute. The only reported metric is final image quality.',
      prompt: 'What is the best next experiment if the goal is to isolate the backbone effect?',
      choices: Object.freeze([
        'Hold dataset, diffusion objective, VAE/latent resolution, sampler, evaluation protocol, and compute budget as comparable as practical, then vary the backbone',
        'Keep every difference because architecture comparisons are valid as long as the final images look better',
        'Compare parameter count only; matching the data, sampler, and evaluation protocol is unnecessary',
      ]),
      answerIndex: 0,
      explanation: 'A backbone comparison is confounded if training data, latent representation, sampler, resolution, and compute all change at once. A controlled ablation cannot make architectures identical, but it should hold surrounding system choices fixed enough to attribute observed changes meaningfully.',
      misconceptionTested: 'Any end-to-end quality difference can be attributed to U-Net versus DiT even when the rest of the training and sampling stack changes simultaneously.',
    }),
  ]),
});
