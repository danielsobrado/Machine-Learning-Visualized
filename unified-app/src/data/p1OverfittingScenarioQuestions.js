export const P1_OVERFITTING_SCENARIOS_BY_LESSON = Object.freeze({
  overfitting: [
    {
      id: 'overfit-validation-divergence-checkpoint-worked',
      level: 'decision',
      relatedComparison: 'final-training-loss-vs-best-validation-checkpoint',
      scenario: 'A neural model has validation losses by epoch 1 through 6 of 0.62, 0.49, 0.41, 0.38, 0.42, and 0.47. Training loss falls every epoch from 0.70 to 0.18.',
      prompt: 'Which checkpoint is the strongest candidate from these curves?',
      choices: [
        'Epoch 4 because validation loss is lowest there even though training loss continues improving afterward',
        'Epoch 6 because the final epoch always has the best learned representation when training loss is lowest',
        'Epoch 1 because the largest train-validation agreement is always preferred to validation quality',
      ],
      answerIndex: 0,
      explanation: 'Validation loss reaches its minimum at epoch 4 and then worsens while training loss keeps falling. That is the classic onset of overfitting: later optimization improves seen-data fit but harms held-out performance. The checkpoint near the validation minimum is the stronger candidate.',
      misconceptionTested: 'The checkpoint with the lowest training loss is automatically the best deployment checkpoint even after validation performance deteriorates.',
    },
    {
      id: 'overfit-early-stopping-patience-decision',
      level: 'decision',
      relatedComparison: 'single-noisy-epoch-vs-patience-window',
      scenario: 'Validation loss improves for many epochs, then rises slightly for one epoch, improves to a new best value on the next epoch, and only afterward rises for five consecutive epochs. Training loss keeps decreasing throughout.',
      prompt: 'Why is an early-stopping patience window preferable to stopping on the first validation increase?',
      choices: [
        'Validation is noisy, so patience avoids reacting to one fluctuation while still stopping when deterioration persists',
        'Patience guarantees the final epoch will always be selected regardless of validation behavior',
        'Early stopping should ignore validation completely and use training loss only',
      ],
      answerIndex: 0,
      explanation: 'One validation increase can be sampling or optimization noise. A patience rule allows short reversals while still terminating when the held-out trend consistently stops improving. The best checkpoint should still track the best validation value observed under the declared protocol.',
      misconceptionTested: 'Any single upward movement in validation loss proves overfitting has permanently begun and should trigger an immediate stop.',
    },
    {
      id: 'overfit-near-duplicate-split-contamination',
      level: 'diagnosis',
      relatedComparison: 'memorization-across-splits-vs-independent-generalization',
      scenario: 'A text classifier reports 98% validation accuracy. Investigation finds that 35% of validation reviews are exact or near-duplicate copies of training reviews from the same original customer interaction.',
      prompt: 'What should be fixed before interpreting the validation score as generalization?',
      choices: [
        'Deduplicate or group related examples before splitting so memorized content cannot cross the evaluation boundary',
        'Keep the split because duplicates make validation more representative of independent unseen examples',
        'Increase model capacity so it can memorize the duplicate reviews even more consistently',
      ],
      answerIndex: 0,
      explanation: 'Near-duplicate content crossing train and validation lets the model succeed through memorization of almost the same example. The split should respect the unit of independence, often by grouping related records before partitioning.',
      misconceptionTested: 'A high held-out score demonstrates generalization even when near-identical examples appear on both sides of the split.',
    },
    {
      id: 'overfit-learning-curve-more-data-decision',
      level: 'diagnosis',
      relatedComparison: 'variance-limited-gap-vs-more-representative-data',
      scenario: 'Using 5,000 examples, training error is 2% and validation error is 14%. With 25,000 examples, they become 4% and 9%. With 100,000 examples, they become 5% and 7%. Model family and preprocessing are unchanged.',
      prompt: 'What does the learning curve most strongly suggest?',
      choices: [
        'The model was strongly variance-limited at small sample sizes, and more representative data is narrowing the generalization gap',
        'The rising training error proves the larger datasets are making the model more overfit',
        'More data is useless because training error did not keep falling toward zero',
      ],
      answerIndex: 0,
      explanation: 'The train-validation gap shrinks from 12 points to 5 and then 2 while validation error improves substantially. Training error can rise as memorizing a larger dataset becomes harder; the important evidence is that held-out performance improves and the gap narrows.',
      misconceptionTested: 'If training error rises as dataset size increases, adding data must be harming generalization.',
    },
    {
      id: 'overfit-small-validation-win-uncertainty',
      level: 'decision',
      relatedComparison: 'tiny-point-estimate-win-vs-score-uncertainty',
      scenario: 'A larger model scores 91.2% accuracy on a validation sample of 500 examples while a simpler model scores 90.8%. The larger model is materially slower and more expensive. No confidence interval, repeated split, or paired error analysis has been run.',
      prompt: 'What is the strongest next step before accepting the larger model?',
      choices: [
        'Quantify whether the 0.4 percentage-point validation difference is stable enough to justify the added complexity and cost',
        'Choose the larger model immediately because any positive held-out difference proves a real generalization improvement',
        'Ignore validation and choose whichever model has the lower training error',
      ],
      answerIndex: 0,
      explanation: 'Finite validation samples produce noisy point estimates. A tiny observed advantage may not be stable, especially when the alternative carries real serving cost. Uncertainty or repeated evidence should inform whether the extra complexity has demonstrated value.',
      misconceptionTested: 'Any positive difference on one validation sample is sufficient evidence that a more complex model generalizes better.',
    },
  ],
});
