function q(value) { return Object.freeze({ ...value, choices: Object.freeze([...value.choices]) }); }
function lesson(items) { return Object.freeze(items.map(q)); }

export const P1_NEURAL_TRAINING_REVIEW_SCENARIOS_BY_LESSON = Object.freeze({
  'neural-network': lesson([{
    id: 'review-neural-linear-stack-nonlinearity', level: 'mechanism',
    relatedComparison: 'stacked-linear-maps-vs-nonlinear-network',
    scenario: 'A network contains five dense layers but every activation function is removed. Each layer applies only an affine transformation to the output of the previous layer.',
    prompt: 'Why does adding more such layers not create a genuinely nonlinear decision function?',
    choices: ['A composition of affine maps is still an affine map, so depth alone cannot represent XOR-like nonlinear structure without nonlinear activations', 'Every extra matrix multiplication automatically creates a new nonlinear basis', 'Bias terms make the network nonlinear even when all activations are removed'],
    answerIndex: 0,
    explanation: 'Multiplying and adding affine transformations can be algebraically collapsed into one affine transformation. Depth becomes expressive only when nonlinear operations break that closure. Biases shift the affine map but do not change the fact that the overall function remains affine.',
    misconceptionTested: 'Deep stacks of linear or affine layers become nonlinear simply because many matrices and biases are composed.',
  }]),
  'computation-graph-backprop': lesson([{
    id: 'review-backprop-shared-parameter-accumulation', level: 'calculation',
    relatedComparison: 'single-path-gradient-vs-shared-parameter-gradient-sum',
    scenario: 'A scalar parameter w is reused in two branches: y = w*x1 + w*x2. The loss is L=y and x1=3, x2=5.',
    prompt: 'What gradient should backpropagation accumulate for dL/dw?',
    choices: ['8, because contributions from both uses of the shared parameter add: 3+5', '5, because only the last occurrence of w contributes', '15, because branch gradients must be multiplied together'],
    answerIndex: 0,
    explanation: 'Reverse-mode autodiff sums gradient contributions whenever multiple graph paths flow into the same leaf parameter. Here dy/dw from the first branch is x1=3 and from the second is x2=5, giving a total of 8. Overwriting rather than accumulating would silently produce an incorrect gradient.',
    misconceptionTested: 'A parameter reused in multiple graph locations receives only one branch gradient or the branch gradients should be multiplied.',
  }]),
  'gradient-problems': lesson([{
    id: 'review-gradient-clipping-symptom-vs-cause', level: 'diagnosis',
    relatedComparison: 'gradient-clipping-containment-vs-training-instability-root-cause',
    scenario: 'A recurrent model occasionally produces enormous gradient norms. Global-norm clipping prevents NaNs, but loss remains erratic and gradients hit the clipping threshold on most steps.',
    prompt: 'How should the team interpret clipping in this situation?',
    choices: ['Clipping contains the immediate update magnitude but does not prove the underlying instability is fixed; initialization, learning rate, normalization, sequence dynamics, or architecture still need diagnosis', 'Frequent clipping proves the model is now numerically healthy because all updates are bounded', 'Gradient clipping permanently changes the objective so exploding gradients cannot recur'],
    answerIndex: 0,
    explanation: 'Clipping is a safety mechanism that limits how large an update can become. If it activates constantly, the training process is still generating pathological gradients and the effective direction can be distorted. Root causes such as unstable recurrence, excessive learning rate, poor scaling, or initialization should still be investigated.',
    misconceptionTested: 'Bounding gradient norm is equivalent to eliminating the mechanism that caused exploding gradients.',
  }]),
  initialization: lesson([{
    id: 'review-init-he-vs-xavier-activation', level: 'decision',
    relatedComparison: 'he-initialization-vs-xavier-signal-preservation',
    scenario: 'Two deep MLPs use the same width. One uses ReLU activations and the other uses tanh. The team wants variance-preserving initial scales matched to the activation behavior.',
    prompt: 'Which pairing is the usual starting point and why?',
    choices: ['He initialization for ReLU and Xavier/Glorot for tanh, because their variance assumptions account differently for activation-induced signal loss', 'Xavier only for ReLU and He only for tanh because bounded activations require larger variance', 'Use identical zero initialization for both so every neuron starts symmetrically'],
    answerIndex: 0,
    explanation: 'ReLU discards roughly half of a symmetric preactivation distribution, motivating the larger He variance scale based on fan-in. Xavier/Glorot is designed to keep forward and backward signal variance balanced for roughly symmetric activations such as tanh. Exact choices can vary, but activation-aware scaling is the key principle.',
    misconceptionTested: 'Initialization scale is independent of activation function, or zero initialization is acceptable for symmetry breaking in dense hidden layers.',
  }]),
  relu: lesson([{
    id: 'review-relu-dead-unit-update', level: 'diagnosis',
    relatedComparison: 'relu-zero-negative-gradient-vs-leaky-relu-negative-slope',
    scenario: 'A ReLU neuron receives a large parameter update that pushes its preactivation below zero for every example in the training set. Its upstream gradient is nonzero, but the local ReLU derivative is zero on all those examples.',
    prompt: 'Why can the unit become effectively dead?',
    choices: ['The zero local derivative blocks parameter gradients through that unit, so it may never receive an update that moves it back into the active region', 'Negative preactivations make ReLU gradients very large and force recovery', 'A dead ReLU is caused only by dropout and cannot arise from optimization'],
    answerIndex: 0,
    explanation: 'For standard ReLU, negative preactivation produces zero output and zero local derivative. If all training examples stay negative, gradients through that unit vanish and its incoming weights and bias may stop changing through that path. Large learning rates or poor initialization can make this more likely.',
    misconceptionTested: 'A ReLU with consistently negative preactivation automatically receives a corrective gradient that restores activity.',
  }]),
  'leaky-relu': lesson([{
    id: 'review-leaky-relu-dead-unit-tradeoff', level: 'comparison',
    relatedComparison: 'zero-negative-slope-vs-small-negative-slope',
    scenario: 'A team replaces ReLU with Leaky ReLU in a network where many units spend long periods with negative preactivations.',
    prompt: 'What benefit and tradeoff should they expect from the negative slope?',
    choices: ['Negative-side gradients can keep units trainable, but negative activations now pass through instead of enforcing exact zero sparsity', 'Leaky ReLU removes all gradient explosion risk and preserves exact ReLU sparsity', 'Leaky ReLU behaves identically to ReLU during both forward and backward passes'],
    answerIndex: 0,
    explanation: 'A small nonzero slope means negative preactivations still propagate some signal and gradient, reducing the chance of permanently dead units. The tradeoff is that negative outputs are no longer clamped exactly to zero, so activation sparsity and representation behavior differ from ordinary ReLU.',
    misconceptionTested: 'Leaky ReLU is a free fix that preserves every property of ReLU while only changing optimization stability.',
  }]),
  'dropout-batchnorm': lesson([{
    id: 'review-dropout-batchnorm-train-eval-contract', level: 'diagnosis',
    relatedComparison: 'training-mode-stochasticity-batch-statistics-vs-eval-mode-determinism-running-statistics',
    scenario: 'A classifier is evaluated while the model accidentally remains in training mode. Dropout continues masking units and BatchNorm continues using current mini-batch statistics instead of stored inference statistics.',
    prompt: 'Why can this create a silent inference bug even when the weights are correct?',
    choices: ['Both layers change behavior between training and evaluation, so leaving training mode active makes predictions stochastic and batch-dependent in a way the deployed model was not intended to use', 'Only dropout changes mode; BatchNorm is mathematically identical in training and evaluation', 'Train/eval mode affects optimizer gradients only and cannot alter forward predictions'],
    answerIndex: 0,
    explanation: 'Dropout is normally disabled at inference, while BatchNorm typically switches from current batch estimates to accumulated running statistics. Forgetting evaluation mode therefore changes the forward function itself: predictions may vary across calls and depend on which examples share the batch.',
    misconceptionTested: 'Training versus evaluation mode is only an optimization concern and cannot change the network function used for inference.',
  }]),
  'layer-normalization': lesson([{
    id: 'review-layernorm-batch-size-one', level: 'comparison',
    relatedComparison: 'per-example-feature-normalization-vs-batch-statistics',
    scenario: 'A sequence model must serve one request at a time. The team compares LayerNorm with BatchNorm and asks why LayerNorm remains natural at batch size one.',
    prompt: 'What statistical axis distinction explains the difference?',
    choices: ['LayerNorm normalizes features within each example/token representation, so it does not require a population of examples in the current batch', 'LayerNorm secretly aggregates statistics across previous batches', 'BatchNorm and LayerNorm use identical axes, so batch size cannot matter'],
    answerIndex: 0,
    explanation: 'LayerNorm computes its mean and variance across feature dimensions for the current representation, independently for each example. BatchNorm relies on statistics across a batch dimension during training. That makes LayerNorm well suited to variable batch sizes and sequence models, including batch size one.',
    misconceptionTested: 'All normalization layers require multiple examples in a batch to compute meaningful statistics.',
  }]),
  'residual-stream': lesson([{
    id: 'review-residual-identity-gradient-path', level: 'mechanism',
    relatedComparison: 'direct-identity-path-vs-transform-only-stack',
    scenario: 'A residual block computes y=x+F(x). During backpropagation the derivative includes an identity contribution in addition to the derivative through F.',
    prompt: 'Why can this structure make deep optimization easier?',
    choices: ['Information and gradients have a direct additive path through the identity term even when F is initially small or poorly conditioned', 'The residual connection removes all nonlinear transformations from the network', 'The identity term forces every layer to learn exactly zero'],
    answerIndex: 0,
    explanation: 'Differentiating y=x+F(x) gives an identity component plus the transformed path. This provides a route for signals and gradients that does not require every intermediate transform to preserve them perfectly. The block can also represent near-identity behavior easily by learning a small residual.',
    misconceptionTested: 'Residual connections help only by adding parameters rather than by creating an explicit identity information and gradient path.',
  }]),
  conv2d: lesson([{
    id: 'review-conv-output-shape-dilation', kind: 'visual-state', visualState: Object.freeze({ inputSize: 32, effectiveKernel: 5, stride: 2, padding: 2, outputSize: 16 }), level: 'calculation',
    relatedComparison: 'input-spatial-size-vs-effective-kernel-stride-padding',
    scenario: 'A 2D convolution receives a 32×32 input, uses a 3×3 kernel, dilation 2, padding 2, and stride 2. The effective kernel size is dilation×(k-1)+1 = 5.',
    prompt: 'What spatial output size results in each dimension using floor((n+2p-k_eff)/s)+1?',
    choices: ['16, because floor((32+4-5)/2)+1 = 16', '15, because dilation should be ignored when computing output size', '32, because padding always preserves the input size'],
    answerIndex: 0,
    explanation: 'Dilation expands the effective 3-wide kernel to 5. Substituting into the convolution size formula gives floor(31/2)+1 = 15+1 = 16. Padding preserves size only for particular stride and effective-kernel combinations; stride two still downsamples here.',
    misconceptionTested: 'Kernel dilation does not affect shape arithmetic, or any positive padding automatically preserves spatial dimensions.',
  }]),
  'max-pooling': lesson([{
    id: 'review-maxpool-invariance-localization-tradeoff', level: 'comparison',
    relatedComparison: 'local-translation-tolerance-vs-exact-spatial-localization',
    scenario: 'A max-pooling layer maps each 2×2 region to one value. A strong activation can move slightly within that region without changing the pooled output.',
    prompt: 'What benefit and cost does this illustrate?',
    choices: ['Pooling can add local translation tolerance while discarding exact within-window spatial information', 'Pooling preserves every pixel location while only reducing channel count', 'Pooling makes the representation more sensitive to one-pixel shifts'],
    answerIndex: 0,
    explanation: 'Taking the maximum summarizes whether a feature was strongly present in a neighborhood rather than exactly where it occurred. That can make nearby shifts less important, but the precise location and other values in the window are lost. Downsampling is therefore a compression tradeoff, not free invariance.',
    misconceptionTested: 'Pooling reduces tensor size without discarding spatial information or affecting invariance properties.',
  }]),
  optimizers: lesson([{
    id: 'review-adamw-vs-l2-adaptive-scaling', level: 'mechanism',
    relatedComparison: 'decoupled-weight-decay-vs-l2-gradient-penalty-under-adaptive-optimizer',
    scenario: 'An engineer assumes adding an L2 penalty to Adam is always mathematically identical to AdamW because both shrink weights. Adam rescales gradient components using adaptive second-moment statistics.',
    prompt: 'Why are the two generally not equivalent?',
    choices: ['An L2 penalty enters the gradient and is therefore affected by Adam\'s adaptive coordinate scaling, while AdamW applies weight decay as a separate parameter shrinkage step', 'AdamW removes all regularization and only changes momentum', 'They differ only because AdamW uses a different loss function chosen by the model'],
    answerIndex: 0,
    explanation: 'For plain SGD, L2 regularization and multiplicative weight decay can coincide under common settings. With adaptive optimizers, adding lambda*w to the gradient means that term is normalized by the same per-parameter statistics as other gradients. AdamW decouples the shrinkage from that adaptive scaling, producing a different update.',
    misconceptionTested: 'L2 regularization and weight decay are universally interchangeable regardless of the optimizer update rule.',
  }]),
});
