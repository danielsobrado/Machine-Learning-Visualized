export const P1_CLASSICAL_ML_GAP_SCENARIOS_BY_LESSON = Object.freeze({
  'classification-metrics': [
    {
      id: 'metrics-calibration-cost-threshold',
      level: 'comparison',
      relatedComparison: 'classification-metrics-vs-calibration',
      scenario: 'Two classifiers have nearly identical ROC and precision-recall curves. Model A is well calibrated, while Model B systematically reports probabilities that are too high. The business converts predicted probability into an expected false-positive versus false-negative cost.',
      prompt: 'Which model property matters for choosing a cost-based probability threshold?',
      choices: ['Calibration matters because the probability values must correspond to event frequencies for cost calculations to be meaningful', 'Only ROC AUC matters because ranking quality guarantees probability accuracy', 'Calibration is irrelevant once precision and recall have been measured at one threshold'],
      answerIndex: 0,
      explanation: 'Ranking metrics describe ordering, while calibration describes whether probability values are numerically trustworthy. A threshold derived from expected business cost depends on calibrated probabilities, not ranking quality alone.',
      misconceptionTested: 'Good discrimination automatically means predicted probabilities are calibrated.',
    },
  ],
  'train-validation-test-split': [
    {
      id: 'split-reused-selection-contamination',
      level: 'diagnosis',
      relatedComparison: 'validation-selection-vs-final-test',
      scenario: 'A team trains 250 candidate pipelines and repeatedly chooses features, hyperparameters, and architectures from the same validation score. The final reported number is also that validation score.',
      prompt: 'What is missing from the evaluation design?',
      choices: ['A final untouched test set used only after model selection is complete', 'More reuse of the same validation set so its estimate becomes unbiased', 'A random split of the validation set back into the training set'],
      answerIndex: 0,
      explanation: 'Repeated model-selection decisions adapt to validation noise. A final untouched test set provides a cleaner estimate after the development process has finished using training and validation feedback.',
      misconceptionTested: 'A validation set remains an unbiased final benchmark no matter how often it guides model selection.',
    },
  ],
  'cross-validation': [
    {
      id: 'cv-nested-model-selection',
      level: 'design',
      relatedComparison: 'ordinary-cv-vs-nested-cv',
      scenario: 'You compare 60 hyperparameter configurations with cross-validation and then want an estimate of how the complete tuning procedure will generalize to unseen data.',
      prompt: 'Which design best separates tuning from performance estimation?',
      choices: ['Nested cross-validation with inner folds for model selection and outer folds for evaluation', 'Report the best inner-fold score as the final generalization estimate', 'Tune on every fold and then average only the winning fold'],
      answerIndex: 0,
      explanation: 'Nested CV keeps model selection inside each outer training partition. The outer validation folds then estimate the performance of the tuning procedure rather than reusing the same folds for both selection and evaluation.',
      misconceptionTested: 'The best score observed during hyperparameter search is an unbiased estimate of the selected model procedure.',
    },
    {
      id: 'cv-rolling-origin-time-series',
      level: 'design',
      relatedComparison: 'shuffled-kfold-vs-rolling-origin',
      scenario: 'A monthly demand model will always predict future months from data available up to the prediction date. Seasonality and business conditions change gradually over time.',
      prompt: 'Which cross-validation structure best matches deployment?',
      choices: ['Rolling-origin or expanding-window validation that trains on the past and validates on later periods', 'Random shuffled K-fold that freely mixes future and past months', 'Leave-one-out CV after sorting the rows back into chronological order'],
      answerIndex: 0,
      explanation: 'Time-series validation must preserve chronology. Rolling-origin evaluation repeatedly trains on earlier periods and validates on later periods, exposing horizon and regime sensitivity without future-to-past leakage.',
      misconceptionTested: 'Ordinary shuffled K-fold is interchangeable with time-aware cross-validation for forecasting.',
    },
  ],
  overfitting: [
    {
      id: 'overfit-final-untouched-test',
      level: 'application',
      relatedComparison: 'development-validation-vs-final-test',
      scenario: 'After months of feature engineering and tuning, the team has repeatedly inspected validation failures and adjusted the model. They now need one number for an external launch decision.',
      prompt: 'How should the final test set be used?',
      choices: ['Evaluate it once after development choices are frozen, and avoid tuning to its result', 'Inspect it after every experiment because it is called the test set', 'Merge it into training before reporting its score so the model sees all available data'],
      answerIndex: 0,
      explanation: 'The final test set should remain outside the feedback loop. Once its results influence further model choices, it becomes another development set and loses much of its value as an independent check.',
      misconceptionTested: 'A dataset is protected from overfitting merely because it is named test.',
    },
  ],
  'bias-variance-tradeoff': [
    {
      id: 'bias-variance-regularization-tradeoff',
      level: 'comparison',
      relatedComparison: 'weak-vs-strong-regularization',
      scenario: 'A flexible model has extremely low training error but unstable validation performance across samples. Increasing regularization raises training error slightly while reducing the train-validation gap and fold-to-fold variance.',
      prompt: 'How should this change be interpreted?',
      choices: ['Regularization added some bias while reducing variance, improving the overall generalization trade-off', 'Any increase in training error proves the model became worse', 'Regularization can reduce variance only when labels are noiseless'],
      answerIndex: 0,
      explanation: 'Regularization deliberately constrains flexibility. A small increase in bias can be worthwhile when it substantially reduces variance and improves expected performance on unseen data.',
      misconceptionTested: 'Lower training error always means a better bias-variance trade-off.',
    },
  ],
  regularization: [
    {
      id: 'regularization-early-stopping-augmentation',
      level: 'comparison',
      relatedComparison: 'penalty-dropout-depth-augmentation-early-stopping',
      scenario: 'A linear model uses L2 weight decay, a neural network uses dropout and data augmentation, a boosted model limits tree complexity, and another neural network stops when validation loss stops improving.',
      prompt: 'What common principle connects these different techniques?',
      choices: ['They constrain effective fitting capacity or discourage fitting training-specific noise, even though the mechanism differs', 'They all add the same mathematical L2 penalty to the objective', 'They guarantee identical bias and variance for every model family'],
      answerIndex: 0,
      explanation: 'Regularization is broader than coefficient penalties. Weight penalties, dropout, augmentation, tree constraints, and early stopping all bias learning away from overly flexible fits through different mechanisms.',
      misconceptionTested: 'Only L1 and L2 penalties count as regularization.',
    },
  ],
  'k-means': [
    {
      id: 'kmeans-choose-k-stability',
      level: 'design',
      relatedComparison: 'elbow-silhouette-stability-domain',
      scenario: 'Inertia decreases for every larger k, the elbow is weak, and silhouette scores for k = 4 and k = 5 are very close. Both solutions are plausible for the product taxonomy.',
      prompt: 'How should k be chosen?',
      choices: ['Combine quantitative diagnostics with restart stability and domain usefulness instead of treating one curve as a proof', 'Choose the largest k because inertia must always be minimized without penalty', 'Choose k = 2 because every clustering problem has a binary natural structure'],
      answerIndex: 0,
      explanation: 'No single diagnostic universally identifies a true k. Elbow and silhouette scores are useful evidence, but stability across restarts and whether the partition serves the real task also matter.',
      misconceptionTested: 'The elbow method always reveals one objectively correct number of clusters.',
    },
    {
      id: 'kmeans-centroid-drift',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { referenceCentroids: 'stable last quarter', currentCentroids: 'two move substantially', assignmentRate: '28% of points change cluster' },
      scenario: 'Visual state: using the same preprocessing and k, two production centroids move far from their reference locations and 28% of comparable points change cluster assignment.',
      prompt: 'What should be investigated before treating the new cluster labels as equivalent?',
      choices: ['Distribution drift and cluster stability because centroid meaning may have changed', 'Nothing, because fixed k guarantees cluster identities remain stable over time', 'Only the ordering of the centroid array in memory'],
      answerIndex: 0,
      explanation: 'K-means cluster identities are data-dependent. Material centroid movement and assignment changes can indicate distribution drift or unstable structure, so cluster semantics should be revalidated.',
      misconceptionTested: 'Keeping k fixed guarantees stable cluster meaning across changing data.',
    },
  ],
  'knn-naive-bayes-svm': [
    {
      id: 'classifier-family-model-choice',
      level: 'comparison',
      relatedComparison: 'knn-naive-bayes-svm-model-choice',
      scenario: 'A text classifier has 80,000 sparse features, limited training data, and a strict prediction-latency budget. A kNN prototype is slow at inference because every query compares against many stored examples.',
      prompt: 'Which comparison is most sensible before deployment?',
      choices: ['Compare a sparse linear SVM and an appropriate Naive Bayes baseline against kNN using validation quality and serving cost', 'Choose kNN automatically because it stores the most training examples', 'Choose the model with the highest training accuracy without measuring inference'],
      answerIndex: 0,
      explanation: 'Model choice depends on representation, sample size, decision boundary, calibration needs, and serving constraints. Sparse text often warrants evaluating linear margin and probabilistic baselines rather than assuming a nearest-neighbor method is best.',
      misconceptionTested: 'One classifier family is universally best independent of feature geometry and serving constraints.',
    },
    {
      id: 'classifier-boundary-brittleness',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { model: '1-NN', localLabels: 'one mislabeled point inside opposite-class region', boundary: 'small isolated island around noisy point' },
      scenario: 'Visual state: a 1-NN classifier creates a tiny class island around one mislabeled training point inside an otherwise uniform region.',
      prompt: 'What does this show about the decision boundary?',
      choices: ['Very local methods can form brittle boundaries around noisy examples, so k and validation robustness matter', 'Nearest-neighbor boundaries are always smoother than margin-based boundaries', 'The isolated island proves the minority class is globally underrepresented'],
      answerIndex: 0,
      explanation: 'With k = 1, each training point can control a local region, making label noise produce jagged or brittle boundaries. Larger k or a different model family may trade local flexibility for stability.',
      misconceptionTested: 'Nearest-neighbor classifiers are automatically robust to mislabeled local examples.',
    },
  ],
  'tree-ensembles': [
    {
      id: 'tree-target-like-leakage',
      level: 'diagnosis',
      scenario: 'A boosted-tree churn model reports near-perfect validation accuracy. Its top feature is cancellation_reason, but that field is populated only after the customer has cancelled.',
      prompt: 'What should be checked first?',
      choices: ['Feature availability at prediction time because the dominant split may be target-like leakage', 'Increase tree depth because the feature is highly predictive', 'Keep the feature because tree ensembles are immune to leakage'],
      answerIndex: 0,
      explanation: 'Tree ensembles readily exploit highly predictive leaked features. Feature importance can therefore highlight leakage rather than legitimate signal, so point-in-time availability must be verified before trusting the score.',
      misconceptionTested: 'A highly important tree feature is automatically a valid causal or deployable predictor.',
    },
  ],
  'recommender-systems-ranking-track': [
    {
      id: 'rec-matrix-factorization',
      level: 'mechanism',
      relatedComparison: 'matrix-factorization-vs-content-signals',
      scenario: 'A user-item interaction matrix is extremely sparse. The recommender learns a low-dimensional vector for each user and item so their dot product predicts preference.',
      prompt: 'What technique is being used?',
      choices: ['Matrix factorization that represents users and items with latent factors', 'K-means on item IDs with no interaction objective', 'A confusion matrix over recommendation positions'],
      answerIndex: 0,
      explanation: 'Matrix factorization approximates a sparse interaction matrix with lower-dimensional user and item embeddings. Their interaction, commonly a dot product, models latent preference structure.',
      misconceptionTested: 'Collaborative filtering requires explicit content features for every user and item.',
    },
    {
      id: 'rec-offline-online-metrics',
      level: 'diagnosis',
      relatedComparison: 'offline-ranking-vs-online-product-metrics',
      scenario: 'A new ranker improves offline nDCG on logged data by 6%, but an online experiment shows no improvement in long-term engagement and slightly worse item diversity.',
      prompt: 'What is the correct conclusion?',
      choices: ['Offline ranking gains are useful evidence but do not replace online product and ecosystem evaluation', 'The online experiment must be ignored because nDCG improved', 'nDCG directly guarantees long-term engagement and diversity'],
      answerIndex: 0,
      explanation: 'Offline metrics measure ranking quality on a fixed logged dataset and can miss exposure effects, feedback loops, diversity, latency, and user behavior. Online evaluation tests the deployed system outcome.',
      misconceptionTested: 'Improving an offline recommender metric guarantees the desired online business outcome.',
    },
  ],
});

export function getP1ClassicalMlGapScenariosForLesson(lessonId) {
  return P1_CLASSICAL_ML_GAP_SCENARIOS_BY_LESSON[lessonId] || [];
}
