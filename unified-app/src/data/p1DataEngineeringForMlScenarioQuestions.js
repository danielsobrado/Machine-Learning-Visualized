export const P1_DATA_ENGINEERING_FOR_ML_SCENARIOS_BY_LESSON = Object.freeze({
  'data-engineering-for-ml-track': [
    {
      id: 'de-event-vs-availability-time-worked',
      level: 'decision',
      relatedComparison: 'event-time-vs-availability-time-at-prediction-cutoff',
      scenario: 'A transaction occurred at 09:50, but the upstream batch delivered it to the feature store at 10:10. A historical model prediction was made at 10:00. The warehouse later records the transaction with event_time = 09:50 and availability_time = 10:10.',
      prompt: 'May that transaction be used in the 10:00 training feature row?',
      choices: [
        'No; although its event time is before prediction, it was not available to the model until 10:10',
        'Yes; event time alone determines point-in-time correctness, so processing delay is irrelevant',
        'Yes, but only if the final test set also includes the transaction',
      ],
      answerIndex: 0,
      explanation: 'Historical training must reproduce what serving could actually know. The transaction existed in the real world at 09:50 but had not crossed the data-system availability boundary by 10:00. Joining it into the row would create time-travel leakage.',
      misconceptionTested: 'A feature is safe whenever its event timestamp is before prediction, even if the data had not arrived in the serving system yet.',
    },
    {
      id: 'de-feature-freshness-slo-decision',
      level: 'decision',
      relatedComparison: 'schema-valid-vs-freshness-valid-feature',
      scenario: 'A fraud feature contract requires account aggregates to be no more than 15 minutes old. The online feature has the correct name, type, and range, but telemetry shows its newest source update is 47 minutes old for 63% of requests.',
      prompt: 'How should the serving pipeline treat this condition?',
      choices: [
        'As a freshness-SLO violation that should alert and invoke the defined fallback, degradation, or blocking policy rather than silently treating the values as healthy',
        'As healthy data because schema and numeric ranges still pass',
        'As model drift that can only be fixed by retraining the estimator',
      ],
      answerIndex: 0,
      explanation: 'Freshness is part of the feature contract independently of schema. A structurally valid value can still be too stale for the decision. The pipeline should surface the breach and follow an explicit operational policy instead of silently serving outdated state.',
      misconceptionTested: 'A feature is production-valid whenever its schema and range are correct, regardless of how stale the underlying value has become.',
    },
    {
      id: 'de-semantic-unit-schema-evolution-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'type-compatible-schema-change-vs-semantic-contract-break',
      scenario: 'An upstream payment service changes amount from dollars to cents but keeps the column name amount and data type FLOAT. The ML pipeline schema check still passes, while typical values jump from roughly 20-500 to 2,000-50,000 overnight.',
      prompt: 'What failed even though the structural schema stayed compatible?',
      choices: [
        'The semantic data contract; unit/range expectations should detect and block or route the incompatible change',
        'Nothing, because identical column names and types guarantee feature semantics are unchanged',
        'Only model calibration, because unit changes cannot be detected before inference',
      ],
      answerIndex: 0,
      explanation: 'Schema compatibility is necessary but not sufficient. Units, ranges, and meaning are part of the feature contract. Range/distribution checks plus versioned semantics can catch a type-compatible change before it silently shifts model inputs by 100×.',
      misconceptionTested: 'Schema evolution is safe whenever column names and primitive data types remain unchanged.',
    },
    {
      id: 'de-backfill-time-travel-leakage-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'current-state-backfill-vs-historical-as-of-reconstruction',
      scenario: 'A team backfills last year’s training rows after correcting a feature job. The new query joins every historical row to today’s latest customer_tier value instead of the tier version that existed at each prediction cutoff. Customer tiers often change after purchases and outcomes occur.',
      prompt: 'What is the primary problem with this backfill?',
      choices: [
        'It destroys historical point-in-time correctness by injecting later customer state into earlier training examples',
        'Backfills are exempt from leakage rules because they run after outcomes are known',
        'Using the newest dimension value is always preferable because current warehouse data is more accurate',
      ],
      answerIndex: 0,
      explanation: 'A backfill must reconstruct the original information boundary, not rewrite history with current state. Versioned or effective-dated records and as-of joins are needed so each historical row receives the value available at its original cutoff.',
      misconceptionTested: 'Historical backfills may safely use the latest current-state dimension values because the recomputation happens offline.',
    },
  ],
});
