export const P1_DIFFUSION_BASICS_SAMPLING_SCENARIOS_BY_LESSON = Object.freeze({
  'diffusion-basics': Object.freeze([
    Object.freeze({
      id: 'diffusion-timestep-conditioning-mismatch',
      level: 'diagnosis',
      scenario: 'A denoiser is trained on samples spanning the full noise schedule, but a bug replaces every timestep embedding with the same constant vector. Low-noise examples are slightly worse, while high-noise examples collapse badly.',
      prompt: 'What mechanism is most directly broken?',
      choices: Object.freeze([
        'The model can no longer tell how much corruption is present, so it cannot adapt its denoising behavior to the current noise level',
        'The model has lost access to the class labels, so every diffusion model becomes unconditional',
        'The forward process has become exact search over the training set',
      ]),
      answerIndex: 0,
      explanation: 'The same noisy-looking pattern can require different corrections at different noise levels. Timestep conditioning tells the denoiser where it is in the schedule; replacing it with a constant removes that information.',
      misconceptionTested: 'A diffusion denoiser can infer the exact noise level reliably without explicit timestep or equivalent noise-level conditioning.',
      relatedComparison: 'Timestep-conditioned denoising versus a timestep-blind denoiser.',
    }),
    Object.freeze({
      id: 'diffusion-training-generation-target-boundary',
      level: 'diagnosis',
      scenario: 'During training, the pipeline samples a clean example x0, a timestep t, and random noise epsilon, constructs xt, and computes loss against epsilon. A new engineer proposes using the same code during generation and asks where the clean x0 target should come from for every reverse step.',
      prompt: 'What is the conceptual mistake?',
      choices: Object.freeze([
        'Generation does not have a clean target x0 for each step; it starts from noise and repeatedly applies the learned denoiser using the current state and timestep',
        'Generation should retrieve the nearest training image and use that as x0 at every step',
        'Generation must continue optimizer updates so a new x0 target can be learned online',
      ]),
      answerIndex: 0,
      explanation: 'Training can synthesize supervised denoising targets because the forward corruption process is known. Generation has no paired clean target; it uses the trained reverse model to transform an initial noise sample toward a data-like sample.',
      misconceptionTested: 'The supervised clean/noise targets available during diffusion training are also available during generation.',
      relatedComparison: 'Known forward corruption targets during training versus learned reverse sampling at inference.',
    }),
    Object.freeze({
      id: 'diffusion-noise-prediction-error-direction',
      level: 'diagnosis',
      scenario: 'For a noisy scalar state, the true noise contribution is positive. Model A consistently predicts a smaller noise magnitude than the truth, while Model B predicts a larger magnitude than the truth.',
      prompt: 'Which qualitative failure should you expect from the two models?',
      choices: Object.freeze([
        'Model A tends to leave residual corruption; Model B tends to remove too much and can erase clean signal',
        'Model A necessarily removes too much signal; Model B necessarily leaves more noise',
        'Both errors are equivalent because only the sign of the predicted noise matters',
      ]),
      answerIndex: 0,
      explanation: 'Underestimating the corruption means the reverse correction is too weak, so residual noise remains. Overestimating it makes the correction too aggressive and can subtract real signal along with the corruption.',
      misconceptionTested: 'Noise-prediction magnitude errors have no directional effect on the denoised state.',
      relatedComparison: 'Under-denoising versus over-denoising.',
    }),
    Object.freeze({
      id: 'diffusion-latent-decoding-contract',
      level: 'decision',
      scenario: 'A latent diffusion pipeline finishes denoising a compact latent tensor and the team tries to display that tensor directly as the final RGB image.',
      prompt: 'What step is missing in the intended latent-diffusion pipeline?',
      choices: Object.freeze([
        'Decode the denoised latent through the learned decoder that maps latent representations back to image space',
        'Run vector retrieval over the latent tensor and use the nearest image as the generated result',
        'Add more forward-process noise until the tensor has three channels',
      ]),
      answerIndex: 0,
      explanation: 'Latent diffusion performs denoising in a compressed representation for efficiency. The final latent still needs the corresponding decoder, commonly from a VAE-style autoencoder, to produce an image-space sample.',
      misconceptionTested: 'A denoised latent representation is automatically identical to the final pixel-space image.',
      relatedComparison: 'Latent-space denoising versus image-space decoding.',
    }),
  ]),
  'diffusion-sampling': Object.freeze([
    Object.freeze({
      id: 'diffusion-ddpm-ddim-reproducibility-choice',
      level: 'comparison',
      scenario: 'Two runs start from exactly the same initial noise and use the same trained denoiser and timestep schedule. Sampler A injects fresh reverse-process noise at each step; Sampler B uses a deterministic DDIM-style path with the extra stochastic term disabled.',
      prompt: 'What reproducibility difference should you expect?',
      choices: Object.freeze([
        'Sampler B should follow the same trajectory for the fixed start and settings, while Sampler A can diverge because it introduces additional random reverse terms',
        'Both must be identical because they share the same denoiser',
        'Sampler A must be deterministic while Sampler B must sample new noise at every step',
      ]),
      answerIndex: 0,
      explanation: 'Sampler choice can change trajectory stochasticity without changing the trained denoiser. DDIM can use a deterministic reverse path, whereas DDPM-style reverse updates retain stochastic noise.',
      misconceptionTested: 'Sampler stochasticity is fixed entirely by the denoiser weights rather than by the reverse update rule.',
      relatedComparison: 'Stochastic DDPM-style reverse updates versus deterministic DDIM-style sampling.',
    }),
    Object.freeze({
      id: 'diffusion-sampling-seed-boundary',
      level: 'diagnosis',
      scenario: 'A team fixes the initial random seed and uses a deterministic sampler. They then change the numerical solver implementation and floating-point execution path and observe tiny output differences.',
      prompt: 'Which reproducibility claim is safest?',
      choices: Object.freeze([
        'A fixed seed controls intended randomness, but bit-for-bit identity can still depend on deterministic kernels, solver implementation, precision, and execution environment',
        'A fixed seed guarantees bit-identical outputs across every hardware backend and numerical implementation',
        'Seeds matter only during training and never influence diffusion sampling',
      ]),
      answerIndex: 0,
      explanation: 'Seeds make stochastic inputs reproducible only within the assumptions of the execution stack. Deterministic sampling improves comparability, but numerical implementation differences can still change results.',
      misconceptionTested: 'A random seed alone is a universal cross-platform bitwise reproducibility guarantee.',
      relatedComparison: 'Randomness control versus full numerical determinism.',
    }),
    Object.freeze({
      id: 'diffusion-sampling-coarse-schedule-instability',
      level: 'diagnosis',
      scenario: 'A 40-step sampler is reduced to 6 uniformly spaced reverse steps without changing the model. Latency improves sharply, but samples develop structural artifacts mainly after the first few high-noise updates.',
      prompt: 'What should be investigated first?',
      choices: Object.freeze([
        'The coarse timestep schedule and large reverse jumps, because each denoiser prediction now has to cover much more trajectory distance',
        'The training labels, because reducing inference steps always corrupts the original dataset',
        'The tokenizer, because diffusion timestep schedules are defined by vocabulary size',
      ]),
      answerIndex: 0,
      explanation: 'Fewer reverse steps mean larger moves through the noise schedule. Coarse stepping can amplify discretization error and model prediction error, especially at high-noise stages that establish global structure.',
      misconceptionTested: 'Inference step count can be reduced arbitrarily without changing the numerical difficulty of the reverse trajectory.',
      relatedComparison: 'Fine-grained reverse schedules versus coarse low-step sampling.',
    }),
    Object.freeze({
      id: 'diffusion-sampling-denoiser-error-step-interaction',
      level: 'decision',
      scenario: 'Two denoisers are evaluated with the same 8-step sampler. Model A has low prediction error across noise levels and produces acceptable samples. Model B has noticeably larger prediction error at high-noise timesteps and produces unstable global structure.',
      prompt: 'Which experiment best tests whether the failure is partly caused by the step-count/error interaction?',
      choices: Object.freeze([
        'Increase reverse steps or use a better-adapted step schedule for Model B while keeping the denoiser fixed, then compare whether structural errors shrink',
        'Increase reverse-process randomness only, because stochasticity always cancels denoiser prediction error',
        'Remove timestep conditioning so the model does not know where its prediction error occurs',
      ]),
      answerIndex: 0,
      explanation: 'Low-step sampling makes each denoiser call carry more of the reverse trajectory. A weaker predictor can therefore need more or better-placed steps. Holding the denoiser fixed while changing the schedule isolates that interaction.',
      misconceptionTested: 'The best sampling step count is independent of denoiser quality.',
      relatedComparison: 'Denoiser prediction quality versus reverse-step budget.',
    }),
  ]),
});
