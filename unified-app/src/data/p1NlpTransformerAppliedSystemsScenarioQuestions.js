export const P1_NLP_TRANSFORMER_APPLIED_SYSTEMS_SCENARIOS_BY_LESSON = Object.freeze({
  'kv-cache': [
    {
      id: 'kv-cache-memory-worked',
      level: 'calculation',
      relatedComparison: 'kv-cache-size-vs-context-length',
      scenario: 'A decoder has 32 layers, 8 KV heads per layer, head dimension 128, FP16 cache values at 2 bytes each, and stores both K and V for every cached token. The active context contains 16,384 tokens.',
      prompt: 'Approximately how much KV-cache memory does one sequence require?',
      choices: ['About 2 GiB, from 32*8*128*2(K,V)*2 bytes*16,384 tokens', 'About 64 MiB, because cache size depends only on the number of layers', 'About 4 GiB squared, because cache storage itself scales with the number of token pairs'],
      answerIndex: 0,
      explanation: 'Multiplying layers, KV heads, head dimension, the K/V factor, bytes per element, and tokens gives 2,147,483,648 bytes, which is 2 GiB. KV-cache storage is linear in cached tokens for fixed model dimensions.',
      misconceptionTested: 'KV-cache memory is not quadratic in context length like dense attention pair computation; it stores per-token key and value states.',
    },
  ],
  'grouped-query-attention': [
    {
      id: 'gqa-kv-cache-reduction-worked',
      level: 'calculation',
      relatedComparison: 'mha-vs-gqa-kv-cache-width',
      scenario: 'Two otherwise identical decoders use 32 query heads with head dimension 128. Model A uses standard multi-head attention with 32 KV heads. Model B uses grouped-query attention with 8 KV heads. Layers, precision, and context length are identical.',
      prompt: 'How does Model B KV-cache width compare with Model A?',
      choices: ['It is 4x smaller for K/V states because 8 KV heads are stored instead of 32 while the query-head count can remain 32', 'It is identical because only query heads determine KV-cache width', 'It is 4x larger because sharing a KV head requires storing four copies'],
      answerIndex: 0,
      explanation: 'For fixed head dimension, layers, precision, and tokens, cache width is proportional to the number of KV heads. Reducing 32 KV heads to 8 cuts K/V cache storage and related bandwidth by a factor of four.',
      misconceptionTested: 'Grouped-query attention can reduce inference cache cost even when the number of query heads and model width remain unchanged.',
    },
  ],
  'flash-attention': [
    {
      id: 'flash-attention-io-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'standard-attention-vs-flashattention-io',
      scenario: 'At sequence length 8,192, a reference attention implementation materializes a large attention matrix and runs out of accelerator memory. A FlashAttention kernel fits and matches the reference output within numerical tolerance using the same model weights and attention pattern.',
      prompt: 'What best explains the improvement?',
      choices: ['The kernel tiles attention to reduce high-bandwidth-memory traffic and avoid materializing the full attention matrix, without changing the mathematical dense-attention objective', 'FlashAttention removes most token-to-token interactions, so it is a sparse-attention approximation', 'FlashAttention reduces the model parameter count at runtime by deleting attention heads'],
      answerIndex: 0,
      explanation: 'FlashAttention reorganizes the exact dense-attention computation around memory-efficient tiling and recomputation, reducing expensive memory reads/writes and avoiding storage of the full score matrix. It is not inherently sparse attention.',
      misconceptionTested: 'FlashAttention speed and memory gains come from an I/O-aware exact algorithm, not from silently changing dense attention into sparse attention.',
    },
  ],
  'native-sparse-attention': [
    {
      id: 'native-sparse-long-range-design',
      level: 'design',
      relatedComparison: 'local-sparse-vs-global-routing',
      scenario: 'A 128K-token document model uses only a 2K local attention window. Evaluation shows that questions whose evidence is 40K tokens away from the query fail, while nearby questions work. Compute budget does not allow restoring full dense attention everywhere.',
      prompt: 'Which sparse-attention change directly targets the observed failure?',
      choices: ['Add a selective global or routed path that can connect important distant tokens while retaining local sparse attention for most positions', 'Reduce the local window from 2K to 512 so distant evidence becomes easier to reach', 'Keep the same local-only pattern because any sparse attention can recover arbitrary long-range dependencies automatically'],
      answerIndex: 0,
      explanation: 'The evidence points to a connectivity problem: purely local windows cannot directly reach a dependency 40K tokens away. A selective global, routed, compressed, or block-level path can restore long-range connectivity without paying full dense cost everywhere.',
      misconceptionTested: 'Sparse attention patterns must preserve the task-relevant connectivity graph; sparsity alone does not guarantee access to distant evidence.',
    },
  ],
  'transformer': [
    {
      id: 'transformer-prenorm-stability-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'pre-norm-vs-post-norm-deep-training',
      scenario: 'An 80-layer transformer using post-norm residual blocks becomes unstable as depth increases: early-layer gradients vary wildly and training requires very small learning rates. A shallower version with the same data and optimizer is stable.',
      prompt: 'Which architecture ablation is most directly motivated?',
      choices: ['Test a pre-norm residual layout while holding depth, width, optimizer, and data fixed to see whether the normalization placement improves gradient flow', 'Remove residual connections entirely so each layer must reconstruct the representation from scratch', 'Increase vocabulary size because normalization placement cannot affect deep optimization'],
      answerIndex: 0,
      explanation: 'Normalization placement changes the residual and gradient paths through deep transformers. A controlled pre-norm versus post-norm ablation directly tests whether the instability is architectural rather than mixing in unrelated optimizer or data changes.',
      misconceptionTested: 'Layer normalization placement is not merely cosmetic; pre-norm and post-norm transformers can have materially different deep-training stability.',
    },
  ],
  'transformer-architecture-families': [
    {
      id: 'transformer-family-workload-decision',
      level: 'decision',
      relatedComparison: 'encoder-vs-encoder-decoder-vs-decoder-only',
      scenario: 'A team has three products: low-latency document classification over a complete input, source-to-target translation where the output should condition on the whole source, and open-ended chat generation that emits tokens autoregressively.',
      prompt: 'Which architecture-family mapping is the most natural starting point?',
      choices: ['Encoder-only for classification, encoder-decoder for translation, and decoder-only for open-ended autoregressive chat', 'Decoder-only for classification, encoder-only for translation, and encoder-decoder with unrestricted future attention for chat', 'Use encoder-only for all three because causal masking is unrelated to generation'],
      answerIndex: 0,
      explanation: 'Bidirectional encoders naturally summarize complete inputs, encoder-decoder models separate source encoding from conditional target generation, and decoder-only causal models directly match open-ended autoregressive generation. Other choices are possible, but this mapping fits the information flow.',
      misconceptionTested: 'Transformer families are not interchangeable labels; their attention structure and conditioning paths align differently with common workloads.',
    },
  ],
  'llm-training-objectives': [
    {
      id: 'causal-objective-target-shift-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'same-token-target-vs-next-token-target',
      scenario: 'A causal language-model implementation feeds tokens [A, B, C, D] and computes loss by comparing each position logit with the token at that same input position. Training accuracy rises suspiciously fast, but next-token generation remains poor.',
      prompt: 'What objective bug should be fixed first?',
      choices: ['Shift the targets so logits from positions A,B,C predict B,C,D respectively, rather than supervising each position with the token it already received', 'Remove the causal mask so every position can copy future tokens during training', 'Replace token IDs with random class labels because next-token language modeling should not use sequence order'],
      answerIndex: 0,
      explanation: 'Causal language modeling predicts the next token from the available prefix. Comparing a position to the same token it was given misaligns the objective and can reward trivial identity information instead of the intended next-token factorization.',
      misconceptionTested: 'Teacher-forced causal LM training still requires a one-token target shift; parallel sequence processing does not make same-position targets correct.',
    },
  ],
  'transformer-token-generation': [
    {
      id: 'generation-temperature-worked',
      level: 'calculation',
      relatedComparison: 'low-temperature-vs-high-temperature-softmax',
      scenario: 'The next-token logits for three tokens are [2, 1, 0]. Before softmax, decoding changes temperature from T = 1.0 to T = 0.5, which divides every logit by 0.5 and therefore changes them to [4, 2, 0].',
      prompt: 'What qualitative change should this produce in the sampling distribution?',
      choices: ['A sharper distribution that gives relatively more probability to the highest-logit token because the logit gaps doubled', 'A flatter distribution because lower temperature always makes all tokens equally likely', 'No change at all because softmax probabilities are invariant to multiplying every logit by the same positive factor'],
      answerIndex: 0,
      explanation: 'Temperature rescales logit differences before softmax. Lowering T from 1.0 to 0.5 doubles the gaps, increasing the odds ratio between high- and low-logit tokens and making the distribution more concentrated around the top token.',
      misconceptionTested: 'Softmax is invariant to adding the same constant to logits, but not to rescaling their differences with temperature.',
    },
  ],
  'sampling-strategies': [
    {
      id: 'sampling-top-p-worked',
      level: 'calculation',
      relatedComparison: 'top-p-vs-top-k-candidate-set',
      scenario: 'Sorted next-token probabilities are [0.40, 0.30, 0.15, 0.10, 0.05]. Nucleus sampling uses top-p = 0.80 and keeps the smallest prefix whose cumulative probability reaches or exceeds p.',
      prompt: 'Which tokens remain eligible before renormalization?',
      choices: ['The first three tokens, because cumulative mass is 0.40, then 0.70, then 0.85, which is the first point that reaches 0.80', 'Only the first two tokens, because 0.70 should be rounded up to 0.80', 'All five tokens, because top-p changes probabilities but never removes candidates'],
      answerIndex: 0,
      explanation: 'Top-p includes the smallest sorted prefix whose cumulative probability crosses the threshold. The first two sum to 0.70, which is insufficient; adding the third reaches 0.85, so those three form the candidate set before renormalization.',
      misconceptionTested: 'Nucleus sampling uses cumulative probability mass, not a fixed token count and not independent per-token probability thresholds.',
    },
  ],
  'fine-tuning': [
    {
      id: 'finetune-catastrophic-forgetting-decision',
      level: 'decision',
      relatedComparison: 'full-finetune-vs-peft-replay-regularization',
      scenario: 'A general model is fully fine-tuned on a small legal-domain dataset. Legal validation improves, but broad reasoning and general-language benchmarks fall sharply. The domain dataset is only a tiny fraction of the data used for pretraining.',
      prompt: 'Which next experiment best isolates and reduces catastrophic forgetting?',
      choices: ['Compare a lower-impact adaptation recipe such as PEFT or a smaller learning rate, optionally with representative replay data, while tracking both domain and general benchmarks', 'Continue full fine-tuning for more epochs and evaluate only the legal validation set', 'Delete the general benchmarks because specialization means regressions outside the domain cannot matter'],
      answerIndex: 0,
      explanation: 'The pattern is consistent with specialization overwriting useful general behavior. A controlled lower-impact adaptation or replay experiment tests the trade-off directly while preserving both domain and general evaluations instead of optimizing only one side.',
      misconceptionTested: 'Fine-tuning success cannot be judged only on the target-domain set when preserving broad pretrained capabilities is also a requirement.',
    },
  ],
});
