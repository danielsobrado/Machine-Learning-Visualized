export const P1_GENERATIVE_RL_SCENARIOS_BY_LESSON = Object.freeze({
  vae: [
    {
      id: 'vae-recon-kl-tradeoff',
      level: 'comparison',
      relatedComparison: 'reconstruction-vs-kl',
      scenario: 'A VAE lowers reconstruction error by letting latent distributions drift far from the prior, making random prior samples poor.',
      prompt: 'Which objective term should counter that behavior?',
      choices: ['The KL-divergence regularization term that keeps the approximate posterior near the prior', 'A larger reconstruction weight only', 'A causal attention mask'],
      answerIndex: 0,
      explanation: 'The VAE objective balances reconstruction quality with KL regularization so the latent space remains structured and samples from the prior remain meaningful.',
      misconceptionTested: 'A VAE should optimize reconstruction alone with no latent regularization.',
    },
    {
      id: 'vae-posterior-collapse',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { klPerDimension: 'near zero', decoderQuality: 'high', latentPerturbationEffect: 'minimal' },
      scenario: 'Visual state: KL is nearly zero in every latent dimension and changing z barely changes the decoder output.',
      prompt: 'What failure mode is most likely?',
      choices: ['Posterior collapse where the decoder largely ignores the latent code', 'Exploding latent variance', 'Perfect disentanglement'],
      answerIndex: 0,
      explanation: 'Near-zero KL together with weak output sensitivity to z suggests the approximate posterior matches the prior while the powerful decoder bypasses latent information.',
      misconceptionTested: 'A very small KL term always means the VAE learned an ideal latent representation.',
    },
  ],
  'diffusion-basics': [
    {
      id: 'diffusion-train-vs-sample',
      level: 'comparison',
      relatedComparison: 'training-noise-prediction-vs-sampling-loop',
      scenario: 'During training, a clean example is noised to a random timestep and the model predicts the injected noise. During generation, the model starts from noise and repeatedly denoises.',
      prompt: 'What is the key distinction?',
      choices: ['Training learns local denoising/noise prediction at sampled timesteps; sampling chains many reverse updates to create a sample', 'Training always runs the full generation chain for every example', 'Sampling requires the original clean training image as input'],
      answerIndex: 0,
      explanation: 'Diffusion training can supervise randomly selected noise levels independently, while generation composes the learned denoising behavior across a reverse-time trajectory.',
      misconceptionTested: 'Diffusion training and sampling execute the same full iterative loop for every example.',
    },
  ],
  'diffusion-sampling': [
    {
      id: 'diffusion-steps-quality-latency',
      level: 'comparison',
      relatedComparison: 'sampling-steps-quality-latency',
      scenario: 'Reducing denoising steps from 50 to 8 cuts latency sharply but increases artifacts on difficult prompts.',
      prompt: 'How should the sampler setting be selected?',
      choices: ['Measure the quality-latency trade-off on representative prompts rather than maximizing or minimizing step count blindly', 'Always use the fewest possible steps because sampling equations guarantee equal quality', 'Always use the most steps because quality increases monotonically forever'],
      answerIndex: 0,
      explanation: 'Step count controls compute and numerical approximation quality, but the useful operating point depends on sampler, model, prompt distribution, and product latency budget.',
      misconceptionTested: 'Diffusion quality is independent of sampling step count.',
    },
  ],
  'classifier-free-guidance': [
    {
      id: 'cfg-overguidance',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { guidanceScale: 18, promptMatch: 'high', diversity: 'low', saturationArtifacts: 'high' },
      scenario: 'Visual state: increasing guidance scale to 18 improves literal prompt match but diversity collapses and saturation/artifacts become obvious.',
      prompt: 'What is the likely cause?',
      choices: ['Over-guidance from pushing too far along the conditional-unconditional difference direction', 'Insufficient guidance because the scale is too high', 'A tokenizer vocabulary mismatch only'],
      answerIndex: 0,
      explanation: 'Classifier-free guidance trades diversity and naturalness for stronger conditioning; very high scales can overshoot and create distorted or saturated samples.',
      misconceptionTested: 'Higher guidance scale is always better if prompt adherence improves.',
    },
  ],
  'unet-vs-dit': [
    {
      id: 'dit-patch-resolution-cost',
      level: 'comparison',
      relatedComparison: 'unet-local-bias-vs-dit-token-cost',
      scenario: 'A DiT halves patch size at the same image resolution, greatly increasing token count and attention pairs.',
      prompt: 'What trade-off is being made?',
      choices: ['Potentially finer spatial detail at substantially higher transformer attention cost', 'Lower token count with no compute change', 'Replacing all global mixing with fixed convolutions'],
      answerIndex: 0,
      explanation: 'Smaller patches create more tokens, which can preserve finer detail but increases attention cost and memory pressure in transformer backbones.',
      misconceptionTested: 'Patch size changes image detail without affecting transformer sequence length.',
    },
  ],
  'multimodal-llm': [
    {
      id: 'multimodal-projector-alignment',
      level: 'mechanism',
      relatedComparison: 'vision-encoder-projector-language-model',
      scenario: 'A vision encoder produces image features whose dimension and representation differ from the language model token embedding space.',
      prompt: 'What role can a multimodal projector play?',
      choices: ['Map modality features into a representation the language model can consume alongside text tokens', 'Replace the tokenizer with a confusion matrix', 'Guarantee visual grounding without any multimodal training'],
      answerIndex: 0,
      explanation: 'Projectors or adapters bridge modality-specific encoder outputs into the language-model representation space, but alignment still depends on multimodal training data and objectives.',
      misconceptionTested: 'A dimensional projection alone guarantees semantic vision-language alignment.',
    },
  ],
  'rl-foundations': [
    {
      id: 'rl-reward-vs-return',
      level: 'mechanism',
      scenario: 'An agent receives reward -1 now, then +10 two steps later with discount factor less than one.',
      prompt: 'What is the return conceptually?',
      choices: ['A discounted accumulation of current and future rewards, not just the immediate -1', 'Only the most recent reward', 'The number of available actions'],
      answerIndex: 0,
      explanation: 'Return summarizes the future reward sequence from a point in time, usually discounted so delayed rewards contribute less than immediate rewards.',
      misconceptionTested: 'Reward and return are interchangeable names for the same single-step quantity.',
    },
  ],
  'q-learning': [
    {
      id: 'qlearn-transition-update-scenario',
      level: 'mechanism',
      scenario: 'Q(s,a)=2, reward=1, gamma=0.9, max_a Q(s_next,a)=5, and alpha=0.5.',
      prompt: 'What updated Q(s,a) should one Q-learning step produce?',
      choices: ['3.75 because target is 5.5 and the update moves halfway from 2 to 5.5', '5.5 because alpha is ignored', '2.0 because only terminal rewards update Q-values'],
      answerIndex: 0,
      explanation: 'The target is 1 + 0.9*5 = 5.5; the TD error is 3.5; adding alpha times that error gives 2 + 0.5*3.5 = 3.75.',
      misconceptionTested: 'Q-learning replaces the old value with the target regardless of learning rate.',
    },
  ],
  'rl-exploration': [
    {
      id: 'exploration-epsilon-vs-uncertainty',
      level: 'comparison',
      relatedComparison: 'epsilon-greedy-vs-directed-exploration',
      scenario: 'An agent has tried one action hundreds of times and another action only twice. Epsilon-greedy explores uniformly among alternatives when it explores.',
      prompt: 'What limitation does this expose?',
      choices: ['Epsilon-greedy does not directly prioritize actions based on uncertainty or how underexplored they are', 'Epsilon-greedy never explores at all', 'Epsilon-greedy requires a known transition model'],
      answerIndex: 0,
      explanation: 'Epsilon-greedy injects undirected random exploration, while uncertainty-aware methods can focus exploration on actions whose values are less well known.',
      misconceptionTested: 'All exploration strategies allocate exploration effort in the same way.',
    },
  ],
  'grpo-reasoning': [
    {
      id: 'grpo-verifier-exploitation',
      level: 'diagnosis',
      scenario: 'Within each rollout group, outputs that include a particular formatting token receive higher reward even when the mathematical answer is wrong.',
      prompt: 'What should be fixed first?',
      choices: ['The reward/verifier because the policy is exploiting a shortcut unrelated to correctness', 'The group size only because reward semantics cannot matter', 'The tokenizer because any rewarded token is automatically valid'],
      answerIndex: 0,
      explanation: 'Policy optimization improves whatever the reward recognizes; a shortcut in the verifier can produce reward hacking instead of better reasoning.',
      misconceptionTested: 'Verifiable-reward RL automatically optimizes the intended concept even with a weak verifier.',
    },
  ],
  'test-time-compute-thinking-budgets': [
    {
      id: 'ttc-marginal-budget-value',
      level: 'application',
      kind: 'visual-state',
      visualState: { samples: '1, 4, 16, 64', accuracy: '62%, 71%, 75%, 76%', relativeCost: '1x, 4x, 16x, 64x' },
      scenario: 'Visual state: Best-of-N accuracy rises quickly from N=1 to N=16 but barely improves from N=16 to N=64 while cost quadruples again.',
      prompt: 'What should an adaptive budget policy learn from this curve?',
      choices: ['Stop increasing compute when marginal accuracy gain no longer justifies marginal cost', 'Always use the maximum N because any gain dominates cost', 'Use N=1 for every problem regardless of difficulty'],
      answerIndex: 0,
      explanation: 'Test-time compute should be allocated against a cost-quality frontier; diminishing returns make maximum search wasteful on easier or already-saturated cases.',
      misconceptionTested: 'More inference compute is always economically optimal when accuracy rises at all.',
    },
  ],
  'tool-using-reasoning-models': [
    {
      id: 'tool-conflicting-observations',
      level: 'diagnosis',
      scenario: 'Two tools return conflicting values for the same current fact, one from a cached source and one from a timestamped authoritative source.',
      prompt: 'What should the reasoning loop do?',
      choices: ['Compare source freshness and authority, resolve the conflict, and avoid silently choosing the convenient result', 'Average the strings returned by both tools', 'Always trust the first tool call because tool order determines truth'],
      answerIndex: 0,
      explanation: 'Tool use requires evidence reconciliation: freshness, authority, scope, and failure modes should determine which observation is trusted or whether uncertainty must be reported.',
      misconceptionTested: 'Any tool output can be treated as ground truth without source evaluation.',
    },
    {
      id: 'tool-partial-failure',
      level: 'application',
      scenario: 'A multi-step plan needs search, a calculator, and a write action. Search succeeds, the calculator times out, and the write action would be irreversible.',
      prompt: 'What is the safest next step?',
      choices: ['Stop before the irreversible action, recover or recompute the missing evidence, then proceed only with complete justified state', 'Perform the write anyway because earlier tools succeeded', 'Invent the missing calculator output so the workflow can finish'],
      answerIndex: 0,
      explanation: 'Partial tool failure should not be hidden when later side effects depend on missing evidence; the agent should recover, retry safely, or surface uncertainty before acting.',
      misconceptionTested: 'Successful early tool calls authorize later irreversible actions after a critical tool failure.',
    },
  ],
  'agentic-coding-systems': [
    {
      id: 'agent-long-horizon-rollback',
      level: 'diagnosis',
      scenario: 'An agent makes six sequential edits. The first four pass focused tests, the fifth introduces a regression, and the sixth builds on the broken state.',
      prompt: 'What workflow best limits error accumulation?',
      choices: ['Use small checkpoints with tests and revert or repair from the last known-good state before continuing', 'Keep every edit because later edits may hide earlier failures', 'Delay all testing until after the final large patch'],
      answerIndex: 0,
      explanation: 'Long-horizon coding is safer when the agent maintains recoverable checkpoints and validates incrementally, preventing one bad edit from contaminating many later decisions.',
      misconceptionTested: 'Long autonomous coding runs are safer when testing is postponed until the end.',
    },
  ],
  'frontier-evaluation-safety': [
    {
      id: 'eval-contamination',
      level: 'diagnosis',
      scenario: 'A benchmark appears verbatim in public training corpora and the model scores extremely well while performance on newly authored equivalent tasks is much lower.',
      prompt: 'What should the evaluation team suspect?',
      choices: ['Benchmark contamination or memorization inflating the reported score', 'Proof that the new tasks are invalid', 'A lower tokenizer vocabulary size in the benchmark'],
      answerIndex: 0,
      explanation: 'Exposure to benchmark items can make scores reflect memorization rather than transferable capability, so fresh or contamination-resistant evaluations are needed.',
      misconceptionTested: 'A high benchmark score always measures generalization rather than possible training exposure.',
    },
  ],
});

export function getP1GenerativeRlScenariosForLesson(lessonId) {
  return P1_GENERATIVE_RL_SCENARIOS_BY_LESSON[lessonId] || [];
}
