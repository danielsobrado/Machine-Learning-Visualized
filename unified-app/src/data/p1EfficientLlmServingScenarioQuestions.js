export const P1_EFFICIENT_LLM_SERVING_SCENARIOS_BY_LESSON = Object.freeze({
  'efficient-llm-serving': Object.freeze([
    Object.freeze({
      id: 'serve-prefill-decode-starvation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'monolithic-prefill-vs-chunked-prefill-with-decode-priority',
      scenario: 'A chat server has 60 active streaming users with healthy 45 ms inter-token latency. One 96K-token prompt enters the queue. During its 1.4 s monolithic prefill, GPU utilization remains high, but active users see inter-token latency jump above 900 ms. After the prefill completes, decode latency immediately returns to normal.',
      prompt: 'What is the most direct serving fix to test first?',
      choices: Object.freeze([
        'Split the long prompt into prefill chunks and interleave those chunks with decode-priority work so active streams keep receiving tokens',
        'Increase the maximum static batch size so the 96K prompt occupies the GPU for an even larger uninterrupted prefill',
        'Disable streaming because high GPU utilization proves the scheduler is already optimal and the delay cannot be prefill-related',
      ]),
      answerIndex: 0,
      explanation: 'The incident is phase interference, not lack of GPU work. A monolithic long prefill monopolizes compute while active decode requests wait. Chunked prefill with appropriate decode priority preserves progress for existing streams while still advancing the long prompt.',
      misconceptionTested: 'High GPU utilization means scheduling is healthy, so long prefill work cannot starve latency-sensitive decode requests.',
    }),
    Object.freeze({
      id: 'serve-kv-capacity-worked',
      level: 'calculation',
      relatedComparison: 'per-token-kv-footprint-vs-concurrent-long-context-capacity',
      scenario: 'A model has 32 layers, 8 KV heads per layer, head dimension 128, and stores both keys and values in FP16 at 2 bytes per value. Ignore allocator overhead. Each request keeps 16,384 cached tokens and the server wants 12 concurrent requests.',
      prompt: 'Approximately how much KV-cache memory is required?',
      choices: Object.freeze([
        '128 KiB per token, about 2 GiB per request, and about 24 GiB for 12 concurrent requests',
        '64 KiB per token, about 1 GiB per request, and about 12 GiB total because keys and values share the same storage',
        '2 GiB per token and about 24 TiB total because the 16,384-token context length must be multiplied twice',
      ]),
      answerIndex: 0,
      explanation: 'Per token, KV storage is 2 for key/value x 32 layers x 8 KV heads x 128 dimensions x 2 bytes = 131,072 bytes = 128 KiB. Multiplying by 16,384 tokens gives 2 GiB per request; 12 requests therefore need about 24 GiB before allocator/runtime overhead.',
      misconceptionTested: 'KV cache is a small constant overhead rather than a concurrency-limiting memory term that scales with layers, KV heads, head dimension, precision, and stored tokens.',
    }),
    Object.freeze({
      id: 'serve-paged-kv-fragmentation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'contiguous-kv-allocation-vs-paged-block-allocation-under-variable-sequences',
      scenario: 'A serving node reports 18 GiB of free KV memory in aggregate, yet admission repeatedly fails for new long requests that need about 5 GiB each. The free space is split among many holes left by variable-length requests; the largest contiguous region is only 1.6 GiB. Model weights and request limits are unchanged.',
      prompt: 'Which diagnosis and remediation best match the evidence?',
      choices: Object.freeze([
        'KV fragmentation is preventing large contiguous allocations; use paged/fixed-block KV allocation so a logical sequence can map across noncontiguous physical blocks',
        'The tokenizer has lost vocabulary entries; enlarge the tokenizer because fragmented GPU memory cannot affect request admission',
        'The model requires at least 18 GiB of contiguous memory per request because aggregate free memory is always unusable for attention caches',
      ]),
      answerIndex: 0,
      explanation: 'The node has enough total free memory for several 5 GiB logical caches but not one sufficiently large contiguous region. Paged KV allocation addresses exactly this allocator failure by mapping logical sequence blocks onto available physical blocks instead of demanding one large contiguous extent.',
      misconceptionTested: 'If aggregate free GPU memory exceeds a request requirement, contiguous KV allocation cannot still fail because of fragmentation.',
    }),
    Object.freeze({
      id: 'serve-prefix-cache-expected-prefill-worked',
      level: 'calculation',
      relatedComparison: 'exact-prefix-cache-hit-rate-vs-expected-prefill-cost',
      scenario: 'A request normally spends 100 ms in prefill: 80 ms for a shared system-and-document prefix and 20 ms for request-specific tokens. With prefix caching, a hit skips the 80 ms shared work. On the current workload the exact-prefix hit rate is 75%.',
      prompt: 'What expected prefill time should capacity planning use, and what deployment change would most directly destroy this benefit?',
      choices: Object.freeze([
        'About 40 ms expected prefill; inserting a different per-request timestamp inside the shared prefix would make those prefixes textually different and collapse exact cache hits',
        'About 25 ms expected prefill; prefix caching removes the full 100 ms on every hit and semantic similarity preserves hits after arbitrary text changes',
        'About 80 ms expected prefill; request-specific tokens are never processed when any prefix cache is enabled',
      ]),
      answerIndex: 0,
      explanation: 'Expected prefill is 0.75 x 20 ms + 0.25 x 100 ms = 40 ms. KV prefix reuse depends on exact token identity. A per-request timestamp inserted into the cached region changes the token sequence and can eliminate otherwise valuable reuse.',
      misconceptionTested: 'Prefix caches reuse semantically similar prompts and therefore remain effective even when dynamic request-specific text is inserted into the supposedly shared prefix.',
    }),
    Object.freeze({
      id: 'serve-speculation-break-even-worked',
      level: 'calculation',
      relatedComparison: 'draft-and-verify-cost-vs-accepted-token-progress',
      scenario: 'Without speculation, the target model needs 5 ms per decoded token. A speculative iteration costs 3 ms for the draft plus 6 ms for one target verification pass. Under workload A, each iteration advances by 3 accepted tokens on average. Under workload B after a domain shift, it advances by only 1 accepted token on average.',
      prompt: 'What should the serving team conclude from the effective cost per accepted token?',
      choices: Object.freeze([
        'Workload A costs about 3 ms per accepted token and is beneficial, while workload B costs about 9 ms per accepted token and is slower than the 5 ms baseline, so speculation should be retuned or disabled for B',
        'Both workloads cost 9 ms per accepted token because accepted-token count never changes speculative-decoding economics',
        'Workload B is faster because accepting fewer draft tokens reduces the amount of target-model verification required to zero',
      ]),
      answerIndex: 0,
      explanation: 'Each speculative iteration costs 9 ms. Advancing 3 tokens gives 3 ms per accepted token, better than the 5 ms baseline. Advancing only 1 token gives 9 ms per token, worse than baseline. Acceptance-driven progress must be compared with draft and verification cost rather than assuming speculation always helps.',
      misconceptionTested: 'Speculative decoding is inherently faster regardless of acceptance rate and the cost of draft plus target verification.',
    }),
    Object.freeze({
      id: 'serve-tensor-parallel-scaling-worked',
      level: 'calculation',
      relatedComparison: 'compute-splitting-vs-interconnect-overhead-in-tensor-parallel-serving',
      scenario: 'A decode step takes 40 ms on one GPU. For a simplified comparison, two-way tensor parallelism halves compute to 20 ms but adds 12 ms of communication per step. Four-way tensor parallelism reduces compute to 10 ms but adds 25 ms of communication per step.',
      prompt: 'Which configuration has the best modeled decode latency, and what scaling lesson follows?',
      choices: Object.freeze([
        'Two GPUs at about 32 ms per step; four GPUs are about 35 ms, so extra devices can lose their compute advantage when synchronization and interconnect costs grow',
        'Four GPUs at exactly 10 ms per step because communication is never on the critical path in tensor parallelism',
        'One GPU at 40 ms and two GPUs at 52 ms because compute and communication must always be multiplied rather than added',
      ]),
      answerIndex: 0,
      explanation: 'The simplified totals are 40 ms for one GPU, 20 + 12 = 32 ms for two GPUs, and 10 + 25 = 35 ms for four GPUs. More devices do not guarantee monotonic latency improvement; communication overhead can erase theoretical compute scaling.',
      misconceptionTested: 'Tensor parallelism scales serving latency inversely with GPU count regardless of collective communication and synchronization overhead.',
    }),
  ]),
});
