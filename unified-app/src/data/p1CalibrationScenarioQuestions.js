export const P1_CALIBRATION_SCENARIOS_BY_LESSON = Object.freeze({
  calibration: [
    {
      id: 'calibration-ece-weighted-worked',
      level: 'calculation',
      relatedComparison: 'unweighted-bin-gap-vs-frequency-weighted-ece',
      scenario: 'A reliability audit has two bins. Bin A contains 100 predictions with mean score 0.20 and observed positive rate 0.10. Bin B contains 300 predictions with mean score 0.70 and observed positive rate 0.65. Use ECE = sum over bins of (bin_count / total_count) * absolute calibration gap.',
      prompt: 'What ECE does this audit produce?',
      choices: [
        '6.25 percentage points because 0.25*0.10 + 0.75*0.05 = 0.0625',
        '7.5 percentage points because ECE is the unweighted mean of the two absolute gaps',
        '15 percentage points because ECE adds the two gaps without weighting or normalization',
      ],
      answerIndex: 0,
      explanation: 'Bin A contributes 100/400 * |0.20-0.10| = 0.025. Bin B contributes 300/400 * |0.70-0.65| = 0.0375. The total is 0.0625, or 6.25 percentage points. ECE weights local reliability errors by how much prediction mass each bin contains.',
      misconceptionTested: 'Expected calibration error is an unweighted average or raw sum of per-bin reliability gaps regardless of bin size.',
    },
    {
      id: 'calibration-ranking-stable-probability-drift',
      level: 'diagnosis',
      relatedComparison: 'stable-discrimination-vs-drifting-calibration',
      scenario: 'A risk model keeps nearly the same ROC AUC after deployment, but recent labeled cases show that scores around 0.60 correspond to only 0.35 observed event frequency. The product uses those scores in expected-cost calculations.',
      prompt: 'What should the team conclude?',
      choices: [
        'Ranking can remain strong while probability calibration drifts, so the probability mapping must be revalidated before cost-based decisions continue',
        'Stable ROC AUC proves the 0.60 scores still mean 60% risk, so no probability audit is needed',
        'Calibration drift necessarily means the model can no longer rank positives above negatives at all',
      ],
      answerIndex: 0,
      explanation: 'Discrimination and calibration are different properties. A model can preserve score ordering while the numeric probability levels become systematically too high or too low. Probability-sensitive decisions therefore need a fresh reliability audit even when AUC is stable.',
      misconceptionTested: 'Stable ranking metrics guarantee stable probability meaning after deployment.',
    },
    {
      id: 'calibration-shift-recalibration-decision',
      level: 'decision',
      relatedComparison: 'one-time-calibration-vs-representative-recalibration',
      scenario: 'A fraud model was calibrated when prevalence was 1%. Six months later, representative recent labels show prevalence near 3% and persistent underprediction across most score bins. Ranking remains useful and feature definitions are unchanged.',
      prompt: 'What is the strongest next action before continuing to interpret scores as probabilities?',
      choices: [
        'Fit and validate an appropriate recalibration mapping on representative recent labeled data, then evaluate it on data not used to fit that mapping',
        'Keep the original mapping permanently because calibration is a property learned once and independent of population shift',
        'Change the hard threshold only and continue reporting the old scores as exact probabilities without checking reliability',
      ],
      answerIndex: 0,
      explanation: 'Persistent reliability drift on representative recent labels is a legitimate recalibration trigger. Recalibration should use a labeled calibration role and still leave untouched evidence for evaluating the updated probability mapping.',
      misconceptionTested: 'Once a model is calibrated, population or prevalence changes cannot invalidate its probability levels.',
    },
    {
      id: 'calibration-isotonic-small-sample-overfit',
      level: 'decision',
      relatedComparison: 'flexible-isotonic-vs-simpler-calibrator-under-low-data',
      scenario: 'A team has only 250 labeled calibration examples, including 18 positives. A highly flexible isotonic map fits the calibration split almost perfectly but creates many sharp steps and performs worse on the next labeled batch than the uncalibrated model.',
      prompt: 'What is the most defensible interpretation and next experiment?',
      choices: [
        'The calibrator likely overfit sparse calibration data; compare a simpler mapping such as Platt or temperature-style scaling and validate out of sample',
        'Perfect fit on the calibration split proves isotonic calibration must generalize better than simpler mappings',
        'Calibration methods cannot overfit because they are applied after the base model is frozen',
      ],
      answerIndex: 0,
      explanation: 'A calibrator is itself a learned model. Flexible monotonic mappings can chase noise when labeled calibration data are scarce, especially with few positive outcomes. Method flexibility should match data support and be evaluated on separate data.',
      misconceptionTested: 'Post-hoc calibration cannot overfit because it does not change the base model parameters.',
    },
    {
      id: 'calibration-subgroup-cancellation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'aggregate-reliability-vs-segment-reliability',
      scenario: 'For score 0.60, the overall population has about 60% positives. In subgroup A, 0.60-scored cases are positive 80% of the time; in subgroup B they are positive 40% of the time. The two subgroups are similarly sized.',
      prompt: 'What does the aggregate result hide?',
      choices: [
        'Offsetting subgroup miscalibration: the model is underconfident for A and overconfident for B even though the aggregate bucket looks calibrated',
        'Perfect subgroup calibration because averaging to 60% guarantees every segment has the same observed rate',
        'A ranking failure that can only be fixed by changing ROC AUC',
      ],
      answerIndex: 0,
      explanation: 'Aggregate calibration can be produced by errors that cancel across subgroups. A 0.60 score understates risk in subgroup A and overstates it in subgroup B, so probability-sensitive decisions need segment-aware reliability checks.',
      misconceptionTested: 'Overall calibration guarantees calibration within important subgroups.',
    },
    {
      id: 'calibration-final-test-leakage-decision',
      level: 'design',
      relatedComparison: 'calibration-fit-data-vs-final-evaluation-data',
      scenario: 'A team trains the base model on train data, evaluates several calibrators on the final test set, then fits the chosen calibrator using those same test labels and reports its post-fit test ECE as the final unbiased result.',
      prompt: 'What is wrong with this workflow?',
      choices: [
        'The final test labels influenced calibrator choice and fitting, so the reported post-fit test ECE is selection-biased; the calibration and final evaluation roles must be separated',
        'Nothing is wrong because only the calibrator, not the base model, used the final test labels',
        'Calibration requires fitting on the final test set because validation predictions are never allowed for probability mapping',
      ],
      answerIndex: 0,
      explanation: 'Once final test labels are used to choose or fit the mapping, that set is no longer an untouched estimate of the complete pipeline. The base model, calibrator, and final assessment need clear data roles or an appropriate cross-fitting/nested protocol.',
      misconceptionTested: 'Using final test labels for post-hoc calibration does not contaminate evaluation as long as the base classifier was trained elsewhere.',
    },
  ],
});
