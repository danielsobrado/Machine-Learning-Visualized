export const P1_SEQUENTIAL_TESTING_PEEKING_SCENARIOS_BY_LESSON = Object.freeze({
  'sequential-testing-peeking': Object.freeze([
    Object.freeze({
      id: 'sequential-naive-peeking-any-look-worked',
      level: 'calculation',
      relatedComparison: 'single-look-alpha-vs-naive-repeated-look-error',
      scenario: 'For intuition, assume 20 interim looks were independent and each used the ordinary alpha = 0.05 threshold under a true null. The approximate chance of at least one false positive is 1 - (1 - 0.05)^20.',
      prompt: 'What approximate any-look false-positive probability does this naive monitoring procedure create?',
      choices: Object.freeze([
        'About 64%, because 1 - 0.95^20 is approximately 0.642',
        'Exactly 5%, because every individual look uses alpha = 0.05',
        'About 1%, because repeated monitoring averages random noise away',
      ]),
      answerIndex: 0,
      explanation: 'Under the simplified independence approximation, 0.95^20 is about 0.358, so the chance of at least one threshold crossing is about 0.642. Real cumulative looks are correlated, so this is not the exact experiment calculation, but it demonstrates why reusing 0.05 at every unplanned look does not preserve a 5% procedure-level error rate.',
      misconceptionTested: 'A nominal 5% threshold at every interim look automatically keeps the whole repeated-monitoring procedure at 5% false-positive risk.',
    }),
    Object.freeze({
      id: 'sequential-monitoring-design-choice',
      level: 'design',
      relatedComparison: 'fixed-horizon-vs-preplanned-sequential-monitoring',
      scenario: 'Before launch, a product team knows it must review the experiment at 25%, 50%, and 75% information because rollout decisions cannot wait until the maximum sample. The team still wants a controlled 5% Type I error rate and approximately the planned power.',
      prompt: 'Which design is most defensible before data collection starts?',
      choices: Object.freeze([
        'Plan a group-sequential or alpha-spending design with declared information times, stopping boundaries, and sample-size/power calculations that account for those looks',
        'Call the experiment fixed-horizon but use p < 0.05 independently at each review because the final maximum sample was declared',
        'Wait until each interim result is visible, then choose whichever stopping threshold gives the fastest decision',
      ]),
      answerIndex: 0,
      explanation: 'The required interim decisions are part of the statistical procedure and should be designed up front. A sequential design can preserve procedure-level error control while explicitly trading expected sample size, maximum sample size, and early-stopping opportunities.',
      misconceptionTested: 'Declaring a final sample size is enough to preserve fixed-horizon validity even when interim results are allowed to trigger early decisions.',
    }),
    Object.freeze({
      id: 'sequential-futility-conditional-power-decision',
      level: 'decision',
      relatedComparison: 'continue-to-maximum-sample-vs-prespecified-futility-stop',
      scenario: 'A trial is at 50% information. Its protocol says a nonbinding futility review may recommend stopping when conditional power is below 10%. The reviewed conditional power is 7%, no safety concern requires continuation, and the efficacy boundary has not been crossed.',
      prompt: 'What conclusion is justified by the stated rule?',
      choices: Object.freeze([
        'Stopping for futility is defensible under the pre-specified rule, while recognizing that futility is not proof that the true effect is exactly zero',
        'Declare the null hypothesis proven because 7% conditional power means there is a 93% probability of no treatment effect',
        'Ignore the futility plan and continue automatically because only efficacy boundaries are valid reasons to stop sequential experiments',
      ]),
      answerIndex: 0,
      explanation: 'Conditional power is a design-dependent projection of eventual success, not a posterior probability that the null is true. A pre-specified futility rule can save expected sample size when evidence suggests continuing is unlikely to be useful.',
      misconceptionTested: 'Crossing a futility boundary proves the null is true, or futility rules are statistically meaningless compared with efficacy boundaries.',
    }),
    Object.freeze({
      id: 'sequential-early-stop-estimate-bias-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'naive-crossing-look-estimate-vs-sequentially-adjusted-inference',
      scenario: 'A valid sequential experiment stops for efficacy at its first allowed look after an unusually large observed lift of +8%. Similar replications and a stopping-rule-aware analysis suggest the underlying effect is closer to +4%. The launch memo reports only the raw +8% estimate and an ordinary fixed-horizon confidence interval.',
      prompt: 'What is wrong with the launch memo?',
      choices: Object.freeze([
        'It ignores selection from stopping on an unusually favorable look; effect estimation and uncertainty should account for the sequential stopping rule',
        'Nothing is wrong because a valid stopping boundary guarantees the raw effect estimate is unbiased',
        'The +8% estimate must be discarded completely because sequential tests cannot estimate treatment effects after stopping',
      ]),
      answerIndex: 0,
      explanation: 'A sequential test can control Type I error and still select an unusually favorable realization when it stops early. Naive point estimates and ordinary intervals can therefore overstate the effect, so stopping-rule-aware estimation and uncertainty should be reported.',
      misconceptionTested: 'Valid sequential hypothesis testing automatically makes the raw effect estimate and ordinary fixed-horizon confidence interval unbiased after early stopping.',
    }),
    Object.freeze({
      id: 'sequential-multi-metric-peeking-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'single-primary-monitoring-rule-vs-look-by-metric-shopping',
      scenario: 'An experiment has one declared primary metric and 24 secondary metrics. At every daily look the team scans all 25 metrics and stops if any metric reaches p < 0.05, then presents that metric as the reason for launch. No multiplicity or sequential correction was planned.',
      prompt: 'What failure should the review identify?',
      choices: Object.freeze([
        'The procedure combines repeated peeking with metric shopping, creating far more opportunities for chance significance than the nominal threshold describes',
        'Only the secondary metrics are invalid; the stopping decision remains a valid 5% test because at least one metric was predeclared',
        'Scanning more metrics makes the experiment more conservative because most metrics will not cross 0.05',
      ]),
      answerIndex: 0,
      explanation: 'The decision rule searches across both time and metrics. Each dimension adds opportunities to select noise, so the monitoring protocol must define which metrics can trigger decisions and how multiplicity and repeated looks are controlled.',
      misconceptionTested: 'Sequential validity can be preserved while freely choosing whichever monitored metric first becomes significant.',
    }),
    Object.freeze({
      id: 'sequential-efficacy-vs-guardrail-decision',
      level: 'decision',
      relatedComparison: 'efficacy-boundary-crossing-vs-prespecified-harm-guardrail',
      scenario: 'At a planned interim look, the primary conversion metric crosses its pre-specified efficacy boundary. At the same look, checkout latency exceeds a pre-specified severe-harm guardrail that requires pausing rollout. The team argues that statistical efficacy should override the latency rule.',
      prompt: 'What should the experiment policy do?',
      choices: Object.freeze([
        'Honor the guardrail action and pause or roll back despite efficacy evidence; benefit and operational harm are separate decision criteria defined in the monitoring plan',
        'Launch immediately because crossing an efficacy boundary proves the treatment is globally beneficial',
        'Ignore both results until the maximum sample because interim guardrails and efficacy boundaries cannot drive actions',
      ]),
      answerIndex: 0,
      explanation: 'A valid efficacy boundary answers whether there is sufficient evidence of benefit on the primary metric. It does not erase a separately pre-specified safety or operational constraint. Guardrails can block rollout even when efficacy is statistically convincing.',
      misconceptionTested: 'Once a sequential efficacy boundary is crossed, pre-specified harm guardrails no longer matter for the launch decision.',
    }),
  ]),
});
