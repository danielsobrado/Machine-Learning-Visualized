export const P1_SVD_SCENARIOS_BY_LESSON = Object.freeze({
  svd: [
    {
      id: 'svd-error-norms-worked',
      level: 'calculation',
      relatedComparison: 'spectral-error-vs-frobenius-error',
      scenario: 'A matrix has singular values [9, 3, 1]. A rank-1 truncated SVD keeps only the singular value 9. For the optimal rank-1 approximation, spectral-norm error is the largest omitted singular value, while squared Frobenius error is the sum of squared omitted singular values.',
      prompt: 'What are the spectral-norm error and squared Frobenius error?',
      choices: [
        'Spectral error = 3 and squared Frobenius error = 10',
        'Spectral error = 9 and squared Frobenius error = 81',
        'Spectral error = 4 and squared Frobenius error = 16',
      ],
      answerIndex: 0,
      explanation: 'The largest omitted singular value is 3, so the optimal rank-1 spectral-norm error is 3. The squared Frobenius error is 3^2 + 1^2 = 10. Different norms summarize different aspects of the discarded singular structure.',
      misconceptionTested: 'All low-rank reconstruction-error metrics are numerically identical or depend on the largest retained singular value.',
    },
    {
      id: 'svd-pseudoinverse-cutoff-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'exact-pseudoinverse-vs-truncated-regularized-inverse',
      scenario: 'A least-squares solver uses SVD. One singular value is 1e-9, and the corresponding component of U^T b contains measurement noise around 1e-5. Dividing by that singular value creates a coefficient contribution around 10,000 and makes the solution wildly unstable across tiny data perturbations.',
      prompt: 'What should the numerical design investigate first?',
      choices: [
        'A documented singular-value cutoff or regularization so the noise-dominated near-null direction is not inverted blindly',
        'Keeping the exact reciprocal of every nonzero floating-point singular value because mathematical nonzero values must always be trusted',
        'Increasing the unstable coefficient further so the reconstructed training residual becomes exactly zero',
      ],
      answerIndex: 0,
      explanation: 'Pseudoinverse directions scale by 1/sigma. A tiny singular value can amplify small noise enormously, so practical solvers use tolerances or regularization to decide which directions are reliable enough to invert.',
      misconceptionTested: 'Every floating-point singular value that is technically nonzero should be inverted exactly regardless of scale or noise.',
    },
    {
      id: 'svd-condition-number-worked',
      level: 'calculation',
      relatedComparison: 'singular-spectrum-vs-solve-sensitivity',
      scenario: 'An invertible matrix has largest singular value 120 and smallest singular value 0.06. For the 2-norm, condition number is sigma_max / sigma_min.',
      prompt: 'What condition number does the singular spectrum imply?',
      choices: [
        '2,000, indicating potentially strong amplification of relative perturbations',
        '120.06, because condition number adds the extreme singular values',
        '0.0005, because conditioning uses sigma_min / sigma_max',
      ],
      answerIndex: 0,
      explanation: '120 / 0.06 = 2,000. A large condition number means the inverse problem can be highly sensitive even when the numerical algorithm itself is implemented correctly.',
      misconceptionTested: 'Condition number is an additive spread statistic or becomes small when the smallest singular value is tiny.',
    },
    {
      id: 'svd-rank-tolerance-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'exact-zero-rank-vs-numerical-rank',
      scenario: 'Two copies of the same matrix differ only by an overall scale factor of 1,000. Their smallest singular values scale from 2e-8 to 2e-5. A rank detector uses the fixed absolute rule sigma > 1e-6 and therefore reports different ranks for the two scaled copies.',
      prompt: 'What is the core defect in the rank detector?',
      choices: [
        'Its tolerance is not scale-aware, so numerical rank changes under a harmless global rescaling',
        'SVD rank must change whenever every matrix entry is multiplied by a nonzero constant',
        'The largest singular value should be discarded so both matrices have the same absolute spectrum',
      ],
      answerIndex: 0,
      explanation: 'Multiplying a matrix by a nonzero scalar does not change its mathematical rank, but it scales every singular value. Numerical-rank thresholds therefore need a documented relative/scale-aware rule tied to matrix size, precision, or the leading singular value.',
      misconceptionTested: 'A single absolute singular-value threshold is a scale-invariant definition of numerical rank.',
    },
    {
      id: 'svd-economy-storage-worked',
      level: 'calculation',
      relatedComparison: 'full-svd-vs-economy-svd-storage',
      scenario: 'A tall matrix has shape 10,000 x 200 and full column rank. A full SVD would store U as 10,000 x 10,000, while an economy SVD stores U with only 200 columns. Count only U entries for this comparison.',
      prompt: 'How many U entries does economy SVD store versus full SVD?',
      choices: [
        '2,000,000 versus 100,000,000 entries, a 50x reduction for U',
        '100,000,000 versus 2,000,000 entries, because economy SVD adds unused basis vectors',
        'Both store 2,000,000 entries because full and economy SVD have identical U shapes for tall matrices',
      ],
      answerIndex: 0,
      explanation: 'Economy U stores 10,000 x 200 = 2,000,000 entries. Full U stores 10,000 x 10,000 = 100,000,000 entries. When only the active singular subspace is needed, economy form avoids a large orthonormal completion.',
      misconceptionTested: 'Full SVD and economy SVD have the same memory footprint on tall matrices.',
    },
    {
      id: 'svd-randomized-topk-decision',
      level: 'decision',
      relatedComparison: 'full-svd-vs-partial-randomized-svd',
      scenario: 'A recommender pipeline has a very large sparse interaction matrix. The product only needs the leading 100 singular components for a low-rank embedding, and computing a complete dense SVD would exceed the batch-time and memory budget.',
      prompt: 'Which implementation strategy best matches the requirement?',
      choices: [
        'Use a tested partial or randomized SVD method that targets the leading components and validate approximation quality on held-out reconstruction or downstream metrics',
        'Materialize a full dense SVD anyway because approximate top-k methods cannot preserve any useful singular structure',
        'Compute only the 100 smallest singular values because low-rank approximation is driven by near-null directions',
      ],
      answerIndex: 0,
      explanation: 'When only leading components are needed at large scale, partial or randomized methods can avoid the cost of a full decomposition. Their approximation error still needs validation against the actual downstream or reconstruction objective.',
      misconceptionTested: 'Using SVD for top-k structure always requires computing the entire exact decomposition first.',
    },
  ],
});
