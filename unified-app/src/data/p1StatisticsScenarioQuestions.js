export const P1_STATISTICS_SCENARIOS_BY_LESSON = Object.freeze({
  'linear-regression': [
    {
      id: 'lr-heteroscedastic-residuals',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { residualSpread: 'fans outward as fitted value rises', meanResidual: 'near zero', fittedLine: 'reasonable center' },
      scenario: 'Visual state: residuals stay centered near zero, but their spread grows steadily as fitted values increase.',
      prompt: 'What issue should be investigated?',
      choices: ['Heteroscedasticity because residual variance changes with prediction level', 'A guaranteed nonlinear mean relationship', 'Perfectly constant error variance'],
      answerIndex: 0,
      explanation: 'A fan-shaped residual plot indicates non-constant variance, which can affect uncertainty estimates even when the conditional mean fit is reasonable.',
      misconceptionTested: 'Residual mean near zero guarantees all linear-regression assumptions are satisfied.',
    },
    {
      id: 'lr-influential-outlier',
      level: 'diagnosis',
      scenario: 'One observation sits far from the rest in x and pulls the fitted line sharply toward itself. Removing it changes the slope from 0.8 to 0.2.',
      prompt: 'What property makes this point especially important?',
      choices: ['High leverage combined with strong influence on the fitted coefficients', 'Low variance because it is far from the mean', 'Proof that every residual is Gaussian'],
      answerIndex: 0,
      explanation: 'An extreme predictor value can have high leverage, and a large coefficient change after removal indicates the point is influential rather than merely unusual in y.',
      misconceptionTested: 'All outliers affect a regression fit in the same way.',
    },
  ],
  'roc-pr-curves': [
    {
      id: 'roc-pr-rare-positive',
      level: 'comparison',
      relatedComparison: 'roc-vs-pr-imbalance',
      scenario: 'Only 0.2% of examples are positive. A model keeps a low false-positive rate but still generates thousands of false alarms because negatives are extremely common.',
      prompt: 'Which curve is often more revealing for positive-class retrieval quality?',
      choices: ['The precision-recall curve because precision reflects false alarms among predicted positives', 'ROC alone because prevalence never matters for operational false alarms', 'A residual plot'],
      answerIndex: 0,
      explanation: 'With extreme imbalance, ROC can look strong while precision remains poor because even a small false-positive rate can create many false positives.',
      misconceptionTested: 'A strong ROC AUC always implies useful precision on rare-positive tasks.',
    },
    {
      id: 'roc-pr-threshold-operating-point',
      level: 'application',
      relatedComparison: 'auc-vs-operating-threshold',
      scenario: 'Two models have similar AUC, but one offers much higher recall at the false-positive budget the product can tolerate.',
      prompt: 'Which model evidence matters more for deployment?',
      choices: ['Performance at the actual operating threshold or constraint', 'AUC alone because it uniquely determines the deployed threshold', 'The model with more parameters'],
      answerIndex: 0,
      explanation: 'AUC summarizes ranking across thresholds, but deployment decisions require evaluating the region and threshold that match actual error costs or constraints.',
      misconceptionTested: 'AUC by itself determines the best deployed classifier.',
    },
  ],
  'train-validation-test-split': [
    {
      id: 'split-grouped-entity',
      level: 'design',
      relatedComparison: 'random-vs-grouped-split',
      scenario: 'A medical dataset contains several visits per patient. A random row split puts different visits from the same patient in train and validation.',
      prompt: 'What split design is safer?',
      choices: ['Group by patient so one patient cannot appear on both sides of the evaluation boundary', 'Keep the row split because visit rows are separate files', 'Duplicate patient visits into both sets to stabilize the score'],
      answerIndex: 0,
      explanation: 'Repeated entities create dependence and can leak person-specific signals, so grouping by patient better estimates performance on unseen patients.',
      misconceptionTested: 'Rows are independent just because they are separate records.',
    },
    {
      id: 'split-train-serve-semantics',
      level: 'diagnosis',
      scenario: 'Offline validation uses a feature computed with a 30-day history window, but production computes the same feature name with a 7-day window.',
      prompt: 'Why can a clean test split still fail to predict production behavior?',
      choices: ['The offline and serving feature semantics differ despite a correct dataset split', 'A held-out test set automatically fixes train/serve skew', 'Changing the history window only affects logging'],
      answerIndex: 0,
      explanation: 'Honest splitting protects evaluation boundaries, but production validity also requires the serving pipeline to reproduce the feature semantics used offline.',
      misconceptionTested: 'A clean train/test split guarantees train/serve parity.',
    },
  ],
  'feature-scaling-preprocessing': [
    {
      id: 'scaling-robust-outliers',
      level: 'comparison',
      relatedComparison: 'standard-vs-robust-scaling',
      scenario: 'A numeric feature has a stable central range but a few extreme values that inflate the mean and standard deviation.',
      prompt: 'Which preprocessing option is worth comparing against ordinary standardization?',
      choices: ['A robust scaler based on median and interquartile range', 'A transform that uses validation labels', 'Dropping the train/test split'],
      answerIndex: 0,
      explanation: 'Median and IQR are less sensitive to extreme values, so robust scaling can preserve the central geometry when a few outliers distort mean and variance.',
      misconceptionTested: 'Standard scaling is equally robust to arbitrary outliers.',
    },
    {
      id: 'scaling-model-family',
      level: 'comparison',
      relatedComparison: 'distance-margin-tree-scaling',
      scenario: 'The same feature rescaling dramatically changes kNN and SVM results but barely changes a standard decision tree split structure.',
      prompt: 'What explains the difference?',
      choices: ['Distance and margin methods depend on feature scale, while ordinary tree thresholds are largely invariant to monotonic rescaling', 'Trees compute Euclidean distance more strongly than kNN', 'Scaling changes class labels for every model family'],
      answerIndex: 0,
      explanation: 'Rescaling changes geometric distances and margins, but a decision tree can usually move its threshold to preserve the same ordering-based split.',
      misconceptionTested: 'Every ML algorithm needs scaling for the same reason.',
    },
  ],
  'bias-variance-tradeoff': [
    {
      id: 'bias-variance-noisy-labels',
      level: 'diagnosis',
      scenario: 'Training error and validation error both stop improving even after model capacity and sample size increase, and manual review finds many inconsistent labels.',
      prompt: 'Which error source is likely limiting progress?',
      choices: ['Irreducible or label noise rather than only bias or variance', 'Variance must always be the sole cause when validation error is nonzero', 'More depth is guaranteed to solve inconsistent labels'],
      answerIndex: 0,
      explanation: 'When labels are inconsistent or intrinsically noisy, added capacity or data may not remove the remaining error because the target itself contains uncertainty or mistakes.',
      misconceptionTested: 'All generalization error can be eliminated by tuning model complexity.',
    },
    {
      id: 'bias-variance-more-data',
      level: 'comparison',
      relatedComparison: 'high-bias-vs-high-variance-remedies',
      scenario: 'Model A has high train and validation error. Model B has very low train error but much higher validation error.',
      prompt: 'Where is collecting more representative data more likely to help directly?',
      choices: ['Model B, because additional data can reduce variance when the model already fits training data well', 'Model A only, because more data automatically fixes underfitting', 'Neither model because sample size never affects variance'],
      answerIndex: 0,
      explanation: 'More data often helps high-variance models by stabilizing estimates, while a high-bias model usually needs a better representation or more appropriate capacity first.',
      misconceptionTested: 'More data is equally effective for high bias and high variance.',
    },
  ],
  'knn-naive-bayes-svm': [
    {
      id: 'classifier-family-scaling',
      level: 'comparison',
      relatedComparison: 'knn-svm-naive-bayes-scaling',
      scenario: 'One continuous feature is measured in millimeters and another in meters. Their raw numeric ranges differ by three orders of magnitude.',
      prompt: 'Which models are most directly at risk from the unit mismatch?',
      choices: ['kNN and many SVM configurations because distance or margin geometry changes', 'Naive Bayes only because it never models feature distributions', 'None because units cannot affect numerical algorithms'],
      answerIndex: 0,
      explanation: 'kNN and SVM rely on geometric relationships that depend on numeric scale, while Naive Bayes sensitivity depends on its feature likelihood model rather than a shared Euclidean geometry.',
      misconceptionTested: 'Feature units are irrelevant to distance and margin classifiers.',
    },
    {
      id: 'classifier-family-imbalance',
      level: 'application',
      relatedComparison: 'class-weight-threshold-resampling',
      scenario: 'A minority class is 2% of data and missing it is expensive. All three candidate classifiers achieve high accuracy by favoring the majority class.',
      prompt: 'What should the comparison include?',
      choices: ['Minority-sensitive metrics plus class weighting, resampling, or threshold choices appropriate to each model', 'Accuracy only because every model sees the same imbalance', 'Dropping the minority examples so the classes are simpler'],
      answerIndex: 0,
      explanation: 'Imbalanced classification requires evaluation and training choices that reflect minority errors; raw accuracy can hide severe minority-class failure.',
      misconceptionTested: 'Using the same dataset makes accuracy a fair enough comparison under severe imbalance.',
    },
  ],
  'tree-ensembles': [
    {
      id: 'tree-attribution-correlated-features',
      level: 'diagnosis',
      relatedComparison: 'impurity-vs-permutation-importance',
      scenario: 'Two features carry nearly the same signal. One receives high split importance while the other receives little, yet removing either alone barely hurts performance.',
      prompt: 'What should you conclude?',
      choices: ['Correlated substitutes can make single-feature importance unstable or misleading', 'The low-importance feature is proven useless', 'Tree importance directly measures causal effect'],
      answerIndex: 0,
      explanation: 'When predictors substitute for one another, a tree may choose one for splits and attribution can be distributed arbitrarily, so importance needs stability and correlation-aware interpretation.',
      misconceptionTested: 'Tree feature importance uniquely identifies causal or necessary features.',
    },
    {
      id: 'boosting-overfit-rounds',
      level: 'visual-state',
      kind: 'visual-state',
      visualState: { boostingRounds: 900, trainLoss: 'keeps falling', validationLoss: 'bottoms at 350 then rises', learningRate: 0.1 },
      scenario: 'Visual state: training loss keeps falling through 900 boosting rounds, but validation loss reaches its minimum near round 350 and then rises.',
      prompt: 'What is the clearest diagnosis?',
      choices: ['Later boosting rounds are overfitting and early stopping around the validation optimum should be considered', 'The model is underfitting because training loss is falling', 'Validation loss should be ignored once boosting begins'],
      answerIndex: 0,
      explanation: 'A widening train-validation gap after the validation optimum indicates extra rounds are fitting training-specific residuals without improving generalization.',
      misconceptionTested: 'More boosting rounds always improve generalization if training loss decreases.',
    },
  ],
  'maximum-likelihood-estimation': [
    {
      id: 'mle-vs-map-prior',
      level: 'comparison',
      relatedComparison: 'mle-vs-map',
      scenario: 'Two parameter values fit a small dataset almost equally well, but prior domain knowledge strongly favors one region of parameter space.',
      prompt: 'Which estimate explicitly combines data likelihood with the prior?',
      choices: ['MAP estimation', 'Maximum likelihood alone', 'A confusion matrix'],
      answerIndex: 0,
      explanation: 'MAP maximizes the posterior, which combines likelihood with a prior, while MLE uses only the likelihood from observed data.',
      misconceptionTested: 'MLE and MAP are identical whenever a prior is informative.',
    },
  ],
  'loss-functions-likelihoods': [
    {
      id: 'loss-categorical-nll',
      level: 'application',
      relatedComparison: 'bernoulli-vs-categorical-nll',
      scenario: 'A model predicts one mutually exclusive class among 12 classes with a softmax output.',
      prompt: 'Which likelihood/loss pairing matches the target most directly?',
      choices: ['Categorical likelihood with multiclass cross-entropy or negative log-likelihood', 'Gaussian likelihood with ordinary squared error on class IDs', 'Poisson likelihood on the class index'],
      answerIndex: 0,
      explanation: 'A softmax over mutually exclusive classes defines a categorical distribution, and minimizing categorical cross-entropy is equivalent to minimizing its negative log-likelihood.',
      misconceptionTested: 'Multiclass labels should be treated as continuous numeric targets.',
    },
    {
      id: 'loss-label-smoothing',
      level: 'comparison',
      relatedComparison: 'hard-target-vs-label-smoothing',
      scenario: 'A classifier becomes extremely confident on training labels and poorly calibrated on slightly noisy data.',
      prompt: 'What does label smoothing change in the training target?',
      choices: ['It replaces the exact one-hot target with a softened categorical target distribution', 'It changes the labels into regression residuals', 'It guarantees perfect calibration after training'],
      answerIndex: 0,
      explanation: 'Label smoothing assigns a small amount of target mass away from the labeled class, discouraging infinitely confident logits without guaranteeing downstream calibration.',
      misconceptionTested: 'Label smoothing is the same as post-hoc probability calibration.',
    },
  ],
  'sampling-confidence-intervals': [
    {
      id: 'ci-bootstrap-skewed-statistic',
      level: 'comparison',
      relatedComparison: 'analytic-vs-bootstrap-interval',
      scenario: 'You need uncertainty for a median on a skewed distribution, and a simple normal-theory standard error is not reliable.',
      prompt: 'Which approach is worth evaluating?',
      choices: ['A bootstrap confidence interval based on repeated resampling', 'A confidence interval that assumes the median equals the mean', 'Using the full dataset as both sample and population with zero uncertainty'],
      answerIndex: 0,
      explanation: 'Bootstrap resampling can approximate the sampling distribution of statistics such as medians when simple analytic normal approximations are poor.',
      misconceptionTested: 'Every statistic has a reliable symmetric normal-theory confidence interval.',
    },
  ],
  'hypothesis-testing-intuition': [
    {
      id: 'hypothesis-one-vs-two-sided',
      level: 'design',
      relatedComparison: 'one-sided-vs-two-sided-test',
      scenario: 'Before collecting data, a safety team cares about both harmful increases and unexpected decreases in an error rate.',
      prompt: 'Which alternative is more appropriate?',
      choices: ['A two-sided test because deviations in either direction matter', 'A one-sided test chosen after seeing the direction of the result', 'No hypothesis test because direction is unknown'],
      answerIndex: 0,
      explanation: 'When both directions are decision-relevant, a two-sided alternative should be specified before observing the data rather than chosen opportunistically afterward.',
      misconceptionTested: 'You can select one-sided versus two-sided testing after inspecting the result without affecting validity.',
    },
    {
      id: 'hypothesis-multiple-testing',
      level: 'diagnosis',
      relatedComparison: 'single-test-vs-familywise-testing',
      scenario: 'A dashboard tests 200 unrelated metrics at alpha = 0.05 and highlights every p-value below 0.05 with no correction.',
      prompt: 'What problem should you expect?',
      choices: ['Many false positives simply from the large number of tests', 'Exactly 5% probability that any false positive occurs across the whole dashboard', 'Multiple testing automatically increases statistical power without cost'],
      answerIndex: 0,
      explanation: 'Running many tests creates many opportunities for chance significance, so familywise error or false-discovery control may be needed depending on the decision problem.',
      misconceptionTested: 'Per-test alpha equals the family-level false-positive probability under many tests.',
    },
  ],
  'power-sample-size': [
    {
      id: 'power-paired-design',
      level: 'comparison',
      relatedComparison: 'paired-vs-independent-design',
      scenario: 'The same users are measured before and after an intervention, and within-user measurements are strongly correlated.',
      prompt: 'Why can a paired analysis need fewer observations than treating the samples as independent?',
      choices: ['Pairing can remove stable between-user variability and reduce the variance of the estimated change', 'Pairing doubles alpha automatically', 'Paired designs make effect size irrelevant'],
      answerIndex: 0,
      explanation: 'When within-pair correlation is useful, analyzing differences can remove between-subject noise and improve power relative to an independent-sample design.',
      misconceptionTested: 'Paired and independent designs always have the same variance and sample-size requirement.',
    },
  ],
  'cuped-variance-reduction': [
    {
      id: 'cuped-bad-covariate',
      level: 'diagnosis',
      scenario: 'A CUPED covariate is measured after treatment begins and is itself affected by the experiment.',
      prompt: 'Why is this unsafe?',
      choices: ['The covariate is no longer purely pre-treatment and can bias the treatment comparison', 'Any correlated covariate is safe regardless of when it is measured', 'CUPED requires treatment to change the covariate'],
      answerIndex: 0,
      explanation: 'Variance-reduction covariates should be pre-treatment; conditioning on a post-treatment variable can remove or distort part of the treatment effect.',
      misconceptionTested: 'CUPED only cares about correlation, not treatment timing.',
    },
  ],
  'confounding-simpsons-paradox': [
    {
      id: 'simpson-standardization',
      level: 'application',
      relatedComparison: 'aggregate-vs-standardized-effect',
      scenario: 'Treatment and control groups have different age mixes, but treatment improves the outcome within every age band. The aggregate comparison reverses direction.',
      prompt: 'What analysis should be compared with the raw aggregate?',
      choices: ['A stratified or standardized comparison that accounts for the age mix', 'A larger aggregate sample with the same imbalance', 'Dropping the age variable because the aggregate is simpler'],
      answerIndex: 0,
      explanation: 'Simpson reversals can come from different group composition; stratification or standardization compares effects at a common covariate mix.',
      misconceptionTested: 'The aggregate effect is always more trustworthy than within-segment effects.',
    },
  ],
  'causal-graphs-dags': [
    {
      id: 'dag-mediator-adjustment',
      level: 'comparison',
      relatedComparison: 'confounder-vs-mediator-adjustment',
      scenario: 'Treatment changes exercise, and exercise changes blood pressure. You want the total treatment effect on blood pressure.',
      prompt: 'What happens if you adjust for exercise?',
      choices: ['You block part of the causal pathway and no longer estimate the total effect', 'You always remove confounding with no change to the estimand', 'You create randomization automatically'],
      answerIndex: 0,
      explanation: 'A mediator lies on the treatment-to-outcome causal path; conditioning on it changes the estimand from total effect toward a direct-effect question.',
      misconceptionTested: 'Every variable related to the outcome should be controlled for.',
    },
  ],
  'treatment-effects': [
    {
      id: 'cate-subgroup-uncertainty',
      level: 'diagnosis',
      scenario: 'A subgroup CATE estimate is +12%, but the subgroup contains only 40 users and its confidence interval spans -5% to +29%.',
      prompt: 'What is the safest interpretation?',
      choices: ['The subgroup effect is highly uncertain and should not be treated as a confirmed +12% benefit', 'The point estimate alone proves the subgroup should receive treatment', 'A positive CATE point estimate removes the need for uncertainty intervals'],
      answerIndex: 0,
      explanation: 'Small subgroup samples can produce unstable treatment-effect estimates, so interval width and multiple subgroup searches matter before targeting decisions.',
      misconceptionTested: 'A large subgroup point estimate is enough to establish a reliable heterogeneous effect.',
    },
  ],
  'propensity-scores': [
    {
      id: 'propensity-balance-smd',
      level: 'application',
      relatedComparison: 'propensity-score-vs-covariate-balance',
      scenario: 'After weighting, treated and control propensity-score histograms overlap well, but one important covariate still has a standardized mean difference of 0.35.',
      prompt: 'What should the analyst conclude?',
      choices: ['Balance remains poor on that covariate despite propensity overlap', 'Propensity overlap guarantees every covariate is balanced', 'A standardized mean difference cannot be used after weighting'],
      answerIndex: 0,
      explanation: 'Propensity methods should be checked using covariate-balance diagnostics such as standardized mean differences; overlap alone does not guarantee successful balancing.',
      misconceptionTested: 'A good-looking propensity-score distribution proves covariate balance.',
    },
    {
      id: 'propensity-extreme-weight-trim',
      level: 'diagnosis',
      scenario: 'A few observations receive inverse-probability weights above 80 because their treatment assignment was extremely unlikely.',
      prompt: 'What risk should be investigated?',
      choices: ['Extreme weights can explode variance and signal weak positivity or overlap', 'Large weights guarantee unbiased low-variance estimates', 'Weights above one are always invalid'],
      answerIndex: 0,
      explanation: 'Very small treatment probabilities create huge inverse weights, making estimates unstable and often indicating poor common support that may require trimming or a revised design.',
      misconceptionTested: 'Propensity weighting automatically solves lack of overlap without variance cost.',
    },
  ],
});

export function getP1StatisticsScenariosForLesson(lessonId) {
  return P1_STATISTICS_SCENARIOS_BY_LESSON[lessonId] || [];
}
