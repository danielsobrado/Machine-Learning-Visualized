export const P1_COCONUT_LATENT_REASONING_SCENARIOS_BY_LESSON = Object.freeze({
  'coconut-latent-reasoning': Object.freeze([
    Object.freeze({
      id: 'coconut-latent-feedback-path-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'language-mode-token-decoding-vs-latent-hidden-state-feedback',
      scenario: 'An implementation enters the Coconut latent region, sends every hidden state through the LM head, samples a token, embeds that token, and feeds the embedding into the next position. The trace still hides those sampled tokens from the user interface.',
      prompt: 'What is the architectural bug?',
      choices: Object.freeze([
        'The loop is still ordinary token decoding internally; latent mode should bypass token sampling and feed the previous hidden state directly as the next input representation',
        'Nothing is wrong because hiding sampled tokens in the interface is sufficient to make the computation latent',
        'The model should skip the transformer during latent positions and reuse one fixed vector until <eot>',
      ]),
      answerIndex: 0,
      explanation: 'Coconut changes the feedback path, not just what the interface displays. In latent mode, h_t is fed forward directly as x_t+1 instead of being decoded through the LM head and re-embedded as a discrete token.',
      misconceptionTested: 'Invisible token decoding is equivalent to continuous hidden-state reasoning.',
    }),
    Object.freeze({
      id: 'coconut-curriculum-loss-mask-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'direct-latent-token-loss-vs-downstream-text-supervision',
      scenario: 'During a later Coconut curriculum stage, the trainer replaces the first two reasoning steps with latent positions but assigns those positions placeholder token IDs and computes cross-entropy loss directly against the placeholders. Remaining reasoning text and the answer are also trained normally.',
      prompt: 'What should be corrected?',
      choices: Object.freeze([
        'Mask direct token loss on the latent positions and train those states through their effect on predicting the remaining future text and answer targets',
        'Keep the placeholder losses because every latent vector must be supervised as one discrete vocabulary token',
        'Remove loss from all text after the latent region so only the question tokens train the model',
      ]),
      answerIndex: 0,
      explanation: 'The latent states do not have gold word labels. The curriculum makes them useful by requiring later visible targets to remain predictable from the question plus the latent-state prefix.',
      misconceptionTested: 'Continuous thoughts should receive the same direct token-label supervision as visible reasoning tokens.',
    }),
    Object.freeze({
      id: 'coconut-delayed-commitment-threshold-worked',
      level: 'calculation',
      relatedComparison: 'early-hard-commitment-vs-delayed-branch-commitment',
      scenario: 'A planning probe tracks the largest candidate-branch probability after each latent step: [0.46, 0.63, 0.78, 0.86]. Define the commitment step as the first latent step where one branch exceeds a dominance threshold of 0.75.',
      prompt: 'At which latent step does commitment occur?',
      choices: Object.freeze([
        'Step 3, because 0.78 is the first maximum branch probability above 0.75',
        'Step 1, because the largest branch is already greater than every competing branch',
        'Step 4, because commitment can only be measured after the last latent position',
      ]),
      answerIndex: 0,
      explanation: 'The first two maxima, 0.46 and 0.63, remain below the 0.75 threshold. The third reaches 0.78, so the operational commitment step is 3. The metric distinguishes keeping alternatives alive from committing merely because one branch is currently largest.',
      misconceptionTested: 'A branch is committed as soon as it is the current argmax, regardless of the defined dominance threshold.',
    }),
    Object.freeze({
      id: 'coconut-probe-vs-causal-faithfulness-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'readable-probe-correlation-vs-causal-latent-evidence',
      scenario: 'A nearest-token probe repeatedly labels a latent state with a plausible planning word. However, ablating that state leaves the branch distribution and final answer unchanged across a representative evaluation set.',
      prompt: 'What is the strongest justified conclusion?',
      choices: Object.freeze([
        'The readable probe is not enough to establish that this state is a causally important reasoning step; stronger interventions or evidence are needed',
        'The probe proves the model reasoned through that word even though removing the state has no measurable effect',
        'The ablation proves all latent reasoning is useless, including every other latent state in the model',
      ]),
      answerIndex: 0,
      explanation: 'Probe readability is correlational. If removing the state does not change the relevant behavior, the evidence does not support a strong causal-faithfulness claim for that state, though redundancy or intervention strength should still be considered.',
      misconceptionTested: 'A semantically plausible nearest-token probe is sufficient proof of faithful causal reasoning.',
    }),
    Object.freeze({
      id: 'coconut-shortcut-latent-dependence-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'causal-latent-dependence-vs-robust-reasoning-content',
      scenario: 'A Coconut model reaches 92% accuracy on the training-style planning distribution and falls to 54% when a superficial shortcut cue is randomized. Ablating the latent region on the original distribution also causes a large accuracy drop.',
      prompt: 'How should these two findings be interpreted together?',
      choices: Object.freeze([
        'The latent region matters causally on the original distribution, but the shortcut-shift failure shows that dependence does not by itself prove robust or semantically faithful reasoning',
        'The ablation result proves the latent states encode the intended planning algorithm, so the shortcut-shift failure can be ignored',
        'The shortcut-shift failure proves the latent region was never used because any causal latent computation must generalize perfectly',
      ]),
      answerIndex: 0,
      explanation: 'Ablation can establish behavioral dependence, while a shortcut-resistant evaluation asks what information or strategy that dependence uses. Both are needed: a model can rely heavily on latent states that still encode a brittle shortcut.',
      misconceptionTested: 'Causal dependence on latent states is equivalent to robust, faithful reasoning.',
    }),
    Object.freeze({
      id: 'coconut-auditability-deployment-decision',
      level: 'decision',
      relatedComparison: 'compact-latent-search-vs-readable-audit-trace',
      scenario: 'A regulated workflow requires reviewers to inspect a human-readable intermediate rationale for every high-impact decision. A Coconut prototype is accurate and concise, but its useful intermediate computation is only visible through opaque latent states and unstable probes.',
      prompt: 'What deployment choice best fits the requirement?',
      choices: Object.freeze([
        'Do not treat the latent path as the required audit trail; provide a validated readable rationale or use a more transparent reasoning mode for the auditable workflow',
        'Ship latent-only reasoning because high final accuracy automatically satisfies intermediate auditability requirements',
        'Display the nearest-token probe output as a definitive transcript of the model\'s hidden reasoning',
      ]),
      answerIndex: 0,
      explanation: 'Coconut can separate internal search from external explanation. When readable intermediate reasoning is a product requirement, the system needs an independently validated explanation surface or a reasoning mode designed for that requirement rather than presenting opaque states as a transcript.',
      misconceptionTested: 'Accurate latent reasoning or token probes automatically satisfy human-auditability requirements.',
    }),
  ]),
});
