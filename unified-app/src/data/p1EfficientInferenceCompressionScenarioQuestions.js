export const P1_EFFICIENT_INFERENCE_COMPRESSION_SCENARIOS_BY_LESSON = Object.freeze({
  'efficient-inference-compression-track': [
    {
      id: 'inference-kv-cache-memory-worked',
      level: 'calculation',
      relatedComparison: 'context-concurrency-vs-kv-cache-memory',
      scenario: 'A decoder serves batch size 4 with sequence length 2,048, 24 layers, 8 KV heads, head dimension 64, and FP16 cache values. Use KV bytes = batch × sequence × layers × KV heads × head_dim × 2 for K and V × 2 bytes per FP16 value.',
      prompt: 'Approximately how much KV-cache memory does this configuration require?',
      choices: [
        '402,653,184 bytes, about 384 MiB',
        '100,663,296 bytes, about 96 MiB, because only keys or values need storage',
        '1,610,612,736 bytes, about 1.5 GiB, because FP16 uses 8 bytes per value',
      ],
      answerIndex: 0,
      explanation: '4 × 2,048 × 24 × 8 × 64 × 2 × 2 = 402,653,184 bytes. Dividing by 1,048,576 gives about 384 MiB. The cache scales directly with active sequences and context length, so concurrency and long contexts can consume substantial memory even when weights already fit.',
      misconceptionTested: 'KV-cache memory is negligible once model weights fit, or it is independent of batch size and context length.',
    },
    {
      id: 'inference-batching-slo-operating-point',
      level: 'decision',
      relatedComparison: 'throughput-vs-queueing-latency',
      scenario: 'On the same interactive workload, batching policy A delivers 800 tokens/s with p95 TTFT 300 ms, B delivers 1,200 tokens/s with p95 TTFT 550 ms, and C delivers 1,600 tokens/s with p95 TTFT 900 ms. The product requires p95 TTFT <= 600 ms.',
      prompt: 'Which policy is the strongest operating point under the stated constraint?',
      choices: [
        'Policy B, because it has the highest throughput among policies that satisfy the TTFT SLO',
        'Policy C, because throughput should always be maximized even when the user-facing latency SLO is violated',
        'Policy A, because the lowest TTFT must always win even when another policy remains within the SLO and serves substantially more work',
      ],
      answerIndex: 0,
      explanation: 'C violates the 600 ms TTFT limit. A and B both satisfy it, but B increases throughput by 50% over A while remaining within the stated latency budget. Serving configuration is constrained optimization rather than single-metric maximization.',
      misconceptionTested: 'Inference batching should be chosen by maximizing throughput or minimizing latency independently of the product SLO.',
    },
    {
      id: 'inference-unstructured-sparsity-no-speedup-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'parameter-sparsity-vs-real-hardware-latency',
      scenario: 'A pruning pass sets 55% of a model weights exactly to zero. Artifact compression improves, but the production runtime still executes the same dense matrix kernels and p95 latency changes from 42 ms to 41.8 ms.',
      prompt: 'Why did theoretical sparsity fail to produce meaningful serving speedup?',
      choices: [
        'The runtime and hardware are not exploiting the unstructured zero pattern, so fewer nonzero parameters do not automatically mean less executed work',
        'Any model with more than 50% zero weights must become slower because zeros require extra floating-point operations',
        'Pruning only affects training and can never reduce inference latency even with structured sparse kernels',
      ],
      answerIndex: 0,
      explanation: 'Unstructured pruning changes parameter values, but dense kernels can still perform essentially the same operations and memory movement. Real acceleration requires a sparsity pattern, representation, and hardware/kernel stack that can skip the removed work efficiently.',
      misconceptionTested: 'A large percentage of zero-valued weights guarantees a proportional latency improvement regardless of kernel and hardware support.',
    },
    {
      id: 'inference-quantization-calibration-domain-shift-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'generic-calibration-set-vs-representative-deployment-calibration',
      scenario: 'An INT8 activation-quantized model is calibrated using short generic prompts. It matches the FP16 model on those prompts but loses substantial quality on production legal documents, where activation ranges are much larger and outlier channels are common.',
      prompt: 'What should be investigated first?',
      choices: [
        'Quantization calibration representativeness and activation outliers, using deployment-like data to set or validate scales and clipping behavior',
        'The GPU brand only, because calibration data cannot affect post-training quantization quality',
        'Increasing batch size until the lost legal-domain accuracy returns automatically',
      ],
      answerIndex: 0,
      explanation: 'Activation quantization depends on runtime value ranges. A calibration set that misses the deployment distribution can choose poor scales or clipping thresholds, especially around outlier channels. Representative calibration and domain-specific quality evaluation are therefore part of the compression pipeline.',
      misconceptionTested: 'Post-training quantization quality is independent of the calibration-data distribution once the bit width is chosen.',
    },
    {
      id: 'inference-compression-quality-release-gate',
      level: 'decision',
      relatedComparison: 'throughput-gain-vs-quality-and-safety-regression',
      scenario: 'An INT4 candidate increases throughput by 80% and reduces GPU memory by 45%. On the frozen release suite, domain accuracy falls from 91% to 84%, calibration worsens materially, and one required safety slice falls below its minimum release threshold.',
      prompt: 'What is the correct release decision under a quality-gated serving policy?',
      choices: [
        'Do not release this candidate as-is; efficiency gains do not override failed quality and safety gates',
        'Release it because throughput and memory improvements are objective while quality regressions are only secondary metrics',
        'Release it only for longer prompts because compression quality cannot depend on task or safety slices',
      ],
      answerIndex: 0,
      explanation: 'Compression is a multi-objective optimization. The candidate is faster and smaller but violates explicit product-quality and safety requirements, so it is not a valid operating point. A less aggressive format, selective precision, better calibration, or a different model should be evaluated.',
      misconceptionTested: 'A sufficiently large throughput or memory improvement justifies shipping a compressed model even when frozen quality or safety release gates fail.',
    },
  ],
});
