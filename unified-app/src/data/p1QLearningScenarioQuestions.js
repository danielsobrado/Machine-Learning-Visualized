export const P1_Q_LEARNING_SCENARIOS_BY_LESSON = Object.freeze({
  'q-learning': Object.freeze([
    Object.freeze({
      id: 'qlearn-nonterminal-update-worked',
      level: 'calculation',
      relatedComparison: 'bellman-target-vs-td-error-vs-updated-q',
      scenario: 'A nonterminal transition has current Q(s,a)=2.0, reward r=1.0, discount gamma=0.9, max next-state action value 5.0, and learning rate alpha=0.5.',
      prompt: 'What target, TD error, and updated Q(s,a) should standard tabular Q-learning compute?',
      choices: Object.freeze([
        'Target 5.5, TD error 3.5, updated Q 3.75',
        'Target 5.5, TD error 5.5, updated Q 4.75',
        'Target 3.0, TD error 1.0, updated Q 2.5',
      ]),
      answerIndex: 0,
      explanation: 'The target is 1 + 0.9*5 = 5.5. The TD error is 5.5 - 2 = 3.5. Applying alpha=0.5 gives 2 + 0.5*3.5 = 3.75. Alpha scales the correction after the Bellman target is formed.',
      misconceptionTested: 'The learning rate belongs inside the Bellman target or the old Q-value is added directly to the target.',
    }),
    Object.freeze({
      id: 'qlearn-off-policy-exploration-target',
      level: 'diagnosis',
      relatedComparison: 'behavior-action-vs-greedy-backup-action',
      scenario: 'An epsilon-greedy behavior policy explores by taking action Left from state s. The transition reaches s_prime with reward 0. At s_prime, Q(Left)=1, Q(Right)=7, and Q(Up)=3. A developer uses Q(s_prime, Left)=1 in the target because Left was the exploratory behavior action.',
      prompt: 'What should standard Q-learning use for the continuation term, and why?',
      choices: Object.freeze([
        'Use 7 from max_a Q(s_prime,a); Q-learning is off-policy and backs up the greedy continuation even when behavior explored',
        'Use 1 from Left because the behavior action must also define the Q-learning target',
        'Average 1, 7, and 3 because Q-learning is an expectation-policy algorithm',
      ]),
      answerIndex: 0,
      explanation: 'The behavior policy determines which transition is collected, but standard Q-learning uses the maximum next-state action value in its target. Using the sampled next action instead would move the update toward a SARSA-style on-policy backup.',
      misconceptionTested: 'The exploratory action chosen by the behavior policy must also be the next action used in the Q-learning Bellman target.',
    }),
    Object.freeze({
      id: 'qlearn-neutral-value-coverage-trap',
      level: 'diagnosis',
      relatedComparison: 'zero-initialization-vs-evidence-of-safety',
      scenario: 'After training, one rarely visited state has Q-values [4.2, 0.0, -1.5, -3.0]. Logs show the action with value 0.0 has never been taken. A reviewer claims that action is proven safer than the negative-valued alternatives because zero is larger.',
      prompt: 'What is the correct interpretation before deploying the greedy policy?',
      choices: Object.freeze([
        'The zero may be untouched initialization, so visit coverage must be checked before treating it as evidence of a good action',
        'Zero is a mathematically certified safe return because unvisited actions converge immediately',
        'The action must be removed because only negative Q-values can represent unexplored choices',
      ]),
      answerIndex: 0,
      explanation: 'A tabular value can remain at its initialization when an action has not been sampled. Greedy comparison alone cannot distinguish learned neutrality from ignorance, so visitation and transition evidence matter.',
      misconceptionTested: 'An unvisited zero-valued action is known to be safe or optimal merely because its initialized value exceeds learned negative values.',
    }),
    Object.freeze({
      id: 'qlearn-alpha-gamma-failure-diagnosis',
      level: 'decision',
      relatedComparison: 'learning-rate-instability-vs-discount-myopia',
      scenario: 'Two independent problems appear in a delayed-reward task. Run A has wildly oscillating Q-values after individual stochastic transitions. Run B is numerically stable but repeatedly chooses a nearby reward of +2 instead of a reliable delayed reward of +20 several steps later.',
      prompt: 'Which first hyperparameter checks best match the two symptoms?',
      choices: Object.freeze([
        'For Run A inspect whether alpha is too high; for Run B inspect whether gamma is too low',
        'For Run A lower gamma only; for Run B lower alpha only',
        'Increase both alpha and gamma to 1 because both symptoms mean updates are too small',
      ]),
      answerIndex: 0,
      explanation: 'Alpha controls how strongly each new target changes an existing estimate, so a large alpha can create noisy oscillation. Gamma controls delayed-reward weight, so a small gamma can make the policy myopic even when updates themselves are stable.',
      misconceptionTested: 'Alpha and gamma are interchangeable knobs for the same failure mode.',
    }),
    Object.freeze({
      id: 'qlearn-reward-safety-failure',
      level: 'diagnosis',
      relatedComparison: 'reward-optimization-vs-intended-safe-behavior',
      scenario: 'A warehouse robot gets +1 for every meter moved toward a destination and +20 for arrival. It discovers a loop near the goal that repeatedly earns progress bonuses without finishing, and epsilon-greedy exploration sometimes drives it into a restricted aisle.',
      prompt: 'What must be fixed before treating the learned Q-table as production ready?',
      choices: Object.freeze([
        'Repair the reward so looping cannot outscore completion and add safety constraints around exploration; the algorithm optimizes the supplied incentives, not unstated intent',
        'Increase the goal Q-value manually while leaving the exploitable reward and unsafe exploration unchanged',
        'Set gamma to zero so the robot ignores all future consequences and therefore becomes safe',
      ]),
      answerIndex: 0,
      explanation: 'This combines reward hacking with unsafe data collection. Q-learning cannot infer the intended objective beyond the reward it receives, and real-world exploration may need explicit constraints because harmful actions cannot be treated as harmless trial-and-error.',
      misconceptionTested: 'A correct Bellman update guarantees intended and safe behavior even when reward design or exploration policy is unsafe.',
    }),
  ]),
});
