export const P1_LONG_CONTEXT_FRONTIER_SCENARIOS_BY_LESSON = Object.freeze({
  'long-context-frontier-models': Object.freeze([
    Object.freeze({
      id: 'longctx-kv-cache-memory-worked',
      level: 'calculation',
      relatedComparison: 'claimed-context-length-vs-serving-memory-footprint',
      scenario: 'A decoder has 32 layers, 8 KV heads per layer, head dimension 128, and stores both keys and values in FP16 at 2 bytes per scalar. Ignore allocator overhead and assume the full KV cache is resident for every context token.',
      prompt: 'Approximately how much KV-cache memory is required for a 200,000-token context?',
      choices: Object.freeze([
        'About 24.4 GiB, because each token needs 32 x 8 x 128 x 2 x 2 = 131,072 bytes and 200,000 tokens need about 26.2 GB',
        'About 0.8 GiB, because KV cache is stored only once for the entire model rather than once per layer',
        'About 48.8 GiB, because the stated key/value factor must be applied twice after already counting both keys and values',
      ]),
      answerIndex: 0,
      explanation: 'Per token, the cache stores 32 layers x 8 KV heads x 128 dimensions x 2 tensors (K and V) x 2 bytes = 131,072 bytes, or 128 KiB. Across 200,000 tokens that is 26,214,400,000 bytes, about 24.4 GiB. Advertised context capacity therefore has a direct concurrency and hardware cost even before allocator and runtime overhead.',
      misconceptionTested: 'Long-context capacity is primarily a tokenizer or position-limit issue and KV-cache memory does not grow materially with stored tokens and layers.',
    }),
    Object.freeze({
      id: 'longctx-lost-middle-repacking-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'evidence-presence-vs-position-sensitive-evidence-use',
      scenario: 'The exact same contract clause produces 94% answer accuracy when placed in the first 5% of a 600K-token prompt and 92% when placed in the last 5%, but only 61% when placed near the middle. Retrieval is disabled and logging confirms the clause is present in every request.',
      prompt: 'What should the team diagnose and try before blaming missing data?',
      choices: Object.freeze([
        'A lost-in-the-middle effective-context failure; try position-aware packing or reordering so critical evidence is placed where the model uses it reliably',
        'A retrieval-recall failure; increase vector-search top-k even though retrieval is not part of this path and the clause is already present',
        'A corrupted source document; replace the clause text because middle-position accuracy proves the content itself is invalid',
      ]),
      answerIndex: 0,
      explanation: 'The evidence is present and unchanged, while reliability changes sharply with position. That is direct evidence of position-sensitive effective context rather than a retrieval miss. Repacking or grouping critical evidence is the first intervention to test.',
      misconceptionTested: 'If evidence is present anywhere inside the accepted context window, its position cannot materially affect answer reliability.',
    }),
    Object.freeze({
      id: 'longctx-hybrid-rag-recall-decision',
      level: 'decision',
      relatedComparison: 'narrow-rag-vs-full-context-vs-broad-retrieval-plus-long-context',
      scenario: 'A compliance corpus has 500 documents, but a typical question depends on 3 to 6 clauses. Full-context prompting fits technically but costs 14 seconds of prefill and adds many near-duplicate distractors. Retrieval top-10 has only 82% required-clause recall, while retrieval top-60 reaches 99% recall; reranking can then pack the best 20 chunks into the model context.',
      prompt: 'Which architecture best matches the measured evidence and cost profile?',
      choices: Object.freeze([
        'Use broader high-recall retrieval, rerank it, and pack the best evidence into long context so recall improves without paying for all 500 documents',
        'Keep top-10 retrieval because smaller context is always better even when required-clause recall is only 82%',
        'Always paste all 500 documents because any model that accepts them cannot suffer from distractor or prefill costs',
      ]),
      answerIndex: 0,
      explanation: 'The narrow retrieval setting misses too much evidence, while full context is expensive and noisy. A hybrid design uses a broad retrieval stage for recall, reranking for precision, and a long enough final context for cross-document reasoning.',
      misconceptionTested: 'The long-context versus RAG choice must be binary; retrieval breadth, reranking, and final context packing cannot be combined to balance recall and noise.',
    }),
    Object.freeze({
      id: 'longctx-citation-grounding-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'answer-correctness-vs-claim-level-grounding',
      scenario: 'A model answers a policy question with the correct final limit of 30 days, but cites paragraph 18, which discusses a different 30-day review process. The governing clause is paragraph 42. This pattern appears repeatedly: answers often look correct while citations point to semantically similar but non-supporting text.',
      prompt: 'What quality failure should be measured separately from answer accuracy?',
      choices: Object.freeze([
        'Claim-level grounding and citation accuracy, because a correct-looking answer can still be supported by the wrong evidence',
        'Only maximum context length, because a larger window guarantees the citation will move to paragraph 42',
        'Only generation temperature, because citation mistakes prove the underlying evidence was never in the prompt',
      ]),
      answerIndex: 0,
      explanation: 'The final value happens to be correct, but the provenance is wrong. Long-context evaluation therefore needs evidence-level attribution checks in addition to answer correctness, especially when similar clauses can act as distractors.',
      misconceptionTested: 'Correct final answers are sufficient evidence that the model selected and grounded on the correct supporting context.',
    }),
    Object.freeze({
      id: 'longctx-compressed-memory-loss-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'compressed-summary-efficiency-vs-exact-fact-retention',
      scenario: 'A support system replaces a 40,000-token customer history with a 700-token summary. General preferences are preserved, but the summary omits an exact device serial number and warranty date that later determine eligibility. The raw history still contains both facts.',
      prompt: 'What should the memory design change?',
      choices: Object.freeze([
        'Preserve critical exact fields with provenance and keep a raw-evidence fallback instead of relying on one lossy free-text summary',
        'Increase summary compression further so fewer exact details compete with the general profile',
        'Delete the raw history once the summary is produced because long-context fallback would only duplicate information',
      ]),
      answerIndex: 0,
      explanation: 'Compression is useful because it reduces repeated context cost, but it can discard details that become important only for later queries. Structured exact fields and source-backed fallback reduce that recall failure without forcing full-history prompting on every turn.',
      misconceptionTested: 'A compact summary can safely replace raw evidence for all future questions once it captures the general meaning of the history.',
    }),
    Object.freeze({
      id: 'longctx-cache-freshness-design',
      level: 'design',
      relatedComparison: 'stable-prefix-prompt-caching-vs-fresh-changing-evidence',
      scenario: 'An employee assistant has a 120K-token policy handbook that changes quarterly and a 20K-token incident bulletin that changes several times per day. The service currently caches the entire combined prompt for 24 hours to reduce prefill latency, and users sometimes receive answers based on superseded incident instructions.',
      prompt: 'Which design preserves most caching benefit without serving stale fast-changing evidence?',
      choices: Object.freeze([
        'Cache the stable handbook prefix separately and retrieve or rebuild the fast-changing bulletin portion with explicit freshness/version invalidation',
        'Keep caching the full combined prompt for 24 hours because prompt caching makes stale evidence more reliable once reused',
        'Disable all caching and always paste every historical version of both sources so no information is ever omitted',
      ]),
      answerIndex: 0,
      explanation: 'Caching works best for stable repeated prefixes. Fast-changing evidence needs a shorter freshness contract or retrieval path. Separating the two lets the service reuse expensive stable prefill work while invalidating the volatile evidence independently.',
      misconceptionTested: 'Prompt caching can be applied to changing evidence without an invalidation or versioning strategy because cache reuse affects only performance, not answer freshness.',
    }),
  ]),
});
