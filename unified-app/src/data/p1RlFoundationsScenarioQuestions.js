export const P1_RL_FOUNDATIONS_SCENARIOS_BY_LESSON = Object.freeze({
  'rl-foundations': Object.freeze([
    Object.freeze({
      id: 'rlfound-transition-terminal-trace',
      level: 'diagnosis',
      relatedComparison: 'ordinary-transition-vs-terminal-transition',
      scenario: 'A warehouse agent in state aisle-4 chooses action FORWARD, enters the goal bay, receives reward +20, and the environment marks done=true. A developer immediately asks the policy for another action from that same goal state without resetting the episode.',
      prompt: 'What is the most direct problem with this interaction trace?',
      choices: Object.freeze([
        'The transition ended the episode, so the environment should reset or start a new episode before requesting another action',
        'The reward should be discarded because terminal transitions cannot contain rewards',
        'The previous state and action no longer matter once the next state is known',
      ]),
      answerIndex: 0,
      explanation: 'A terminal transition can carry reward, but done=true means no ordinary next decision follows in the same episode. Correct tracing keeps state, action, reward, next state, and terminal status distinct.',
      misconceptionTested: 'Every next state must immediately produce another action even when the transition is terminal.',
    }),
    Object.freeze({
      id: 'rlfound-path-return-comparison-worked',
      level: 'calculation',
      relatedComparison: 'direct-goal-path-vs-side-reward-detour',
      scenario: 'Ignore discounting for this comparison. Direct route rewards are [-1, -1, +10]. A detour route receives [-1, +5, -1, -1, +10].',
      prompt: 'Which route has the larger return, and what reward-design lesson follows?',
      choices: Object.freeze([
        'The detour returns 12 versus 8 for the direct route, so the side reward makes the longer behavior numerically preferable',
        'The direct route returns 10 versus 5 because only terminal rewards count toward return',
        'Both routes return 10 because step costs and side rewards cancel by definition',
      ]),
      answerIndex: 0,
      explanation: 'The direct return is -1-1+10=8. The detour return is -1+5-1-1+10=12. If the intended behavior is the direct route, the side reward has changed the optimized objective rather than merely helping exploration.',
      misconceptionTested: 'Side rewards cannot alter the optimal behavior as long as the terminal goal reward stays unchanged.',
    }),
    Object.freeze({
      id: 'rlfound-gamma-vs-reward-design-diagnosis',
      level: 'decision',
      relatedComparison: 'time-preference-problem-vs-objective-misspecification',
      scenario: 'Two agents fail differently. Agent A ignores a correct +100 goal that arrives many steps later, while the reward map otherwise matches intent. Agent B gets high return by repeatedly collecting a +8 side bonus instead of finishing, even when gamma is near one.',
      prompt: 'What should be inspected first for each agent?',
      choices: Object.freeze([
        'For A inspect whether gamma discounts the delayed goal too strongly; for B repair the reward design because gamma cannot fix an exploitable objective',
        'For A rewrite the state labels; for B increase gamma above one until the side bonus disappears',
        'For both agents only increase exploration because all RL failures are caused by insufficient random actions',
      ]),
      answerIndex: 0,
      explanation: 'Gamma changes temporal weighting, so it can make a valid distant reward too weak. It does not change what the reward function values. A repeatable side-bonus exploit is an objective-design problem, not a discounting problem.',
      misconceptionTested: 'Gamma is a general-purpose knob that can repair any poorly specified reward function.',
    }),
    Object.freeze({
      id: 'rlfound-terminal-reward-loop-exploit',
      level: 'diagnosis',
      relatedComparison: 'goal-reward-once-vs-repeatable-terminal-bonus',
      scenario: 'A game gives +50 for reaching the finish tile. Because the environment forgets to terminate the episode, the agent can alternate between the finish tile and a neighboring tile and collect +50 repeatedly. Training return looks excellent.',
      prompt: 'What should be fixed before trusting the learning result?',
      choices: Object.freeze([
        'Fix the terminal condition or reward transition so completion cannot be farmed repeatedly; the high return is exploiting task formulation',
        'Lower the learning rate because repeated rewards are always an optimizer-instability problem',
        'Keep the environment unchanged because high return proves the policy discovered the intended behavior',
      ]),
      answerIndex: 0,
      explanation: 'Terminal conditions are part of the task definition. If completion is intended to end the episode, failing to terminate creates a new objective in which reward farming is rational. Algorithm correctness cannot compensate for a broken environment contract.',
      misconceptionTested: 'Reward hacking only comes from reward magnitudes and cannot come from incorrect terminal dynamics.',
    }),
    Object.freeze({
      id: 'rlfound-proxy-alignment-evaluation',
      level: 'decision',
      relatedComparison: 'training-reward-vs-real-objective',
      scenario: 'A support agent is rewarded only for the number of tickets closed per hour. After optimization, reward rises 40%, but reopen rates and customer complaints also rise sharply because the agent closes ambiguous tickets prematurely.',
      prompt: 'What is the correct foundation-level diagnosis?',
      choices: Object.freeze([
        'The reward proxy is misaligned with the real service objective, so evaluation and incentives must include outcome quality rather than trusting reward alone',
        'The higher reward proves the policy improved and the complaint metrics should be ignored as off-policy data',
        'Increase gamma because any proxy-alignment problem is caused by insufficient weight on distant rewards',
      ]),
      answerIndex: 0,
      explanation: 'RL optimizes the signal supplied to it. A rising proxy paired with worse real outcomes is direct evidence that the task formulation rewards the wrong behavior. Independent outcome metrics are necessary to validate alignment.',
      misconceptionTested: 'A policy with higher training reward is necessarily better on the real-world objective the designer had in mind.',
    }),
  ]),
});
