export const P1_RL_EXPLORATION_SCENARIOS_BY_LESSON = Object.freeze({
  'rl-exploration': Object.freeze([
    Object.freeze({
      id: 'rlexplore-short-sample-frequency-trap',
      level: 'diagnosis',
      relatedComparison: 'configured-epsilon-vs-observed-short-run-frequency',
      scenario: 'An epsilon-greedy agent is configured with epsilon=0.20. During only 20 decisions, 8 decisions happen to enter the random exploration branch. A reviewer concludes that the implementation must secretly be using epsilon=0.40 because 8/20=0.40.',
      prompt: 'What is the correct diagnosis before changing the implementation?',
      choices: Object.freeze([
        'Twenty decisions are too few to identify the configured probability from observed frequency; inspect the selector and collect a much larger sample before treating the deviation as a bug',
        'Change epsilon to 0.10 because observed exploration frequency is always exactly twice the configured epsilon',
        'The implementation is definitely wrong because a Bernoulli exploration process must produce exactly epsilon times the sample size in every finite run',
      ]),
      answerIndex: 0,
      explanation: 'Epsilon defines a probability, not an exact quota for every short run. Finite samples can deviate substantially from their expectation, so a short-run frequency mismatch is not sufficient evidence of an implementation defect.',
      misconceptionTested: 'Observed explore/exploit counts must exactly equal their configured probabilities even over a very small number of decisions.',
    }),
    Object.freeze({
      id: 'rlexplore-decay-lifecycle-decision',
      level: 'decision',
      relatedComparison: 'fixed-exploration-vs-decayed-exploration-lifecycle',
      scenario: 'A training run starts with highly uncertain action values. Epsilon=0.50 helps discover useful routes early, but after the estimates stabilize the same fixed epsilon continues causing many unnecessary random detours and occasional failures.',
      prompt: 'What is the most defensible change if the environment is reasonably stationary?',
      choices: Object.freeze([
        'Use an exploration schedule that is higher early and decays toward a smaller floor, while validating that enough exploration remains for the task',
        'Set epsilon permanently to 1.0 so late training gathers the maximum possible amount of random experience',
        'Set epsilon permanently to 0.0 from the first episode because any exploration that causes a failure is mathematically invalid',
      ]),
      answerIndex: 0,
      explanation: 'The value of exploration changes over the learning lifecycle. Higher early exploration supports discovery, while reducing it later can shift behavior toward performance once estimates are more reliable. A floor can preserve some continued discovery when appropriate.',
      misconceptionTested: 'One fixed exploration rate is automatically optimal from the first training step through late training and deployment.',
    }),
    Object.freeze({
      id: 'rlexplore-cliff-risk-worked',
      level: 'calculation',
      relatedComparison: 'nominal-shortest-route-vs-risk-adjusted-expected-return',
      scenario: 'Two policies are evaluated under execution noise. The cliff-edge route returns +10 on 96% of episodes and -100 on 4%. A longer safe route returns +7 on 99.8% of episodes and -100 on 0.2%. Assume these are the only outcomes.',
      prompt: 'Which route has the larger expected episode return under this noise model?',
      choices: Object.freeze([
        'The safe route: 0.998*7 + 0.002*(-100) = 6.786, versus 0.96*10 + 0.04*(-100) = 5.6 for the edge route',
        'The edge route because its successful episodes return +10 instead of +7, so failure probability can be ignored',
        'Both routes have expected return +8.5 because only the two successful rewards should be averaged',
      ]),
      answerIndex: 0,
      explanation: 'Catastrophic failures must be probability-weighted along with successful outcomes. The edge route loses four expected-return points from its 4% failure rate, while the safer route loses only 0.2, making the longer route better under the stated noise model.',
      misconceptionTested: 'The route with the highest nominal successful reward is automatically best even when rare failures have severe cost.',
    }),
    Object.freeze({
      id: 'rlexplore-deployment-noise-gap',
      level: 'diagnosis',
      relatedComparison: 'training-exploration-rate-vs-deployment-execution-noise',
      scenario: 'A policy is trained with epsilon-greedy exploration and then deployed with epsilon=0. The greedy policy hugs a hazardous boundary because clean evaluation episodes never deviate. Real actuators, however, execute an unintended neighboring action about 3% of the time.',
      prompt: 'Why is setting deployment epsilon to zero not enough to establish safety?',
      choices: Object.freeze([
        'Exploration randomness and execution noise are different mechanisms; the deployed policy must be stress-tested under realistic actuator errors and may need a more robust route',
        'Epsilon zero removes every possible source of stochastic behavior, including hardware error, so clean greedy evaluation is sufficient',
        'The correct fix is to increase deployment epsilon because random policy actions cancel actuator noise on average',
      ]),
      answerIndex: 0,
      explanation: 'Disabling deliberate exploration only removes one source of randomness. Environment or actuator noise can still perturb actions, so deployment evaluation must model those perturbations rather than assuming deterministic execution.',
      misconceptionTested: 'A policy evaluated with epsilon zero is guaranteed robust because all real-world action noise disappears with training exploration.',
    }),
    Object.freeze({
      id: 'rlexplore-safe-real-world-exploration',
      level: 'design',
      relatedComparison: 'unconstrained-random-exploration-vs-safety-constrained-data-collection',
      scenario: 'A warehouse robot is still learning and has legal actions that could enter a human-only aisle or collide with equipment. Those actions are informative but cannot be treated as harmless trial-and-error in the physical environment.',
      prompt: 'How should exploration be designed before collecting more real-world experience?',
      choices: Object.freeze([
        'Use simulation where possible and enforce safety constraints such as action masks or a safety shield so exploration cannot select prohibited physical actions',
        'Raise epsilon to 1.0 on the real robot because maximum random coverage is always the safest way to learn quickly',
        'Keep unconstrained epsilon-greedy exploration and rely on a large negative reward after a collision to make future episodes safe',
      ]),
      answerIndex: 0,
      explanation: 'Some exploratory outcomes are too costly to learn from by experiencing them. Real systems often need constrained action spaces, safety interlocks, simulators, or other safeguards so information gathering happens within acceptable operational bounds.',
      misconceptionTested: 'A sufficiently negative reward makes unsafe real-world exploratory actions acceptable to execute during learning.',
    }),
    Object.freeze({
      id: 'rlexplore-epsilon-alpha-gamma-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'behavior-randomness-vs-update-instability-vs-discount-myopia',
      scenario: 'Three runs fail differently. Run A keeps taking many random actions despite useful values. Run B uses mostly greedy actions but its value estimates swing sharply after individual transitions. Run C is stable but rejects a longer safe route whose reward arrives later.',
      prompt: 'Which first hyperparameter checks best match the three symptoms?',
      choices: Object.freeze([
        'Run A: epsilon may be too high; Run B: alpha may be too high; Run C: gamma may be too low',
        'Run A: gamma may be too low; Run B: epsilon may be too high; Run C: alpha may be too high',
        'All three symptoms are controlled only by epsilon because alpha and gamma do not affect reinforcement learning behavior',
      ]),
      answerIndex: 0,
      explanation: 'Epsilon controls how often behavior deliberately explores, alpha controls how strongly new targets move learned values, and gamma controls the weight of delayed outcomes. Similar-looking poor returns can therefore require very different fixes.',
      misconceptionTested: 'Epsilon, alpha, and gamma are interchangeable knobs for the same failure mode.',
    }),
  ]),
});
