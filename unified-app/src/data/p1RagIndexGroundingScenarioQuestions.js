export const P1_RAG_INDEX_GROUNDING_SCENARIOS_BY_LESSON = Object.freeze({
  'rag-vector-indexing': Object.freeze([
    Object.freeze({
      id: 'rag-index-distance-metric-mismatch',
      level: 'diagnosis',
      scenario: 'An embedding model was validated with cosine similarity on normalized vectors. A new vector-store configuration indexes the same embeddings with raw L2 distance and no normalization. Retrieval quality drops even though the documents and queries are unchanged.',
      prompt: 'What should the team verify first?',
      choices: Object.freeze([
        'That vector normalization and the index distance metric match the embedding model assumptions used during retrieval evaluation',
        'That the generator temperature is low enough to compensate for the changed nearest-neighbor geometry',
        'That chunk overlap is increased until L2 and cosine always return identical rankings',
      ]),
      answerIndex: 0,
      explanation: 'Cosine, inner product, and L2 can induce different rankings unless the representation and normalization assumptions make them equivalent. The retrieval index must use the metric the embedding pipeline was validated for.',
      misconceptionTested: 'All vector distance metrics produce interchangeable nearest-neighbor rankings for any embedding model.',
      relatedComparison: 'Embedding similarity assumptions versus vector-index distance configuration.',
    }),
    Object.freeze({
      id: 'rag-index-ann-breadth-recall-debugging',
      level: 'diagnosis',
      scenario: 'Exact search finds the gold chunk at rank 4 for 97% of benchmark queries. HNSW with the current low search breadth finds it in top 20 for only 86%, while latency is comfortably below the product SLO.',
      prompt: 'What is the best next experiment?',
      choices: Object.freeze([
        'Increase HNSW search breadth and trace the recall-versus-P95-latency curve against the exact baseline',
        'Lower reranker top-k because a smaller downstream candidate set makes missing ANN neighbors reappear',
        'Change answer decoding from sampling to greedy because generation mode controls HNSW graph exploration',
      ]),
      answerIndex: 0,
      explanation: 'The exact baseline shows that the relevant vectors exist and are rankable. The gap is approximation loss. Increasing ANN exploration is the targeted experiment while measuring the latency cost.',
      misconceptionTested: 'ANN recall losses should be fixed downstream rather than by tuning or replacing the approximate search operating point.',
      relatedComparison: 'Exact-search baseline versus HNSW approximation breadth.',
    }),
    Object.freeze({
      id: 'rag-index-postfilter-starvation',
      level: 'diagnosis',
      scenario: 'A multi-tenant index retrieves 20 nearest neighbors globally, then applies a tenant_id filter. For one tenant, 19 of the 20 candidates belong to other tenants, leaving only one allowed result even though many relevant tenant-local chunks exist deeper in the index.',
      prompt: 'What is the core retrieval-design problem?',
      choices: Object.freeze([
        'Applying a restrictive metadata filter only after a narrow ANN candidate search can starve the allowed candidate set; filtering must be integrated or search breadth increased safely',
        'Tenant filtering should be removed because vector similarity is sufficient authorization',
        'The generator should be instructed to infer the missing tenant-local evidence from the one surviving chunk',
      ]),
      answerIndex: 0,
      explanation: 'Authorization and metadata constraints change the effective search space. Post-filtering a small global candidate set can destroy recall. The index/query plan should respect filters during search where supported, or deliberately over-retrieve before safe filtering.',
      misconceptionTested: 'Metadata and authorization filters can be applied after any fixed ANN top-k without affecting retrieval recall.',
      relatedComparison: 'Filtered ANN search versus post-filtering a narrow global candidate set.',
    }),
    Object.freeze({
      id: 'rag-index-stale-entry-maintenance',
      level: 'diagnosis',
      scenario: 'A product catalog is updated hourly, but the vector index is rebuilt only weekly. Retrieval frequently returns discontinued prices and misses newly added products even though the source database is current.',
      prompt: 'Which subsystem should be fixed first?',
      choices: Object.freeze([
        'Index freshness and update/delete maintenance so vector entries track the source-of-truth changes',
        'Grounding strictness only, because stale index contents are purely a citation-label problem',
        'Generation temperature, because lower randomness forces the model to know new products automatically',
      ]),
      answerIndex: 0,
      explanation: 'A stale index is an ingestion/index-maintenance failure. Grounding may reject stale evidence, but it cannot retrieve documents that were never indexed or delete obsolete candidates from the retrieval structure.',
      misconceptionTested: 'Once a vector index is built, corpus changes do not require index maintenance or freshness monitoring.',
      relatedComparison: 'Source-of-truth freshness versus vector-index freshness.',
    }),
  ]),
  'rag-reranking-grounding': Object.freeze([
    Object.freeze({
      id: 'rag-reranker-missing-evidence-limit',
      level: 'diagnosis',
      scenario: 'A cross-encoder reranker is highly accurate on its candidate set, but the correct policy chunk is absent from first-pass top 50 retrieval on 18% of queries. The team proposes a larger reranker model as the only fix.',
      prompt: 'Why is that insufficient?',
      choices: Object.freeze([
        'A reranker cannot promote evidence it never receives; first-pass retrieval recall must be improved for those queries',
        'Cross-encoders automatically perform a second full-corpus search when their model size increases',
        'Grounding strictness will synthesize the missing policy chunk if the reranker score is high enough',
      ]),
      answerIndex: 0,
      explanation: 'Reranking is bounded by candidate recall. A stronger scorer can improve ordering among returned chunks, but missing candidates require changes to retrieval, indexing, query expansion, hybrid search, or candidate breadth.',
      misconceptionTested: 'A sufficiently strong reranker can recover relevant documents that were absent from the candidate pool.',
      relatedComparison: 'Candidate recall ceiling versus reranking quality.',
    }),
    Object.freeze({
      id: 'rag-grounding-claim-level-support',
      level: 'diagnosis',
      scenario: 'An answer says: "The Pro plan costs $49 and includes unlimited exports." The cited chunk supports the $49 price but says nothing about export limits.',
      prompt: 'How should grounding treat this answer?',
      choices: Object.freeze([
        'Ground the price claim but mark the unlimited-exports claim unsupported unless separate evidence directly supports it',
        'Mark the entire sentence grounded because one clause has a valid citation',
        'Mark both claims grounded because the cited chunk is ranked first',
      ]),
      answerIndex: 0,
      explanation: 'Grounding should operate at claim granularity. One citation can support one proposition while leaving another unsupported, even when both appear in the same sentence.',
      misconceptionTested: 'A citation that supports part of a sentence automatically validates every claim in that sentence.',
      relatedComparison: 'Claim-level support versus sentence-level citation presence.',
    }),
    Object.freeze({
      id: 'rag-grounding-conflict-recency-resolution',
      level: 'decision',
      scenario: 'Two authoritative policy chunks disagree. One is dated January 2025 and says refunds are allowed for 60 days; the other is dated August 2026 and says the window is 30 days. The user asks for the current refund policy.',
      prompt: 'What should the grounding layer do?',
      choices: Object.freeze([
        'Prefer or surface the newer valid policy for a current-policy claim while recording that older evidence conflicts and is stale for this question',
        'Choose whichever chunk has the higher vector-similarity score regardless of date',
        'Average the two policies into a 45-day refund window',
      ]),
      answerIndex: 0,
      explanation: 'Current-state claims require temporal validity. Relevance rank alone cannot resolve contradictory versions. The system should use version/recency metadata and may explicitly flag conflicts rather than inventing a compromise.',
      misconceptionTested: 'Relevance score alone determines which conflicting source is valid for a time-sensitive claim.',
      relatedComparison: 'Semantic relevance versus temporal validity and source conflict.',
    }),
    Object.freeze({
      id: 'rag-grounding-abstention-no-support',
      level: 'decision',
      scenario: 'All retrieved chunks are relevant to the topic, but none states or entails the exact warranty duration requested by the user. The generator can produce a plausible duration from prior model knowledge.',
      prompt: 'What is the safest grounded behavior?',
      choices: Object.freeze([
        'Abstain or state that the retrieved evidence does not establish the warranty duration instead of presenting an unsupported citation',
        'Use the model prior and cite the most semantically similar chunk anyway',
        'Lower grounding strictness until one of the related chunks passes',
      ]),
      answerIndex: 0,
      explanation: 'Grounded answering requires evidence support, not topical similarity. If no evidence justifies the requested claim, the system should abstain, seek more evidence, or clearly separate ungrounded model knowledge from sourced claims.',
      misconceptionTested: 'A relevant-looking chunk is enough to justify a citation even when it does not support the claim.',
      relatedComparison: 'Evidence-backed answering versus unsupported model-prior completion.',
    }),
    Object.freeze({
      id: 'rag-grounding-strictness-tradeoff',
      level: 'decision',
      scenario: 'On a labeled grounding benchmark, a lenient threshold accepts 96% of truly supported claims but also accepts 14% of unsupported claims. A stricter threshold accepts 88% of supported claims and only 3% of unsupported claims. The product is a high-stakes compliance assistant.',
      prompt: 'Which direction is more defensible, assuming the product values false support much more heavily than abstention?',
      choices: Object.freeze([
        'Use the stricter operating point or an equivalently calibrated threshold, accepting lower grounded coverage to reduce false support',
        'Always use the lenient threshold because maximum accepted-claim recall dominates risk',
        'Ignore grounding thresholds because reranking rank already proves support',
      ]),
      answerIndex: 0,
      explanation: 'Grounding threshold choice is a risk trade-off. In high-stakes settings, false acceptance of unsupported claims can be more costly than abstaining on some valid claims, so a stricter calibrated operating point can be appropriate.',
      misconceptionTested: 'Grounding strictness has one universally optimal value independent of false-accept versus false-reject costs.',
      relatedComparison: 'Grounded-claim coverage versus false-support risk.',
    }),
  ]),
});
