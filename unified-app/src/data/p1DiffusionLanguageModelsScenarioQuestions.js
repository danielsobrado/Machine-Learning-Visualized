export const P1_DIFFUSION_LANGUAGE_MODELS_SCENARIOS_BY_LESSON = Object.freeze({
  'diffusion-language-models': Object.freeze([
    Object.freeze({
      id: 'difflm-latency-quality-frontier-worked',
      level: 'calculation',
      relatedComparison: 'parallel-token-updates-vs-end-to-end-latency-at-target-quality',
      scenario: 'A 64-token task has a 90% quality requirement. An autoregressive baseline needs 64 sequential decode steps at 3 ms each and reaches 93% quality. A diffusion LM updates all positions in parallel; each denoising pass takes 12 ms. At 4 passes it reaches 82% quality, while at 8 passes it reaches 91% quality. Ignore prompt-prefill time for this simplified comparison.',
      prompt: 'Which comparison is valid at the required quality level?',
      choices: Object.freeze([
        'The 8-pass diffusion configuration qualifies at 96 ms and is faster than the 192 ms AR decode, while the 4-pass result cannot be used to claim the speedup because it misses the quality target',
        'The 4-pass diffusion result proves a 4x production speedup because 48 ms is lower than 192 ms even though its 82% quality misses the stated requirement',
        'Autoregressive decoding is always faster because it emits one token per step whereas diffusion predicts many tokens per pass',
      ]),
      answerIndex: 0,
      explanation: 'The comparison must hold quality constant enough to satisfy the workload requirement. AR takes 64 × 3 = 192 ms. Eight diffusion passes take 8 × 12 = 96 ms and reach 91%, so that configuration is the valid faster result. Four passes are faster still but fail the quality constraint.',
      misconceptionTested: 'Parallel token prediction can be advertised as a speedup from the fastest schedule even when that schedule fails the required output-quality target.',
    }),
    Object.freeze({
      id: 'difflm-confidence-shift-locking-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'confidence-threshold-locking-vs-calibrated-token-correctness-under-shift',
      scenario: 'A diffusion assistant locks any token with predicted confidence above 0.85. On validation data, tokens in the 0.85-0.95 confidence band are correct 91% of the time. After deployment to a new domain, the same band is only 63% correct, yet the decoder still locks those tokens early and final answers contain persistent wrong entities that later denoising cannot repair.',
      prompt: 'What should be fixed before simply adding more denoising steps?',
      choices: Object.freeze([
        'Re-evaluate confidence calibration on the shifted domain and make locking/remasking policy depend on reliable uncertainty rather than treating the old 0.85 threshold as universal',
        'Keep the threshold unchanged because confidence above 0.85 proves the token is correct regardless of deployment distribution',
        'Disable remasking and lower the threshold further so even more shifted-domain predictions become permanent earlier',
      ]),
      answerIndex: 0,
      explanation: 'The decoder policy is using a confidence signal whose relationship to correctness changed after distribution shift. Extra passes cannot repair tokens that the policy has already frozen. Calibration, threshold selection, and remasking behavior should be validated on the actual deployment distribution.',
      misconceptionTested: 'A confidence threshold learned on one distribution remains a trustworthy correctness threshold after domain shift, so premature locking cannot be caused by calibration failure.',
    }),
    Object.freeze({
      id: 'difflm-remasking-oscillation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'useful-revision-vs-denoising-oscillation-and-stop-policy',
      scenario: 'During evaluation, answer quality peaks at denoising step 7. Between steps 8 and 12, the same six positions repeatedly alternate between two token choices, total token revisions rise by 45%, and final exact-match quality drops from 88% to 84%. The extra passes do not uncover new masked positions.',
      prompt: 'What does this trajectory indicate?',
      choices: Object.freeze([
        'Revision instability: the remasking/update policy should add a stability or stopping criterion instead of assuming more revision passes always improve the sequence',
        'Healthy convergence, because repeated token oscillation is evidence that every additional denoising step adds useful information',
        'A tokenizer vocabulary failure, because any token that changes more than once must be unrepresentable by the vocabulary',
      ]),
      answerIndex: 0,
      explanation: 'The model already reached better quality at step 7, then revisited the same positions without resolving them and degraded the answer. That is a schedule or stopping-policy problem. Per-step revision traces are exactly the evidence needed to distinguish useful repair from oscillation.',
      misconceptionTested: 'Allowing more remasking and denoising steps is monotonically beneficial, so a decoder should never stop while tokens are still revisable.',
    }),
    Object.freeze({
      id: 'difflm-block-boundary-coherence-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'within-block-quality-vs-cross-block-seam-coherence',
      scenario: 'A block-diffusion model generates 32-token blocks. Token accuracy inside blocks is 94%, but evaluation shows that 61% of factual contradictions occur within four tokens of a block boundary. Increasing the model size leaves this boundary concentration almost unchanged.',
      prompt: 'What experiment should be prioritized?',
      choices: Object.freeze([
        'Vary block size and boundary context or overlap while measuring seam-specific coherence, because strong within-block accuracy can hide a generation-structure failure at block transitions',
        'Measure only aggregate token accuracy, because boundary-localized contradictions are already represented well enough by the 94% average',
        'Increase vocabulary size first, because block-boundary errors are necessarily caused by missing token types rather than how adjacent blocks condition on each other',
      ]),
      answerIndex: 0,
      explanation: 'The errors are spatially concentrated at generation seams, so the block schedule and cross-block conditioning are the natural suspects. Aggregate accuracy hides this structure. Boundary-aware evaluation and block-size/context experiments directly test the mechanism.',
      misconceptionTested: 'If tokens inside each block are accurate on average, block diffusion has no special coherence risk at the interfaces between separately generated blocks.',
    }),
    Object.freeze({
      id: 'difflm-length-control-architecture-decision',
      level: 'decision',
      relatedComparison: 'fixed-full-sequence-denoising-vs-adaptive-block-growth-for-unknown-output-length',
      scenario: 'A support product handles tasks whose correct responses range from 20 to 600 tokens and cannot reliably predict final length from the prompt. A full-sequence diffusion prototype must choose a length before denoising; conservative over-allocation wastes substantial compute, while under-allocation truncates long answers. A block-diffusion prototype can append another block when the answer is not finished.',
      prompt: 'Which architecture is the better default for this workload?',
      choices: Object.freeze([
        'Block diffusion or another adaptive-length scheme, because the workload requires flexible sequence growth and the full-sequence length guess is already causing truncation or wasted denoising',
        'Rigid full-sequence diffusion, because needing the correct output length before generation is never a practical serving constraint',
        'Any architecture chosen only by parameter count, because output-length uncertainty is unrelated to generation strategy',
      ]),
      answerIndex: 0,
      explanation: 'The workload has large and unpredictable output-length variation. A generation strategy that can grow incrementally is better matched to it. Full-sequence diffusion can still work with a strong length controller, but the observed truncation/waste shows that fixed allocation is currently the dominant failure.',
      misconceptionTested: 'Full-sequence diffusion handles unknown and highly variable response length as naturally as incremental generation without needing an explicit length-control mechanism.',
    }),
    Object.freeze({
      id: 'difflm-minimal-change-editing-audit',
      level: 'diagnosis',
      relatedComparison: 'target-span-correctness-vs-unnecessary-context-rewrite',
      scenario: 'Two models repair a wrong function call in the middle of a 500-token code file. Both produce the correct replacement span in 96% of cases. Model A changes a median of 3 tokens outside the requested span; Model B changes a median of 74 outside-span tokens and introduces unrelated formatting or naming changes in 18% of otherwise correct edits.',
      prompt: 'What metric changes the model-selection decision for an editing product?',
      choices: Object.freeze([
        'Measure outside-span edits or minimal-change distance in addition to target correctness; Model A is safer because it preserves unrelated context much more reliably',
        'Use only target-span correctness, because any rewrite outside the requested span is irrelevant once the central fix is correct',
        'Prefer Model B because changing more surrounding tokens proves the decoder used more bidirectional context',
      ]),
      answerIndex: 0,
      explanation: 'Editing quality is not the same as free-form generation quality. When the requested change is local, unnecessary rewrites are a product failure even if the target span is correct. Minimal-change metrics expose that distinction directly.',
      misconceptionTested: 'Correctly filling the requested span is sufficient evidence of a good infilling/editing model even when it rewrites unrelated surrounding text.',
    }),
    Object.freeze({
      id: 'difflm-ar-conversion-alignment-regression-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'checkpoint-knowledge-retention-vs-post-conversion-assistant-alignment',
      scenario: 'A team converts a strong autoregressive assistant checkpoint to block diffusion and successfully trains the denoising objective. General knowledge QA remains close to the original model, but instruction-following success falls from 91% to 68% and preference-evaluation win rate drops sharply. The team skipped the post-conversion SFT and preference-alignment stages because the source checkpoint had already been aligned.',
      prompt: 'What is the most likely missing step?',
      choices: Object.freeze([
        'Re-run instruction and preference alignment for the new denoising generation behavior; preserved checkpoint knowledge does not guarantee preserved assistant behavior after objective and decoding changes',
        'Only increase the number of mask tokens in the tokenizer, because alignment behavior cannot change when source weights came from an aligned AR checkpoint',
        'Remove the denoising objective and keep the model name unchanged, because AR-to-diffusion conversion never requires behavioral retraining',
      ]),
      answerIndex: 0,
      explanation: 'The conversion changed the training objective and generation process. Retaining factual knowledge is not enough to prove that instruction-following and preferences survived the adaptation. Post-conversion SFT and preference alignment should be treated as behavioral stages, not assumed to transfer automatically.',
      misconceptionTested: 'An aligned autoregressive checkpoint remains equivalently instruction-following and preference-aligned after diffusion conversion without re-validating or re-training those behaviors.',
    }),
  ]),
});
