export const P1_TIME_SERIES_FORECASTING_SCENARIOS_BY_LESSON = Object.freeze({
  'time-series-forecasting-track': [
    {
      id: 'tsf-naive-baseline-gating-worked',
      level: 'decision',
      relatedComparison: 'learned-model-vs-seasonal-naive-baseline',
      scenario: 'Across four rolling-origin cutoffs, a candidate demand model has MAE values 8, 10, 9, and 11. A seasonal-naive baseline has MAE values 7, 9, 8, and 9 on the same horizons and cutoffs.',
      prompt: 'What is the strongest default deployment conclusion from these results?',
      choices: [
        'Do not promote the candidate yet because it loses to the simple seasonal-naive baseline at every cutoff',
        'Promote the candidate because learned models should be preferred even when their backtest error is higher',
        'Average the candidate and baseline MAEs together and treat the combined number as proof that both are equally good',
      ],
      answerIndex: 0,
      explanation: 'A forecasting model should justify its extra complexity against simple time-aware baselines. Here the seasonal-naive forecast is better at every historical launch point, so the candidate has not demonstrated incremental forecasting value.',
      misconceptionTested: 'A learned forecasting model is automatically useful if it looks sophisticated, even when a simple seasonal baseline consistently performs better.',
    },
    {
      id: 'tsf-recursive-error-propagation-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'recursive-vs-direct-multi-step-forecasting',
      scenario: 'A one-step model forecasts day 1 well. For a seven-day recursive forecast, each predicted day is fed back as an input to predict the next day. Errors are small at horizons 1-2 but grow sharply by horizons 5-7 even though the data distribution is stable.',
      prompt: 'What mechanism should be investigated first?',
      choices: [
        'Recursive error propagation because later steps depend on earlier model predictions rather than the true future history',
        'Label leakage because any increase in long-horizon error proves future labels entered training',
        'Class imbalance because recursive forecasting always becomes a classification problem after two steps',
      ],
      answerIndex: 0,
      explanation: 'Recursive forecasting reuses model-generated values as future inputs. Prediction error can therefore compound as the horizon grows. Direct horizon-specific models or multi-output strategies are useful comparisons when this propagation dominates.',
      misconceptionTested: 'A one-step model with good accuracy must retain the same error level when recursively rolled forward many steps.',
    },
    {
      id: 'tsf-prediction-interval-coverage-worked',
      level: 'calculation',
      relatedComparison: 'nominal-vs-empirical-interval-coverage',
      scenario: 'A model publishes nominal 90% prediction intervals. In a representative evaluation containing 200 future observations, 160 actual values fall inside their corresponding intervals.',
      prompt: 'What empirical coverage did the intervals achieve, and what does that suggest?',
      choices: [
        '80% coverage, suggesting the nominal 90% intervals are too narrow or otherwise under-cover on this evaluation',
        '90% coverage because the model labeled the intervals as 90%',
        '125% coverage because 200 / 160 = 1.25',
      ],
      answerIndex: 0,
      explanation: 'Empirical coverage is 160 / 200 = 0.80, or 80%. A nominal 90% interval should contain roughly 90% of future outcomes under the target conditions, so persistent 80% coverage indicates under-coverage and an uncertainty model that needs correction.',
      misconceptionTested: 'The nominal percentage printed on a prediction interval guarantees that the interval has that empirical coverage in deployment-like data.',
    },
    {
      id: 'tsf-future-exogenous-availability-decision',
      level: 'design',
      relatedComparison: 'realized-future-covariate-vs-cutoff-available-forecast',
      scenario: 'A demand model predicts electricity load 48 hours ahead. Training rows use the weather station’s realized temperature from each target hour. At the real 48-hour prediction cutoff, those realized future temperatures do not exist yet, but a numerical weather forecast is available.',
      prompt: 'How should this feature be represented in an honest training and backtest pipeline?',
      choices: [
        'Use the weather forecast that would have been available at each historical cutoff, not the later realized target-hour temperature',
        'Keep realized future temperature because external variables are exempt from point-in-time leakage rules',
        'Use whichever temperature version produces the lowest final-test error, even if it was unavailable at the historical cutoff',
      ],
      answerIndex: 0,
      explanation: 'Exogenous variables are only safe when their values are actually available at prediction time. If production sees a 48-hour weather forecast, backtests must recreate those historical forecast vintages or another cutoff-available representation rather than using realized future weather.',
      misconceptionTested: 'An exogenous feature is automatically leakage-safe even when its realized value is only known after the forecast is issued.',
    },
  ],
});
