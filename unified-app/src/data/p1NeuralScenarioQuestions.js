export const P1_NEURAL_SCENARIOS_BY_LESSON = Object.freeze({
  'computation-graph-backprop': [
    {
      id: 'backprop-chain-rule-missing-path',
      level: 'diagnosis',
      scenario: 'A computation graph has y = f(g(x)), but an implementation multiplies by df/dg and forgets dg/dx when computing dy/dx.',
      prompt: 'What is the bug?',
      choices: ['The chain rule path is incomplete, so the upstream gradient is not propagated through g', 'Backpropagation should add derivatives instead of multiplying along a single path', 'The forward pass should be removed'],
      answerIndex: 0,
      explanation: 'For a composed path, the local derivatives multiply: dy/dx = df/dg times dg/dx. Omitting one local derivative breaks gradient propagation.',
      misconceptionTested: 'Backpropagation can skip intermediate local derivatives without changing the result.',
    },
    {
      id: 'backprop-exploding-product',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { layerGradientNorms: '1.2, 4.9, 22, 130, 880', direction: 'toward early layers' },
      scenario: 'Visual state: gradient norms grow from about 1 at the output side to nearly 900 in early layers.',
      prompt: 'What failure mode does this indicate?',
      choices: ['Exploding gradients caused by repeated amplification through the backward chain', 'Vanishing gradients because the early-layer gradient is too small', 'A perfectly conditioned backward pass'],
      answerIndex: 0,
      explanation: 'Products of local Jacobians with magnitudes above one can amplify gradients exponentially across depth, producing unstable updates in early layers.',
      misconceptionTested: 'Gradient problems only occur when gradients become small.',
    },
  ],
  initialization: [
    {
      id: 'init-variance-explosion',
      level: 'visual-state',
      kind: 'visual-state',
      visualState: { layerStd: '1.0, 1.8, 3.4, 7.1, 15.6', activation: 'ReLU', initializer: 'too large' },
      scenario: 'Visual state: activation standard deviation grows rapidly with depth under a very large initialization scale.',
      prompt: 'What should the initializer be trying to preserve?',
      choices: ['A stable signal variance across layers appropriate to the activation and fan-in', 'The largest possible activation magnitude at every layer', 'Exactly identical neuron outputs'],
      answerIndex: 0,
      explanation: 'Variance-aware initialization such as He or Xavier aims to keep forward and backward signal scales from exploding or disappearing as depth increases.',
      misconceptionTested: 'Larger initial weights always speed up learning without stability cost.',
    },
    {
      id: 'init-variance-suppression',
      level: 'diagnosis',
      scenario: 'With a deep tanh network and extremely tiny weights, each layer output becomes closer to zero and gradients nearly disappear.',
      prompt: 'What is the most likely initialization problem?',
      choices: ['Signal variance is being suppressed through depth, contributing to vanishing activations and gradients', 'The weights are too large and causing overflow', 'The labels are necessarily corrupted'],
      answerIndex: 0,
      explanation: 'Tiny weights can shrink activations and Jacobians across many layers, leaving early layers with very small learning signals.',
      misconceptionTested: 'Very small initialization is always safe because it prevents exploding values.',
    },
  ],
  optimizers: [
    {
      id: 'optimizer-momentum-warmup',
      level: 'application',
      relatedComparison: 'momentum-warmup-vs-full-step',
      scenario: 'A large model with momentum diverges in the first few hundred updates when the full learning rate is used immediately, but later training is stable at that same rate.',
      prompt: 'Which training strategy is worth testing?',
      choices: ['A learning-rate warm-up that gradually reaches the target rate while optimizer state stabilizes', 'Doubling the initial learning rate further', 'Removing all gradient monitoring'],
      answerIndex: 0,
      explanation: 'Warm-up can reduce unstable early updates when activations, gradient scales, and momentum estimates have not yet settled into a stable regime.',
      misconceptionTested: 'If a learning rate works later in training, it must be safe from the first update.',
    },
    {
      id: 'optimizer-sparse-gradients',
      level: 'comparison',
      relatedComparison: 'sgd-vs-adaptive-sparse-gradient',
      scenario: 'A very large embedding table receives gradients for only a tiny fraction of rows in each batch.',
      prompt: 'Why can adaptive per-parameter scaling behave differently from plain SGD here?',
      choices: ['Rarely updated parameters can accumulate different effective step scales based on their gradient history', 'Sparse gradients force every parameter to receive the same update', 'SGD automatically stores second moments for every parameter'],
      answerIndex: 0,
      explanation: 'Adaptive optimizers track per-parameter statistics, so infrequently updated sparse parameters may receive different effective learning rates from frequently updated ones.',
      misconceptionTested: 'Optimizer choice has no interaction with gradient sparsity.',
    },
  ],
  'training-loop-dynamics': [
    {
      id: 'loop-batch-lr-interaction',
      level: 'comparison',
      relatedComparison: 'batch-size-learning-rate',
      scenario: 'Batch size increases 16x. Gradient noise falls, but the team keeps every optimizer setting unchanged and throughput rises while convergence quality worsens.',
      prompt: 'What interaction should be reviewed first?',
      choices: ['Learning rate and schedule because batch size changes gradient-noise and update-frequency behavior', 'Only the model name because batch size cannot affect optimization', 'The number of classes because it determines batch size'],
      answerIndex: 0,
      explanation: 'Changing batch size alters gradient variance, number of updates per epoch, and often the suitable learning-rate schedule, so batch and step size should be tuned together.',
      misconceptionTested: 'Batch size can be changed independently of all optimizer settings.',
    },
    {
      id: 'loop-gradient-accumulation-equivalence',
      level: 'mechanism',
      scenario: 'A GPU can hold 8 examples, so gradients are accumulated for four microbatches before one optimizer step to emulate an effective batch of 32.',
      prompt: 'What must remain clear when comparing with a physical batch of 32?',
      choices: ['The optimizer steps once after the accumulated gradient, while stateful layers and per-microbatch behavior may still differ', 'Four microbatches always produce four optimizer steps', 'Gradient accumulation removes the need for loss scaling or normalization details'],
      answerIndex: 0,
      explanation: 'Accumulation can match the summed gradient for many models, but optimizer-step frequency and stateful operations such as BatchNorm can make it differ from one physical batch.',
      misconceptionTested: 'Gradient accumulation is identical to a larger physical batch in every implementation detail.',
    },
  ],
  'gradient-problems': [
    {
      id: 'gradient-problem-differential-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'vanishing-exploding-dead-conditioning',
      scenario: 'Early-layer gradient norms are near zero, but later layers have healthy gradients. Activations are mostly saturated rather than exactly zero.',
      prompt: 'Which failure is most consistent?',
      choices: ['Vanishing gradients through saturated/deep paths', 'A dead ReLU pattern where activations are exactly zero from negative pre-activations', 'Exploding gradients because early norms are tiny'],
      answerIndex: 0,
      explanation: 'Tiny early-layer gradients with saturated activations point to vanishing propagation, while dead ReLUs have a more specific zero-activation mechanism and exploding gradients show growing norms.',
      misconceptionTested: 'All small-gradient failures are the same mechanism.',
    },
  ],
  'dropout-batchnorm': [
    {
      id: 'batchnorm-small-batch',
      level: 'diagnosis',
      scenario: 'Training uses batches of two examples. BatchNorm running statistics are noisy, and validation behavior changes sharply when batch composition changes.',
      prompt: 'What should be investigated?',
      choices: ['Whether BatchNorm is appropriate at this tiny batch size or should be replaced/frozen with a more stable normalization strategy', 'Increasing dropout to 100%', 'Removing validation because BatchNorm only matters during training'],
      answerIndex: 0,
      explanation: 'BatchNorm estimates batch statistics during training, so very small batches can produce noisy estimates and unstable running statistics.',
      misconceptionTested: 'BatchNorm behaves independently of batch size.',
    },
    {
      id: 'dropout-train-inference',
      level: 'comparison',
      relatedComparison: 'dropout-training-vs-inference',
      scenario: 'A model is evaluated while dropout is accidentally left in training mode, causing predictions to change across repeated identical requests.',
      prompt: 'What is the bug?',
      choices: ['Dropout masking should be disabled or handled in inference mode for ordinary deterministic evaluation', 'Dropout must stay random at inference for every model', 'The input should be shuffled between requests'],
      answerIndex: 0,
      explanation: 'Standard dropout is stochastic during training and disabled with appropriate scaling at inference; leaving training mode active creates unintended randomness.',
      misconceptionTested: 'Dropout uses the same stochastic masks during standard inference.',
    },
  ],
  'layer-normalization': [
    {
      id: 'layernorm-vs-batchnorm',
      level: 'comparison',
      relatedComparison: 'layernorm-vs-batchnorm',
      scenario: 'A transformer processes variable sequence batches, including batch size one at inference.',
      prompt: 'Why is LayerNorm a natural fit compared with BatchNorm?',
      choices: ['LayerNorm normalizes within each example/token representation and does not depend on batch statistics', 'LayerNorm requires large batches to estimate a population mean', 'BatchNorm is mandatory for attention layers'],
      answerIndex: 0,
      explanation: 'LayerNorm computes statistics across features within a representation, making it independent of other batch members and suitable for variable-batch sequence models.',
      misconceptionTested: 'LayerNorm and BatchNorm use the same normalization axis and batch dependence.',
    },
  ],
  relu: [
    {
      id: 'relu-dead-neuron',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { preActivation: 'negative for every batch', activation: 0, localDerivative: 0, incomingGradient: 0 },
      scenario: 'Visual state: a ReLU unit receives negative pre-activation for every observed batch, outputs zero, and its local derivative remains zero.',
      prompt: 'What failure mode is visible?',
      choices: ['A dead ReLU that may stop receiving useful incoming-weight updates', 'An exploding activation because the output is zero', 'A calibrated probability estimate'],
      answerIndex: 0,
      explanation: 'A ReLU with consistently negative pre-activation has zero output and zero local derivative, so gradient through that unit can remain blocked.',
      misconceptionTested: 'Every zero ReLU activation is harmless regardless of persistence.',
    },
  ],
  conv2d: [
    {
      id: 'conv-output-shape',
      level: 'mechanism',
      scenario: 'A 32 x 32 input uses a 3 x 3 kernel, stride 2, padding 1, and dilation 1.',
      prompt: 'What is the spatial output size along one dimension?',
      choices: ['16 because floor((32 + 2 - 3) / 2) + 1 = 16', '32 because padding always preserves size', '30 because stride does not affect output shape'],
      answerIndex: 0,
      explanation: 'The convolution output formula gives floor((input + 2*padding - effectiveKernel) / stride) + 1, producing 16 here.',
      misconceptionTested: 'Padding one always preserves spatial size regardless of stride.',
    },
    {
      id: 'conv-dilation-receptive-field',
      level: 'comparison',
      relatedComparison: 'kernel-size-vs-dilation',
      scenario: 'A 3 x 3 kernel changes from dilation 1 to dilation 2 without adding kernel parameters.',
      prompt: 'What changes most directly?',
      choices: ['The effective receptive field grows because kernel taps are spaced farther apart', 'The kernel gains four times as many learned weights', 'The input channels are automatically halved'],
      answerIndex: 0,
      explanation: 'Dilation spreads the same kernel taps over a wider input area, increasing effective receptive field without increasing the number of kernel weights.',
      misconceptionTested: 'A wider receptive field always requires more kernel parameters.',
    },
  ],
});

export function getP1NeuralScenariosForLesson(lessonId) {
  return P1_NEURAL_SCENARIOS_BY_LESSON[lessonId] || [];
}
