export const P1_FEATURE_SCALING_PREPROCESSING_SCENARIOS_BY_LESSON = Object.freeze({
  'feature-scaling-preprocessing': [
    {
      id: 'scaling-train-only-fit-leakage-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'prefit-scaler-vs-fold-local-preprocessing',
      scenario: 'A team standardizes the full 120,000-row dataset once, then passes the already scaled matrix into 5-fold cross-validation. Labels were never used by the scaler. Cross-validation improves slightly compared with a pipeline that fits StandardScaler separately inside each training fold.',
      prompt: 'What is the strongest diagnosis of the optimistic cross-validation result?',
      choices: [
        'The preprocessing leaked held-out feature-distribution information because each validation fold influenced the mean and standard deviation used for its own transformation',
        'There is no leakage because StandardScaler never reads the target labels',
        'Cross-validation requires one global scaler so every fold has identical feature statistics',
      ],
      answerIndex: 0,
      explanation: 'Scaling parameters are learned from feature data. Fitting them before the folds are formed lets every validation fold influence the representation used to train its corresponding model. The scaler belongs inside the cross-validation pipeline so each fold fits preprocessing only on its training rows.',
      misconceptionTested: 'Only transformations that directly inspect target labels can leak information across an evaluation boundary.',
    },
    {
      id: 'scaling-sparse-centering-memory-worked',
      level: 'calculation',
      relatedComparison: 'sparse-preserving-scale-vs-dense-centering',
      scenario: 'A bag-of-words matrix has 1,000,000 rows and 20,000 columns but only 0.05% of entries are nonzero. A preprocessing change subtracts a nonzero column mean from every entry. Ignoring sparse-index overhead, a dense float32 representation would need 1,000,000 x 20,000 x 4 bytes.',
      prompt: 'What memory consequence best explains why the job suddenly becomes infeasible?',
      choices: [
        'Centering can densify nearly the whole matrix, requiring about 80 GB for dense float32 values alone',
        'The centered matrix still needs only about 40 MB because subtracting means preserves all implicit zeros',
        'Centering reduces the matrix to 20,000 values because only one mean per column remains',
      ],
      answerIndex: 0,
      explanation: 'The dense element count is 20 billion. At 4 bytes each that is 80 billion bytes, about 80 GB before other arrays or overhead. Subtracting a nonzero mean turns implicit zeros into nonzero values, so sparse text pipelines normally use sparsity-preserving transforms or avoid centering.',
      misconceptionTested: 'Centering a sparse matrix changes values but cannot change its sparsity or memory complexity.',
    },
    {
      id: 'scaling-regularization-penalty-geometry-worked',
      level: 'calculation',
      relatedComparison: 'feature-units-vs-l2-coefficient-penalty',
      scenario: 'Two otherwise equivalent linear-model features encode the same quantity in different units. Feature A is measured in meters and needs coefficient 2 for a given physical effect. Feature B is measured in centimeters, so the equivalent coefficient is 0.02. With an L2 penalty proportional to beta squared, compare the raw coefficient penalties before scaling.',
      prompt: 'How different are the two L2 penalty contributions for the same physical effect?',
      choices: [
        'Feature A is penalized 10,000 times more because 2^2 / 0.02^2 = 4 / 0.0004 = 10,000',
        'They are penalized equally because L2 regularization is invariant to feature units',
        'Feature B is penalized 100 times more because centimeters use numerically larger feature values',
      ],
      answerIndex: 0,
      explanation: 'Changing feature units changes the coefficient magnitude required for the same prediction. L2 acts on coefficient magnitude, so without comparable feature scales an identical physical effect can receive radically different regularization pressure. Scaling continuous features makes the penalty geometry more comparable.',
      misconceptionTested: 'Regularized linear models automatically compensate for arbitrary feature units so scaling cannot affect which coefficients are shrunk.',
    },
    {
      id: 'scaling-train-serve-state-parity-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'versioned-training-transform-vs-live-refit',
      scenario: 'A model was trained with a StandardScaler snapshot whose mean and standard deviation were stored with model version 42. Production deploys the same model weights, but a background job refits the scaler every hour on recent traffic. Raw feature distributions drift slowly and prediction scores jump whenever the scaler refreshes even though model weights do not change.',
      prompt: 'What deployment failure best explains the score jumps?',
      choices: [
        'The model input coordinate system is changing independently of the fixed weights; preprocessing state must be versioned and updated consistently with the model',
        'Hourly scaler refits are always safe because standardization preserves the semantic meaning seen by fixed model weights',
        'The model should refit the scaler separately for every request so each example has zero mean',
      ],
      answerIndex: 0,
      explanation: 'Fitted preprocessing parameters are part of the deployed model contract. Changing them changes the numeric inputs presented to fixed weights. Updating scaler state independently can create train/serve skew and discontinuities; preprocessing and model versions need coordinated validation and rollout.',
      misconceptionTested: 'A scaler is stateless formatting that can be refitted independently of a trained model without changing model behavior.',
    },
  ],
});
