export const P2_SCENARIOS_BY_LESSON = Object.freeze({
  'determinant-volume': [
    {
      id: 'det-orientation-sign',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { determinant: -3, areaScale: 3, orientation: 'flipped' },
      scenario: 'Visual state: a 2D linear transform maps a unit square to a parallelogram with area 3 and reverses orientation.',
      prompt: 'What determinant is consistent with this transform?',
      choices: ['-3', '3', '0'],
      answerIndex: 0,
      explanation: 'The determinant magnitude gives the area scale, while a negative sign indicates an orientation flip, so the determinant is -3.',
      misconceptionTested: 'The determinant sign is irrelevant whenever only area is shown.',
    },
    {
      id: 'det-zero-singular',
      level: 'mechanism',
      scenario: 'A linear transform collapses every point in a plane onto one line.',
      prompt: 'What does this imply about the determinant and invertibility?',
      choices: ['The determinant is zero and the transform is singular', 'The determinant must be one because distances remain finite', 'The determinant sign alone determines invertibility'],
      answerIndex: 0,
      explanation: 'Collapsing dimension destroys volume and information, so the determinant is zero and no inverse can recover the original points.',
      misconceptionTested: 'A finite linear transform is always invertible.',
    },
  ],
  'sequential-testing-peeking': [
    {
      id: 'sequential-boundary-shape',
      level: 'comparison',
      relatedComparison: 'pocock-obrien-fleming',
      scenario: 'Two group-sequential designs have the same overall Type I error budget. One uses a relatively even significance threshold across looks; the other is much stricter early and approaches the ordinary threshold later.',
      prompt: 'Which comparison is correct?',
      choices: ['Pocock-style boundaries are more even; O’Brien-Fleming-style boundaries are stricter early', 'O’Brien-Fleming is always more permissive at the first look', 'The two designs must use identical boundaries at every look'],
      answerIndex: 0,
      explanation: 'Pocock-style designs spend error more evenly, while O’Brien-Fleming-style designs require stronger early evidence and relax toward later looks.',
      misconceptionTested: 'All valid sequential boundaries have the same shape.',
    },
  ],
  'spearman-correlation': [
    {
      id: 'spearman-tied-ranks',
      level: 'mechanism',
      scenario: 'Several observations share the same value, so their positions in the sorted order are tied.',
      prompt: 'How should a standard Spearman calculation handle the tied observations?',
      choices: ['Assign the tied observations their average rank before correlating ranks', 'Break ties randomly so every rank is unique', 'Drop every tied observation from the dataset'],
      answerIndex: 0,
      explanation: 'The usual rank transformation assigns tied values their average rank, preserving a deterministic rank representation before computing correlation.',
      misconceptionTested: 'Spearman correlation requires every observed value to be unique.',
    },
    {
      id: 'spearman-nonmonotonic',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { relationship: 'U-shaped', leftTrend: 'decreasing', rightTrend: 'increasing', spearman: 'near zero' },
      scenario: 'Visual state: Y is large for very small and very large X, but small around the middle, producing a strong U-shaped relationship while Spearman correlation is near zero.',
      prompt: 'Why can Spearman be small despite strong dependence?',
      choices: ['The relationship is non-monotonic, so one global rank direction cancels out', 'Spearman can only detect linear relationships', 'A near-zero Spearman value proves the variables are independent'],
      answerIndex: 0,
      explanation: 'Spearman measures monotonic association. A U-shaped relationship is strongly dependent but changes direction, so its global monotonic rank correlation can be small.',
      misconceptionTested: 'Zero Spearman correlation proves independence.',
    },
  ],
  relu: [
    {
      id: 'relu-dead-unit',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { preActivation: 'negative for all batch examples', output: 0, localGradient: 0 },
      scenario: 'Visual state: one ReLU unit receives negative pre-activations for every example, outputs zero, and its local derivative remains zero.',
      prompt: 'What failure mode is visible?',
      choices: ['A dead ReLU that may stop receiving useful gradient updates', 'An exploding activation because the output is zero', 'Perfect normalization because every output matches'],
      answerIndex: 0,
      explanation: 'A ReLU that stays on the negative side outputs zero and has zero local derivative, which can prevent the unit from recovering through ordinary gradient updates.',
      misconceptionTested: 'ReLU units always recover automatically after becoming inactive.',
    },
  ],
  'leaky-relu': [
    {
      id: 'leaky-relu-negative-gradient',
      level: 'comparison',
      relatedComparison: 'relu-leaky-relu-negative-region',
      scenario: 'A unit spends most of training with negative pre-activation. Standard ReLU gives zero output and zero local derivative there, while Leaky ReLU uses a small negative-side slope.',
      prompt: 'What advantage can the Leaky ReLU slope provide?',
      choices: ['It can preserve a small gradient through the negative region and reduce the risk of permanently inactive units', 'It guarantees the network cannot overfit', 'It makes positive activations exactly linear with slope zero'],
      answerIndex: 0,
      explanation: 'The small negative-side slope lets gradients continue through negative activations, which can help units recover, though it does not guarantee better performance in every network.',
      misconceptionTested: 'Leaky ReLU completely solves every optimization or dead-unit problem.',
    },
  ],
  'conv-relu': [
    {
      id: 'conv-relu-feature-map-meaning',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { convolutionResponse: 'positive on vertical edges, negative elsewhere', afterRelu: 'vertical edges remain, negative responses clipped' },
      scenario: 'Visual state: a convolution filter responds positively on vertical edges and negatively in other regions. After ReLU, only the positive edge responses remain visible.',
      prompt: 'What should the learner infer from the Conv+ReLU feature map?',
      choices: ['The convolution detects a pattern and ReLU gates the sign of that response rather than creating the feature by itself', 'ReLU performs the spatial convolution and the filter is decorative', 'Negative convolution responses are converted into equally strong positive edges'],
      answerIndex: 0,
      explanation: 'The convolution produces the spatial feature response; ReLU then applies a pointwise nonlinearity that keeps positive activations and clips negative ones.',
      misconceptionTested: 'Conv+ReLU can be understood as ReLU creating the spatial feature instead of gating a convolution response.',
    },
  ],
  'max-pooling': [
    {
      id: 'pooling-robustness-information-loss',
      level: 'comparison',
      relatedComparison: 'pooling-downsampling-stride',
      scenario: 'A 2x2 max-pooling layer makes a feature less sensitive to a one-pixel shift, but two different activation patterns now produce the same pooled output.',
      prompt: 'What trade-off does this illustrate?',
      choices: ['Local invariance and reduced spatial detail are gained together', 'Pooling preserves every spatial detail while improving invariance', 'Pooling increases spatial resolution by construction'],
      answerIndex: 0,
      explanation: 'Pooling can improve local translation robustness and reduce computation, but aggregation discards exact spatial information and can merge distinct patterns.',
      misconceptionTested: 'Pooling adds invariance without losing information.',
    },
  ],
  'native-sparse-attention': [
    {
      id: 'nsa-kernel-realized-speedup',
      level: 'diagnosis',
      relatedComparison: 'theoretical-sparsity-realized-speedup',
      scenario: 'A sparse-attention pattern reduces theoretical attention FLOPs by 70%, but the deployed kernel uses irregular memory access and delivers only a 10% latency improvement.',
      prompt: 'What should the performance analysis conclude?',
      choices: ['Algorithmic sparsity does not guarantee proportional wall-clock speedup without efficient kernels and memory access', 'The FLOP reduction proves latency must also fall by exactly 70%', 'Sparse attention cannot ever reduce compute'],
      answerIndex: 0,
      explanation: 'Realized speed depends on kernels, memory traffic, utilization, batching, and hardware; lower arithmetic work is only one part of system performance.',
      misconceptionTested: 'Theoretical FLOP reduction translates directly into equal latency reduction.',
    },
  ],
  'dapo-reasoning-rl': [
    {
      id: 'dapo-ablation-interpretation',
      level: 'paper-reading',
      scenario: 'A paper adds four training changes together and reports a large gain. An ablation removes one change at a time, but several removals interact and the gains are not additive.',
      prompt: 'What is the safest interpretation of the ablation?',
      choices: ['Component effects are conditional on the rest of the training recipe and should not be treated as independent additive gains', 'Each removal gives the exact standalone contribution of that component', 'The largest single ablation drop proves all other components are unnecessary'],
      answerIndex: 0,
      explanation: 'Ablations estimate effects inside a particular combined system. Interactions mean one-at-a-time differences need not equal independent causal contributions that sum cleanly.',
      misconceptionTested: 'Ablation deltas are always independent additive component values.',
    },
  ],
  'coconut-latent-reasoning': [
    {
      id: 'coconut-latent-probe-limit',
      level: 'diagnosis',
      scenario: 'A linear probe predicts several reasoning-step labels from latent states with moderate accuracy, but the probe itself was trained using those labels.',
      prompt: 'What can the result support?',
      choices: ['The latent states contain linearly decodable information about the labels, not proof that the model explicitly reasons using those human-defined states', 'The model must internally execute the exact labeled symbolic program', 'Probe accuracy proves the latent representation is causally necessary for the final answer'],
      answerIndex: 0,
      explanation: 'Probe success shows information is decodable from a representation under that probe, but it does not by itself establish the model’s causal algorithm or semantic interpretation.',
      misconceptionTested: 'A successful representation probe proves the model uses exactly the probed concept causally.',
    },
  ],
  'frontier-moe-systems': [
    {
      id: 'moe-interconnect-bottleneck',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { expertComputeUtilization: '55%', allToAllTime: '43% step time', routerBalance: 'acceptable' },
      scenario: 'Visual state: expert compute is only 55% utilized while all-to-all token exchange consumes 43% of each training step, even though router load balance is acceptable.',
      prompt: 'Which bottleneck should be investigated first?',
      choices: ['Expert-parallel communication and interconnect efficiency', 'The number of vocabulary tokens only', 'Class-imbalance threshold calibration'],
      answerIndex: 0,
      explanation: 'When all-to-all communication dominates step time despite balanced routing, interconnect bandwidth, topology, and communication overlap become the likely system bottleneck.',
      misconceptionTested: 'MoE efficiency depends only on expert FLOPs and router balance.',
    },
  ],
});

export function getP2ScenariosForLesson(lessonId) {
  return P2_SCENARIOS_BY_LESSON[lessonId] || [];
}
