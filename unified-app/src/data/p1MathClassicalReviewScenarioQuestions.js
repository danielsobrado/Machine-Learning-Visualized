function q(value) { return Object.freeze({ ...value, choices: Object.freeze([...value.choices]) }); }
function lesson(items) { return Object.freeze(items.map(q)); }

export const P1_MATH_CLASSICAL_REVIEW_SCENARIOS_BY_LESSON = Object.freeze({
  'change-of-basis': lesson([{
    id: 'review-basis-vector-vs-coordinates', level: 'mechanism',
    relatedComparison: 'geometric-vector-vs-coordinate-representation',
    scenario: 'A geometric vector v is fixed in the plane. The team changes from the standard basis to a rotated basis, and the coordinate pair used to represent v changes even though the arrow drawn in physical space stays in the same place.',
    prompt: 'What actually changed when the basis changed?',
    choices: ['Only the coordinates changed; the underlying geometric vector stayed the same', 'The vector itself changed because coordinates define the physical arrow', 'Both bases must produce identical coordinates for the same vector'],
    answerIndex: 0,
    explanation: 'A basis is a coordinate system. The abstract or geometric vector is basis-independent, while its coordinate representation depends on which basis vectors are used to reconstruct it. Change-of-basis matrices convert coordinates without changing the represented vector.',
    misconceptionTested: 'Changing basis changes the vector itself rather than changing only the coordinates used to describe that vector.',
  }]),
  'condition-number': lesson([{
    id: 'review-condition-number-sensitivity', kind: 'visual-state', visualState: Object.freeze({ conditionNumber: 100000000, inputRelativeError: 0.000001, worstCaseAmplification: 100000000 }), level: 'diagnosis',
    relatedComparison: 'well-conditioned-vs-ill-conditioned-linear-systems',
    scenario: 'A linear system has condition number around 10^8 in the chosen norm. Input coefficients are measured with small relative error and floating-point arithmetic also introduces rounding noise.',
    prompt: 'What practical warning does the large condition number provide?',
    choices: ['Small relative input or numerical errors can be amplified dramatically in the solution, so many digits may be unreliable', 'The matrix is guaranteed to be exactly singular', 'The solution is guaranteed accurate because a large condition number means strong numerical separation'],
    answerIndex: 0,
    explanation: 'Condition number measures worst-case sensitivity. A value near 10^8 means relative perturbations can, in unfavorable directions, be amplified by roughly eight orders of magnitude. That does not prove exact singularity, but it indicates severe numerical fragility and loss of trustworthy digits.',
    misconceptionTested: 'Condition number measures computational speed or proves exact singularity rather than quantifying sensitivity to perturbations.',
  }]),
  'determinant-volume': lesson([{
    id: 'review-determinant-zero-rank', level: 'mechanism',
    relatedComparison: 'nonzero-volume-map-vs-collapsed-subspace',
    scenario: 'A square matrix maps a unit cube into a flat shape with zero volume. Its determinant is therefore zero.',
    prompt: 'Which linked conclusion follows for this square matrix?',
    choices: ['The map collapses at least one dimension, so the matrix is rank-deficient and not invertible', 'The matrix preserves all directions but reverses orientation', 'A zero determinant implies every matrix entry is zero'],
    answerIndex: 0,
    explanation: 'The absolute determinant is the volume-scaling factor. Zero volume means independent input directions became linearly dependent after transformation. A square matrix with determinant zero therefore lacks full rank and has no inverse, though many entries can still be nonzero.',
    misconceptionTested: 'A zero determinant means the matrix must be the all-zero matrix rather than any transformation that collapses dimension.',
  }]),
  eigenvalue: lesson([{
    id: 'review-eigen-defective-diagonalization', level: 'diagnosis',
    relatedComparison: 'repeated-eigenvalue-vs-complete-eigenbasis',
    scenario: 'A 2×2 matrix has one repeated eigenvalue but only one linearly independent eigenvector. A student assumes every matrix with known eigenvalues can be diagonalized.',
    prompt: 'Why does diagonalization fail here?',
    choices: ['Diagonalization requires enough linearly independent eigenvectors to form a basis, and this defective matrix has only one', 'Repeated eigenvalues are never allowed in diagonalizable matrices', 'Diagonalization depends only on determinant and not on eigenvectors'],
    answerIndex: 0,
    explanation: 'A matrix A=PDP^-1 needs the columns of P to be a full eigenvector basis. Repeated eigenvalues are compatible with diagonalization when their eigenspaces provide enough independent eigenvectors, but algebraic multiplicity alone does not guarantee that.',
    misconceptionTested: 'Knowing all eigenvalues or having repeated eigenvalues automatically guarantees a complete eigenvector basis.',
  }]),
  'fundamental-subspaces': lesson([{
    id: 'review-rank-nullity-four-subspaces', level: 'calculation',
    relatedComparison: 'column-row-rank-vs-null-left-null-dimensions',
    scenario: 'A matrix A has shape 6×10 and rank 4. The team wants the dimensions of its null space and left-null space without computing explicit bases.',
    prompt: 'What are dim N(A) and dim N(A^T)?',
    choices: ['6 and 2 respectively, because nullity is 10-4 and left-nullity is 6-4', '2 and 6 respectively', '4 and 4 because both null spaces always have dimension equal to rank'],
    answerIndex: 0,
    explanation: 'Rank-nullity on A gives dim N(A)=n-r=10-4=6. Applying the same reasoning to A^T gives dim N(A^T)=m-r=6-4=2. Row and column spaces both have dimension r=4.',
    misconceptionTested: 'The null-space dimensions equal rank or are determined by matrix shape without subtracting rank from the relevant ambient dimension.',
  }]),
  'least-squares-projection': lesson([{
    id: 'review-least-squares-qr-vs-normal-equations', level: 'decision',
    relatedComparison: 'qr-solve-vs-explicit-normal-equations',
    scenario: 'A tall design matrix is moderately ill-conditioned. Two implementations are proposed: form X^T X and solve the normal equations, or solve the least-squares problem with QR without explicitly squaring the condition number.',
    prompt: 'Which implementation is numerically safer and why?',
    choices: ['QR is generally safer because forming X^T X worsens conditioning and can magnify floating-point error', 'Normal equations are always safer because multiplying by X^T removes noise', 'Both methods are numerically identical in finite precision'],
    answerIndex: 0,
    explanation: 'The condition number of X^T X is roughly the square of the condition number of X in the 2-norm, so explicit normal equations can lose substantial numerical accuracy. QR avoids that squaring and is the standard stable approach for ordinary full-rank least squares.',
    misconceptionTested: 'Normal equations are merely a faster algebraic rewrite with no numerical-stability cost.',
  }]),
  'low-rank-approximation': lesson([{
    id: 'review-low-rank-truncated-svd-optimality', level: 'mechanism',
    relatedComparison: 'truncated-svd-vs-arbitrary-rank-k-factorization',
    scenario: 'A matrix must be compressed to rank k under Frobenius-norm reconstruction error. Many rank-k factorizations are possible.',
    prompt: 'Why are the top k singular components special?',
    choices: ['Truncated SVD gives an optimal rank-k approximation under Frobenius norm by discarding the smallest singular directions', 'Any k columns of the matrix achieve the same reconstruction error', 'The smallest singular values must be kept because they represent the rarest information'],
    answerIndex: 0,
    explanation: 'The Eckart-Young-Mirsky result states that keeping the k largest singular values and associated singular vectors minimizes common unitarily invariant reconstruction norms, including Frobenius norm. Arbitrary rank-k factorizations need not achieve that minimum.',
    misconceptionTested: 'All rank-k approximations are equivalent or optimality comes from keeping the smallest singular values.',
  }]),
  'matrix-decompositions': lesson([{
    id: 'review-decomposition-near-rank-deficient', level: 'decision',
    relatedComparison: 'qr-efficiency-vs-svd-rank-diagnosis',
    scenario: 'A rectangular least-squares matrix is nearly rank-deficient and the team must both solve the system and understand which directions are numerically unsupported.',
    prompt: 'Which decomposition is the strongest diagnostic choice?',
    choices: ['SVD, because its singular values expose near-null directions while still supporting a stable least-squares solution', 'LU, because pivoting makes rectangular rank diagnosis unnecessary', 'Eigen decomposition of A directly, because every rectangular matrix has a square eigenbasis'],
    answerIndex: 0,
    explanation: 'QR is efficient for well-behaved least squares, but SVD is especially informative when numerical rank is uncertain. Tiny singular values show weakly constrained directions and support pseudoinverse or regularized solutions. LU and ordinary eigendecomposition do not provide the same rectangular rank diagnostic.',
    misconceptionTested: 'All decompositions are interchangeable for near-rank-deficient rectangular systems.',
  }]),
  'matrix-multiplication': lesson([{
    id: 'review-matmul-order-shape', level: 'calculation',
    relatedComparison: 'ab-defined-vs-ba-undefined',
    scenario: 'Let A have shape 3×2 and B have shape 2×4.',
    prompt: 'Which multiplication statement is correct?',
    choices: ['AB exists and has shape 3×4, while BA is undefined because the inner dimensions 4 and 3 do not match', 'Both AB and BA exist and have the same shape', 'BA exists as 2×2 while AB is undefined'],
    answerIndex: 0,
    explanation: 'Matrix multiplication A_(m×n) B_(n×p) produces m×p. Thus 3×2 times 2×4 gives 3×4. Reversing the order would require multiplying 2×4 by 3×2, whose inner dimensions 4 and 3 disagree.',
    misconceptionTested: 'Matrix multiplication is shape-commutative or reversing factors preserves dimensional validity.',
  }]),
  'projection-matrices': lesson([{
    id: 'review-projection-oblique-idempotent', level: 'mechanism',
    relatedComparison: 'orthogonal-projection-vs-oblique-projection',
    scenario: 'A matrix P satisfies P^2=P but P^T is not equal to P. It therefore applies the same projection again without changing the result, but it is not symmetric.',
    prompt: 'What kind of projection can P represent?',
    choices: ['An oblique projection; idempotence defines projection behavior while symmetry is additionally required for an orthogonal projection', 'No projection at all because every projection matrix must be symmetric', 'Only a rotation because idempotence is the defining property of rotations'],
    answerIndex: 0,
    explanation: 'Idempotence means projected points stay fixed under repeated application. Orthogonal projection adds symmetry in the standard Euclidean inner product. Without symmetry, the projection can occur along a non-orthogonal direction, producing an oblique projection.',
    misconceptionTested: 'Idempotence and symmetry are equivalent requirements, so every projection must be orthogonal.',
  }]),
  'qr-decomposition': lesson([{
    id: 'review-qr-reduced-form', level: 'design',
    relatedComparison: 'full-qr-vs-reduced-qr-for-tall-matrices',
    scenario: 'A full-column-rank matrix A has shape 1,000×20 and is used for least squares. The full QR factorization would build a 1,000×1,000 orthogonal Q even though only the column space of A is needed.',
    prompt: 'Why is reduced QR sufficient here?',
    choices: ['It keeps only the 1,000×20 orthonormal basis for col(A) and a 20×20 R, which contains everything needed for the least-squares solve', 'Reduced QR discards R and therefore solves by matrix inversion', 'Full Q is required because least squares uses all 1,000 orthogonal directions explicitly'],
    answerIndex: 0,
    explanation: 'For tall full-column-rank A, the economy factorization A=Q_r R uses Q_r with n orthonormal columns spanning col(A) and square R. Least squares needs Q_r^T b and R, not an orthonormal completion for the entire ambient space.',
    misconceptionTested: 'A tall least-squares problem requires materializing the entire full orthogonal matrix Q.',
  }]),
  svd: lesson([{
    id: 'review-svd-numerical-rank', level: 'diagnosis',
    relatedComparison: 'exact-zero-singular-values-vs-small-numerical-singular-values',
    scenario: 'A matrix has singular values [12, 4.1, 0.003, 0.000002]. Measurement noise is around 0.001 in the relevant scaled units.',
    prompt: 'What should the tiny singular values tell the analyst?',
    choices: ['Directions associated with values below the noise scale may be effectively unsupported, so numerical rank can be smaller than exact algebraic rank', 'All nonzero singular values are equally reliable because only exact zero indicates instability', 'Small singular values increase stability because they suppress inverse amplification'],
    answerIndex: 0,
    explanation: 'Exact rank counts strictly nonzero singular values, but numerical work must compare them with noise and precision scales. Very small values correspond to nearly null directions and become huge reciprocals in inverse-like operations, so they can signal effective rank deficiency and instability.',
    misconceptionTested: 'Only mathematically exact zero singular values matter for rank or numerical stability.',
  }]),
  'bag-of-words': lesson([{
    id: 'review-bow-word-order-loss', level: 'diagnosis',
    relatedComparison: 'token-count-representation-vs-sequence-aware-representation',
    scenario: 'Two sentences are "dog bites man" and "man bites dog". A plain unigram bag-of-words vector records the same count for dog, bites, and man in both sentences.',
    prompt: 'What representational limitation does this expose?',
    choices: ['Unigram bag of words discards word order, so distinct meanings can collapse to the same vector', 'Bag of words preserves syntax exactly because counts imply position', 'The sentences differ only if stop-word removal is disabled'],
    answerIndex: 0,
    explanation: 'A unigram bag represents which vocabulary items occur and how often, not where they occur. Reordering the same words leaves the vector unchanged even when grammatical roles and meaning reverse. N-grams or sequence models are needed to encode ordering information.',
    misconceptionTested: 'Word-frequency vectors retain enough positional information to distinguish arbitrary reorderings of the same tokens.',
  }]),
  'cosine-similarity': lesson([{
    id: 'review-cosine-discards-norm', level: 'calculation',
    relatedComparison: 'directional-similarity-vs-vector-magnitude',
    scenario: 'Two nonzero vectors satisfy b=100a. Their norms differ by a factor of 100, but they point in exactly the same direction.',
    prompt: 'What cosine similarity do they have, and what information is intentionally ignored?',
    choices: ['Cosine similarity is 1, and vector magnitude is discarded by normalization', 'Cosine similarity is 100 because it preserves scale', 'Cosine similarity is 0 because different norms imply orthogonality'],
    answerIndex: 0,
    explanation: 'Cosine similarity divides the dot product by both norms. Positive rescaling cancels out, leaving a value of one for perfectly aligned vectors. This is useful when direction encodes semantics, but it deliberately ignores norm information that may matter in some embeddings or applications.',
    misconceptionTested: 'Cosine similarity measures both orientation and absolute vector magnitude.',
  }]),
  embeddings: lesson([{
    id: 'review-embeddings-static-polysemy', level: 'diagnosis',
    relatedComparison: 'static-word-vector-vs-contextual-token-representation',
    scenario: 'A static embedding table assigns one vector to the word "bank" whether the sentence discusses a river bank or a financial bank.',
    prompt: 'What limitation follows from this one-vector-per-token design?',
    choices: ['Distinct senses are conflated unless context is represented elsewhere, motivating contextual embeddings for polysemous words', 'The vector automatically changes with surrounding words despite being a static lookup', 'Polysemy is solved only by increasing vector norm'],
    answerIndex: 0,
    explanation: 'A static lookup cannot directly produce a different representation for the same token in different contexts. It can encode an average of usage patterns, but contextual models compute token representations conditioned on neighboring text and can therefore separate senses more effectively.',
    misconceptionTested: 'A static embedding lookup inherently changes a word vector according to its sentence context.',
  }]),
  fasttext: lesson([{
    id: 'review-fasttext-oov-vs-polysemy', level: 'comparison',
    relatedComparison: 'subword-oov-composition-vs-contextual-sense-disambiguation',
    scenario: 'A FastText model encounters an unseen morphological variant whose character n-grams were seen during training, and separately encounters the ambiguous word "bank" in two unrelated senses.',
    prompt: 'Which capability does subword composition provide directly, and which problem remains?',
    choices: ['It can construct a useful OOV vector from subword pieces, but a static representation still does not by itself resolve context-dependent polysemy', 'It solves both OOV morphology and contextual polysemy completely', 'It solves neither because unseen whole words always map to an unknown token'],
    answerIndex: 0,
    explanation: 'FastText can sum learned character n-gram vectors to represent unseen surface forms, which improves morphological generalization and OOV handling. But its resulting word representation is still fundamentally static unless combined with a contextual model, so different senses of the same spelling can remain conflated.',
    misconceptionTested: 'Subword modeling and contextual sense disambiguation are the same capability.',
  }]),
  'feature-scaling-preprocessing': lesson([{
    id: 'review-scaling-algorithm-sensitivity', level: 'decision',
    relatedComparison: 'distance-margin-regularization-sensitive-models-vs-tree-splits',
    scenario: 'One feature ranges from 0 to 1 and another from 0 to 100,000. The team compares k-NN, an RBF SVM, L2-regularized logistic regression, and a standard decision tree.',
    prompt: 'Which models most clearly require scale-aware preprocessing for fair geometry or penalties?',
    choices: ['k-NN, the RBF SVM, and regularized logistic regression are scale-sensitive, while ordinary tree splits are largely invariant to monotonic rescaling', 'Only the decision tree is scale-sensitive because it compares thresholds', 'None are scale-sensitive because every algorithm learns its own units automatically'],
    answerIndex: 0,
    explanation: 'Distance-based k-NN and kernel SVM depend directly on feature geometry. Regularization also penalizes coefficients relative to feature scales. Axis-aligned tree splits depend mainly on ordering and threshold placement, so monotonic rescaling usually leaves their split logic equivalent.',
    misconceptionTested: 'Feature scaling is either universally required or universally irrelevant across model families.',
  }]),
  'k-means': lesson([{
    id: 'review-kmeans-nonspherical-clusters', level: 'diagnosis',
    relatedComparison: 'centroid-voronoi-clusters-vs-curved-density-structure',
    scenario: 'Data form two interlocking crescent-shaped clusters with similar density. The requested k is correctly set to 2, but K-Means repeatedly cuts across both crescents.',
    prompt: 'Why can K-Means fail even when the number of clusters is correct?',
    choices: ['Nearest-centroid Voronoi partitions favor roughly convex, spherical groups and cannot naturally represent interlocking curved cluster shapes', 'K-Means requires k to be larger than the number of true groups', 'K-Means can represent any topology once initialized enough times'],
    answerIndex: 0,
    explanation: 'K-Means minimizes squared distance to centroids, which induces convex Voronoi regions. Curved or non-convex structures can therefore be split incorrectly even with perfect knowledge of k. More restarts address local minima, not the mismatch between the objective and cluster geometry.',
    misconceptionTested: 'Choosing the correct k guarantees K-Means can represent the true clustering structure.',
  }]),
  'tree-ensembles': lesson([{
    id: 'review-tree-bagging-vs-boosting', level: 'comparison',
    relatedComparison: 'parallel-variance-reduction-vs-sequential-error-correction',
    scenario: 'A team compares random forests with gradient-boosted trees and describes both simply as "many trees averaged together".',
    prompt: 'What distinction is essential?',
    choices: ['Random forests mainly reduce variance through randomized parallel trees and aggregation, while boosting builds trees sequentially to correct current model errors', 'Boosting trains every tree independently and random forests fit residuals sequentially', 'Both algorithms use exactly the same training dependency structure'],
    answerIndex: 0,
    explanation: 'Bagging-style forests decorrelate many trees and aggregate them, primarily attacking variance. Boosting is stage-wise: each new weak learner is chosen to improve the current ensemble objective, often by fitting residual-like gradients. Both use trees, but the optimization logic is fundamentally different.',
    misconceptionTested: 'All tree ensembles differ only in the number of trees and are trained independently before averaging.',
  }]),
});
