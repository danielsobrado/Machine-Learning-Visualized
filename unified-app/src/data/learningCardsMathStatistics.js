import { cardSet } from './learningCardUtils.js';

export const MATH_STATISTICS_LEARNING_CARD_OVERRIDES = Object.freeze({
  'least-squares-projection': cardSet(
    'Least squares finds the point in a model subspace that is closest to the observed target.',
    'Picture the target vector floating above a flat plane. The best prediction is its perpendicular shadow on that plane.',
    'The residual r=b-Ax is orthogonal to every column of A at the optimum, giving A^T(Ax-b)=0.',
    'Move b while keeping the column space fixed and watch the fitted point slide along the subspace while the residual stays perpendicular.',
    'Mistake to avoid: least squares does not make every residual zero unless the target already lies inside the column space.',
    'Try it: predict whether adding a new independent column to A can increase the minimum squared error.',
  ),
  pseudoinverse: cardSet(
    'The pseudoinverse extends inverse-like solving to rectangular, rank-deficient, and underdetermined systems.',
    'Think of it as “invert only the directions the matrix actually supports, and ignore directions it cannot see.”',
    'Using A=UΣV^T, the pseudoinverse is A^+=VΣ^+U^T, where nonzero singular values are reciprocated.',
    'For an underdetermined system with many exact solutions, compare their lengths and watch A^+b select the minimum-norm one.',
    'Mistake to avoid: the pseudoinverse is not a magical exact inverse; unsupported singular directions cannot be recovered from the data.',
    'Try it: shrink one singular value toward zero and predict what happens to the corresponding reciprocal and numerical sensitivity.',
  ),
  'change-of-basis': cardSet(
    'Change of basis rewrites the same vector using a different set of coordinate axes.',
    'The arrow in space stays put; only the labels on the ruler change.',
    'If B contains basis vectors as columns, then v=B[v]_B and [v]_B=B^{-1}v when B is invertible.',
    'Rotate the basis while keeping the geometric vector fixed and watch its coordinate pair change even though the drawn arrow does not move.',
    'Mistake to avoid: changing coordinates is not the same as applying a physical transformation to the vector.',
    'Try it: predict the coordinates of a basis vector in its own basis before switching back to standard coordinates.',
  ),
  'condition-number': cardSet(
    'The condition number measures how strongly a problem can amplify small input or numerical errors.',
    'Imagine stretching a rubber sheet enormously in one direction and barely in another; recovering the original point becomes fragile.',
    'For the 2-norm, κ(A)=σ_max/σ_min, so a tiny smallest singular value makes inverse-like operations sensitive.',
    'Drag the smallest singular value toward zero and watch the estimated error amplification grow even before the matrix becomes exactly singular.',
    'Mistake to avoid: ill-conditioned is not the same as singular; a matrix can be invertible and still produce unreliable numerical answers.',
    'Try it: compare κ=10 and κ=10^8 and predict which solution keeps more trustworthy digits after a tiny perturbation.',
  ),
  'determinant-volume': cardSet(
    'The determinant tells how a square linear map scales oriented area or volume.',
    'Imagine a unit square turning into a parallelogram: the signed area multiplier is the determinant.',
    'For a square matrix A, |det(A)| is the volume scale; det(A)=0 means at least one dimension collapses.',
    'Move two transformed basis vectors until they become parallel and watch the enclosed area—and determinant magnitude—fall to zero.',
    'Mistake to avoid: determinant is not the sum of matrix entries and zero determinant does not mean every entry is zero.',
    'Try it: swap two matrix rows and predict what happens to determinant magnitude and sign.',
  ),
  'projection-matrices': cardSet(
    'A projection matrix maps every vector onto a chosen subspace and leaves already-projected vectors unchanged.',
    'Think of shining a light so vectors cast shadows onto a line or plane.',
    'Projection satisfies P^2=P; an orthogonal projection also satisfies P^T=P.',
    'Project a vector twice and compare the second result with the first, then tilt the projection direction to contrast orthogonal and oblique projection.',
    'Mistake to avoid: idempotence defines projection behavior, but symmetry is what makes the projection orthogonal in Euclidean space.',
    'Try it: predict whether a vector already inside the target subspace moves after applying P.',
  ),
  'low-rank-approximation': cardSet(
    'Low-rank approximation compresses a matrix by keeping only its strongest independent patterns.',
    'Think of an image built from layers of structure: broad shapes matter first, tiny detail comes later.',
    'Truncated SVD keeps the top k singular triplets, A_k=sum_{i=1}^k σ_i u_i v_i^T, and minimizes Frobenius reconstruction error among rank-k matrices.',
    'Increase k one component at a time and watch reconstruction error fall as progressively weaker singular directions are restored.',
    'Mistake to avoid: low rank is not the same as small matrix entries; rank counts independent structure, not magnitude.',
    'Try it: remove the smallest singular component and predict whether reconstruction error changes more or less than removing the largest one.',
  ),
  'conditional-probability': cardSet(
    'Conditional probability asks how likely an event is after restricting attention to cases where another event is known to have happened.',
    'Imagine filtering a dataset first, then recomputing proportions only inside the rows that survived the filter.',
    'P(A|B)=P(A∩B)/P(B), provided P(B)>0; the denominator restricts attention to cases where B occurred.',
    'Select a subgroup with event B and compare the rate of A inside that subgroup with the overall rate of A.',
    'Mistake to avoid: P(A|B) and P(B|A) answer different questions and can differ dramatically.',
    'Try it: hold P(A∩B) fixed while making B rarer and predict how P(A|B) changes.',
  ),
  'expected-value-variance': cardSet(
    'Expected value describes the long-run center of a random quantity; variance describes how widely outcomes spread around that center.',
    'Expectation is the balance point of the probability distribution, while variance measures the average squared distance from that balance point.',
    'E[X]=sum_x x p(x) and Var(X)=E[(X-E[X])^2]; for sums, covariance matters to variance but not to linearity of expectation.',
    'Move probability mass from the center toward extreme outcomes while keeping the mean fixed and watch variance increase.',
    'Mistake to avoid: the expected value does not have to be one of the outcomes, and independent variables are not required for expectations to add.',
    'Try it: double every possible outcome and predict how the mean and variance scale.',
  ),
  entropy: cardSet(
    'Entropy measures the average uncertainty in a probability distribution.',
    'A coin that is almost guaranteed to land heads is easy to predict; a 50/50 coin carries more surprise before each flip.',
    'H(p)=-sum_i p_i log p_i, with maximum entropy for a uniform distribution over a fixed finite support.',
    'Move probability mass from one dominant class toward an even split and watch entropy rise.',
    'Mistake to avoid: high entropy means uncertainty in the distribution, not necessarily noisy data or a bad model.',
    'Try it: compare [0.99,0.01] with [0.5,0.5] and predict which distribution has greater entropy before calculating it.',
  ),
  'cross-entropy': cardSet(
    'Cross-entropy measures how costly a predicted probability distribution is when the target distribution generates the outcomes.',
    'It punishes confident probability assigned to the wrong place much more than cautious uncertainty.',
    'H(p,q)=-sum_i p_i log q_i; for a one-hot target this becomes minus log of the predicted probability assigned to the true class.',
    'Lower the true-class probability from 0.9 to 0.1 and watch the loss grow nonlinearly.',
    'Mistake to avoid: cross-entropy is not accuracy; two models can classify the same examples correctly while assigning very different probability quality.',
    'Try it: compare a correct prediction at 0.55 versus 0.95 confidence and predict which one receives lower loss.',
  ),
  'markov-chains': cardSet(
    'A Markov chain models a system that moves between states using transition probabilities.',
    'The next step depends on where you are now, not on the full path you took to get there, once the current state contains all relevant information.',
    'A transition matrix P stores P_{ij}=P(X_{t+1}=j|X_t=i), and a state distribution evolves as π_{t+1}=π_t P.',
    'Start all probability mass in one state and apply the transition matrix repeatedly to watch the distribution spread or settle.',
    'Mistake to avoid: the Markov property is a statement about the chosen state representation, not a claim that the real world has no history.',
    'Try it: identify a missing piece of history that would need to be added to the state if the next-step probabilities still depend on it.',
  ),
  pagerank: cardSet(
    'PageRank assigns importance through a random-surfer model: important pages receive links from other important pages.',
    'Think of votes that are weighted by the authority of the voter and divided among that voter’s outgoing links.',
    'PR(v)=(1-d)/N + d sum_{u->v} PR(u)/L(u), with teleportation preventing traps and dead ends.',
    'Increase one page’s incoming links and compare how rank redistributes after several iterations instead of only after one local update.',
    'Mistake to avoid: PageRank is not raw in-degree; one link from a highly ranked page can matter more than many weak links.',
    'Try it: create a dangling node with no outgoing links and predict why teleportation or dangling-mass redistribution is necessary.',
  ),
});
