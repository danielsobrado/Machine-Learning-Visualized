export const P1_GENERATION_SAMPLING_SCENARIOS_BY_LESSON = Object.freeze({
  'transformer-token-generation': Object.freeze([
    Object.freeze({
      id: 'generation-last-position-logits',
      level: 'diagnosis',
      scenario: 'A decoder-only model receives a prompt of 40 tokens and returns vocabulary logits for all 40 positions. The generation code samples from position 0 instead of the final visible position.',
      prompt: 'What is the core inference bug?',
      choices: Object.freeze([
        'Next-token selection should use logits from the current final visible position, not the first prompt position',
        'Generation should average logits from all prompt positions before sampling',
        'The model should sample from the tokenizer embedding of the first token instead of vocabulary logits',
      ]),
      answerIndex: 0,
      explanation: 'For standard decoder-only next-token generation, the logits at the final visible position represent the distribution for the token that comes next. Using an earlier position creates an off-by-position error even if the transformer forward pass itself is correct.',
      misconceptionTested: 'All position-wise vocabulary logits in a causal decoder describe the same next-token distribution.',
      relatedComparison: 'Training-time position-wise losses versus inference-time selection from the current final position.',
    }),
    Object.freeze({
      id: 'generation-context-state-update',
      level: 'diagnosis',
      scenario: 'A generation loop samples token X after the prompt, but the next decode step accidentally conditions on the original prompt again instead of prompt + X. The same first-token distribution keeps reappearing.',
      prompt: 'What state transition is missing?',
      choices: Object.freeze([
        'The sampled token must become part of the visible context or equivalent cached decode state before the next-token step',
        'The model weights must be fine-tuned on token X before continuing',
        'Softmax should be removed so the model can remember the previous sample',
      ]),
      answerIndex: 0,
      explanation: 'Autoregressive generation is stateful across steps: each selected token changes the prefix used for the following prediction. Reusing the unchanged prompt repeats the same conditional prediction problem instead of advancing the sequence.',
      misconceptionTested: 'Generated tokens are display-only output and do not condition later token predictions.',
      relatedComparison: 'Prompt-only context versus prompt-plus-generated-token context.',
    }),
    Object.freeze({
      id: 'generation-stop-condition-precedence',
      level: 'decision',
      scenario: 'A service has EOS handling, a user-configured stop sequence, and max_new_tokens=200. During decoding, a stop sequence appears after 37 generated tokens before EOS is sampled.',
      prompt: 'What should a correct generation loop normally do?',
      choices: Object.freeze([
        'Stop when the configured stop condition fires rather than continuing until EOS or the hard token cap',
        'Ignore the stop sequence because only EOS is allowed to terminate generation',
        'Continue to exactly 200 generated tokens because max_new_tokens overrides earlier stopping conditions',
      ]),
      answerIndex: 0,
      explanation: 'EOS, external stop sequences, and maximum-length limits are separate termination mechanisms. A matching stop sequence should halt generation according to the serving policy even when EOS has not yet appeared.',
      misconceptionTested: 'Maximum token count is the only valid stopping rule once decoding starts.',
      relatedComparison: 'Model-selected EOS versus application-level stop sequences versus hard length caps.',
    }),
    Object.freeze({
      id: 'generation-cache-position-invariance',
      level: 'diagnosis',
      scenario: 'With KV cache disabled, a fixed prompt and greedy decoding produce token A next. With cache enabled, the same model and prompt produce token B immediately. Logging shows cached keys/values are reused, but generated-token position indices restart at zero after prefill.',
      prompt: 'What is the strongest diagnosis?',
      choices: Object.freeze([
        'The cache path is applying incorrect position state, so it is no longer equivalent to full-context recomputation',
        'Different next-token probabilities are expected whenever KV cache is enabled',
        'Greedy decoding becomes stochastic when cached keys and values are reused',
      ]),
      answerIndex: 0,
      explanation: 'A correct KV cache is an efficiency optimization and should preserve the same distribution as recomputing the identical context. Resetting positions after prefill changes positional information and therefore changes attention and logits.',
      misconceptionTested: 'KV caching may legitimately alter the model distribution as long as it makes decoding faster.',
      relatedComparison: 'Cached incremental decoding versus full-prefix recomputation.',
    }),
    Object.freeze({
      id: 'generation-batched-finished-sequences',
      level: 'design',
      scenario: 'A batch contains three active generations. Sequence A emits EOS at step 12, while B and C continue. The implementation keeps sampling real continuation tokens for A until the longest sequence finishes.',
      prompt: 'What serving behavior should be implemented?',
      choices: Object.freeze([
        'Track finished sequences independently so A stops contributing generated content while B and C continue',
        'Force B and C to stop whenever any sequence in the batch emits EOS',
        'Ignore EOS in batched decoding because all rows must have identical output lengths',
      ]),
      answerIndex: 0,
      explanation: 'Batched generation needs per-sequence termination state. Finished rows can be masked or otherwise excluded from meaningful token selection while unfinished rows continue decoding.',
      misconceptionTested: 'All sequences in a generation batch must terminate on the same decoding step.',
      relatedComparison: 'Tensor batching for efficiency versus independent logical sequence state.',
    }),
  ]),
  'sampling-strategies': Object.freeze([
    Object.freeze({
      id: 'sampling-top-p-variable-candidate-count',
      level: 'calculation',
      scenario: 'At one decode step the sorted token probabilities are [0.55, 0.25, 0.12, 0.05, 0.03]. Nucleus sampling uses top_p=0.80.',
      prompt: 'Which smallest candidate set reaches the requested cumulative probability mass?',
      choices: Object.freeze([
        'The first two tokens, because 0.55 + 0.25 = 0.80',
        'The first four tokens, because top_p=0.80 means keeping 80% of the token count',
        'Exactly one token, because top-p always degenerates to greedy selection when the top token exceeds 0.5',
      ]),
      answerIndex: 0,
      explanation: 'Top-p is based on cumulative probability mass, not a fixed number or percentage of vocabulary entries. The candidate count adapts to the shape of the distribution.',
      misconceptionTested: 'Top-p keeps a fixed proportion of vocabulary tokens rather than enough ranked tokens to reach probability mass p.',
      relatedComparison: 'Adaptive top-p candidate count versus fixed-count top-k filtering.',
    }),
    Object.freeze({
      id: 'sampling-temperature-ranking-vs-sharpness',
      level: 'mechanism',
      scenario: 'Three logits are [4, 2, 1]. The temperature is lowered from 1.0 to 0.5 before softmax, with no other filter.',
      prompt: 'What changes most directly?',
      choices: Object.freeze([
        'The probability distribution becomes sharper while the ranking of these logits stays the same',
        'The lowest-logit token becomes the highest-ranked token',
        'Temperature permanently changes the model weights and vocabulary',
      ]),
      answerIndex: 0,
      explanation: 'Dividing logits by a smaller positive temperature increases their scale differences before softmax. It sharpens the distribution without reversing the ordering of logits.',
      misconceptionTested: 'Temperature changes token rank arbitrarily rather than reshaping probability concentration.',
      relatedComparison: 'Probability sharpness control versus candidate truncation.',
    }),
    Object.freeze({
      id: 'sampling-combined-filter-order',
      level: 'diagnosis',
      scenario: 'Two serving implementations use the same model, prompt, temperature, top_k, and top_p values but occasionally produce different retained candidate sets before sampling. The only discovered difference is the order in which their logits processors apply truncation rules.',
      prompt: 'What should the team conclude?',
      choices: Object.freeze([
        'Filter composition/order is part of decoding semantics and can change the final eligible candidate set',
        'The two implementations must be equivalent because decoding controls commute mathematically in every case',
        'Any difference proves the underlying transformer weights changed during inference',
      ]),
      answerIndex: 0,
      explanation: 'Temperature scaling and candidate filters form a processing pipeline. Some operations do not commute, so implementations should define and test their order rather than treating the settings as an unordered bag of knobs.',
      misconceptionTested: 'Temperature, top-k, top-p, and other logits processors always produce identical results regardless of processing order.',
      relatedComparison: 'Documented logits-processing pipeline versus unordered decoding configuration.',
    }),
    Object.freeze({
      id: 'sampling-seed-reproducibility-boundary',
      level: 'diagnosis',
      scenario: 'A stochastic generation test fixes the prompt, model, decoding parameters, and random seed. It is reproducible on one execution path, but changing to a different backend kernel produces a different sample sequence even with the same seed.',
      prompt: 'Which statement is safest?',
      choices: Object.freeze([
        'A seed controls the random draws, but exact end-to-end reproducibility can still depend on deterministic execution and identical implementation details',
        'A fixed seed mathematically guarantees byte-identical output across every backend and hardware implementation',
        'Seeds affect model weights permanently, so a different backend necessarily retrains the model',
      ]),
      answerIndex: 0,
      explanation: 'Seeded stochastic decoding is repeatable only when the relevant computation and random-number-consumption path are also stable. Backend or numerical differences can change probabilities or draw order and therefore change sampled tokens.',
      misconceptionTested: 'Setting a random seed alone guarantees identical sampled text across all runtimes and hardware paths.',
      relatedComparison: 'Seed control versus full deterministic execution.',
    }),
    Object.freeze({
      id: 'sampling-overrandomized-degeneration',
      level: 'diagnosis',
      scenario: 'A factual assistant uses temperature=1.8 and top_p=1.0. Outputs vary widely and frequently drift into low-probability, irrelevant continuations even though the base model ranks a correct answer highly.',
      prompt: 'What is the first decoding-level diagnosis?',
      choices: Object.freeze([
        'The candidate distribution is too permissive for the task, so lower randomness or tighter filtering should be evaluated',
        'The only possible cause is missing training data because inference controls cannot affect coherence',
        'Increase temperature further so low-probability tail tokens become even more competitive',
      ]),
      answerIndex: 0,
      explanation: 'High temperature plus an unrestricted nucleus keeps a broad tail eligible. For a precision-oriented task, that can convert a good model distribution into unstable output selection.',
      misconceptionTested: 'Sampling settings cannot degrade a model that has good next-token probabilities.',
      relatedComparison: 'Model quality versus decoding-policy quality.',
    }),
    Object.freeze({
      id: 'sampling-overconstrained-repetition',
      level: 'diagnosis',
      scenario: 'A brainstorming assistant uses greedy decoding with effectively one surviving candidate at every step. It repeatedly produces the same generic opening across runs and prompts with similar structure.',
      prompt: 'Which change is most aligned with the observed decoding failure?',
      choices: Object.freeze([
        'Introduce controlled stochasticity or a broader candidate set and evaluate whether diversity improves without unacceptable quality loss',
        'Make the candidate set even narrower so the same local maximum is selected more reliably',
        'Remove all model probabilities and sample uniformly from the vocabulary',
      ]),
      answerIndex: 0,
      explanation: 'An over-constrained decoder can repeatedly choose safe local maxima. Controlled diversity can help brainstorming, while uniform random sampling would discard too much model signal.',
      misconceptionTested: 'More deterministic decoding is always better regardless of the product objective.',
      relatedComparison: 'Precision-oriented decoding versus diversity-oriented decoding.',
    }),
  ]),
});
