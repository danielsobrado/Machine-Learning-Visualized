export const P1_DAPO_REASONING_SCENARIOS_BY_LESSON = Object.freeze({
  'dapo-reasoning-rl': Object.freeze([
    Object.freeze({
      id: 'dapo-clip-higher-worked',
      level: 'calculation',
      relatedComparison: 'symmetric-ppo-upper-clip-vs-dapo-clip-higher',
      scenario: 'A positive-advantage token has policy ratio r=1.35 and advantage A=2.0. For this simplified comparison, use min(r*A, upperClip*A). Standard symmetric clipping has upperClip=1.20, while DAPO Clip-Higher uses upperClip=1.40. The lower clip bound remains unchanged.',
      prompt: 'What surrogate contribution survives under each upper clip?',
      choices: Object.freeze([
        'Standard: 2.40; Clip-Higher: 2.70, because ratio 1.35 is capped by 1.20 but remains below 1.40',
        'Standard: 2.70; Clip-Higher: 2.40, because a larger upper clip must reduce positive updates',
        'Both: 2.80, because the clip bound replaces the actual policy ratio whenever advantage is positive',
      ]),
      answerIndex: 0,
      explanation: 'The unclipped contribution is 1.35*2=2.70. Standard clipping limits it to 1.20*2=2.40. With upperClip=1.40, ratio 1.35 is not capped, so 2.70 remains. Clip-Higher gives successful traces more positive room without removing the lower-side guardrail.',
      misconceptionTested: 'Clip-Higher removes clipping entirely or loosens both sides of the ratio constraint equally.',
    }),
    Object.freeze({
      id: 'dapo-token-level-credit-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'sample-level-completion-block-vs-token-level-policy-terms',
      scenario: 'A reasoning completion is 1,800 generated tokens long and receives a useful nonzero group-relative advantage. The implementation reduces the entire response to one coarse sample-level policy term, and the dashboard reports very few token-gradient contributions despite many long useful traces.',
      prompt: 'Which DAPO mechanism most directly addresses this loss-granularity problem?',
      choices: Object.freeze([
        'Token-Level Policy Gradient Loss so generated-token policy terms contribute across the long completion',
        'Dynamic Sampling, because filtering groups automatically creates token-level gradient terms',
        'Overlong Reward Shaping, because a length penalty is the same thing as denser token credit assignment',
      ]),
      answerIndex: 0,
      explanation: 'Dynamic Sampling selects useful groups and overlong shaping changes reward near length limits. The described problem is coarse policy-loss aggregation across many generated decisions, which is what token-level aggregation targets.',
      misconceptionTested: 'Batch filtering or length shaping automatically fixes coarse policy-gradient credit across long responses.',
    }),
    Object.freeze({
      id: 'dapo-overlong-soft-margin-worked',
      level: 'calculation',
      relatedComparison: 'hard-length-cliff-vs-graded-overlong-shaping',
      scenario: 'Use a simplified linear overlong shaping rule for a correct answer: reward stays 1.0 through 80 tokens, then decreases linearly to 0.0 at the 100-token maximum. A correct completion has length 90. A separate hard rule would give full reward below 100 and zero at or above 100.',
      prompt: 'What shaped correctness reward does the 90-token answer receive under the stated linear rule, and why can this be preferable near the boundary?',
      choices: Object.freeze([
        '0.50; it gives graded concision pressure before the maximum instead of an abrupt reward cliff',
        '1.00; soft shaping cannot change reward until the hard maximum has already been crossed',
        '0.00; every answer inside the soft-margin region must be treated exactly like an over-limit answer',
      ]),
      answerIndex: 0,
      explanation: 'Ninety tokens is halfway from 80 to 100, so the linear reward is halfway from 1 to 0: 0.5. The point of a soft margin is to introduce gradual length pressure rather than a discontinuous jump at one token boundary.',
      misconceptionTested: 'Overlong shaping is just a renamed hard cutoff or rewards verbosity instead of applying graded concision pressure.',
    }),
    Object.freeze({
      id: 'dapo-entropy-collapse-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'reward-gain-vs-reasoning-diversity-health',
      scenario: 'Training reward is rising, but rollout entropy has fallen sharply and sibling completions for each prompt have become nearly identical. Many positive-advantage token ratios also hit the standard upper clip immediately.',
      prompt: 'What should be investigated before continuing to increase update pressure?',
      choices: Object.freeze([
        'Sampling diversity and clipping pressure, including whether Clip-Higher is appropriate, because the policy may be collapsing onto too few reasoning paths',
        'Only the final benchmark score, because rising reward proves exploration health is irrelevant',
        'The hard maximum response length only, because entropy collapse is caused solely by overlong answers',
      ]),
      answerIndex: 0,
      explanation: 'DAPO treats entropy as a training-health signal. Low diversity plus heavy positive clipping suggests useful alternative traces may be disappearing or capped too aggressively, so sampling and clip diagnostics should be reviewed together rather than blindly increasing pressure.',
      misconceptionTested: 'Rising reward is sufficient evidence that exploration remains healthy.',
    }),
    Object.freeze({
      id: 'dapo-bad-verifier-survives-dynamic-sampling',
      level: 'diagnosis',
      relatedComparison: 'useful-group-contrast-vs-valid-reward-semantics',
      scenario: 'A verifier gives high reward whenever the response contains the required XML tags, even when the mathematical answer is wrong. Dynamic Sampling successfully finds many mixed-reward groups under this verifier, and the effective-group count looks excellent.',
      prompt: 'Why is the run still unsafe to trust?',
      choices: Object.freeze([
        'Dynamic Sampling can select groups with reward contrast, but it cannot make a flawed verifier measure correctness; reward validity must be repaired first',
        'A high effective-group count guarantees the reward is aligned because mixed groups cannot occur under a bad verifier',
        'Clip-Higher will automatically convert format reward into mathematical correctness during the policy update',
      ]),
      answerIndex: 0,
      explanation: 'DAPO mechanics optimize the signal they are given. Filtering can improve contrast under that signal, but it does not validate what the signal means. A format-heavy verifier can therefore produce efficient optimization of the wrong behavior.',
      misconceptionTested: 'Dynamic Sampling or clipping can repair reward-model misspecification.',
    }),
    Object.freeze({
      id: 'dapo-dashboard-health-decision',
      level: 'decision',
      relatedComparison: 'mean-reward-vs-training-system-health',
      scenario: 'Two DAPO runs have the same mean reward of 0.82. Run A has 68% effective mixed groups, stable entropy, 18% clip fraction, 4% overlong rate, and stable held-out accuracy. Run B has 14% effective groups, collapsing entropy, 72% clip fraction, 31% overlong rate, and falling held-out accuracy.',
      prompt: 'Which conclusion is most defensible?',
      choices: Object.freeze([
        'Run A is healthier; equal mean reward does not override poor effective-group, entropy, clipping, length, and held-out signals in Run B',
        'The runs are equally healthy because mean reward is the only DAPO diagnostic that matters',
        'Run B is healthier because higher clip fraction and overlong rate prove the optimizer is learning more aggressively',
      ]),
      answerIndex: 0,
      explanation: 'DAPO is a system recipe with interacting diagnostics. Run B shows weak useful-batch signal, exploration collapse, excessive clipping, length-control problems, and worse external performance despite the same reward average.',
      misconceptionTested: 'A high or equal mean training reward is sufficient evidence that a DAPO run is healthy.',
    }),
  ]),
});
