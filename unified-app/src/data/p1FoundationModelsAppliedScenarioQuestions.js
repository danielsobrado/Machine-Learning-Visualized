export const P1_FOUNDATION_MODELS_APPLIED_SCENARIOS_BY_LESSON = Object.freeze({
  bert: Object.freeze([
    Object.freeze({
      id: 'bert-mlm-corruption-budget',
      level: 'calculation',
      scenario: 'A BERT pretraining batch contains 20,000 eligible token positions. Following the original masked-language-model corruption recipe, 15% of positions are selected as prediction targets. Of those selected positions, 80% are replaced by [MASK], 10% by a random token, and 10% are left unchanged while still contributing to the MLM loss.',
      prompt: 'How many positions are MLM targets, and how are those targets split across masked, random-token, and unchanged inputs?',
      choices: Object.freeze([
        '3,000 targets: 2,400 [MASK], 300 random-token, and 300 unchanged positions',
        '3,000 targets: all 3,000 must be replaced by [MASK] so the model cannot copy visible tokens',
        '17,000 targets: 13,600 [MASK], 1,700 random-token, and 1,700 unchanged positions',
      ]),
      answerIndex: 0,
      explanation: 'Fifteen percent of 20,000 is 3,000 prediction targets. The 80/10/10 corruption split gives 2,400 masked positions, 300 random replacements, and 300 unchanged positions. The unchanged and random cases reduce the pretrain-to-fine-tune mismatch created by an input stream containing [MASK] everywhere a target was selected.',
      misconceptionTested: 'Every token selected for BERT masked-language-model prediction must literally be replaced by the [MASK] token, so the number of MLM targets equals the number of visible [MASK] positions.',
      relatedComparison: 'BERT masked-language-model corruption versus treating masking as a simple delete-or-hide operation.',
    }),
    Object.freeze({
      id: 'bert-classifier-head-params',
      level: 'calculation',
      scenario: 'A BERT-base encoder produces a 768-dimensional pooled representation for each input. A five-class downstream classifier uses one dense layer from that representation directly to five logits, with one bias value per class. Ignore every parameter inside the pretrained encoder and count only this new task head.',
      prompt: 'How many trainable scalar parameters does the five-class classification head add?',
      choices: Object.freeze([
        '3,845 parameters: 768 × 5 weights plus 5 biases',
        '768 parameters because the pooled representation already contains all five class scores',
        '5,120 parameters because every class needs a separate 1,024-dimensional BERT representation',
      ]),
      answerIndex: 0,
      explanation: 'A linear classifier with input width 768 and output width 5 stores 768 × 5 = 3,840 weights. Adding five output biases gives 3,845 parameters. The pretrained encoder may contain many millions of parameters, but changing the number of task labels only changes this small output head unless the encoder itself is also modified.',
      misconceptionTested: 'Adding a BERT classification task requires changing the encoder hidden size or allocating a full new encoder representation for every output class.',
      relatedComparison: 'Shared pretrained BERT encoder parameters versus the much smaller task-specific classification head.',
    }),
  ]),
  'gpt2-comprehensive': Object.freeze([
    Object.freeze({
      id: 'gpt2-next-token-target-count',
      level: 'calculation',
      scenario: 'A GPT-2 causal-language-model training batch has 8 sequences, each containing 512 tokens with no padding. Training uses the standard one-token shift: the representation at position t predicts the token at position t+1. The last input position has no next token inside the sequence and therefore contributes no target.',
      prompt: 'How many next-token loss targets are evaluated across the whole batch?',
      choices: Object.freeze([
        '4,088 targets because each sequence contributes 511 next-token predictions and 8 × 511 = 4,088',
        '4,096 targets because every one of the 512 input positions predicts itself',
        '512 targets because causal language modeling computes only one loss value per sequence',
      ]),
      answerIndex: 0,
      explanation: 'Each 512-token sequence supplies 511 within-sequence next-token targets: positions 0 through 510 predict tokens 1 through 511. Eight sequences therefore contribute 8 × 511 = 4,088 token-level targets. Implementations often pass same-length input and label tensors, but the causal-LM loss performs this shift internally.',
      misconceptionTested: 'A causal language model predicts the token at the same position, or produces only one supervised target for an entire sequence.',
      relatedComparison: 'GPT-2 next-token target shifting versus same-position reconstruction objectives such as masked-token prediction.',
    }),
    Object.freeze({
      id: 'gpt2-context-quadratic-cost',
      level: 'calculation',
      scenario: 'A GPT-2-style decoder uses dense causal self-attention. An experiment increases usable context length from 1,024 tokens to 4,096 tokens while keeping batch size, number of layers, heads, and hidden width fixed. Consider only the number of query-key attention-score entries materialized per head and layer.',
      prompt: 'By what factor does the attention-score count grow when context length increases from 1,024 to 4,096?',
      choices: Object.freeze([
        '16×, because dense attention score count scales with sequence length squared and (4,096 / 1,024)² = 16',
        '4×, because every additional token adds only one new attention score regardless of earlier context',
        '64×, because context length, head count, and hidden width all quadruple together',
      ]),
      answerIndex: 0,
      explanation: 'Dense self-attention compares each query position with each permitted key position, so its score matrix is quadratic in context length. Quadrupling L from 1,024 to 4,096 multiplies L² by 4² = 16. The causal triangular mask reduces the constant factor relative to a fully bidirectional matrix, but it does not change the O(L²) scaling law.',
      misconceptionTested: 'Extending GPT-2 context from L to 4L increases dense attention memory and score work only fourfold because there are only four times as many tokens.',
      relatedComparison: 'Dense GPT-style causal attention scaling versus linear-growth components such as token embeddings or KV-cache length per generated token.',
    }),
  ]),
  'multimodal-llm': Object.freeze([
    Object.freeze({
      id: 'multimodal-projector-params',
      level: 'calculation',
      scenario: 'A multimodal LLM uses a frozen vision encoder whose per-token features have width 1,024 and a language model whose hidden width is 4,096. The simplest connector is one learned linear projection from every vision feature into the language-model hidden space, including one bias per output dimension.',
      prompt: 'How many trainable scalar parameters are in this single linear vision-to-language projector?',
      choices: Object.freeze([
        '4,198,400 parameters: 1,024 × 4,096 weights plus 4,096 biases',
        '5,120 parameters because the projector only stores the two hidden widths and their biases',
        '16,777,216 parameters because every projected vision token needs its own independent 4,096 × 4,096 matrix',
      ]),
      answerIndex: 0,
      explanation: 'A dense projection from 1,024 inputs to 4,096 outputs contains 1,024 × 4,096 = 4,194,304 weights. Adding 4,096 output biases gives 4,198,400 parameters. The same projector is reused for every visual token, so parameter count does not multiply by the number of image patches.',
      misconceptionTested: 'Multimodal projection parameters scale with the number of image tokens because every visual patch needs a separate learned projection matrix.',
      relatedComparison: 'Shared vision-to-language projector parameters versus runtime compute that repeats the same projection over many visual tokens.',
    }),
    Object.freeze({
      id: 'multimodal-resolution-token-cost',
      level: 'calculation',
      scenario: 'A vision transformer feeding a multimodal LLM uses non-overlapping 14 × 14 pixel patches and no extra class token. One configuration accepts a 224 × 224 image; another accepts 448 × 448. Assume the downstream vision transformer uses dense self-attention over all patch tokens.',
      prompt: 'How do the visual-token count and dense vision-attention score count change when resolution doubles from 224 to 448 pixels per side?',
      choices: Object.freeze([
        'Tokens grow from 256 to 1,024 (4×), so dense attention-score entries grow by about 16×',
        'Tokens grow from 256 to 512 (2×), so dense attention-score entries grow by about 2×',
        'Tokens stay at 256 because patch size is fixed, so attention cost is unchanged',
      ]),
      answerIndex: 0,
      explanation: 'At 224 pixels, each side has 224 / 14 = 16 patches, so there are 16² = 256 visual tokens. At 448 pixels, each side has 32 patches, giving 32² = 1,024 tokens: four times as many. Dense attention scales quadratically in token count, so the attention-score matrix grows by roughly 4² = 16 times.',
      misconceptionTested: 'Doubling image resolution doubles visual token count and therefore only doubles transformer attention cost.',
      relatedComparison: 'Image-area growth in patch tokenization versus quadratic attention growth after those patches become transformer tokens.',
    }),
  ]),
});
