export const P1_CLASSIFIER_FREE_GUIDANCE_SCENARIOS_BY_LESSON = Object.freeze({
  'classifier-free-guidance': Object.freeze([
    Object.freeze({
      id: 'cfg-conditioning-dropout-training-contract',
      level: 'diagnosis',
      scenario: 'A text-conditioned diffusion model was trained with the prompt present on every training example. At inference, the team tries classifier-free guidance by running one prompted pass and one blank-prompt pass through the same denoiser. The blank-prompt prediction is unstable and produces implausible guidance directions.',
      prompt: 'What training change most directly fixes the missing CFG capability?',
      choices: Object.freeze([
        'Randomly drop conditioning on a fraction of training examples so the same denoiser learns both conditional and unconditional behavior',
        'Keep conditioning on every training example and increase guidance scale until the blank-prompt branch becomes accurate',
        'Train only the blank-prompt branch and remove conditional examples entirely',
      ]),
      answerIndex: 0,
      explanation: 'Classifier-free guidance relies on the same denoiser being meaningful with and without conditioning. Conditioning dropout during training creates examples for the unconditional branch; merely blanking a prompt at inference does not teach behavior the model never learned.',
      misconceptionTested: 'Any conditional diffusion model automatically supports a reliable unconditional CFG branch at inference.',
      relatedComparison: 'Conditional-only training versus conditioning-dropout training for CFG.',
    }),
    Object.freeze({
      id: 'cfg-scale-quality-operating-point',
      level: 'decision',
      scenario: 'A fixed sampler and prompt set is evaluated at three CFG scales. Scale 3 gives prompt-adherence score 0.76 with 2% artifact rate. Scale 7 gives 0.89 adherence with 6% artifacts. Scale 12 gives 0.93 adherence with 19% artifacts. The product requires adherence of at least 0.85 and artifact rate no higher than 8%.',
      prompt: 'Which tested guidance scale is the strongest valid operating point?',
      choices: Object.freeze([
        'Scale 7, because it satisfies both the adherence floor and artifact ceiling',
        'Scale 3, because the lowest artifact rate should always dominate prompt adherence requirements',
        'Scale 12, because maximum prompt adherence should always dominate visible quality regressions',
      ]),
      answerIndex: 0,
      explanation: 'Scale 3 misses the explicit adherence target and scale 12 violates the artifact ceiling. Scale 7 satisfies both. CFG scale is a quality/alignment operating-point choice rather than a value to maximize blindly.',
      misconceptionTested: 'The highest CFG scale is automatically the best setting because prompt adherence is the only metric that matters.',
      relatedComparison: 'Prompt adherence versus artifact/diversity cost as guidance scale increases.',
    }),
    Object.freeze({
      id: 'cfg-overshoot-rescale-diagnosis',
      level: 'diagnosis',
      scenario: 'At guidance scale 10, most prompts work, but a subset produces blown highlights and distorted textures. Logging shows the norm of conditional-minus-unconditional prediction is much larger for those prompts than for normal cases.',
      prompt: 'What is the best first diagnosis and mitigation direction?',
      choices: Object.freeze([
        'The scaled guidance direction is overshooting; reduce scale or apply a validated guidance-rescaling/clipping strategy and re-evaluate quality',
        'The issue proves the unconditional branch should be deleted because larger guidance vectors are always desirable',
        'Increase scale further so the sampler crosses the unstable region faster',
      ]),
      answerIndex: 0,
      explanation: 'CFG multiplies the conditional-minus-unconditional direction. When that direction is unusually large, a fixed high scale can push the denoising state outside a reliable region. Lower scale or controlled rescaling targets the mechanism directly.',
      misconceptionTested: 'Guidance scale has the same effective strength for every prompt regardless of the magnitude of the guidance direction.',
      relatedComparison: 'Raw guidance magnitude versus rescaled or lower-strength guidance.',
    }),
    Object.freeze({
      id: 'cfg-sampler-step-interaction-debugging',
      level: 'diagnosis',
      scenario: 'A pipeline changes from a 30-step sampler to an 8-step sampler while keeping CFG scale 11. Prompt match stays high, but geometry artifacts increase sharply. The denoiser checkpoint and prompts are unchanged.',
      prompt: 'What should be tested first?',
      choices: Object.freeze([
        'Re-tune guidance jointly with the shorter step schedule because fewer, larger guided updates can amplify overshoot',
        'Assume CFG cannot interact with sampler step count because guidance is computed before the sampler update',
        'Increase guidance scale again because fewer steps always require proportionally stronger prompt steering',
      ]),
      answerIndex: 0,
      explanation: 'The guided prediction is consumed by the sampler update, so scale and update schedule interact. Coarser trajectories can make each guided move more aggressive; sampler and CFG settings should be evaluated together.',
      misconceptionTested: 'A CFG scale calibrated for one sampler and step schedule transfers unchanged to every other sampling trajectory.',
      relatedComparison: 'Guidance strength versus sampler integration step size.',
    }),
    Object.freeze({
      id: 'cfg-negative-baseline-semantics',
      level: 'mechanism',
      scenario: 'A text-to-image pipeline places the embedding of a negative prompt in the branch normally used as the unconditional baseline. The positive prompt branch is unchanged.',
      prompt: 'What changes mathematically in the CFG update?',
      choices: Object.freeze([
        'The baseline changes, so the amplified direction becomes positive-conditioned prediction minus the negative-prompt prediction',
        'Nothing changes because negative prompts are applied only after the image is fully generated',
        'The negative prompt simply subtracts tokens from the positive prompt before tokenization and never affects denoiser predictions',
      ]),
      answerIndex: 0,
      explanation: 'CFG is defined relative to its baseline branch. Replacing blank conditioning with negative conditioning changes that baseline and therefore changes the direction being amplified during every guided denoising update.',
      misconceptionTested: 'Negative prompts are an unrelated post-processing filter rather than a change to the conditioning baseline used by many CFG pipelines.',
      relatedComparison: 'Blank unconditional baseline versus negative-prompt conditioning baseline.',
    }),
  ]),
});
