export const P1_NLP_TRANSFORMER_SCENARIOS_BY_LESSON = Object.freeze({
  'bag-of-words': [
    {
      id: 'bow-word-order-loss',
      level: 'diagnosis',
      scenario: 'The sentences "dog bites man" and "man bites dog" contain the same words with the same counts.',
      prompt: 'Why can a basic bag-of-words representation map them to the same vector?',
      choices: ['It keeps token counts but discards word order', 'It always converts every sentence to one scalar', 'It uses causal attention that hides later words'],
      answerIndex: 0,
      explanation: 'A basic bag-of-words vector records vocabulary counts or presence and therefore loses sequence order unless extra n-gram features are added.',
      misconceptionTested: 'Bag-of-words preserves sentence syntax because it preserves vocabulary.',
    },
    {
      id: 'bow-vocabulary-sparsity',
      level: 'application',
      relatedComparison: 'bow-vs-subword-embedding',
      scenario: 'A corpus has millions of distinct surface forms, many appearing once, and the downstream model stores one coordinate per vocabulary item.',
      prompt: 'What representation problem should you expect?',
      choices: ['A very high-dimensional sparse feature space with weak handling of unseen forms', 'A fixed dense semantic vector independent of vocabulary size', 'Automatic character-level generalization'],
      answerIndex: 0,
      explanation: 'Bag-of-words dimensionality grows with vocabulary size and unseen words have no learned coordinate unless the vocabulary or representation strategy changes.',
      misconceptionTested: 'Bag-of-words automatically handles arbitrary unseen words.',
    },
  ],
  word2vec: [
    {
      id: 'w2v-skipgram-cbow-choice',
      level: 'comparison',
      relatedComparison: 'skipgram-vs-cbow',
      scenario: 'One training objective predicts surrounding context words from a center word, while another predicts the center word from surrounding context.',
      prompt: 'Which objective describes skip-gram?',
      choices: ['Predict context words from the center word', 'Predict the center word from the average context representation', 'Predict the next byte autoregressively'],
      answerIndex: 0,
      explanation: 'Skip-gram uses the center token to predict nearby context tokens, while CBOW combines context to predict the center token.',
      misconceptionTested: 'Skip-gram and CBOW reverse no roles and are the same objective.',
    },
    {
      id: 'w2v-negative-sampling',
      level: 'mechanism',
      scenario: 'A vocabulary has one million words, making a full softmax update expensive for every local context pair.',
      prompt: 'What does negative sampling change?',
      choices: ['It trains against a small set of sampled non-context words instead of normalizing over the full vocabulary each step', 'It removes all positive word-context pairs', 'It makes embeddings one-hot'],
      answerIndex: 0,
      explanation: 'Negative sampling replaces the expensive full-vocabulary objective with binary discrimination between observed pairs and a small sampled set of negatives.',
      misconceptionTested: 'Negative sampling means discarding rare positive examples.',
    },
  ],
  fasttext: [
    {
      id: 'fasttext-oov-subword',
      level: 'application',
      relatedComparison: 'word2vec-vs-fasttext-oov',
      scenario: 'The model encounters an unseen inflected word whose character n-grams were seen inside many training words.',
      prompt: 'Why can FastText represent it better than a pure whole-word lookup?',
      choices: ['It composes information from subword character n-grams', 'It memorizes every possible word in advance', 'It ignores spelling and uses only sentence position'],
      answerIndex: 0,
      explanation: 'FastText represents words using subword n-gram vectors, allowing morphology and shared character pieces to contribute even when the complete word was unseen.',
      misconceptionTested: 'Subword embeddings provide no benefit for unseen or morphologically related words.',
    },
  ],
  embeddings: [
    {
      id: 'embedding-anisotropy',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { pairwiseCosine: 'most pairs between 0.72 and 0.90', expectedDiversity: 'high', observedCloud: 'narrow cone' },
      scenario: 'Visual state: almost every unrelated embedding pair has a high cosine similarity and vectors occupy a narrow cone instead of spreading broadly.',
      prompt: 'What geometry issue should be investigated?',
      choices: ['Embedding anisotropy or a dominant common direction', 'Perfect semantic separation', 'A requirement that every embedding be orthogonal'],
      answerIndex: 0,
      explanation: 'When most vectors share a strong common direction, cosine similarities can bunch together and become less discriminative for semantic comparison.',
      misconceptionTested: 'High cosine values always imply strong task-relevant semantic similarity.',
    },
  ],
  'cosine-similarity': [
    {
      id: 'cosine-sparse-vs-dense',
      level: 'comparison',
      relatedComparison: 'sparse-lexical-vs-dense-semantic-cosine',
      scenario: 'One system computes cosine on sparse TF-IDF vectors and another computes cosine on dense neural embeddings.',
      prompt: 'Why can the same cosine formula have different meaning in the two systems?',
      choices: ['The vector coordinates encode different representations, so angular similarity reflects different notions of relatedness', 'Cosine always measures identical semantics regardless of representation', 'Sparse vectors cannot use cosine similarity'],
      answerIndex: 0,
      explanation: 'Cosine only compares vector direction; the interpretation comes from how coordinates were constructed, such as lexical weights versus learned semantic features.',
      misconceptionTested: 'Cosine similarity defines semantics independently of the embedding representation.',
    },
    {
      id: 'cosine-relevance-audit',
      level: 'diagnosis',
      scenario: 'A retrieval result has cosine similarity 0.94 but is from the wrong product category and gives an unsafe answer for the user query.',
      prompt: 'What should the system conclude?',
      choices: ['High vector similarity is evidence, not proof of task relevance or safety', 'Cosine above 0.9 guarantees the result should be used', 'The query should be ignored because embeddings are numeric'],
      answerIndex: 0,
      explanation: 'Embedding similarity is a retrieval signal; relevance, authority, metadata constraints, and downstream task requirements still need validation.',
      misconceptionTested: 'A high cosine score is equivalent to ground-truth relevance.',
    },
  ],
  tokenization: [
    {
      id: 'tokenization-multilingual-fragmentation',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { englishCharsPerToken: 4.1, targetLanguageCharsPerToken: 1.3, sameMeaningTokenRatio: '3.2x more tokens' },
      scenario: 'Visual state: the same-length semantic content needs about 3.2 times more tokens in one language than in English.',
      prompt: 'What practical consequence should be considered?',
      choices: ['Higher token cost and faster context-window consumption for that language', 'The model automatically becomes more accurate in that language', 'Token count has no effect on context or serving cost'],
      answerIndex: 0,
      explanation: 'Tokenizer fragmentation can make equivalent content consume many more tokens, affecting context capacity, latency, and cost across languages.',
      misconceptionTested: 'Tokenizers have uniform efficiency across languages and scripts.',
    },
  ],
  bert: [
    {
      id: 'bert-encoder-objective',
      level: 'comparison',
      relatedComparison: 'bert-vs-gpt-objective',
      scenario: 'A model reads tokens on both sides of a masked position during pretraining and learns to reconstruct the hidden token.',
      prompt: 'Which model/objective family does this describe?',
      choices: ['BERT-style encoder pretraining with masked language modeling', 'GPT-style strictly causal next-token prediction', 'An autoregressive decoder that can attend only leftward'],
      answerIndex: 0,
      explanation: 'Masked language modeling allows bidirectional encoder context around masked tokens, unlike causal decoder pretraining that predicts the next token from the left context.',
      misconceptionTested: 'BERT and GPT use the same attention mask and pretraining objective.',
    },
  ],
  'gpt-2': [
    {
      id: 'gpt2-causal-mask',
      level: 'mechanism',
      scenario: 'During training, token position t predicts the next token while all future positions are present in the batch tensor.',
      prompt: 'What prevents information leakage from future tokens?',
      choices: ['A causal attention mask that blocks attention to positions after t', 'Removing positional information entirely', 'Using bidirectional attention and hoping the loss ignores future tokens'],
      answerIndex: 0,
      explanation: 'Decoder-only language modeling uses a triangular causal mask so each position can attend only to itself and earlier positions even though the whole sequence is processed in parallel during training.',
      misconceptionTested: 'Parallel training means GPT can read future tokens when predicting the next token.',
    },
  ],
  'attention-mechanism': [
    {
      id: 'attention-qkv-role',
      level: 'mechanism',
      relatedComparison: 'query-key-value-roles',
      scenario: 'For one token, a query vector is compared with key vectors from candidate source positions, producing scores that weight their value vectors.',
      prompt: 'Which description is correct?',
      choices: ['Queries express what to look for, keys support matching, and values carry the information mixed into the output', 'Values determine which keys exist before scoring', 'Queries are cached outputs from the final classifier only'],
      answerIndex: 0,
      explanation: 'Attention scores come from query-key compatibility, while the normalized scores weight value vectors to create the attended representation.',
      misconceptionTested: 'Queries, keys, and values are interchangeable labels with identical roles.',
    },
  ],
  'self-attention': [
    {
      id: 'self-attention-quadratic-pairs',
      level: 'mechanism',
      kind: 'visual-state',
      visualState: { sequenceLength: 4096, attentionPairs: 16777216, doubledLengthPairs: 67108864 },
      scenario: 'Visual state: a dense self-attention layer at length 4096 considers about 16.8 million token pairs; doubling length produces about 67.1 million pairs.',
      prompt: 'What scaling behavior is visible?',
      choices: ['Dense attention-pair count grows quadratically with sequence length', 'Attention-pair count grows linearly with sequence length', 'Doubling context reduces pair count by half'],
      answerIndex: 0,
      explanation: 'Dense self-attention compares each query position with every key position, so pair count scales as n squared and quadruples when sequence length doubles.',
      misconceptionTested: 'Dense self-attention memory/computation grows only linearly with context length.',
    },
  ],
  'attention-masks': [
    {
      id: 'mask-causal-vs-padding',
      level: 'comparison',
      relatedComparison: 'causal-vs-padding-mask',
      scenario: 'A batch contains variable-length sequences padded to the same tensor width, and a decoder must also prevent tokens from reading future positions.',
      prompt: 'Which masking requirements are needed?',
      choices: ['A padding mask for invalid padded positions and a causal mask for future-token positions', 'Only a causal mask because padding is always meaningful content', 'Only a padding mask because decoders may read future tokens during generation'],
      answerIndex: 0,
      explanation: 'Padding and causality solve different problems: one hides nonexistent padded tokens and the other enforces autoregressive information flow.',
      misconceptionTested: 'All attention masks have the same purpose.',
    },
  ],
  'positional-encoding': [
    {
      id: 'position-absolute-vs-relative',
      level: 'comparison',
      relatedComparison: 'absolute-vs-relative-position',
      scenario: 'Two token pairs have the same relative separation but appear at different absolute locations in two sequences.',
      prompt: 'Which positional representation most directly preserves the same pairwise distance relationship?',
      choices: ['A relative-position representation', 'A one-hot class label', 'A bag-of-words count with no position'],
      answerIndex: 0,
      explanation: 'Relative position methods encode relationships such as token distance directly, while absolute embeddings attach position identities to individual locations.',
      misconceptionTested: 'Absolute and relative position encodings represent sequence relationships identically.',
    },
  ],
  rope: [
    {
      id: 'rope-relative-geometry',
      level: 'mechanism',
      scenario: 'RoPE rotates query and key components by angles determined by token position.',
      prompt: 'Why does the resulting dot product encode relative-position information?',
      choices: ['The interaction between two rotated vectors depends on the difference between their position angles', 'RoPE stores the full absolute sequence as a separate value vector', 'RoPE removes position from the attention score'],
      answerIndex: 0,
      explanation: 'Rotary embeddings apply position-dependent rotations whose query-key interaction can be expressed in terms of relative angular differences between positions.',
      misconceptionTested: 'RoPE only adds an arbitrary absolute position ID with no relative effect.',
    },
    {
      id: 'rope-context-extrapolation',
      level: 'diagnosis',
      scenario: 'A model trained mostly up to 8K tokens is run at 128K tokens without any long-context adaptation, and quality degrades sharply despite the rotary formula being defined.',
      prompt: 'What should you conclude?',
      choices: ['A mathematically defined positional encoding does not guarantee reliable extrapolation far beyond training context', 'RoPE guarantees unchanged model quality at arbitrary length', 'Context length depends only on tokenizer vocabulary size'],
      answerIndex: 0,
      explanation: 'Long-context reliability depends on training distribution, scaling strategy, attention behavior, and implementation; positional formulas alone do not guarantee effective extrapolation.',
      misconceptionTested: 'RoPE by itself guarantees unlimited context generalization.',
    },
  ],
  'residual-stream': [
    {
      id: 'residual-add-not-replace',
      level: 'mechanism',
      scenario: 'A transformer block computes a transformation F(x) and sends x + F(x) to the next stage.',
      prompt: 'What is the residual-stream role of x?',
      choices: ['The prior representation remains available through addition while the block contributes an update', 'The prior representation is discarded and fully replaced by F(x)', 'x is used only as a label for the loss function'],
      answerIndex: 0,
      explanation: 'Residual connections preserve an identity path and add a learned update, allowing information and gradients to flow through depth without every block replacing the full representation.',
      misconceptionTested: 'Each transformer block overwrites the residual representation from scratch.',
    },
  ],
  transformer: [
    {
      id: 'transformer-tensor-flow',
      level: 'application',
      relatedComparison: 'embedding-attention-mlp-logits-flow',
      scenario: 'A decoder-only transformer processes token IDs into embeddings, applies repeated attention and MLP blocks, normalizes the final hidden states, and projects them to vocabulary logits.',
      prompt: 'Which ordering is correct?',
      choices: ['Token representation -> transformer blocks -> final hidden representation -> vocabulary projection/logits', 'Vocabulary logits -> tokenizer -> attention -> raw token IDs', 'Loss function -> token IDs -> embedding table update without a forward pass'],
      answerIndex: 0,
      explanation: 'The forward path maps discrete tokens into hidden representations, repeatedly transforms them, and only then projects hidden states into scores over the vocabulary.',
      misconceptionTested: 'Vocabulary logits are the input representation consumed by the first transformer block.',
    },
  ],
  'transformer-architecture-families': [
    {
      id: 'transformer-family-task-choice',
      level: 'comparison',
      relatedComparison: 'encoder-decoder-family-choice',
      scenario: 'One task requires bidirectional representation of a fixed input for classification; another requires free-form autoregressive text generation.',
      prompt: 'Which architecture pairing is the natural starting point?',
      choices: ['Encoder-style for bidirectional representation; decoder-only for autoregressive generation', 'Decoder-only for every task because encoders cannot process text', 'Encoder-only for free-form next-token generation with unrestricted future attention'],
      answerIndex: 0,
      explanation: 'Encoder attention naturally builds bidirectional input representations, while decoder-only causal attention directly supports autoregressive generation.',
      misconceptionTested: 'Transformer architecture families differ only in parameter count, not information flow.',
    },
  ],
  'llm-training-objectives': [
    {
      id: 'objective-causal-vs-mlm',
      level: 'comparison',
      relatedComparison: 'causal-lm-vs-mlm-vs-seq2seq',
      scenario: 'A model should learn open-ended generation where each next token is produced from the prefix available so far.',
      prompt: 'Which pretraining objective aligns most directly with that inference process?',
      choices: ['Causal next-token language modeling', 'Masked-token reconstruction using both left and right context only', 'K-means clustering over token IDs'],
      answerIndex: 0,
      explanation: 'Causal language modeling trains the same left-to-right factorization used during autoregressive generation, predicting each next token from its prefix.',
      misconceptionTested: 'Masked language modeling and causal next-token prediction train identical information flow.',
    },
  ],
  'transformer-token-generation': [
    {
      id: 'generation-logits-softmax-sample',
      level: 'mechanism',
      relatedComparison: 'logits-probabilities-sampling',
      scenario: 'The model produces one logit per vocabulary token for the next position.',
      prompt: 'What happens before a stochastic next token is chosen?',
      choices: ['Logits are transformed into a probability distribution, then a decoding rule samples or selects from it', 'The largest embedding vector is appended directly with no probability step', 'The tokenizer randomly changes the vocabulary'],
      answerIndex: 0,
      explanation: 'Generation turns vocabulary logits into probabilities, often after temperature or filtering, and the decoding strategy then chooses the next token.',
      misconceptionTested: 'The model emits a final text token directly without a probability/decoding step.',
    },
  ],
  'kv-cache': [
    {
      id: 'kv-cache-context-scaling',
      level: 'mechanism',
      kind: 'visual-state',
      visualState: { contextTokens: 32768, layers: 32, kvHeads: 8, headDim: 128, precisionBytes: 2, behavior: 'memory grows linearly with cached tokens' },
      scenario: 'Visual state: layer count, KV heads, head dimension, and precision stay fixed while context length doubles.',
      prompt: 'How should KV-cache memory change?',
      choices: ['It should approximately double because one more K/V state is cached per additional token per layer', 'It should stay constant because model parameters did not change', 'It should quadruple solely because attention pair count is quadratic'],
      answerIndex: 0,
      explanation: 'KV cache stores per-token key and value states, so cache memory grows roughly linearly with cached sequence length when model dimensions and precision are fixed.',
      misconceptionTested: 'KV-cache memory scales quadratically with context just because dense attention compute has a quadratic training-time pair structure.',
    },
    {
      id: 'kv-prefill-vs-decode',
      level: 'comparison',
      relatedComparison: 'prefill-vs-decode',
      scenario: 'A long prompt is processed once, then tokens are generated one at a time while previous key/value states are reused.',
      prompt: 'Which phase is the one-time parallel prompt processing?',
      choices: ['Prefill', 'Autoregressive decode step', 'Tokenizer training'],
      answerIndex: 0,
      explanation: 'Prefill processes the input prompt and builds cache state, while decode repeatedly generates individual new tokens using the accumulated cache.',
      misconceptionTested: 'Prefill and decode have identical execution patterns and bottlenecks.',
    },
  ],
  'grouped-query-attention': [
    {
      id: 'gqa-cache-quality-tradeoff',
      level: 'comparison',
      relatedComparison: 'mha-vs-gqa-vs-mqa',
      scenario: 'A model keeps many query heads but reduces the number of key/value heads shared among query groups.',
      prompt: 'What trade-off is GQA targeting?',
      choices: ['Lower KV-cache memory/bandwidth while retaining more head diversity than a single shared MQA K/V head', 'Higher KV-cache width than standard MHA', 'Removing attention scores entirely'],
      answerIndex: 0,
      explanation: 'GQA shares K/V representations across groups of query heads, reducing cache width while sitting between full MHA independence and MQA-style maximal sharing.',
      misconceptionTested: 'GQA changes only training loss and has no inference-memory effect.',
    },
  ],
  'multi-head-latent-attention': [
    {
      id: 'mla-cache-vs-gqa',
      level: 'comparison',
      relatedComparison: 'gqa-vs-mla-cache',
      scenario: 'Two attention designs have similar cache budgets. One shares K/V heads directly; the other caches a learned latent bottleneck and reconstructs richer effective head behavior through projections.',
      prompt: 'Which distinction best describes MLA relative to GQA?',
      choices: ['MLA changes the cached representation through latent compression rather than only sharing explicit K/V heads', 'MLA is simply another name for MQA', 'GQA always stores fewer numbers than any MLA configuration'],
      answerIndex: 0,
      explanation: 'MLA uses a compressed latent cache and learned projection structure, whereas GQA reduces cache width through explicit sharing of K/V heads among query groups.',
      misconceptionTested: 'All KV-cache compression methods are equivalent forms of head sharing.',
    },
  ],
  lstm: [
    {
      id: 'lstm-cell-gates',
      level: 'mechanism',
      scenario: 'An LSTM receives a new input while carrying a previous cell state. The forget gate is near 1 and input gate is near 0.',
      prompt: 'What should happen to the cell memory in that step?',
      choices: ['Most previous cell memory is retained while little new candidate information is written', 'The previous cell memory is erased and replaced completely', 'The output vocabulary is normalized'],
      answerIndex: 0,
      explanation: 'A high forget gate preserves prior cell state, while a low input gate suppresses how much new candidate state is written into memory.',
      misconceptionTested: 'LSTM gates all perform the same operation on memory.',
    },
  ],
});

export function getP1NlpTransformerScenariosForLesson(lessonId) {
  return P1_NLP_TRANSFORMER_SCENARIOS_BY_LESSON[lessonId] || [];
}
