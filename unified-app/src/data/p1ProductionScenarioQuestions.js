export const P1_PRODUCTION_SCENARIOS_BY_LESSON = Object.freeze({
  'efficient-inference-compression-track': [
    {
      id: 'inference-weight-memory-quantization',
      level: 'mechanism',
      kind: 'visual-state',
      visualState: { parametersBillions: 7, fp16WeightMemoryGB: 14, int8WeightMemoryGB: 7, note: 'weights only' },
      scenario: 'Visual state: a 7B-parameter model needs roughly 14 GB for FP16 weights and roughly 7 GB for INT8 weights before runtime overheads.',
      prompt: 'What does the comparison show?',
      choices: ['Reducing bits per weight can roughly reduce weight-memory footprint proportionally, while other runtime memory still remains', 'INT8 guarantees total serving memory is exactly 7 GB', 'Quantization removes KV-cache and activation memory'],
      answerIndex: 0,
      explanation: 'Weight quantization reduces parameter storage, but total serving memory also includes KV cache, activations, temporary buffers, framework overhead, and batching state.',
      misconceptionTested: 'Model weight size is identical to total inference memory.',
    },
    {
      id: 'inference-batching-latency-throughput',
      level: 'comparison',
      relatedComparison: 'batching-throughput-vs-latency',
      scenario: 'Increasing dynamic batch size raises GPU utilization and tokens per second, but requests wait longer in the queue and P99 latency breaches the SLO.',
      prompt: 'What operating decision is required?',
      choices: ['Choose a batching policy that balances throughput against latency/SLO constraints', 'Always maximize batch size because throughput is the only serving metric', 'Disable latency measurement once GPU utilization rises'],
      answerIndex: 0,
      explanation: 'Batching can improve hardware efficiency and throughput while increasing queueing delay, so the serving policy must optimize against both throughput and latency objectives.',
      misconceptionTested: 'Higher batching throughput automatically means better user-facing serving performance.',
    },
    {
      id: 'inference-compression-quality-risk',
      level: 'diagnosis',
      relatedComparison: 'quantization-pruning-distillation',
      scenario: 'A compressed model is 40% faster, but accuracy drops mainly on rare long-context queries while average benchmark score changes little.',
      prompt: 'What should release evaluation do?',
      choices: ['Measure quality by representative slices and failure modes, not only the aggregate score', 'Ship because average quality barely changed', 'Assume compression errors are uniformly distributed across workloads'],
      answerIndex: 0,
      explanation: 'Compression can affect difficult or rare slices disproportionately, so latency gains should be evaluated alongside slice-level quality and operational risk.',
      misconceptionTested: 'A small aggregate quality change proves compression is safe for every workload.',
    },
  ],
  'efficient-llm-serving': [
    {
      id: 'serving-kv-cache-batch-pressure',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { concurrentRequests: 32, averageContext: 64000, kvCacheUsage: '88% of GPU memory', weightMemory: 'stable', oomRisk: 'high' },
      scenario: 'Visual state: model weight memory is unchanged, but 32 concurrent long-context requests push KV-cache usage to 88% of GPU memory.',
      prompt: 'What resource is driving the new memory pressure?',
      choices: ['Per-request KV-cache state growing with context and concurrency', 'The static model weights suddenly becoming larger', 'Tokenizer vocabulary changing on every request'],
      answerIndex: 0,
      explanation: 'Long contexts and concurrent sequences multiply the amount of cached key/value state even when model parameters remain fixed, often making KV memory the serving bottleneck.',
      misconceptionTested: 'Inference memory is determined only by static model weights.',
    },
    {
      id: 'serving-prefill-decode-slo',
      level: 'comparison',
      relatedComparison: 'ttft-vs-intertoken-latency',
      scenario: 'A chat application has slow time-to-first-token on 100K-token prompts, but once generation starts each next token arrives quickly.',
      prompt: 'Which serving phase should be optimized first?',
      choices: ['Prompt prefill because the delay occurs before the first generated token', 'Decode-only kernels because inter-token latency is already low', 'Tokenizer training because vocabulary size determines all latency'],
      answerIndex: 0,
      explanation: 'Long prompt processing dominates time-to-first-token during prefill, while decode performance governs the cadence of subsequent generated tokens.',
      misconceptionTested: 'All LLM serving latency comes from the autoregressive decode loop.',
    },
  ],
  'time-series-forecasting-track': [
    {
      id: 'ts-prediction-interval-coverage',
      level: 'application',
      kind: 'visual-state',
      visualState: { nominalInterval: '90%', observedCoverage: '63%', intervalWidth: 'narrow', horizon: 14 },
      scenario: 'Visual state: a forecast advertises a 90% prediction interval, but only 63% of held-out outcomes fall inside it at horizon 14.',
      prompt: 'What should be diagnosed?',
      choices: ['The predictive uncertainty is under-covered or miscalibrated at that horizon', 'The point forecast must be exactly unbiased', 'A 90% interval only needs to contain 50% of outcomes'],
      answerIndex: 0,
      explanation: 'Prediction intervals should be checked for empirical coverage on held-out data, ideally by horizon and slice; 63% coverage for a nominal 90% interval is far too narrow or miscalibrated.',
      misconceptionTested: 'A model-provided uncertainty interval is valid without empirical coverage checks.',
    },
  ],
  'model-monitoring': [
    {
      id: 'monitoring-delayed-labels-proxy',
      level: 'application',
      relatedComparison: 'proxy-vs-final-outcome-monitoring',
      scenario: 'True fraud labels arrive 45 days after predictions, but feature distributions, score distributions, manual-review rates, and serving health are available immediately.',
      prompt: 'How should monitoring work before labels arrive?',
      choices: ['Track early proxy/data/serving signals separately while waiting to confirm final quality with delayed labels', 'Declare model quality unknowable and monitor nothing for 45 days', 'Treat any proxy metric as a guaranteed replacement for final labels'],
      answerIndex: 0,
      explanation: 'Delayed-label systems need layered monitoring: immediate operational and proxy signals can detect risk early, but they must remain distinguished from final outcome metrics.',
      misconceptionTested: 'A proxy metric becomes ground truth when final labels are delayed.',
    },
  ],
  'model-interpretability': [
    {
      id: 'interpretability-stability-check',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { predictionChange: '0.3%', attributionTopFeatureChanges: '7 of 10 runs', perturbation: 'tiny input noise' },
      scenario: 'Visual state: model predictions barely move under tiny input perturbations, but the top-attributed feature changes in 7 of 10 runs.',
      prompt: 'What should be reported?',
      choices: ['The explanation is unstable even though the prediction itself is stable', 'The attribution is trustworthy because the predicted score is stable', 'Tiny perturbations are irrelevant to explanation quality'],
      answerIndex: 0,
      explanation: 'Explanation methods should be checked for stability under reasonable perturbations; unstable attribution can make feature-level claims unreliable even when predictions are robust.',
      misconceptionTested: 'Stable predictions automatically imply stable explanations.',
    },
  ],
  'ml-security-robustness-track': [
    {
      id: 'security-defense-in-depth',
      level: 'application',
      relatedComparison: 'input-filter-vs-authorization-defense',
      scenario: 'A prompt-injection filter blocks known attack phrases, but retrieved documents can still contain novel instructions and the agent has access to side-effecting tools.',
      prompt: 'Which design is safer?',
      choices: ['Use defense in depth: untrusted-content boundaries, least-privilege tools, authorization checks, and output/action validation in addition to filtering', 'Rely only on the phrase filter because unknown attacks cannot exist', 'Give retrieved documents higher authority so the model can resolve ambiguity'],
      answerIndex: 0,
      explanation: 'Input filtering is only one layer; robust agent security also needs authority separation, permission boundaries, least privilege, and validation around side effects.',
      misconceptionTested: 'Prompt-injection security can be solved completely with a keyword or classifier filter.',
    },
  ],
  'data-engineering-for-ml-track': [
    {
      id: 'de-feature-freshness-slo',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { requiredFreshness: '< 5 min', observedP95Freshness: '47 min', modelQuality: 'degrades on recent events' },
      scenario: 'Visual state: a feature should be fresher than five minutes, but P95 freshness is 47 minutes and errors concentrate after recent customer events.',
      prompt: 'What pipeline contract is being violated?',
      choices: ['The feature freshness requirement for serving-time correctness', 'The number of model layers', 'The class label vocabulary'],
      answerIndex: 0,
      explanation: 'Feature freshness is part of the train/serve data contract; stale values can create semantic skew even when schema and feature names match.',
      misconceptionTested: 'A feature is correct at serving time as long as it eventually updates.',
    },
  ],
});

export function getP1ProductionScenariosForLesson(lessonId) {
  return P1_PRODUCTION_SCENARIOS_BY_LESSON[lessonId] || [];
}
