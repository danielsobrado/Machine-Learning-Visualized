export const P1_LOSS_FUNCTIONS_LIKELIHOODS_SCENARIOS_BY_LESSON = Object.freeze({
  'loss-functions-likelihoods': [
    {
      id: 'losslik-categorical-nll-worked',
      level: 'calculation',
      relatedComparison: 'true-class-probability-vs-categorical-nll',
      scenario: 'A three-class classifier assigns probabilities [0.70, 0.20, 0.10] and the observed class is the first class. Categorical negative log-likelihood for one example is -log(p_true). Use natural logarithms.',
      prompt: 'Approximately what loss should this example contribute?',
      choices: [
        '0.357, because -log(0.70) is about 0.357',
        '0.700, because cross-entropy equals the true-class probability directly',
        '1.204, because the loss should use -log(0.30), the total probability of the wrong classes',
      ],
      answerIndex: 0,
      explanation: 'Categorical NLL reads the probability assigned to the observed class. Here -ln(0.70) is about 0.357. The probabilities on incorrect classes matter only through normalization that determines the true-class probability.',
      misconceptionTested: 'Cross-entropy is the true-class probability itself or the negative log of the combined wrong-class probability.',
    },
    {
      id: 'losslik-logsumexp-stability-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'softmax-then-log-vs-fused-cross-entropy',
      scenario: 'A classifier emits logits [1000, 999, 998]. One implementation computes exp(logit) explicitly, divides by the sum, then takes log of the selected probability. It returns NaN even though the relative logit differences are modest. A fused cross-entropy implementation is stable on the same input.',
      prompt: 'What is the correct diagnosis and implementation fix?',
      choices: [
        'The explicit exponentials overflow; use a fused logits cross-entropy or subtract the maximum logit and evaluate log-sum-exp stably',
        'The logits are invalid because any value above 1 must be clipped to a probability before softmax',
        'The labels are necessarily wrong because stable cross-entropy requires all logits to be negative',
      ],
      answerIndex: 0,
      explanation: 'Softmax is shift-invariant, so large absolute logits need not be a mathematical problem. The numerical problem is exp(1000). Stable implementations subtract the maximum or use a fused log-sum-exp formulation without materializing fragile probabilities.',
      misconceptionTested: 'Large logits are semantically invalid probabilities rather than unnormalized scores that require numerically stable normalization.',
    },
    {
      id: 'losslik-gaussian-variance-nll-worked',
      level: 'calculation',
      relatedComparison: 'squared-residual-only-vs-gaussian-scale-term',
      scenario: 'For one residual r = 3, compare two Gaussian predictions using NLL up to the shared constant: log(sigma) + r^2/(2 sigma^2). Model A predicts sigma = 1. Model B predicts sigma = 3.',
      prompt: 'Which model has lower NLL for this observation, and approximately by how much?',
      choices: [
        'Model B: A gives 4.5 while B gives log(3) + 0.5 ≈ 1.60, so B is lower by about 2.90',
        'Model A: smaller sigma is always better because uncertainty should be minimized independently of the residual',
        'They tie because Gaussian NLL depends only on the squared residual and not on predicted variance',
      ],
      answerIndex: 0,
      explanation: 'For sigma=1, the parameter-dependent NLL is 0 + 9/2 = 4.5. For sigma=3 it is ln(3) + 9/18 ≈ 1.099 + 0.5 = 1.599. Gaussian NLL balances residual fit against the log-scale penalty, so uncertainty cannot collapse or grow without cost.',
      misconceptionTested: 'A learned Gaussian variance only rescales residuals and has no countervailing log-variance penalty.',
    },
    {
      id: 'losslik-quantile-pinball-worked',
      level: 'calculation',
      relatedComparison: 'mean-loss-vs-upper-quantile-asymmetric-loss',
      scenario: 'A delivery model targets the 90th percentile with pinball loss L_tau(y, q) = tau*(y-q) when y >= q and (1-tau)*(q-y) when y < q. Let tau = 0.9 and compare two errors of equal magnitude 10 minutes.',
      prompt: 'What losses are assigned to underpredicting by 10 minutes versus overpredicting by 10 minutes?',
      choices: [
        'Underprediction costs 9 and overprediction costs 1, reflecting the asymmetric 90th-percentile objective',
        'Both cost 10 because quantile loss is symmetric absolute error',
        'Underprediction costs 1 and overprediction costs 9 because higher quantiles penalize overprediction more strongly',
      ],
      answerIndex: 0,
      explanation: 'For y-q = +10, loss is 0.9*10 = 9. For q-y = +10, loss is 0.1*10 = 1. The asymmetry is exactly what moves the fitted prediction from the conditional median toward the 90th percentile.',
      misconceptionTested: 'Quantile loss is just absolute error with a different name and therefore penalizes equal-size misses symmetrically.',
    },
    {
      id: 'losslik-weighted-reduction-scale-worked',
      level: 'calculation',
      relatedComparison: 'weighted-sum-vs-weight-normalized-mean',
      scenario: 'A batch has two examples with unweighted BCE losses 0.2 and 0.8. Their importance weights are 1 and 4. The training specification says to compute the weighted mean by dividing the weighted sum by the sum of weights, not by the raw batch size.',
      prompt: 'What batch loss should be reported under that specification?',
      choices: [
        '0.68, because (1*0.2 + 4*0.8) / (1+4) = 3.4/5',
        '1.70, because the weighted sum 3.4 should be divided by the batch size 2',
        '0.50, because weighting should not change the mean of the original losses',
      ],
      answerIndex: 0,
      explanation: 'The specified reduction normalizes by total weight: 3.4/5 = 0.68. Dividing instead by the raw example count changes objective scale as weights change, which can unintentionally change gradient magnitudes and optimization behavior.',
      misconceptionTested: 'Example weighting changes only relative importance and can never affect loss scale through the chosen reduction convention.',
    },
    {
      id: 'losslik-surrogate-metric-mismatch-decision',
      level: 'decision',
      relatedComparison: 'training-surrogate-vs-deployment-ranking-metric',
      scenario: 'A retrieval product is judged by recall@10 on rare relevant items. Training NLL continues to improve, but validation recall@10 has flattened and then declined. The team proposes shipping because the optimized loss is still decreasing.',
      prompt: 'What is the strongest next decision?',
      choices: [
        'Do not ship based on NLL alone; treat recall@10 as the deployment gate and reconsider the surrogate, sampling, weighting, or ranking objective if validation misalignment persists',
        'Ship because any strictly decreasing differentiable loss guarantees every downstream metric must improve',
        'Remove recall@10 from validation because non-differentiable metrics cannot be legitimate deployment objectives',
      ],
      answerIndex: 0,
      explanation: 'A surrogate is useful only insofar as it drives the desired deployment behavior. If held-out recall@10 degrades while NLL improves, the optimization objective is not a sufficient release criterion and the mismatch should be addressed explicitly.',
      misconceptionTested: 'Improving the training surrogate guarantees monotonic improvement in the final business or ranking metric.',
    },
    {
      id: 'losslik-bce-logits-double-sigmoid-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'raw-logits-bcewithlogits-vs-probabilities-bce',
      scenario: 'A binary classifier emits raw logits. The training code first applies sigmoid to convert them to probabilities and then passes those probabilities into a BCE-with-logits loss function, which internally applies the stable sigmoid/logistic formulation again.',
      prompt: 'What is wrong with this pipeline?',
      choices: [
        'The sigmoid is effectively applied twice; pass raw logits directly to the logits-aware BCE loss, or use an ordinary probability BCE only if the input is already a valid probability',
        'Nothing is wrong because every binary loss requires an explicit sigmoid before any library loss function',
        'The fix is to apply softmax after sigmoid so the single positive-class probability sums to one',
      ],
      answerIndex: 0,
      explanation: 'A logits-aware BCE expects unbounded logits and combines the sigmoid with the log-loss algebra for numerical stability. Feeding already-sigmoided probabilities changes the objective and compresses the signal a second time. Output representation and loss API must agree.',
      misconceptionTested: 'Probabilities and logits are interchangeable inputs to binary cross-entropy APIs, so applying sigmoid before a logits-aware loss is harmless.',
    },
  ],
});
