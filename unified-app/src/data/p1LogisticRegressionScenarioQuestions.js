export const P1_LOGISTIC_REGRESSION_SCENARIOS_BY_LESSON = Object.freeze({
  'logistic-regression': [
    {
      id: 'logreg-odds-to-probability-worked',
      level: 'calculation',
      relatedComparison: 'coefficient-log-odds-vs-probability-change',
      scenario: 'A customer has baseline event probability 0.20, so the baseline odds are 0.20 / 0.80 = 0.25. A binary feature has logistic-regression coefficient ln(2), so turning that feature on doubles the odds while all other features stay fixed.',
      prompt: 'What probability should the model assign after applying that coefficient effect?',
      choices: [
        'About 33.3%, because odds 0.25 double to 0.50 and probability becomes 0.50 / (1 + 0.50)',
        'About 89%, because ln(2) should be added directly to the baseline probability',
        'Exactly 40%, because doubling the odds always doubles the probability',
      ],
      answerIndex: 0,
      explanation: 'Logistic coefficients are additive in log-odds, not probability. The baseline odds are 0.25; multiplying by exp(ln 2) = 2 gives odds 0.50. Converting back gives p = 0.50 / 1.50 = 1/3. The probability change therefore depends on the starting probability.',
      misconceptionTested: 'An odds ratio can be applied as the same additive or multiplicative probability change regardless of the baseline probability.',
    },
    {
      id: 'logreg-separation-regularization-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'unregularized-separable-fit-vs-regularized-probability-model',
      scenario: 'A binary dataset is almost perfectly separable. During unregularized training, validation accuracy stays nearly unchanged while coefficient norm grows from 6 to 18 to 55 and predicted probabilities become increasingly close to 0 or 1. Validation log loss eventually worsens.',
      prompt: 'What is the strongest diagnosis and next step?',
      choices: [
        'Separation is driving coefficient magnitudes toward extreme confidence; add appropriate regularization and select it using held-out probability-quality and decision metrics',
        'The growing coefficients prove the classifier is becoming better calibrated, so training should continue until every probability is exactly 0 or 1',
        'The sigmoid function is failing because separable data requires replacing logistic regression with ordinary least squares',
      ],
      answerIndex: 0,
      explanation: 'On separable or nearly separable data, unregularized logistic likelihood can keep improving as weights grow even when the decision boundary barely changes. Extreme logits can damage probability quality and coefficient stability. Regularization constrains this behavior and should be validated on the deployment objective.',
      misconceptionTested: 'Falling training loss and growing confidence on separable data necessarily mean better probability estimates and more reliable coefficients.',
    },
    {
      id: 'logreg-nonlinear-interaction-design',
      level: 'design',
      relatedComparison: 'raw-linear-logit-vs-engineered-interaction',
      scenario: 'A classifier uses two binary features x1 and x2. The positive class occurs mainly when exactly one feature is on: (0,1) and (1,0) are positive, while (0,0) and (1,1) are negative. A logistic model using only x1 and x2 performs near chance.',
      prompt: 'Which next experiment most directly tests whether the failure comes from the linear-logit feature representation?',
      choices: [
        'Add an interaction feature such as x1*x2 or otherwise expand the feature basis, then re-evaluate with the same split and regularization protocol',
        'Keep the same raw features and only lower the classification threshold until training accuracy becomes high',
        'Increase coefficient magnitudes manually because any binary pattern is linearly separable in the original two features',
      ],
      answerIndex: 0,
      explanation: 'Plain logistic regression creates a linear decision boundary in the supplied feature space. XOR-like structure cannot be separated by one linear boundary in x1 and x2 alone. Adding interaction or nonlinear basis features changes the representation while preserving the logistic decision model.',
      misconceptionTested: 'Threshold tuning or larger coefficients can fix a representation whose class boundary is not linearly expressible in the supplied features.',
    },
    {
      id: 'logreg-prior-shift-calibration-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'stable-ranking-vs-shifted-probability-calibration',
      scenario: 'A churn model was calibrated on a population with 20% churn. After a product-policy change, live churn prevalence falls to 5%. On recent labeled data, ranking AUC is almost unchanged, but reliability bins show that predictions around 0.40 now correspond to about 0.15 observed churn.',
      prompt: 'What should the team do before continuing to use probability-based business thresholds?',
      choices: [
        'Treat this as probability-calibration drift, recalibrate or refit on representative recent labeled data, and then revalidate the cost-based threshold',
        'Keep the old probabilities because unchanged AUC proves that probability values remain calibrated',
        'Fix the issue only by moving the hard threshold while continuing to interpret the old scores as accurate event probabilities',
      ],
      answerIndex: 0,
      explanation: 'Ranking can remain strong while probability values become miscalibrated under a prevalence or distribution shift. If business decisions use expected probabilities or costs, the score calibration must be revalidated on representative data before a threshold is trusted.',
      misconceptionTested: 'Stable discrimination metrics guarantee that probability values and cost-derived thresholds remain valid after a base-rate shift.',
    },
    {
      id: 'logreg-stable-bce-logits-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'naive-sigmoid-log-vs-stable-logits-loss',
      scenario: 'A custom training loop computes sigmoid(z) first and then applies -y*log(p) - (1-y)*log(1-p). For logits near +1000 or -1000, p rounds to exactly 1 or 0 in floating point and the loss occasionally becomes infinity or NaN.',
      prompt: 'What implementation change should be made first?',
      choices: [
        'Use a numerically stable binary-cross-entropy-from-logits implementation that combines the sigmoid and log-loss algebra without explicitly taking log(0)',
        'Clip every model logit to zero so sigmoid always returns exactly 0.5',
        'Round all probabilities to 0 or 1 before computing loss so the labels and predictions use the same representation',
      ],
      answerIndex: 0,
      explanation: 'Stable BCE-with-logits formulations use log-sum-exp or softplus-style algebra to avoid overflow and log(0) when logits have large magnitude. This preserves the intended Bernoulli likelihood while improving numerical stability.',
      misconceptionTested: 'Computing sigmoid and then taking logarithms is numerically equivalent to a stable logits-based loss for arbitrarily large-magnitude logits.',
    },
    {
      id: 'logreg-correlated-coefficient-instability-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'stable-prediction-vs-unstable-correlated-coefficients',
      scenario: 'Two predictors measure nearly the same customer behavior and have correlation 0.995. Across cross-validation folds, the model AUC and predicted probabilities are stable, but coefficient pairs vary from (+2.1, -0.3) to (+0.7, +1.1) to (-0.2, +2.0).',
      prompt: 'What is the safest interpretation of these coefficient swings?',
      choices: [
        'The duplicated predictors make individual coefficient attribution unstable; avoid strong feature-level stories and consider regularization, redundancy reduction, or grouped interpretation',
        'The sign changes prove the model predictions are unreliable because stable predictions require every correlated coefficient to be identical across folds',
        'Each coefficient is a causal effect, so the fold with the largest positive coefficient reveals which predictor truly causes the outcome',
      ],
      answerIndex: 0,
      explanation: 'Highly correlated predictors can substitute for each other in the linear score, so many coefficient combinations yield similar predictions. Predictive performance can remain stable while individual coefficients are poorly identified. Regularization and domain-aware feature handling can improve stability, but coefficients should not be treated as automatic causal effects.',
      misconceptionTested: 'Stable predictive performance implies stable and causally interpretable individual coefficients under severe multicollinearity.',
    },
    {
      id: 'logreg-oversampling-calibration-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'balanced-training-sample-vs-population-probability-calibration',
      scenario: 'Fraud prevalence is 1% in production. To make training easier, the team oversamples positive examples until the training set is 50% fraud and 50% non-fraud. The resulting logistic model ranks cases well on a representative validation set, but its raw predicted probabilities are far too high compared with observed production frequencies.',
      prompt: 'What is the correct interpretation and next step?',
      choices: [
        'Oversampling changed the class prior seen during fitting; evaluate and correct or recalibrate probabilities on data with representative prevalence before using probability-based thresholds',
        'A balanced training sample guarantees calibrated 50% priors are valid in production regardless of the real base rate',
        'The only valid fix is to force the classification threshold back to 0.5 while continuing to interpret the raw scores as population probabilities',
      ],
      answerIndex: 0,
      explanation: 'Resampling can improve learning signal for rare classes, but it changes the effective class distribution used during fitting. Ranking may remain useful while the intercept and raw probability scale no longer reflect the deployment population. Probability-sensitive decisions need representative validation and an appropriate prior correction or recalibration.',
      misconceptionTested: 'Class balancing through oversampling changes optimization only and can never affect the probability scale or calibration of logistic-regression outputs.',
    },
  ],
});
