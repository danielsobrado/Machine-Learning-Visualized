export const P1_KV_CACHE_SCENARIOS = Object.freeze([
  {
    id: 'kv-cache-batch-capacity-worked',
    level: 'calculation',
    relatedComparison: 'per-sequence-kv-memory-vs-serving-batch-capacity',
    scenario: 'After model weights and runtime workspace are reserved, a GPU has 20 GiB available for KV cache. At the target context length, each active sequence needs approximately 2 GiB of KV cache.',
    prompt: 'Ignoring allocator overhead and safety headroom, what is the largest batch that can fit in the KV-cache budget?',
    choices: ['10 active sequences', '20 active sequences', '5 active sequences'],
    answerIndex: 0,
    explanation: 'The cache budget is 20 GiB and each active sequence consumes about 2 GiB, so the theoretical limit is 20 / 2 = 10 concurrent sequences. Real serving needs additional headroom for fragmentation and transient allocations.',
    misconceptionTested: 'KV-cache memory is a per-request serving cost, so context length can directly limit concurrency even when model weights already fit.',
  },
  {
    id: 'kv-cache-bandwidth-tpot-worked',
    level: 'calculation',
    relatedComparison: 'kv-cache-bytes-read-vs-decode-latency',
    scenario: 'At a long context, one decode step must stream about 2 GiB of cached K/V for one sequence. Assume an idealized sustained memory bandwidth of 1 TiB/s and ignore all other compute and memory traffic.',
    prompt: 'What bandwidth-only lower bound does this imply for one output token?',
    choices: ['About 2 ms per token', 'About 0.002 ms per token', 'About 2 seconds per token'],
    answerIndex: 0,
    explanation: '1 TiB/s is 1024 GiB/s. Reading 2 GiB therefore takes 2 / 1024 seconds, about 0.00195 seconds or 1.95 ms. Actual TPOT is higher because attention math, projections, synchronization, and other memory traffic still remain.',
    misconceptionTested: 'KV caching removes repeated projections, but long-context decode can still be bounded by the bandwidth required to read cached K/V.',
  },
  {
    id: 'kv-cache-paged-fragmentation-worked',
    level: 'calculation',
    relatedComparison: 'contiguous-allocation-vs-paged-kv-blocks',
    scenario: 'A paged KV allocator uses blocks that each hold 16 token positions. An active request currently has 33 cached tokens, and partially filled blocks cannot be shared with another request.',
    prompt: 'How many blocks and unused token slots does this request occupy?',
    choices: ['3 blocks with 15 unused slots', '2 blocks with 1 unused slot', '3 blocks with 31 unused slots'],
    answerIndex: 0,
    explanation: 'Two blocks hold only 32 positions, so 33 tokens require three blocks, or 48 allocated slots. The request uses 33 of them, leaving 15 unused slots in its final block. Paging reduces external fragmentation but block granularity can still create internal waste.',
    misconceptionTested: 'Paged KV allocation improves reuse and fragmentation behavior, but it does not make allocation overhead or partially filled blocks disappear.',
  },
  {
    id: 'kv-cache-prefix-reuse-boundary-design',
    level: 'design',
    relatedComparison: 'exact-prefix-reuse-vs-cross-request-state-mixing',
    scenario: 'Two requests use the same model and adapter. Their first 256 token IDs are an identical public system prompt, but token 257 starts different user content for different tenants.',
    prompt: 'What is the safest valid prefix-cache reuse plan?',
    choices: ['Reuse only the K/V for the identical 256-token prefix, then branch into isolated per-request cache state from token 257 onward', 'Reuse the full first request cache because the model and adapter match', 'Never reuse any prefix cache across requests, even when the exact public prefix and model configuration match'],
    answerIndex: 0,
    explanation: 'Cached K/V remain reusable only while the tokenized prefix and model configuration are identical. Once the requests diverge, each continuation needs isolated cache state. Reusing beyond the common prefix can mix private context and produce incorrect attention state.',
    misconceptionTested: 'Prefix-cache reuse is valid for an exact common prefix, not for arbitrary continuations that merely share the same model.',
  },
  {
    id: 'kv-cache-position-offset-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'fresh-decode-position-vs-reused-prefix-position',
    scenario: 'A service reuses a valid 2,048-token prefix cache. The first uncached token is then passed to the model with position index 0 instead of the next position after the cached prefix. Shapes and cache lengths otherwise look correct.',
    prompt: 'What should be fixed first?',
    choices: ['Advance the new token position to the offset implied by the 2,048 cached positions', 'Clear only the value cache while keeping the key cache', 'Reset the attention mask so the new token cannot see the reused prefix'],
    answerIndex: 0,
    explanation: 'Position-dependent attention must stay aligned with the cached sequence. After 2,048 cached positions, the next token must use the next logical position rather than restarting at zero. A wrong offset can corrupt RoPE or other position-dependent attention even when tensor shapes are valid.',
    misconceptionTested: 'A KV cache is not just tensors with matching shapes; positional metadata must continue from the cached prefix.',
  },
  {
    id: 'kv-cache-speculative-rollback-worked',
    level: 'diagnosis',
    relatedComparison: 'accepted-draft-prefix-vs-rejected-cache-suffix',
    scenario: 'A speculative decoder appends six draft tokens to temporary KV state. Verification accepts the first two draft tokens and rejects the third, so draft tokens three through six must not become part of the committed sequence.',
    prompt: 'How should the target-model cache be left after verification?',
    choices: ['Commit cache state only through the two accepted draft tokens and discard or roll back the rejected speculative suffix', 'Keep all six draft-token cache entries because their K/V projections were already computed', 'Discard the entire original prefix cache and restart the conversation from token one'],
    answerIndex: 0,
    explanation: 'The committed cache must represent exactly the accepted token prefix. Once verification rejects a draft token, cache entries derived from that rejected token and its suffix cannot remain visible to later decoding. Efficient implementations roll back or avoid committing those entries.',
    misconceptionTested: 'Speculative decoding needs transactional cache semantics: rejected draft tokens cannot remain in committed KV state.',
  },
]);
