export const P1_SCENARIO_QUESTIONS_BY_LESSON = Object.freeze({
  'logistic-regression': [
    {
      id: 'logreg-imbalance-threshold-cost',
      level: 'application',
      relatedComparison: 'threshold-cost-imbalance',
      scenario: 'Only 1% of transactions are fraud. A threshold of 0.50 gives high accuracy but misses most fraud, and false negatives are much more expensive than false positives.',
      prompt: 'What should determine the operating threshold?',
      choices: ['Validation performance under the real false-positive and false-negative costs', 'Accuracy alone because the majority class dominates', 'The default threshold of 0.50 regardless of business cost'],
      answerIndex: 0,
      explanation: 'With strong class imbalance and asymmetric error costs, threshold selection should optimize the decision trade-off rather than default accuracy.',
      misconceptionTested: 'A probability threshold of 0.50 is automatically optimal.',
    },
    {
      id: 'logreg-odds-coefficient',
      level: 'mechanism',
      scenario: 'A logistic-regression coefficient for a feature is 0.69 while other features are held fixed.',
      prompt: 'What is the approximate multiplicative change in odds for a one-unit feature increase?',
      choices: ['About 2 times because exp(0.69) is about 2', 'The probability always increases by exactly 0.69', 'The odds are divided by 0.69'],
      answerIndex: 0,
      explanation: 'Logistic-regression coefficients are additive in log-odds, so exponentiating a coefficient gives the multiplicative odds ratio.',
      misconceptionTested: 'A logistic coefficient is a fixed additive change in probability.',
    },
  ],
  'classification-metrics': [
    {
      id: 'metrics-subgroup-slicing',
      level: 'diagnosis',
      relatedComparison: 'aggregate-vs-slice-metrics',
      scenario: 'Overall recall is 0.90, but recall is 0.97 for desktop traffic and 0.58 for one mobile device family that represents 8% of requests.',
      prompt: 'What should the evaluation report do?',
      choices: ['Report the subgroup regression and investigate the affected slice', 'Use only the overall recall because it has more samples', 'Average the device-family recall with precision and hide the slice'],
      answerIndex: 0,
      explanation: 'Aggregate metrics can hide concentrated failures; slice-level metrics reveal whether a subgroup experiences materially worse behavior.',
      misconceptionTested: 'A strong aggregate metric guarantees acceptable subgroup performance.',
    },
  ],
  'cross-validation': [
    {
      id: 'cv-repeated-stratified',
      level: 'design',
      relatedComparison: 'kfold-stratified-repeated',
      scenario: 'A small imbalanced classification dataset shows noticeable score variance depending on the exact stratified fold assignment.',
      prompt: 'Which validation design can estimate sensitivity to fold assignment more directly?',
      choices: ['Repeated stratified cross-validation with multiple independent fold partitions', 'One fixed shuffled split used forever', 'Leave the target column inside preprocessing features'],
      answerIndex: 0,
      explanation: 'Repeated stratified CV preserves class balance while repeating the fold construction, exposing variance caused by one particular partition.',
      misconceptionTested: 'One stratified K-fold run fully characterizes validation uncertainty.',
    },
    {
      id: 'cv-grouped-time-boundary',
      level: 'design',
      relatedComparison: 'grouped-vs-temporal-cv',
      scenario: 'A forecasting dataset contains repeated customer rows across months. Future months must not train earlier validation periods, and one customer must not appear in both train and validation.',
      prompt: 'What constraint must the splitter satisfy?',
      choices: ['Both temporal ordering and customer-group separation', 'Random stratification only', 'Ordinary shuffled K-fold because rows are independent by default'],
      answerIndex: 0,
      explanation: 'When both time and entity dependence matter, validation must respect chronology and keep repeated entities from crossing the evaluation boundary.',
      misconceptionTested: 'Only one split constraint can matter at a time.',
    },
  ],
  'k-means': [
    {
      id: 'kmeans-nonspherical-failure',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { clusterShape: 'two interlocking crescents', centroids: 2, assignmentPattern: 'cuts across crescents' },
      scenario: 'Visual state: two interlocking crescent-shaped groups are present, but k-means draws roughly convex Voronoi regions that cut across both crescents.',
      prompt: 'What is the main failure?',
      choices: ['Euclidean centroid clusters do not match the non-spherical geometry', 'The number of rows is automatically too small', 'K-means requires labeled targets to separate the crescents'],
      answerIndex: 0,
      explanation: 'K-means partitions space by distance to centroids, which works best for roughly compact convex clusters and can fail on curved non-spherical groups.',
      misconceptionTested: 'K-means can recover any cluster shape if k is correct.',
    },
    {
      id: 'kmeans-scaling-sensitivity',
      level: 'application',
      relatedComparison: 'kmeans-feature-scaling',
      scenario: 'One feature ranges from 0 to 1 while another ranges from 0 to 100000, and both are included in Euclidean distance.',
      prompt: 'What should be checked before trusting the clusters?',
      choices: ['Whether feature scaling makes the distance reflect intended importance', 'Whether the larger-scale feature should always dominate because it has bigger numbers', 'Whether the target labels can be added to the distance'],
      answerIndex: 0,
      explanation: 'Euclidean distance is scale-sensitive, so large numeric ranges can dominate assignments even when that feature should not dominate the clustering objective.',
      misconceptionTested: 'K-means is invariant to feature units.',
    },
    {
      id: 'kmeans-initialization-instability',
      level: 'diagnosis',
      scenario: 'Two runs with the same k converge to noticeably different centroids and inertia values because they started from different random seeds.',
      prompt: 'What is the best response?',
      choices: ['Use robust initialization such as k-means++ and multiple restarts, then compare stability', 'Assume the first run is the unique global optimum', 'Increase k until every point has its own centroid'],
      answerIndex: 0,
      explanation: 'The k-means objective is non-convex, so initialization can lead to different local optima; multiple good initializations reduce that risk.',
      misconceptionTested: 'K-means always finds one unique global optimum.',
    },
  ],
  calibration: [
    {
      id: 'calibration-shift-recalibration',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { trainingReliability: 'near diagonal', liveReliability: 'below diagonal at high scores', liveBaseRate: 'lower' },
      scenario: 'Visual state: the validation reliability curve was close to the diagonal, but live high-confidence predictions are systematically overconfident after the positive base rate falls.',
      prompt: 'What should the team investigate?',
      choices: ['Dataset shift and recalibration on representative recent data', 'Increasing confidence scores further', 'Replacing probability outputs with class IDs only'],
      answerIndex: 0,
      explanation: 'Calibration can degrade after deployment when the data distribution or prevalence changes; recalibration should use representative current data without hiding underlying model drift.',
      misconceptionTested: 'A model calibrated once remains calibrated under arbitrary distribution shift.',
    },
  ],
  overfitting: [
    {
      id: 'overfit-validation-reuse',
      level: 'diagnosis',
      scenario: 'A team tries 400 model variants and repeatedly chooses changes based on the same validation set. Validation improves steadily, but a fresh test set is much worse.',
      prompt: 'What happened?',
      choices: ['The development process overfit the validation set through repeated selection', 'The fresh test set must be invalid because it was not used for tuning', 'More validation reuse always makes the estimate less biased'],
      answerIndex: 0,
      explanation: 'Even without training directly on validation rows, repeated human and hyperparameter decisions can adapt to validation noise and make the set part of the tuning process.',
      misconceptionTested: 'Validation data cannot be overfit unless gradients are computed on it.',
    },
  ],
  regularization: [
    {
      id: 'regularization-family-comparison',
      level: 'comparison',
      relatedComparison: 'l1-l2-dropout-depth-augmentation',
      scenario: 'A linear model overfits through unstable large coefficients, a neural network memorizes through co-adapted hidden units, and a tree overfits through excessive depth.',
      prompt: 'What is the shared idea across the likely remedies?',
      choices: ['Constrain effective model flexibility using a mechanism appropriate to each model family', 'Apply L1 penalties literally to every possible model and data pipeline', 'Use the same numeric lambda for linear models, dropout, and tree depth'],
      answerIndex: 0,
      explanation: 'Regularization is a broader bias toward simpler or more stable solutions; the concrete mechanism differs across coefficients, neural networks, trees, and data augmentation.',
      misconceptionTested: 'Regularization means only adding an L1 or L2 term to a loss.',
    },
  ],
  'recommender-systems-ranking-track': [
    {
      id: 'rec-ndcg-graded-ranking',
      level: 'application',
      relatedComparison: 'precision-recall-ndcg-ranking',
      scenario: 'A recommendation list has relevance grades from 0 to 3, and placing a grade-3 item near the top should matter more than placing it at rank 20.',
      prompt: 'Which offline metric directly captures graded relevance with rank discounting?',
      choices: ['nDCG', 'Unordered accuracy', 'Mean squared error on user IDs'],
      answerIndex: 0,
      explanation: 'nDCG rewards placing highly relevant items near the top while discounting gains at lower ranks and supporting graded relevance.',
      misconceptionTested: 'All ranking metrics treat item position as irrelevant.',
    },
    {
      id: 'rec-feedback-loop',
      level: 'diagnosis',
      scenario: 'A recommender promotes already popular items. Those items receive more exposure, collect more interactions, and become even more likely to be recommended.',
      prompt: 'What system risk is visible?',
      choices: ['A popularity feedback loop that can reduce coverage and discovery', 'Proof that popularity is an unbiased estimate of user utility', 'A train/serve schema mismatch'],
      answerIndex: 0,
      explanation: 'Recommendation exposure changes the data later used for training, so popularity can become self-reinforcing and crowd out less-exposed items.',
      misconceptionTested: 'Observed interaction frequency is independent of previous recommendation exposure.',
    },
    {
      id: 'rec-cold-start-content',
      level: 'design',
      relatedComparison: 'collaborative-content-cold-start',
      scenario: 'A brand-new item has no interaction history but has rich text and category metadata.',
      prompt: 'Which signal is most useful while collaborative evidence is absent?',
      choices: ['Content or metadata similarity combined with exploration', 'Matrix-factorization interaction factors that do not yet exist for the item', 'Only global popularity with no item features'],
      answerIndex: 0,
      explanation: 'Cold-start items lack collaborative interactions, so content features and controlled exploration can provide an initial ranking signal.',
      misconceptionTested: 'Collaborative filtering alone solves new-item cold start.',
    },
  ],
  'gradient-descent': [
    {
      id: 'gd-saddle-gradient-small',
      level: 'diagnosis',
      kind: 'visual-state',
      visualState: { gradientNorm: 0.002, curvatureAxisA: 'positive', curvatureAxisB: 'negative', loss: 'not minimal' },
      scenario: 'Visual state: the gradient norm is almost zero, but curvature is positive along one direction and negative along another, and nearby points have lower loss.',
      prompt: 'What point is this most consistent with?',
      choices: ['A saddle point rather than a local minimum', 'A guaranteed global optimum', 'A data-leakage boundary'],
      answerIndex: 0,
      explanation: 'A saddle can have a near-zero gradient while curving upward in one direction and downward in another, so small gradient alone does not prove convergence to a minimum.',
      misconceptionTested: 'Near-zero gradient always means optimization has reached a minimum.',
    },
  ],
  'neural-network': [
    {
      id: 'nn-architecture-ablation',
      level: 'application',
      relatedComparison: 'capacity-ablation',
      scenario: 'A large network gets near-zero training error but weak validation performance. A smaller network trains slightly worse but validates better.',
      prompt: 'Which experiment best tests whether excess capacity contributes to the gap?',
      choices: ['Run a controlled architecture ablation while keeping data, optimizer, and evaluation fixed', 'Change architecture, optimizer, data augmentation, and metric simultaneously', 'Choose the larger network because lower training error is always better'],
      answerIndex: 0,
      explanation: 'A controlled ablation isolates capacity as the changed factor, making the train-validation trade-off interpretable rather than confounded by many simultaneous changes.',
      misconceptionTested: 'More parameters are automatically better when training error decreases.',
    },
  ],
  'rag-chunking-context': [
    {
      id: 'rag-chunk-semantic-vs-fixed',
      level: 'comparison',
      relatedComparison: 'fixed-semantic-chunking',
      scenario: 'Fixed 500-token chunks repeatedly split short policy sections across boundaries, while the document has strong heading and paragraph structure.',
      prompt: 'What chunking change is worth evaluating?',
      choices: ['Structure-aware or semantic chunk boundaries with measured retrieval impact', 'Always double overlap until every document is duplicated many times', 'Remove all chunk boundaries and send the full corpus to the model'],
      answerIndex: 0,
      explanation: 'When fixed windows cut coherent units, structure-aware boundaries can preserve meaning, but the choice should still be evaluated on retrieval and answer quality.',
      misconceptionTested: 'One fixed token size is optimal for every document type.',
    },
    {
      id: 'rag-overlap-duplication',
      level: 'diagnosis',
      scenario: 'Chunk overlap rises from 10% to 80%. Recall changes little, but the top results contain many near-duplicate passages and consume most of the context window.',
      prompt: 'What is the likely downside?',
      choices: ['Duplicate evidence crowds out diverse useful context', 'Large overlap guarantees better grounding', 'Overlap removes all retrieval latency'],
      answerIndex: 0,
      explanation: 'Excessive overlap can create redundant candidates, waste retrieval/context budget, and reduce evidence diversity without meaningful recall gains.',
      misconceptionTested: 'More chunk overlap is always better.',
    },
  ],
  'rag-vector-indexing': [
    {
      id: 'rag-index-recall-latency',
      level: 'comparison',
      relatedComparison: 'ann-recall-latency',
      scenario: 'Increasing ANN search effort raises Recall@20 from 0.84 to 0.95 but doubles P95 retrieval latency beyond the product budget.',
      prompt: 'How should the index setting be chosen?',
      choices: ['Use the recall-latency trade-off that satisfies the end-to-end quality and latency target', 'Always maximize ANN recall regardless of latency', 'Always minimize latency even if relevant evidence disappears'],
      answerIndex: 0,
      explanation: 'Approximate search exposes a quality-latency trade-off; the right operating point depends on the system SLO and downstream answer sensitivity to missed evidence.',
      misconceptionTested: 'ANN search quality should be maximized independently of serving constraints.',
    },
  ],
  'rag-reranking-grounding': [
    {
      id: 'rag-cross-encoder-budget',
      level: 'comparison',
      relatedComparison: 'bi-encoder-cross-encoder-rerank',
      scenario: 'A bi-encoder retrieves 200 candidates cheaply. A cross-encoder gives much better pairwise relevance scores but is too expensive to run over the whole corpus.',
      prompt: 'Where should the cross-encoder usually be applied?',
      choices: ['On a limited retrieved candidate set as a reranking stage', 'Before retrieval over every document in the corpus for every query', 'Only after the answer is generated'],
      answerIndex: 0,
      explanation: 'Bi-encoders make large-scale candidate retrieval practical; cross-encoders are commonly reserved for reranking a much smaller candidate set because they jointly score query-document pairs.',
      misconceptionTested: 'A more accurate relevance model should replace candidate retrieval regardless of cost.',
    },
  ],
});

export function getP1ScenarioQuestionsForLesson(lessonId) {
  return P1_SCENARIO_QUESTIONS_BY_LESSON[lessonId] || [];
}
