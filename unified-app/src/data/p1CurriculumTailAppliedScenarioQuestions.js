export const P1_CURRICULUM_TAIL_APPLIED_SCENARIOS_BY_LESSON = Object.freeze({
  'frontier-llm-architecture-overview': [
    {
      id: 'tail-frontier-kv-cache-bottleneck-worked',
      level: 'calculation',
      relatedComparison: 'mha-vs-gqa-kv-cache-width',
      scenario: 'A decoder layer uses 32 query heads with head dimension 128. Standard multi-head attention stores 32 K heads and 32 V heads per token. A GQA variant keeps the same query heads but stores only 8 K heads and 8 V heads. Assume datatype, layer count, batch size, and context length stay fixed.',
      prompt: 'How does the per-token KV-cache memory of the GQA variant compare with standard multi-head attention?',
      choices: [
        'It is 4 times smaller because stored K/V head count falls from 32 to 8 while head width is unchanged',
        'It is 2 times smaller because K and V should be counted as one shared tensor',
        'It is unchanged because the number of query heads is still 32',
      ],
      answerIndex: 0,
      explanation: 'KV-cache memory is proportional to the number of stored key/value heads when all other dimensions are fixed. Both designs store K and V, so the common factor of two cancels in the ratio: 32 divided by 8 gives a 4x reduction. Query-head count affects attention computation but does not force GQA to cache a separate K/V pair for every query head.',
      misconceptionTested: 'Keeping the same query-head count means grouped-query attention cannot reduce the decode-time KV cache.',
    },
  ],
  'frontier-moe-systems': [
    {
      id: 'tail-frontier-moe-capacity-worked',
      level: 'calculation',
      relatedComparison: 'top-k-routing-average-load-vs-capacity-factor',
      scenario: 'An MoE layer receives 8,192 tokens, routes every token to top-2 experts, and has 64 experts. Capacity is provisioned as capacity_factor times the perfectly balanced average assignments per expert. The capacity factor is 1.25.',
      prompt: 'What capacity should be provisioned per expert before considering rounding details?',
      choices: [
        '320 assignments because 8,192 x 2 / 64 = 256 average assignments and 256 x 1.25 = 320',
        '160 assignments because top-2 routing divides the average load by two',
        '10,240 assignments because the capacity factor should be applied before dividing across experts',
      ],
      answerIndex: 0,
      explanation: 'Top-2 routing creates 16,384 expert assignments. Perfectly balanced across 64 experts, that is 256 assignments per expert. Multiplying by the 1.25 capacity factor gives 320. Real systems still need to handle imbalance, routing skew, communication, and overflow, but the capacity baseline starts from assignment count rather than raw token count alone.',
      misconceptionTested: 'MoE expert capacity can be sized from raw batch tokens without accounting for top-k routing assignments and expert count.',
    },
  ],
  rag: [
    {
      id: 'tail-rag-retrieval-ceiling-worked',
      level: 'calculation',
      relatedComparison: 'candidate-recall-vs-reranker-success-ceiling',
      scenario: 'For a support RAG system, the gold evidence appears somewhere in the retriever top-50 for 68% of evaluation queries. When the gold evidence is present, the reranker puts it into the final context 90% of the time. Ignore generation failures for this calculation.',
      prompt: 'What is the highest end-to-end evidence-in-context rate implied by these two measured stages?',
      choices: [
        '61.2% because the evidence must survive both stages: 0.68 x 0.90 = 0.612',
        '90% because reranker accuracy replaces retriever recall once reranking is enabled',
        '158% because independent stage percentages should be added to estimate pipeline quality',
      ],
      answerIndex: 0,
      explanation: 'The reranker can only promote evidence that retrieval supplied. Under the stated conditional rate, evidence reaches the final context with probability 0.68 x 0.90 = 0.612, or 61.2%. This exposes a pipeline ceiling: improving generation or reranking alone cannot recover the 32% of queries where the candidate set never contained the gold evidence.',
      misconceptionTested: 'A strong downstream reranker can erase an upstream retrieval-recall ceiling even when the relevant evidence never entered its candidate set.',
    },
    {
      id: 'tail-rag-grounding-release-decision',
      level: 'decision',
      relatedComparison: 'retrieval-quality-vs-grounded-generation-quality',
      scenario: 'A release candidate raises recall@20 from 0.86 to 0.93 and keeps latency within budget, but citation-entailment checks fall from 0.91 to 0.72 because the generator increasingly cites retrieved passages that are related to the topic but do not support the exact claim. The product requires citation entailment of at least 0.88.',
      prompt: 'What is the best release decision based on the measured pipeline behavior?',
      choices: [
        'Do not ship yet; preserve the retrieval gain while fixing grounding or generation so citation entailment returns above the required threshold',
        'Ship because higher retrieval recall automatically implies the answer is better grounded',
        'Remove retrieval evaluation because citation entailment is the only metric that matters in a RAG pipeline',
      ],
      answerIndex: 0,
      explanation: 'The candidate improves candidate availability but violates an explicit grounding requirement. Retrieval and grounded generation measure different failure surfaces, so the correct response is not to discard either metric. Keep the retrieval improvement, diagnose why the generator is using merely related passages as support, and release only when the full pipeline satisfies both evidence availability and entailment constraints.',
      misconceptionTested: 'Improving retrieval recall is sufficient evidence for shipping a RAG change even when the generator becomes materially less faithful to the retrieved evidence.',
    },
  ],
  optimization: [
    {
      id: 'tail-optimization-projected-step-worked',
      level: 'calculation',
      relatedComparison: 'unconstrained-gradient-step-vs-projected-feasible-step',
      scenario: 'A one-dimensional constrained optimization problem requires x <= 5. The current point is x = 4.5, the gradient is -3, and gradient descent uses learning rate 0.5. The algorithm first takes the ordinary step x_new = x - learning_rate * gradient and then projects the result back onto the feasible set.',
      prompt: 'What value of x remains after this projected-gradient step?',
      choices: [
        '5 because the unconstrained step reaches 6.0 and projection clips it to the feasible boundary x = 5',
        '6 because constraints are checked only after optimization has fully converged',
        '3 because the negative gradient must always make the parameter numerically smaller',
      ],
      answerIndex: 0,
      explanation: 'The unconstrained update is 4.5 - 0.5 x (-3) = 6.0. That point violates x <= 5, so Euclidean projection onto the feasible interval maps it to the nearest feasible point, x = 5. The example separates the descent direction from constraint handling: a valid optimizer must respect the feasible region rather than simply accepting the unconstrained iterate.',
      misconceptionTested: 'Gradient descent automatically respects operational constraints, or a negative gradient necessarily means the parameter value must decrease.',
    },
  ],
  'tokenizer-bpe': [
    {
      id: 'tail-bpe-merge-count-worked',
      level: 'calculation',
      relatedComparison: 'character-sequence-vs-bpe-merged-token-count',
      scenario: 'Before a BPE merge, a training sequence is represented by 12 tokens. The selected adjacent pair occurs three times in non-overlapping positions in that sequence. One merge operation replaces every one of those three pair occurrences with a single new token.',
      prompt: 'How many tokens represent the sequence immediately after applying that merge everywhere?',
      choices: [
        '9 tokens because each of the three merged pairs replaces two tokens with one and therefore removes one token',
        '6 tokens because each merge should remove both original tokens without adding a replacement token',
        '11 tokens because one vocabulary merge can change only one occurrence in the corpus',
      ],
      answerIndex: 0,
      explanation: 'Each non-overlapping occurrence changes a two-token span into one token, reducing sequence length by exactly one. Three occurrences therefore reduce 12 tokens to 9. BPE learns one pair rule at a time, but that learned rule is applied to every applicable occurrence during the merge step rather than only to a single position.',
      misconceptionTested: 'A BPE merge either deletes both source tokens entirely or applies to only one occurrence of the selected pair.',
    },
    {
      id: 'tail-bpe-domain-shift-decision',
      level: 'decision',
      relatedComparison: 'fixed-tokenizer-compatibility-vs-domain-token-efficiency',
      scenario: 'A deployed language model moves into a biomedical workload. The existing tokenizer can encode every new term, but common biomedical words split into many more pieces, increasing context usage and latency. Retraining the tokenizer would shorten those terms but would also change token IDs and invalidate direct reuse of the model input embedding table without an adaptation plan.',
      prompt: 'What is the sound engineering conclusion?',
      choices: [
        'Measure the token-budget cost first and treat tokenizer replacement as a model-interface migration that requires embedding/model adaptation, not as a drop-in vocabulary swap',
        'Replace the tokenizer immediately because any reduction in token count is automatically compatible with the existing embedding matrix',
        'Keep the tokenizer forever because subword tokenizers cannot experience domain-dependent efficiency changes',
      ],
      answerIndex: 0,
      explanation: 'Subword tokenizers usually preserve coverage under domain shift, but segmentation efficiency can degrade. A new tokenizer may improve sequence length and throughput, yet token IDs define the interface into learned embeddings. Changing that interface requires retraining, remapping, or another validated adaptation strategy. The decision should compare measured efficiency gains against migration cost and model-quality risk.',
      misconceptionTested: 'Tokenizer vocabulary and model embeddings are independent components that can be swapped without affecting learned token identities or downstream behavior.',
    },
  ],
});
