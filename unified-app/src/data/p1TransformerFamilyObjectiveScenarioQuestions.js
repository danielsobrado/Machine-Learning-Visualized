export const P1_TRANSFORMER_FAMILY_OBJECTIVE_SCENARIOS_BY_LESSON = Object.freeze({
  'transformer-architecture-families': Object.freeze([
    Object.freeze({
      id: 'transformer-family-mask-mismatch-diagnosis',
      level: 'diagnosis',
      scenario: 'A sentiment classifier uses an encoder-style transformer, but a refactor accidentally applies a causal mask. For the sentence "The setup looked terrible, but the ending was excellent", early token representations can no longer use words to their right.',
      prompt: 'What is the most direct architecture fix for the intended fixed-input classification task?',
      choices: Object.freeze([
        'Restore bidirectional self-attention over the non-padding input tokens',
        'Keep the causal mask because every transformer should hide future input tokens',
        'Replace the classification head with autoregressive sampling while keeping the same mask',
      ]),
      answerIndex: 0,
      explanation: 'A fixed input is fully available before classification, so encoder-style bidirectional attention can legitimately use context from both sides. A causal mask unnecessarily removes information the task is allowed to use.',
      misconceptionTested: 'Causal masking is a universal transformer requirement rather than a visibility rule chosen to match the task and architecture family.',
      relatedComparison: 'Bidirectional encoder visibility versus causal decoder visibility.',
    }),
    Object.freeze({
      id: 'transformer-family-encoder-generation-mismatch',
      level: 'decision',
      scenario: 'A team has an encoder-only model with a pooled classification head. The new product requirement is open-ended chat completion that must append an unknown number of tokens to a prompt.',
      prompt: 'Which redesign most directly matches the new output contract?',
      choices: Object.freeze([
        'Use a decoder-style causal language model with a vocabulary head and autoregressive generation loop',
        'Keep the pooled classifier and map each class id to an arbitrarily long paragraph',
        'Make encoder attention more bidirectional so it can inspect future generated tokens before they exist',
      ]),
      answerIndex: 0,
      explanation: 'Open-ended completion requires repeated next-token prediction from the prefix. A causal decoder with vocabulary logits and an autoregressive loop has that behavior natively; a pooled encoder classifier does not.',
      misconceptionTested: 'Any transformer can perform free-form generation merely by changing the final label names.',
      relatedComparison: 'Representation-oriented encoder output versus autoregressive decoder output.',
    }),
    Object.freeze({
      id: 'transformer-family-missing-cross-attention',
      level: 'diagnosis',
      scenario: 'A translation system has a bidirectional source encoder and a causal target decoder. The decoder produces fluent target-language sentences, but they are often unrelated to the source. Inspection shows that decoder layers never read encoder states.',
      prompt: 'Which missing connection best explains this failure?',
      choices: Object.freeze([
        'Decoder cross-attention to the encoded source memory',
        'Future-target visibility inside decoder self-attention',
        'A second classification head over the source tokens',
      ]),
      answerIndex: 0,
      explanation: 'Causal target self-attention can make locally fluent continuations, but source-conditioned generation needs a path from decoder states to encoded source representations. Cross-attention supplies that path without exposing future target tokens.',
      misconceptionTested: 'An encoder and decoder become a source-conditioned sequence-to-sequence model merely by being placed next to each other.',
      relatedComparison: 'Decoder self-attention versus decoder-to-encoder cross-attention.',
    }),
    Object.freeze({
      id: 'transformer-family-three-task-routing',
      level: 'decision',
      scenario: 'You must choose starting architectures for three products: (1) classify a completed support ticket, (2) continue a user-written paragraph, and (3) translate a completed English sentence into Spanish.',
      prompt: 'Which family mapping best matches the information-flow requirements?',
      choices: Object.freeze([
        'Encoder-only for classification; decoder-only for continuation; encoder-decoder for translation',
        'Decoder-only for classification; encoder-only for continuation; encoder-only for translation',
        'Encoder-decoder for classification; encoder-only for continuation; decoder-only with future-target access for translation',
      ]),
      answerIndex: 0,
      explanation: 'Classification can use the whole fixed input bidirectionally, continuation needs prefix-only autoregressive decoding, and classic translation naturally separates a fully visible source from a causally generated target conditioned through cross-attention.',
      misconceptionTested: 'Architecture-family choice should follow model popularity instead of input availability and output generation requirements.',
      relatedComparison: 'Encoder-only, decoder-only, and encoder-decoder task contracts.',
    }),
  ]),
  'llm-training-objectives': Object.freeze([
    Object.freeze({
      id: 'llm-objective-next-token-shift-worked',
      level: 'calculation',
      scenario: 'A causal-LM example contains the four tokens [A, B, C, D] with no padding. Training uses the standard one-token shift and only within-sequence next-token targets.',
      prompt: 'Which prediction-target pairs contribute to the token loss?',
      choices: Object.freeze([
        'A→B, B→C, and C→D; the final D position has no within-sequence next-token target',
        'A→A, B→B, C→C, and D→D because language models reconstruct each input position',
        'Only C→D because causal language modeling computes one target per sequence',
      ]),
      answerIndex: 0,
      explanation: 'At position t, the model predicts token t+1 from the visible prefix. A four-token sequence therefore supplies three within-sequence next-token targets unless an additional continuation token is provided.',
      misconceptionTested: 'Causal language-model labels are aligned to the same token positions rather than shifted by one position.',
      relatedComparison: 'Next-token prediction versus same-position reconstruction.',
    }),
    Object.freeze({
      id: 'llm-objective-sft-response-loss-scope',
      level: 'diagnosis',
      scenario: 'An instruction-tuning record contains 120 prompt tokens followed by 30 assistant-response tokens. The intended objective is to condition on the prompt and train the assistant answer. A bug computes cross-entropy over all 150 tokens, including the user prompt.',
      prompt: 'What loss-scope change most directly matches the intended supervised fine-tuning objective?',
      choices: Object.freeze([
        'Mask prompt tokens from the supervised loss and keep loss on the assistant response tokens',
        'Remove the prompt from model context so only assistant tokens are visible',
        'Keep prompt-token loss because the model should learn to regenerate the user before answering',
      ]),
      answerIndex: 0,
      explanation: 'The prompt should remain visible as conditioning context, but response-only SFT commonly excludes prompt tokens from the loss. Context visibility and loss participation are separate concerns.',
      misconceptionTested: 'Any token visible to the model must also contribute to the supervised training loss.',
      relatedComparison: 'Conditioning tokens versus supervised target tokens in instruction tuning.',
    }),
    Object.freeze({
      id: 'llm-objective-preference-pair-context-bug',
      level: 'diagnosis',
      scenario: 'A preference dataset row labels response A as chosen for prompt X and response B as rejected for a different prompt Y. The training code treats A and B as one preference pair.',
      prompt: 'Why is this pair invalid for ordinary chosen-versus-rejected preference optimization?',
      choices: Object.freeze([
        'The responses are not alternatives for the same conditioning prompt, so the comparison does not isolate which answer is preferred for one input',
        'Preference optimization requires both responses to contain exactly the same tokens',
        'Rejected responses must always come from the pretrained model while chosen responses must always be human-written',
      ]),
      answerIndex: 0,
      explanation: 'A preference pair is meaningful when alternatives are judged under the same prompt context. Mixing prompts confounds prompt difficulty and response preference, so the relative training signal no longer represents the intended comparison.',
      misconceptionTested: 'Any chosen response and any rejected response can be paired regardless of the prompt that produced them.',
      relatedComparison: 'Within-prompt preference comparison versus unrelated examples.',
    }),
    Object.freeze({
      id: 'llm-objective-knowledge-vs-behavior-choice',
      level: 'decision',
      scenario: 'A base model follows instructions politely but lacks facts from a private product manual. The team proposes collecting only preference pairs that rank polite answers higher, without exposing the manual content anywhere in training or retrieval.',
      prompt: 'What is the core problem with that plan?',
      choices: Object.freeze([
        'Preference labels can shape response behavior, but they cannot reliably supply factual content the model never receives',
        'Preference optimization automatically imports missing facts from the prompt distribution',
        'The model must switch from token prediction to image reconstruction before it can learn private facts',
      ]),
      answerIndex: 0,
      explanation: 'Behavior shaping and knowledge exposure are different requirements. The private facts must enter through suitable training examples, retrieval, or another information path; preference-only data that never contains them cannot teach those facts reliably.',
      misconceptionTested: 'Alignment or preference tuning is a universal fact-injection mechanism.',
      relatedComparison: 'Behavior alignment versus factual knowledge coverage.',
    }),
    Object.freeze({
      id: 'llm-objective-preference-overoptimization',
      level: 'diagnosis',
      scenario: 'After preference tuning, evaluation shows responses are much longer and score better with the preference model, but factual accuracy and task success both decline. The preference dataset strongly favored verbose answers regardless of correctness.',
      prompt: 'What should the team diagnose first?',
      choices: Object.freeze([
        'Objective/data mismatch: the preference signal rewards verbosity more reliably than the real product goal',
        'The tokenizer must be broken because preference tuning cannot alter response length',
        'The causal mask should be removed so the model can inspect future correct answers',
      ]),
      answerIndex: 0,
      explanation: 'Optimization follows the supplied signal. If preference labels correlate with verbosity rather than factual task success, the model can improve the proxy while degrading the actual goal. Evaluation must therefore measure the intended behavior independently of the training reward.',
      misconceptionTested: 'Improving the preference-training score guarantees improvement on the real product objective.',
      relatedComparison: 'Training proxy optimization versus held-out task success.',
    }),
  ]),
});
