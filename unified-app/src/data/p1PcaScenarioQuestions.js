const scenarios = [
  {
    id: 'pca-explained-variance-threshold-worked',
    level: 'calculation',
    relatedComparison: 'fixed-component-count-vs-cumulative-explained-variance',
    scenario: 'A fitted PCA model has component variances [9, 4, 1, 1]. A compression policy chooses the smallest number of components that retain at least 90% of total variance.',
    prompt: 'What is the smallest acceptable component count k?',
    choices: [
      'k = 2 because the first two components explain 13/15 = 86.7%, which is already above 90%',
      'k = 3 because the first two explain only 86.7%, while the first three explain 14/15 = 93.3%',
      'k = 4 because any discarded component makes explained variance invalid',
    ],
    answerIndex: 1,
    explanation: 'Total variance is 9 + 4 + 1 + 1 = 15. Two components retain 13/15 = 86.7%, below the 90% policy. Three retain 14/15 = 93.3%, so k = 3 is the smallest acceptable choice.',
    misconceptionTested: 'A component-count decision can be made from the largest eigenvalues individually without checking the cumulative explained-variance threshold.',
  },
  {
    id: 'pca-reconstruction-error-worked',
    level: 'calculation',
    relatedComparison: 'retained-score-vs-discarded-residual-energy',
    scenario: 'A centered two-feature point is x = [3, 4]. A one-component PCA keeps only PC1 = [1, 0], so the retained score is 3 and the reconstruction in centered coordinates is [3, 0].',
    prompt: 'What is the squared reconstruction error for this point?',
    choices: [
      '9 because only the retained score is squared',
      '16 because the discarded residual is [0, 4] and its squared norm is 4²',
      '25 because PCA reconstruction error is always the squared norm of the original point',
    ],
    answerIndex: 1,
    explanation: 'The reconstruction keeps the horizontal component and discards the vertical component. The residual is [0, 4], whose squared Euclidean norm is 16. PCA compression error is the energy left outside the retained subspace.',
    misconceptionTested: 'PCA reconstruction error is determined by the retained score or by the full original vector norm rather than the discarded orthogonal residual.',
  },
  {
    id: 'pca-scale-dominance-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'covariance-pca-vs-standardized-pca',
    scenario: 'A PCA pipeline uses two equally important physical measurements. Feature A is recorded in meters and varies around 0.5, while feature B is the same type of magnitude recorded in millimeters and varies around 500. Without standardization, PC1 is almost entirely aligned with feature B.',
    prompt: 'What is the strongest diagnosis?',
    choices: [
      'The millimeter feature dominates covariance PCA because its numeric variance is much larger; standardization should be evaluated if equal unit influence is intended',
      'PC1 proves feature B is more predictive of every downstream label because PCA is supervised',
      'The component is invalid because PCA requires every feature to have identical raw units and cannot be used otherwise',
    ],
    answerIndex: 0,
    explanation: 'Covariance PCA follows variance in the supplied units. A unit conversion can therefore rotate the learned components even when the underlying physical information is comparable. Standardization changes the geometry to correlation-like PCA and should be chosen deliberately.',
    misconceptionTested: 'PCA is invariant to arbitrary feature units, so a large-scale feature cannot dominate merely because of its measurement scale.',
  },
  {
    id: 'pca-train-fit-leakage-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'fit-on-training-vs-fit-before-split',
    scenario: 'A team standardizes all rows, fits PCA on the full dataset, then splits the already transformed scores into train and test sets. The test labels were never used, and offline accuracy looks unusually stable.',
    prompt: 'What is wrong with this evaluation pipeline?',
    choices: [
      'Nothing: unsupervised preprocessing cannot leak because it does not use labels',
      'The test feature distribution influenced the fitted means, scales, and PCA directions, so preprocessing must be fit on training data only and then applied unchanged to test data',
      'The only problem is that PCA should be refit separately on every individual test row',
    ],
    answerIndex: 1,
    explanation: 'Leakage is not limited to labels. Fitting standardization or PCA before the split lets held-out feature statistics influence the representation used for training and evaluation. The complete preprocessing transform must be learned inside the training boundary.',
    misconceptionTested: 'Unsupervised transforms are automatically safe to fit on held-out rows because they never inspect target labels.',
  },
  {
    id: 'pca-near-equal-eigenspace-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'individual-component-vector-vs-principal-subspace-stability',
    scenario: 'Two retrainings produce nearly equal leading eigenvalues. In run A, PC1 and PC2 point along two particular orthogonal directions. In run B, those two vectors rotate substantially within the same two-dimensional plane, yet the 2D projections and reconstruction error are almost unchanged.',
    prompt: 'What should the monitoring system conclude?',
    choices: [
      'Treat the model as broken because every individual principal-component vector must remain numerically identical across retraining',
      'Compare the retained subspace or projections rather than raw component vectors, because near-tied eigenvalues allow unstable basis orientation within an otherwise stable principal subspace',
      'Force all component entries to be positive so the eigenvalues separate and the basis becomes unique',
    ],
    answerIndex: 1,
    explanation: 'When eigenvalues are close, small data changes can rotate the basis inside the nearly equal-variance subspace. Sign flips are also arbitrary. Stability checks should therefore distinguish basis-vector orientation from stability of the subspace and downstream representation.',
    misconceptionTested: 'A stable PCA representation requires each eigenvector to keep exactly the same sign and orientation across retraining, even when the associated eigenvalues are nearly tied.',
  },
  {
    id: 'pca-low-variance-label-signal-decision',
    level: 'decision',
    relatedComparison: 'explained-input-variance-vs-supervised-validation',
    scenario: 'Keeping 20 PCA components retains 99% of input variance, but a classifier trained on those scores loses 12 percentage points of recall. Inspection shows that a low-variance direction discarded by PCA strongly separates the positive class.',
    prompt: 'What is the best next decision?',
    choices: [
      'Keep the 20-component transform because 99% explained variance guarantees near-optimal supervised performance',
      'Revisit k or skip PCA for this model and choose the representation using held-out task performance, because PCA does not optimize label information',
      'Increase the explained-variance threshold only after fitting PCA on the validation labels so components become supervised',
    ],
    answerIndex: 1,
    explanation: 'Explained variance measures retained input variation, not predictive relevance. A low-variance direction can carry important class signal. Component count should therefore be validated against the downstream task and constraints rather than accepted solely from an unsupervised variance threshold.',
    misconceptionTested: 'Retaining almost all input variance guarantees preservation of the information needed by a downstream supervised model.',
  },
];

export const P1_PCA_SCENARIOS_BY_LESSON = Object.freeze({
  pca: Object.freeze(scenarios.map((scenario) => Object.freeze(scenario))),
});
