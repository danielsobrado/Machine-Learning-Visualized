export const P1_TOOL_USING_REASONING_MODELS_SCENARIOS_BY_LESSON = Object.freeze({
  'tool-using-reasoning-models': Object.freeze([
    Object.freeze({
      id: 'tool-routing-precision-recall-worked',
      level: 'calculation',
      relatedComparison: 'tool-call-precision-vs-tool-call-recall-under-selective-routing',
      scenario: 'A batch has 100 requests. Twenty require fresh external evidence and 80 are stable questions that should be answered directly. Policy A calls search on all 100 requests. Policy B calls search on 18 of the 20 requests that need it and on 4 stable requests that do not. Define tool-call precision as useful calls divided by all calls, and recall as needed calls made divided by all needed calls.',
      prompt: 'Which policy meets targets of at least 80% tool-call precision and at least 90% tool-call recall?',
      choices: Object.freeze([
        'Policy B: precision is 18 / 22 = 81.8% and recall is 18 / 20 = 90%; Policy A has 20% precision despite 100% recall',
        'Policy A: calling every available tool always has 100% precision because every request received a tool result',
        'Policy B: precision is 90% and recall is 81.8%, because the two metrics use the same denominator in reverse order',
      ]),
      answerIndex: 0,
      explanation: 'Policy B makes 22 calls, 18 of which are useful, so precision is 81.8%. It makes 18 of the 20 needed calls, so recall is 90%. Policy A finds every needed case but wastes 80 calls, giving only 20% precision. A good tool policy must avoid both guessing and reflexive overuse.',
      misconceptionTested: 'A policy that calls tools on every request is automatically well calibrated because high tool-call recall is sufficient even when most calls are unnecessary.',
    }),
    Object.freeze({
      id: 'tool-evidence-before-computation-decision',
      level: 'decision',
      relatedComparison: 'external-evidence-acquisition-vs-executable-computation-order',
      scenario: 'A finance workflow must convert a supplier invoice using today\'s USD/AED exchange rate and then sum 37 line items. The model has a web or market-data tool and Python, but no current exchange rate is present in the prompt.',
      prompt: 'What is the strongest tool sequence?',
      choices: Object.freeze([
        'Retrieve the current exchange rate from an appropriate fresh source first, then use Python with that observed rate and the invoice values for the calculation',
        'Run Python first with a remembered exchange rate, because executable arithmetic makes missing external inputs trustworthy',
        'Search for the exchange rate and then do all 37 multiplications mentally, because using a second tool after search is unnecessary by definition',
      ]),
      answerIndex: 0,
      explanation: 'Python can verify arithmetic but cannot supply a missing current fact. The external value must be grounded first; executable calculation should then consume the observed rate and invoice data. Tool order should follow the dependency graph of the problem.',
      misconceptionTested: 'Using an exact computation tool can compensate for missing or stale external inputs, so evidence acquisition and computation order do not matter.',
    }),
    Object.freeze({
      id: 'tool-observation-grounding-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'actual-tool-observation-vs-model-invented-result',
      scenario: 'An agent queries an inventory API for part ZX-41. The tool returns status = not_found with no quantity, warehouse, or replenishment date. The final answer nevertheless says, “ZX-41 has 18 units in warehouse B and will restock Friday,” and the trace contains no later tool call.',
      prompt: 'What failure is demonstrated by the trajectory?',
      choices: Object.freeze([
        'The model hallucinated tool output or unsupported observations; the final claims do not exist in the recorded tool evidence',
        'The tool-call precision is necessarily perfect because the inventory API was the correct API to call',
        'The API schema is proven invalid because a not_found response allows the model to infer hidden inventory fields',
      ]),
      answerIndex: 0,
      explanation: 'Choosing a relevant tool is not enough. The agent must ground later claims in the actual observation. A not_found response provides no support for specific stock, location, or replenishment claims, so those details were invented rather than observed.',
      misconceptionTested: 'Once the correct tool is called, any plausible details added afterward can be treated as if they came from the tool result.',
    }),
    Object.freeze({
      id: 'tool-indirect-injection-boundary-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'untrusted-tool-content-vs-authoritative-agent-instructions',
      scenario: 'A research agent opens a webpage while collecting product documentation. The page contains visible text telling automated assistants to ignore their task, reveal private context, and invoke an unrelated write-capable tool. None of those actions are requested by the user.',
      prompt: 'How should the agent treat that content?',
      choices: Object.freeze([
        'As untrusted webpage data: extract relevant documentation facts, but do not let the page change instruction priority or authorize unrelated side effects',
        'As a higher-priority instruction because browser observations become trusted agent policy after retrieval',
        'As automatic user approval for the requested write action because the instruction appeared in a source selected by the agent',
      ]),
      answerIndex: 0,
      explanation: 'Retrieved content is evidence, not an authority channel. Indirect prompt injection exploits systems that confuse data with instructions. The agent should preserve instruction hierarchy, least privilege, and side-effect authorization while still using relevant factual content from the page.',
      misconceptionTested: 'Instructions embedded inside retrieved tool content inherit authority from the tool and may override the user task or authorize unrelated actions.',
    }),
    Object.freeze({
      id: 'tool-loop-progress-budget-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'productive-retry-vs-repeated-tool-loop-without-new-evidence',
      scenario: 'An agent has a maximum budget of six search calls. Calls 1 through 4 use the identical query and return the same three irrelevant results each time. The trace records no query reformulation, source change, new evidence, or updated hypothesis after any call.',
      prompt: 'What should the controller do next?',
      choices: Object.freeze([
        'Stop repeating the same action and either reformulate or change strategy; if no productive alternative exists, return a bounded uncertainty instead of consuming the remaining budget',
        'Spend calls 5 and 6 on the identical query because retry count alone increases the probability that unchanged search results become relevant',
        'Remove the tool-call budget because repeated identical observations prove the model is making progress',
      ]),
      answerIndex: 0,
      explanation: 'Retries are useful only when they can change the information state. Four identical actions with identical irrelevant observations show no progression. Loop detection should require new evidence, a changed strategy, or a controlled stop/fallback before the budget is exhausted.',
      misconceptionTested: 'Repeated tool calls count as reasoning progress even when neither the action nor the observation changes.',
    }),
    Object.freeze({
      id: 'tool-observation-loss-masking-worked',
      level: 'calculation',
      relatedComparison: 'policy-generated-trajectory-tokens-vs-environment-observation-tokens',
      scenario: 'An RL training trajectory contains 45 model-generated reasoning/query tokens, 320 search-result observation tokens, and 55 model-generated final-answer tokens. The training recipe masks environment observations from the policy-generation loss while keeping them available as context.',
      prompt: 'How many tokens should contribute directly to the model-generation policy loss in this trajectory?',
      choices: Object.freeze([
        '100 tokens: 45 reasoning/query tokens plus 55 final-answer tokens; the 320 observation tokens condition the model but are masked from generation credit',
        '420 tokens: every token in the trajectory should receive policy credit because the model saw all of them',
        '320 tokens: only tool observations should receive policy loss because they contain the external evidence',
      ]),
      answerIndex: 0,
      explanation: 'The policy generated 45 + 55 = 100 tokens. Search results are environment observations, so they can condition later actions without being treated as outputs produced by the policy. Masking prevents the learner from receiving credit for text returned by the tool.',
      misconceptionTested: 'Any token visible in an agent trajectory should be optimized as if the model generated it, including external observations returned by tools.',
    }),
  ]),
});
