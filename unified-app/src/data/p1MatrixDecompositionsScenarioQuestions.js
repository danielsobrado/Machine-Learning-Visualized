export const P1_MATRIX_DECOMPOSITIONS_SCENARIOS_BY_LESSON = Object.freeze({
  'matrix-decompositions': Object.freeze([
    Object.freeze({
      id: 'decomp-lu-pivoting-worked',
      level: 'calculation',
      relatedComparison: 'unpivoted-lu-vs-partial-pivoting',
      scenario: 'A solver must handle A = [[0, 2], [1, 3]] and b = [4, 5]. An unpivoted LU routine immediately encounters a zero first pivot. Partial pivoting swaps the two rows before elimination.',
      prompt: 'After the row swap, what solution should the triangular solve recover, and why was pivoting necessary?',
      choices: [
        'x = [-1, 2]; pivoting moves the nonzero entry into the first pivot position so elimination can proceed',
        'x = [2, -1]; pivoting changes the mathematical system to a different one',
        'x = [4, 5]; a row swap removes the need for substitution',
      ],
      answerIndex: 0,
      explanation: 'After swapping rows, the equations are x1 + 3x2 = 5 and 2x2 = 4. Thus x2 = 2 and x1 = -1. Applying the same permutation to A and b preserves the original system while avoiding the zero pivot that breaks unpivoted elimination.',
      misconceptionTested: 'Pivoting changes the problem being solved, or a zero pivot means the original linear system has no solution.',
    }),
    Object.freeze({
      id: 'decomp-cholesky-indefinite-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'symmetric-vs-symmetric-positive-definite',
      scenario: 'A team sends A = [[1, 2], [2, 1]] to Cholesky because A is symmetric. The first diagonal step succeeds, but the next diagonal quantity becomes 1 - 2^2 = -3, so the implementation cannot take a real square root.',
      prompt: 'What is the correct diagnosis and safer next step?',
      choices: [
        'Symmetry alone is insufficient: A is indefinite, so use a factorization appropriate for indefinite/general systems rather than forcing Cholesky',
        'The matrix is automatically positive definite because both diagonal entries are positive, so the negative pivot should be clipped to zero',
        'Cholesky only fails because the right-hand side was not normalized',
      ],
      answerIndex: 0,
      explanation: 'A symmetric matrix can still be indefinite. Here the eigenvalues are 3 and -1, and the negative Schur-complement pivot exposes that violation directly. Production code should validate SPD assumptions or fall back to an indefinite/general solver instead of hiding the failure.',
      misconceptionTested: 'Symmetry or positive diagonal entries alone guarantee positive definiteness and make Cholesky safe.',
    }),
    Object.freeze({
      id: 'decomp-normal-equations-conditioning-worked',
      level: 'calculation',
      relatedComparison: 'normal-equations-vs-qr-svd-conditioning',
      scenario: 'A tall least-squares design matrix has condition number kappa(A) = 10^6. A proposed implementation forms A^T A explicitly and solves the normal equations.',
      prompt: 'Approximately what condition number should the team expect for A^T A, and what decomposition is the safer direction?',
      choices: [
        'About 10^12; QR or SVD avoids squaring the condition number',
        'About 10^6; forming A^T A preserves conditioning exactly',
        'About 10^3; normal equations improve conditioning by taking a square root',
      ],
      answerIndex: 0,
      explanation: 'For the 2-norm, kappa(A^T A) is approximately kappa(A)^2 when A has full column rank. Squaring 10^6 gives 10^12, which can dramatically amplify floating-point sensitivity. QR or SVD is the safer numerical route.',
      misconceptionTested: 'Normal equations are numerically equivalent to QR/SVD because they produce the same exact-arithmetic least-squares minimizer.',
    }),
    Object.freeze({
      id: 'decomp-svd-tolerance-rank-worked',
      level: 'decision',
      relatedComparison: 'literal-nonzero-singular-values-vs-numerical-rank',
      scenario: 'An SVD-based inverse sees singular values [10, 1, 1e-8]. The data and floating-point policy treat singular values below 1e-6 as unresolved noise directions.',
      prompt: 'What numerical rank and inverse policy best match that stated tolerance?',
      choices: [
        'Use numerical rank 2 and truncate or regularize the 1e-8 direction instead of inverting it',
        'Use rank 3 and invert 1e-8 to 1e8 because every nonzero singular value must be trusted equally',
        'Use rank 1 because only the largest singular value can be retained in a stable SVD',
      ],
      answerIndex: 0,
      explanation: 'The tolerance defines which singular directions are treated as numerically resolved. The 1e-8 value falls below 1e-6, so using rank 2 avoids multiplying noise in that direction by roughly 1e8 during inversion.',
      misconceptionTested: 'Numerical rank is always the literal count of mathematically nonzero singular values regardless of scale, noise, or tolerance.',
    }),
    Object.freeze({
      id: 'decomp-defective-eigenbasis-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'eigendecomposition-vs-schur-for-defective-matrix',
      scenario: 'A dynamics implementation tries to diagonalize A = [[1, 1], [0, 1]]. The matrix has repeated eigenvalue 1, but the eigensolver returns only one independent eigenvector and V cannot be inverted reliably.',
      prompt: 'What should the engineer conclude?',
      choices: [
        'A is defective and does not have a full eigenvector basis; use a more robust square-matrix form such as Schur rather than forcing V Lambda V^-1',
        'Every square matrix is diagonalizable, so duplicate the one eigenvector to make V square',
        'The failure proves A should be converted to a nonnegative matrix and solved with NMF',
      ],
      answerIndex: 0,
      explanation: 'A Jordan block with repeated eigenvalue 1 has only one independent eigenvector. Duplicating that vector makes V singular, so diagonalization is not valid. Schur decomposition provides a numerically robust representation without requiring a complete eigenbasis.',
      misconceptionTested: 'Every square matrix has a complete, well-conditioned eigenvector basis suitable for diagonalization.',
    }),
    Object.freeze({
      id: 'decomp-nmf-multistart-stability-design',
      level: 'design',
      relatedComparison: 'single-nmf-run-vs-multistart-stability',
      scenario: 'Three NMF runs on the same nonnegative matrix and rank produce reconstruction errors 1.8, 1.2, and 1.6, with visibly different parts. The pipeline currently keeps the first run and presents its factors as unique explanations.',
      prompt: 'What should the production evaluation do instead?',
      choices: [
        'Run multiple initializations, select by the stated objective/validation rule, and report factor stability because NMF is non-convex and need not have unique factors',
        'Keep the first run because nonnegativity guarantees a unique global optimum',
        'Average the factor matrices elementwise without aligning components because factor ordering and local optima cannot differ',
      ],
      answerIndex: 0,
      explanation: 'NMF is a non-convex optimization problem. Different starts can reach different local solutions, and factor permutations/scalings can complicate direct comparison. Multi-start selection and stability checks are therefore part of a defensible interpretation workflow.',
      misconceptionTested: 'Nonnegative constraints make NMF factors unique and initialization-independent.',
    }),
  ]),
});
