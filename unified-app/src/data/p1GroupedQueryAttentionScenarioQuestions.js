export const P1_GROUPED_QUERY_ATTENTION_SCENARIO_QUESTIONS = Object.freeze([
  Object.freeze({
    id: 'gqa-group-mapping-broadcast-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'contiguous-gqa-grouping-vs-wrong-modulo-broadcast',
    scenario: 'A checkpoint was trained with 32 query heads and 8 KV heads using contiguous groups of four query heads per KV head. A custom kernel instead maps query head h to KV head h % 8. Shapes are valid and the kernel runs, but quality drops sharply.',
    prompt: 'What is the most likely implementation failure?',
    choices: [
      'The kernel is reading the wrong KV head for most query heads because its mapping does not match the checkpoint grouping layout',
      'GQA requires every query head to have a unique KV head, so 32 query heads and 8 KV heads is invalid',
      'The model should remove causal masking whenever KV heads are shared',
    ],
    answerIndex: 0,
    explanation: 'For contiguous groups of four, query heads 0-3 use KV head 0, heads 4-7 use KV head 1, and so on. A modulo mapping creates a different sharing pattern even though tensor shapes still look legal.',
    misconceptionTested: 'Any broadcast from fewer KV heads to more query heads is equivalent as long as the dimensions match.',
  }),
  Object.freeze({
    id: 'gqa-serving-capacity-worked',
    level: 'calculation',
    relatedComparison: 'mha-vs-gqa-cache-concurrency',
    scenario: 'A serving GPU has 24 GiB available for KV cache after model weights and runtime buffers. Under MHA, each active long-context request needs 6 GiB of KV cache. The GQA version reduces KV width by 4x, so each request needs 1.5 GiB. Ignore allocator overhead for this calculation.',
    prompt: 'What maximum KV-cache-limited concurrency does each design support?',
    choices: [
      'MHA supports 4 requests and GQA supports 16 requests',
      'MHA supports 6 requests and GQA supports 24 requests',
      'Both support 4 requests because query-head count is unchanged',
    ],
    answerIndex: 0,
    explanation: '24 / 6 = 4 active MHA requests, while 24 / 1.5 = 16 GQA requests. Reducing KV width can directly increase serving capacity when cache memory is the binding resource.',
    misconceptionTested: 'GQA cache savings cannot increase batch or concurrency because the number of query heads stays fixed.',
  }),
  Object.freeze({
    id: 'gqa-bandwidth-reduction-worked',
    level: 'calculation',
    relatedComparison: 'mha-vs-gqa-decode-bandwidth-floor',
    scenario: 'At a long context, an MHA decoder reads about 3.2 GiB of cached K/V per generated token. An otherwise equivalent GQA decoder with one quarter as many KV heads reads about 0.8 GiB. Assume this cache traffic is the dominant bottleneck and memory bandwidth is unchanged.',
    prompt: 'What does the GQA change imply for the cache-read portion of decode time?',
    choices: [
      'It cuts cache traffic by 4x, so the bandwidth-limited cache-read time can fall to about one quarter, though total TPOT need not improve by a full 4x',
      'It cuts cache traffic by only 25%, so cache-read time can improve by at most 1.25x',
      'It cannot affect decode time because GQA changes storage but not memory traffic',
    ],
    answerIndex: 0,
    explanation: '0.8 GiB is one quarter of 3.2 GiB. If K/V reads dominate, that component has a fourfold smaller byte requirement. Other compute, synchronization, and memory traffic prevent assuming a guaranteed fourfold end-to-end speedup.',
    misconceptionTested: 'Reducing KV-cache width changes capacity but cannot reduce decode bandwidth pressure.',
  }),
  Object.freeze({
    id: 'gqa-checkpoint-conversion-decision',
    level: 'decision',
    relatedComparison: 'direct-mha-head-merging-vs-gqa-uptraining',
    scenario: 'A team converts a strong 32-head MHA checkpoint to 8 KV heads by averaging each group of four K/V projections. Cache memory improves as expected, but task quality drops across several benchmarks while tensor shapes and kernels are correct.',
    prompt: 'What is the most defensible next step?',
    choices: [
      'Uptrain or fine-tune the converted GQA model so the network can adapt to shared K/V representations, while measuring whether quality recovers at the target serving savings',
      'Declare the conversion complete because correct cache size proves the architecture is behaviorally equivalent',
      'Delete query heads until query and KV head counts match again, which preserves the same GQA design',
    ],
    answerIndex: 0,
    explanation: 'Averaging or merging K/V heads changes representational capacity. Correct shapes and memory savings do not guarantee a converted checkpoint will preserve behavior without adaptation.',
    misconceptionTested: 'MHA-to-GQA conversion is purely a tensor-shape rewrite and never requires training adaptation.',
  }),
  Object.freeze({
    id: 'gqa-aggressive-sharing-quality-diagnosis',
    level: 'diagnosis',
    relatedComparison: 'intermediate-gqa-vs-mqa-quality-slices',
    scenario: 'A 32-query-head model is tested with 8 KV heads and then 1 KV head. Moving from 8 to 1 KV head improves memory and TPOT slightly more, but long-context multi-entity retrieval accuracy falls from 91% to 78% while short prompts barely change.',
    prompt: 'What conclusion best fits the evidence?',
    choices: [
      'The one-KV-head design is sharing K/V too aggressively for this workload; an intermediate GQA point may be the better quality-serving trade-off',
      'The quality drop proves KV-cache memory is unrelated to attention-head sharing',
      'Because short prompts are stable, the long-context regression should be ignored when choosing the production architecture',
    ],
    answerIndex: 0,
    explanation: 'MQA-style maximal sharing can remove useful K/V specialization. The correct operating point should be selected from measured quality and serving metrics, including the long-context slices that expose the regression.',
    misconceptionTested: 'The fewest possible KV heads is always optimal once memory and latency improve.',
  }),
]);
