function q(value) { return Object.freeze({ ...value, choices: Object.freeze([...value.choices]) }); }
function lesson(items) { return Object.freeze(items.map(q)); }

export const P1_RL_DIFFUSION_SYSTEMS_REVIEW_SCENARIOS_BY_LESSON = Object.freeze({
  'q-learning': lesson([{
    id: 'review-qlearning-off-policy-target', kind: 'visual-state', visualState: Object.freeze({ behaviorPolicy: 'epsilon-greedy', updateTarget: 'r + gamma max_a Q(s_next,a)', targetPolicy: 'greedy' }), level: 'mechanism',
    relatedComparison: 'behavior-policy-action-vs-greedy-target-policy',
    scenario: 'An agent follows an epsilon-greedy behavior policy and sometimes takes exploratory actions. Its update target is r + gamma * max_a Q(s_next,a) for non-terminal transitions.',
    prompt: 'Why is ordinary Q-learning described as off-policy?',
    choices: ['The behavior policy may take exploratory actions, while the bootstrap target evaluates the greedy action implied by Q rather than the action the behavior policy would necessarily take next', 'The agent never uses data generated by its own policy', 'Off-policy means rewards are ignored during learning'],
    answerIndex: 0,
    explanation: 'Q-learning can learn toward the greedy target policy while data are generated by another behavior policy such as epsilon-greedy. The max operator in the target selects the action with highest current Q-value rather than sampling the next behavior action. That separation is the core off-policy property.',
    misconceptionTested: 'An algorithm is on-policy whenever its experience was generated by the same agent, regardless of which policy its update target evaluates.',
  }]),
  'policy-gradients': lesson([{
    id: 'review-policy-gradient-baseline-unbiased', level: 'mechanism',
    relatedComparison: 'raw-return-reinforce-vs-baseline-centered-advantage',
    scenario: 'REINFORCE has high variance, so the team subtracts a state-dependent baseline b(s) from the return before multiplying by the score-function gradient. The baseline does not depend on the sampled action.',
    prompt: 'Why can this reduce variance without changing the expected policy gradient?',
    choices: ['The expected score-function gradient times an action-independent baseline is zero, so centering changes variance but not the expectation', 'The baseline makes every sampled return exactly correct and removes all stochasticity', 'Any baseline changes the objective unless it is identically zero'],
    answerIndex: 0,
    explanation: 'For a fixed state, the policy-weighted expectation of grad log pi(a|s) is zero. Multiplying that by a baseline independent of action therefore adds a zero-mean term. Choosing a good baseline can substantially reduce gradient variance while preserving the expected policy-gradient direction.',
    misconceptionTested: 'Variance reduction with a baseline necessarily biases the policy gradient because the sampled learning signal is numerically changed.',
  }]),
  'actor-critic': lesson([{
    id: 'review-actor-critic-bias-variance-bootstrap', level: 'comparison',
    relatedComparison: 'monte-carlo-return-vs-bootstrapped-critic-target',
    scenario: 'A policy-gradient system replaces full Monte Carlo returns with an advantage estimate that bootstraps from a learned value function. Training becomes less noisy, but the critic is imperfect.',
    prompt: 'What tradeoff has the actor-critic design introduced?',
    choices: ['Bootstrapping usually reduces variance but can introduce bias when the learned value estimate is inaccurate', 'Bootstrapping removes both bias and variance by definition', 'Using a critic affects only runtime speed and not the statistical properties of the gradient estimate'],
    answerIndex: 0,
    explanation: 'Monte Carlo returns are unbiased under standard assumptions but can be extremely noisy. A learned critic supplies a lower-variance baseline and bootstrapped estimate, but approximation error in that critic can bias the advantage signal. Actor-critic methods manage this bias-variance tradeoff rather than eliminating it.',
    misconceptionTested: 'A learned critic is purely an optimization accelerator with no effect on bias or variance of the learning signal.',
  }]),
  'ppo-clipped-policy-gradient': lesson([{
    id: 'review-ppo-clipping-update-control', level: 'mechanism',
    relatedComparison: 'unconstrained-policy-ratio-vs-clipped-surrogate',
    scenario: 'During a PPO update, some action probability ratios move far from 1 even though the collected advantages are positive. The clipped surrogate stops rewarding ratios beyond a configured neighborhood.',
    prompt: 'What failure mode is clipping intended to reduce?',
    choices: ['Destructively large policy updates that exploit one batch of advantage estimates and move the new policy too far from the behavior policy', 'All stochastic exploration in the policy', 'Value-function approximation error by forcing the critic to zero'],
    answerIndex: 0,
    explanation: 'PPO uses the probability ratio between new and old policies. The clipped objective limits the incentive to keep increasing that ratio once it moves beyond a trust-region-like range, reducing instability from overly aggressive updates on one batch. It does not guarantee an exact KL bound, so practitioners may still monitor KL.',
    misconceptionTested: 'PPO clipping exists mainly to remove exploration or directly repair value-function errors.',
  }]),
  'reward-shaping': lesson([{
    id: 'review-reward-shaping-potential-policy-invariance', level: 'design',
    relatedComparison: 'arbitrary-dense-bonus-vs-potential-based-shaping',
    scenario: 'A sparse-reward navigation task is difficult to explore. The team wants to add denser shaping feedback without accidentally changing which policy is optimal in the original task.',
    prompt: 'Which shaping construction has a known policy-invariance guarantee under standard conditions?',
    choices: ['Potential-based shaping of the form gamma*Phi(s_next)-Phi(s)', 'An arbitrary positive reward for every step so longer trajectories are preferred', 'A bonus chosen after training based on which policy happened to win'],
    answerIndex: 0,
    explanation: 'Potential-based reward shaping adds a difference in state potentials that telescopes through returns in a way that preserves the ordering of optimal policies under the usual formulation. Arbitrary bonuses can create reward hacking or favor behavior that was not optimal under the original objective.',
    misconceptionTested: 'Any dense shaping reward is harmless as long as it makes training faster.',
  }]),
  'diffusion-basics': lesson([{
    id: 'review-diffusion-prediction-parameterizations', level: 'comparison',
    relatedComparison: 'epsilon-prediction-vs-x0-prediction-vs-v-prediction',
    scenario: 'Three diffusion implementations train networks to predict different but algebraically related targets from a noisy sample: the added noise epsilon, the clean sample x0, or a velocity-style v parameterization.',
    prompt: 'What is the important conceptual point when comparing these parameterizations?',
    choices: ['They encode related information under the known noise schedule, but their scaling and optimization behavior can differ across noise levels even when they describe the same underlying denoising process', 'Only epsilon prediction corresponds to a valid diffusion model; x0 and v cannot be converted into denoising estimates', 'The target choice changes the forward diffusion distribution itself after training begins'],
    answerIndex: 0,
    explanation: 'Given the schedule and noisy sample, epsilon, x0, and v parameterizations can be transformed into one another algebraically. Training dynamics are not necessarily identical because target scales and signal-to-noise weighting differ across timesteps. The distinction is about parameterization of the reverse prediction problem, not inventing a different forward corruption process.',
    misconceptionTested: 'Different diffusion prediction targets are unrelated model families or only one of them can represent the same reverse process.',
  }]),
  'classifier-free-guidance': lesson([{
    id: 'review-cfg-guidance-diversity-artifacts', level: 'decision',
    relatedComparison: 'conditional-alignment-strength-vs-diversity-and-artifacts',
    scenario: 'A text-to-image system raises classifier-free guidance scale far above the value used in validation. Prompt adherence initially improves, then samples become oversaturated and less diverse.',
    prompt: 'What tradeoff is being exposed?',
    choices: ['Stronger guidance pushes sampling more aggressively toward the conditional direction, which can improve alignment but reduce diversity and amplify artifacts when overdone', 'Guidance scale changes only inference speed and cannot affect image distribution', 'Higher guidance always improves every quality dimension monotonically'],
    answerIndex: 0,
    explanation: 'Classifier-free guidance extrapolates between unconditional and conditional predictions. Increasing the scale strengthens conditioning pressure, but excessive extrapolation can push samples away from the natural model distribution, reducing diversity and causing visual artifacts or saturation. The best operating point is therefore task-dependent.',
    misconceptionTested: 'Classifier-free guidance has a one-directional benefit where larger scale is always better if compute is unchanged.',
  }]),
  'flow-matching': lesson([{
    id: 'review-flow-matching-vector-field-ode', level: 'mechanism',
    relatedComparison: 'learned-vector-field-transport-vs-stepwise-noise-removal',
    scenario: 'A flow-matching model learns a time-dependent vector field that transports samples from a simple base distribution toward the data distribution. At inference, samples follow an ODE defined by that field.',
    prompt: 'What viewpoint distinguishes this from the usual informal picture of diffusion sampling?',
    choices: ['The model directly learns a continuous transport velocity field and generation integrates that field through time, rather than being described primarily as repeatedly predicting and removing added noise', 'Flow matching requires a discrete Markov chain with no continuous-time interpretation', 'Flow matching generates samples by nearest-neighbor lookup from the training set'],
    answerIndex: 0,
    explanation: 'Flow matching frames generative modeling as learning a vector field whose trajectories transform one distribution into another. Sampling solves the corresponding ODE numerically. Diffusion and flow formulations are related mathematically, but the vector-field transport view changes how objectives and samplers are derived and reasoned about.',
    misconceptionTested: 'Flow matching is just a different name for discrete nearest-neighbor denoising and has no dynamical-system interpretation.',
  }]),
  'bloom-filter': lesson([{
    id: 'review-bloom-filter-false-positive-asymmetry', level: 'mechanism',
    relatedComparison: 'possible-false-positive-vs-no-false-negative-after-insertion',
    scenario: 'A correctly implemented Bloom filter inserts an item by setting every bit selected by its hash functions. Later, membership queries inspect those same bit positions. Bit collisions from other items are common.',
    prompt: 'Which error pattern follows from this structure under ordinary operation without deletions?',
    choices: ['A non-member can be a false positive because collisions may set all queried bits, while an inserted member should not be a false negative', 'Inserted members are often false negatives but non-members can never be false positives', 'Bloom filters provide exact membership because hashing removes collisions'],
    answerIndex: 0,
    explanation: 'Insertion only turns bits on. Therefore every bit required by an inserted item remains on, so a standard Bloom filter does not produce false negatives unless the structure is corrupted or modified by unsupported deletion. Other items can coincidentally set the same bits, producing false positives.',
    misconceptionTested: 'Probabilistic membership means Bloom filters have symmetric false-positive and false-negative errors.',
  }]),
  pagerank: lesson([{
    id: 'review-pagerank-damping-dangling', level: 'mechanism',
    relatedComparison: 'raw-random-walk-vs-teleporting-ergodic-chain',
    scenario: 'A web graph contains a dangling page with no outgoing links and a tightly connected component with no links leaving it. A raw random walk can become ill-defined at the dangling page or trapped in a closed component.',
    prompt: 'Why does PageRank introduce damping and teleportation, together with dangling-node handling?',
    choices: ['They produce a well-defined stochastic process with a route out of sinks and closed components, supporting a stable stationary ranking under standard conditions', 'They make every page receive exactly equal rank regardless of links', 'They remove the need to normalize outgoing link probabilities'],
    answerIndex: 0,
    explanation: 'Teleportation mixes link-following with a positive probability of jumping according to a chosen distribution. Dangling mass is redistributed rather than disappearing. This avoids absorbing traps and makes the transition process ergodic under typical assumptions, enabling power iteration to converge to a unique stationary distribution.',
    misconceptionTested: 'Damping is only a numerical speed trick and has no role in defining a robust random-walk model on real graphs.',
  }]),
  'recommender-systems-ranking-track': lesson([{
    id: 'review-recommender-missing-feedback-not-negative', level: 'diagnosis',
    relatedComparison: 'explicit-negative-label-vs-unobserved-implicit-feedback',
    scenario: 'A recommendation system logs clicks but not explicit dislikes. The training pipeline labels every item the user did not click as a negative example, even though most items were never shown to that user.',
    prompt: 'Why is that label construction risky?',
    choices: ['Missing interaction mixes true disinterest with lack of exposure, so treating every unobserved item as a negative creates biased implicit-feedback labels', 'Every unseen item is known to be disliked because no click exists', 'Implicit-feedback systems cannot use negative sampling at all'],
    answerIndex: 0,
    explanation: 'A click requires both exposure and interest, while a missing click can mean the item was never presented, was presented too low, or was seen and ignored. Recommender training therefore distinguishes unobserved from explicit negatives and often uses exposure-aware weighting or sampled negatives with careful evaluation.',
    misconceptionTested: 'Absence of a positive interaction is equivalent to an observed negative preference.',
  }]),
  'time-series-forecasting-track': lesson([{
    id: 'review-timeseries-random-kfold-leakage', level: 'design',
    relatedComparison: 'random-kfold-exchangeability-vs-forward-chaining-temporal-validation',
    scenario: 'A forecasting model predicts next-month demand from lagged features. Standard random K-fold places rows from 2027 in training while rows from 2025 appear in validation, even though deployment always predicts the future from the past.',
    prompt: 'What validation design better matches deployment?',
    choices: ['Use chronological holdouts or rolling/expanding-window evaluation so every validation period occurs after the data used to fit its model', 'Use more random folds so future information is averaged out', 'Shuffle dates before feature construction to make samples independent'],
    answerIndex: 0,
    explanation: 'Forecasting has an intrinsic information direction. Random folds can let future regimes, transformed statistics, or related lag structure influence models evaluated on earlier periods. Forward-chaining or rolling backtests preserve the train-before-test ordering and better estimate future deployment behavior.',
    misconceptionTested: 'Random K-fold is universally valid once lag features have been created, even when the real prediction task is time-ordered.',
  }]),
  'ml-security-robustness-track': lesson([{
    id: 'review-security-threat-model-boundaries', level: 'comparison',
    relatedComparison: 'training-time-poisoning-vs-inference-evasion-vs-prompt-injection',
    scenario: 'Three incidents occur: an attacker inserts malicious examples into training data, another perturbs an image at inference to cause misclassification, and a retrieved web page tells an LLM tool agent to ignore system instructions and exfiltrate data.',
    prompt: 'Which threat-model mapping is correct?',
    choices: ['Training-data poisoning, inference-time evasion/adversarial example, and indirect prompt injection respectively', 'All three are data poisoning because malicious input exists somewhere', 'The first is prompt injection, the second is model extraction, and the third is ordinary distribution shift'],
    answerIndex: 0,
    explanation: 'Security controls depend on when and where the adversary acts. Poisoning corrupts the learning process, evasion manipulates inference inputs to induce wrong predictions, and prompt injection exploits instruction-following channels—often through untrusted retrieved content or tools. Conflating them leads to the wrong mitigations.',
    misconceptionTested: 'All adversarial ML failures are interchangeable and can be mitigated with the same generic robustness technique.',
  }]),
});
