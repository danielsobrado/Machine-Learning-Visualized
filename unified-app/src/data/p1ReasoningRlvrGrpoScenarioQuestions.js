export const P1_REASONING_RLVR_GRPO_SCENARIOS_BY_LESSON = Object.freeze({
  'reasoning-rlvr-grpo': Object.freeze([
    Object.freeze({
      id: 'rlvr-grpo-normalized-advantage-worked',
      level: 'calculation',
      relatedComparison: 'raw-reward-vs-group-normalized-advantage',
      scenario: 'A GRPO rollout group has verifier rewards [0, 1, 1, 2]. For this exercise use population standard deviation and A_i = (r_i - mean(r)) / std(r), with epsilon omitted because the spread is nonzero. The group mean is 1 and the population standard deviation is sqrt(0.5), about 0.707.',
      prompt: 'What normalized advantage does the reward-2 completion receive?',
      choices: Object.freeze([
        'About +1.41, because (2 - 1) / 0.707 is about 1.41',
        '+2.00, because GRPO uses raw verifier reward directly as advantage',
        '0.00, because the best completion becomes the group baseline and cannot have positive advantage',
      ]),
      answerIndex: 0,
      explanation: 'GRPO centers a completion against sibling rewards and scales by group spread. Here the reward-2 sample is one point above the mean, so its normalized advantage is about 1 / 0.707 = 1.41 rather than the raw reward 2.',
      misconceptionTested: 'Group-relative optimization can treat raw verifier reward as the policy advantage without centering or scaling by sibling outcomes.',
    }),
    Object.freeze({
      id: 'rlvr-all-negative-relative-trap-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'relative-ranking-vs-absolute-task-success',
      scenario: 'A hard math prompt produces four wrong answers. The correctness verifier gives all four zero, but auxiliary style rewards make their totals [0.10, 0.15, 0.20, 0.55]. GRPO therefore assigns the polished 0.55 response a positive relative advantage even though its mathematical answer is still wrong.',
      prompt: 'What should the training system do before treating that positive advantage as useful learning signal?',
      choices: Object.freeze([
        'Respect absolute task failure: resample, adjust task difficulty or group diversity, and prevent auxiliary reward from reinforcing an all-wrong group as if it contained a success',
        'Increase the style reward further because any positive relative advantage proves the response solved the prompt',
        'Disable correctness verification so the group-relative ranking becomes the only source of truth',
      ]),
      answerIndex: 0,
      explanation: 'Relative ranking can identify the least bad sibling without establishing that any sibling succeeded. When all candidates fail the actual task, auxiliary differences must not silently turn a wrong response into a training target; the batch needs better contrast or an absolute-success guardrail.',
      misconceptionTested: 'A positive GRPO advantage implies absolute correctness even when every completion in the group fails the task verifier.',
    }),
    Object.freeze({
      id: 'rlvr-kl-drift-health-decision',
      level: 'decision',
      relatedComparison: 'reasoning-gain-vs-reference-policy-drift',
      scenario: 'Two RLVR runs gain the same +6 points on held-out math. Run A has moderate KL drift and unchanged general-chat quality. Run B has 4x the KL drift, frequent language mixing, and a 9-point drop on a general helpfulness suite. Both have similar training reward.',
      prompt: 'Which conclusion is most defensible before continuing training?',
      choices: Object.freeze([
        'Run A is healthier; Run B needs stronger reference/alignment control or a smaller update because equal math gain does not justify much larger behavioral drift and general-capability loss',
        'Run B is healthier because larger KL drift always means the optimizer discovered more reasoning capability',
        'The runs are equivalent because training reward is similar and KL should never affect deployment decisions',
      ]),
      answerIndex: 0,
      explanation: 'KL is a stability guardrail, not a score to maximize. If two runs deliver the same target gain, the one that preserves general behavior with less drift is the better operating point; excessive drift is evidence to adjust regularization or update pressure.',
      misconceptionTested: 'More policy drift is automatically desirable when the target-task reward improves.',
    }),
    Object.freeze({
      id: 'rlvr-orm-canceling-errors-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'outcome-reward-vs-process-reward-credit',
      scenario: 'A generated algebra trace makes an incorrect sign change in step 3 and a second compensating mistake in step 6, ending at the correct numeric answer. An outcome reward model gives the trace full credit because it only sees the final answer.',
      prompt: 'What evaluation or reward addition most directly exposes the brittle reasoning?',
      choices: Object.freeze([
        'Process-level checking or a PRM that scores intermediate steps, because final-answer correctness alone cannot reveal the canceling errors',
        'A larger format bonus, because well-formed tags determine whether the algebra was valid',
        'A stronger length penalty, because shorter traces are guaranteed to contain fewer logical errors',
      ]),
      answerIndex: 0,
      explanation: 'Outcome-only reward cannot distinguish a genuinely valid derivation from one that reaches the answer accidentally. Step-level validation provides evidence about where the trace first becomes invalid and avoids teaching brittle reasoning paths as if they were sound.',
      misconceptionTested: 'A correct final answer proves every intermediate reasoning step was valid.',
    }),
    Object.freeze({
      id: 'rlvr-rollout-contrast-health-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'rollout-count-vs-useful-within-group-contrast',
      scenario: 'A training job doubles each GRPO group from 8 to 16 samples, but 14 of the 16 responses are near-duplicates and all 16 receive almost identical rewards. Rollout cost roughly doubles while effective reward contrast barely changes.',
      prompt: 'What should be tuned instead of assuming the larger group is automatically better?',
      choices: Object.freeze([
        'Sampling diversity, prompt difficulty/data mix, or group construction so extra rollouts create meaningful success/failure contrast rather than duplicate low-information samples',
        'Only the group size upward again, because more samples guarantee useful relative advantages regardless of diversity',
        'Remove verifier scoring because identical rewards prove all responses are equally correct',
      ]),
      answerIndex: 0,
      explanation: 'Group size buys learning signal only when the added samples increase useful variation or discover successes. Near-duplicate completions with almost identical rewards consume rollout compute without supplying stronger relative supervision.',
      misconceptionTested: 'Increasing GRPO group size monotonically improves learning even when the added rollouts are redundant and reward-identical.',
    }),
    Object.freeze({
      id: 'rlvr-rejection-sampling-curation-decision',
      level: 'decision',
      relatedComparison: 'teacher-reward-vs-distillation-data-quality',
      scenario: 'A teacher generates three candidate traces for student SFT. Trace A is correct, concise, parseable, and passes hidden checks. Trace B receives the highest training reward but exploits a parser quirk and fails hidden checks. Trace C is correct but contains a long repetitive loop and unstable formatting.',
      prompt: 'Which candidate should the rejection-sampling pipeline prefer as student training data?',
      choices: Object.freeze([
        'Trace A, because distillation should preserve verified task success and robust output quality rather than blindly copy the teacher sample with the highest exploitable reward',
        'Trace B, because the maximum training reward is sufficient evidence that it is the best reasoning trace',
        'Trace C, because longer traces always teach a student more reasoning than concise traces',
      ]),
      answerIndex: 0,
      explanation: 'Offline curation is a chance to remove reward exploits, brittle parser behavior, and unnecessary loops before they become supervised targets. Student data should be selected for verified correctness, robustness, readability, and contract compliance, not reward alone.',
      misconceptionTested: 'Rejection-sampling SFT should copy the highest-reward teacher output even when independent checks show that reward was gamed.',
    }),
  ]),
});
