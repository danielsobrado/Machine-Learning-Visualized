export const P1_FUNDAMENTAL_SUBSPACES_SCENARIOS_BY_LESSON = Object.freeze({
  'fundamental-subspaces': [
    {
      id: 'subspaces-dimension-budget-worked',
      level: 'calculation',
      relatedComparison: 'rank-vs-nullity-vs-left-nullity',
      scenario: 'A matrix A has shape 120 x 200 and rank 85. A production feature map uses A to send 200-dimensional inputs into a 120-dimensional output space.',
      prompt: 'What are the dimensions of Row(A), Col(A), Null(A), and Null(A^T)?',
      choices: [
        '85, 85, 115, and 35 respectively',
        '85, 120, 115, and 85 respectively',
        '200, 120, 85, and 35 respectively',
      ],
      answerIndex: 0,
      explanation: 'Row(A) and Col(A) both have dimension rank(A) = 85. Rank-nullity on the 200-dimensional domain gives dim Null(A) = 200 - 85 = 115. The output-side budget gives dim Null(A^T) = 120 - 85 = 35.',
      misconceptionTested: 'The four fundamental subspaces all use the same ambient dimension, or row and column spaces have dimensions equal to the matrix width and height rather than the rank.',
    },
    {
      id: 'subspaces-null-family-nonidentifiability-worked',
      level: 'calculation',
      relatedComparison: 'particular-solution-vs-null-family',
      scenario: 'For Ax = b, one solution is x_p = (2, 1, 0). The null space is one-dimensional with basis z = (1, -1, 1). Two teams fit coefficients to the same observations and obtain x_p and x_p + 3z.',
      prompt: 'What is the second coefficient vector, and why can both produce exactly the same fitted output?',
      choices: [
        '(5, -2, 3), because A(x_p + 3z) = Ax_p + 3Az = b when Az = 0',
        '(5, 4, 3), because null-space vectors must be added componentwise using absolute values',
        '(2, 1, 0), because a consistent linear system cannot have two different coefficient vectors',
      ],
      answerIndex: 0,
      explanation: 'x_p + 3z = (2,1,0) + (3,-3,3) = (5,-2,3). Since z is in Null(A), Az = 0, so adding any multiple of z changes coefficients without changing predictions. This is a concrete form of non-identifiability.',
      misconceptionTested: 'Different coefficient vectors must produce different fitted outputs even when their difference lies in the null space.',
    },
    {
      id: 'subspaces-least-squares-residual-worked',
      level: 'calculation',
      relatedComparison: 'column-space-projection-vs-left-null-residual',
      scenario: 'The column space of A is the line spanned by u = (1, 1). A target vector is b = (3, 1). The least-squares fitted output is the orthogonal projection of b onto that line.',
      prompt: 'What fitted output and residual should least squares produce?',
      choices: [
        'Fitted output (2, 2) and residual (1, -1), with the residual orthogonal to Col(A)',
        'Fitted output (3, 1) and residual (0, 0), because least squares always interpolates b',
        'Fitted output (1, 1) and residual (2, 0), because projection always returns the spanning vector unchanged',
      ],
      answerIndex: 0,
      explanation: 'The projection coefficient is (b dot u)/(u dot u) = 4/2 = 2, so the fitted vector is (2,2). The residual is b - fitted = (1,-1), whose dot product with u is zero. The residual therefore lies in the orthogonal complement of Col(A), equivalently the left-null direction for this map.',
      misconceptionTested: 'Least squares must make the residual zero, or projection onto a one-dimensional column space simply returns its basis vector.',
    },
    {
      id: 'subspaces-pivot-column-basis-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'rref-pivot-positions-vs-original-column-basis',
      scenario: 'A team row-reduces a matrix A and finds pivots in columns 1 and 3. It then publishes columns 1 and 3 of RREF(A) as a basis for the original Col(A). The pivot positions are correct, but the row operations materially changed the column vectors.',
      prompt: 'What should be corrected?',
      choices: [
        'Use pivot positions 1 and 3 to select columns 1 and 3 from the original A; row operations preserve row relations but can change the actual column space vectors',
        'Keep the RREF columns because row operations preserve the original column space exactly',
        'Use the free-variable columns from RREF(A), because non-pivot columns are always the independent columns of A',
      ],
      answerIndex: 0,
      explanation: 'RREF is safe for discovering pivot positions and for solving homogeneous systems, but elementary row operations generally change the column space as a subset of R^m. A basis for the original column space should use the corresponding pivot columns from the original matrix.',
      misconceptionTested: 'A basis for Col(A) can always be read directly from the nonzero columns of RREF(A).',
    },
    {
      id: 'subspaces-minimum-norm-row-space-worked',
      level: 'calculation',
      relatedComparison: 'arbitrary-solution-vs-minimum-norm-row-space-solution',
      scenario: 'All exact solutions of Ax = b have the form x(t) = (3, 1) + t(1, -1), where (1, -1) spans Null(A). The particular solution (3,1) is not orthogonal to the null direction.',
      prompt: 'Which value of t gives the minimum-norm exact solution?',
      choices: [
        't = -1, giving x = (2, 2), because the minimum-norm solution is orthogonal to the null direction',
        't = 0, because the first particular solution written down is always minimum norm',
        't = 1, giving x = (4, 0), because concentrating mass in one coordinate minimizes Euclidean norm',
      ],
      answerIndex: 0,
      explanation: 'Minimum norm requires x(t) dot (1,-1) = 0. That gives (3+t) - (1-t) = 2 + 2t = 0, so t = -1 and x = (2,2). Geometrically, the minimum-norm solution removes the unnecessary null-space component and lies in Row(A).',
      misconceptionTested: 'Any particular exact solution is automatically the pseudoinverse/minimum-norm solution, regardless of its null-space component.',
    },
    {
      id: 'subspaces-svd-zero-direction-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'nonzero-singular-directions-vs-null-directions',
      scenario: 'An SVD of a 4 x 6 matrix reports rank 3. Three singular values are nonzero and the remaining singular directions are zero. An engineer claims that every zero-singular direction belongs to the same null space regardless of whether it is a right or left singular vector.',
      prompt: 'What is the correct diagnosis?',
      choices: [
        'Zero-singular right vectors span Null(A), while zero-singular left vectors span Null(A^T); they live on different sides of the map',
        'All zero-singular vectors span Col(A) because zero singular values mean the output is fully reachable',
        'Right and left zero-singular vectors are interchangeable because both always live in R^6',
      ],
      answerIndex: 0,
      explanation: 'Right singular vectors live in the input space and zero singular values identify directions erased by A, so they span Null(A). Left singular vectors live in the output space; the unused left directions span Null(A^T). The two spaces have different ambient dimensions for a rectangular matrix.',
      misconceptionTested: 'Zero singular values identify one undifferentiated null space rather than distinct input-side and output-side null directions.',
    },
  ],
});
