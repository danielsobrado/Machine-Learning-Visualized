export const P1_RESIDUAL_STREAM_SCENARIOS_BY_LESSON = Object.freeze({
  'residual-stream': [
    {
      id: 'residual-write-scale-dominance-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'balanced-residual-write-vs-dominating-branch-update',
      scenario: 'A 48-layer transformer normally enters a late MLP block with residual-state norm near 18 and receives MLP writes with norm near 10. After a configuration change, the same MLP writes have norm around 140, the post-add residual norm jumps above 130, and logits become highly unstable. The tokenizer, data, and attention code are unchanged.',
      prompt: 'What should be investigated first?',
      choices: [
        'Residual-branch scaling, normalization, and initialization because the MLP update is now dominating the shared stream',
        'The tokenizer vocabulary because hidden-state norm growth is primarily controlled by token IDs',
        'The KV-cache eviction policy because cache size directly sets training residual norms',
      ],
      answerIndex: 0,
      explanation: 'Residual addition only preserves a useful running representation when component writes stay on a compatible scale. A branch norm far larger than the incoming residual can swamp earlier features and destabilize later layers, so scaling, normalization placement, and initialization are the first suspects.',
      misconceptionTested: 'Residual addition automatically protects earlier information even when one branch writes at a much larger scale than the state it updates.',
    },
    {
      id: 'residual-width-projection-contract-design',
      level: 'design',
      relatedComparison: 'internal-expansion-width-vs-residual-model-width',
      scenario: 'A transformer has model width d_model = 2048. Its MLP expands each token to width 8192, applies a nonlinearity, and an engineer accidentally wires that 8192-wide activation directly into the residual addition with the 2048-wide stream.',
      prompt: 'What is the correct architectural fix?',
      choices: [
        'Project the MLP result back to width 2048 before the residual add so the branch output matches the residual-stream shape',
        'Pad the 2048-wide residual stream with arbitrary zeros and permanently change the model width to 8192 for that one addition',
        'Remove the residual connection because transformer branches are not required to return the model width',
      ],
      answerIndex: 0,
      explanation: 'Attention and MLP blocks may use larger internal representations, but a residual branch must return to the model width before elementwise addition. The output projection is part of the architecture contract, not an optional formatting step.',
      misconceptionTested: 'Any internal transformer activation can be added directly to the residual stream regardless of width as long as it came from the same token.',
    },
    {
      id: 'residual-prenorm-checkpoint-mismatch-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'checkpoint-matched-pre-norm-vs-refactored-post-norm',
      scenario: 'A 96-layer checkpoint was trained with pre-norm blocks: each sublayer reads LayerNorm(x) and its output is added back to x. A serving refactor changes the block to add first and normalize afterward without retraining. Weights load successfully, but perplexity and hidden-state statistics degrade sharply from the first few layers.',
      prompt: 'What is the most likely explanation?',
      choices: [
        'Normalization placement is part of the learned residual computation, so changing pre-norm to post-norm makes the serving architecture incompatible with the checkpoint',
        'The checkpoint must be corrupt because normalization placement cannot affect a residual network once the weights load',
        'The tokenizer must be re-trained because pre-norm and post-norm differ only in vocabulary handling',
      ],
      answerIndex: 0,
      explanation: 'A checkpoint encodes parameters learned for a specific computation graph. Moving normalization changes the states seen by every attention and MLP block and changes the residual update dynamics, so the same weights no longer implement the trained model.',
      misconceptionTested: 'Pre-norm and post-norm are interchangeable implementation details that can be changed after training without changing model behavior.',
    },
    {
      id: 'residual-patching-causal-evidence-decision',
      level: 'decision',
      relatedComparison: 'decodable-feature-probe-vs-causal-activation-patching',
      scenario: 'A linear probe can decode the correct city name from residual states across layers 10 through 20. In a corrupted prompt the model answers incorrectly. Copying only the clean run residual state at the target token and layer 14 into the corrupted run restores the correct answer on 82% of cases, while patching an unrelated token or a random layer has almost no effect.',
      prompt: 'What is the strongest conclusion supported by this experiment?',
      choices: [
        'The layer-14 target-token residual state makes a causal contribution to the recovered behavior, which is stronger evidence than decodability alone but does not prove a unique human-readable internal algorithm',
        'The probe proves the model literally stores one symbolic city variable in a dedicated neuron and patching only confirms that interpretation',
        'Because the feature is decodable across many layers, the layer-14 state cannot have any causal role in the final answer',
      ],
      answerIndex: 0,
      explanation: 'A probe establishes that information is decodable. Targeted activation patching tests intervention: replacing a specific state and observing behavioral recovery provides causal evidence that the patched state contributes to the computation. It still does not uniquely identify the model\'s full internal algorithm or semantic representation.',
      misconceptionTested: 'Feature decodability and causal necessity are the same claim, or a successful patch uniquely reveals the model\'s complete internal reasoning representation.',
    },
    {
      id: 'residual-feature-interference-worked',
      level: 'calculation',
      relatedComparison: 'additive-accumulation-vs-opposing-feature-write',
      scenario: 'Let u be a unit direction associated with one diagnostic feature. Before a block, the residual stream has projection +3.5 onto u. The attention branch adds a write with projection +1.0 onto u, then the MLP branch adds a write with projection -5.0 onto u.',
      prompt: 'What is the final projection onto u, and what does it demonstrate?',
      choices: [
        '-0.5; additive residual updates can counteract or erase an earlier feature direction even though the old state remains in the vector sum',
        '+9.5; residual connections force every later write to increase any feature already present',
        '+3.5; residual additions cannot change a feature direction that existed before the block',
      ],
      answerIndex: 0,
      explanation: 'The projection is 3.5 + 1.0 - 5.0 = -0.5. Residual connections provide an identity path, but later vector writes can point against an earlier feature and suppress or reverse its projection.',
      misconceptionTested: 'Because residual connections add rather than replace states, every previously represented feature is guaranteed to remain preserved with the same sign and strength.',
    },
    {
      id: 'residual-vs-kv-cache-serving-design',
      level: 'design',
      relatedComparison: 'current-residual-state-vs-past-key-value-cache',
      scenario: 'An autoregressive serving implementation tries to reduce complexity by treating the residual stream and KV cache as the same object. It stores only past residual vectors, discards the per-layer projected keys and values, and then repeatedly recomputes attention projections for all past tokens on every decode step.',
      prompt: 'Which design best matches the roles of these two states?',
      choices: [
        'Keep the current token residual state flowing through each layer, and cache the required past per-layer key/value projections separately so old attention work is not recomputed every step',
        'Keep only one global residual vector for the entire sequence because the KV cache is just another name for that vector',
        'Discard both residual states and cached keys/values after each token because autoregressive attention never needs prior-token information',
      ],
      answerIndex: 0,
      explanation: 'The residual stream is the evolving hidden representation used by the active forward computation. The KV cache stores projected keys and values from past positions for reuse during later attention. Confusing the two either loses required state or forces expensive recomputation.',
      misconceptionTested: 'Residual-stream activations and the KV cache are interchangeable forms of the same serving memory and can be substituted without changing computation or cost.',
    },
  ],
});
