export const P1_OMNI_MULTIMODAL_ARCHITECTURES_SCENARIOS_BY_LESSON = Object.freeze({
  'omni-multimodal-architectures': Object.freeze([
    Object.freeze({
      id: 'omni-projector-semantic-alignment-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'dimension-compatible-projector-vs-semantically-aligned-modality-bridge',
      scenario: 'A vision encoder outputs 1,024-dimensional features and a projector correctly maps them to the language model 4,096-dimensional hidden size. Shapes and training loss are valid, but image-question accuracy stays near the text-only baseline and shuffling image features barely changes answers.',
      prompt: 'What should be investigated before increasing the language-model size?',
      choices: Object.freeze([
        'Whether the projector and multimodal training actually align visual semantics with the language-model representation, because matching dimensions alone does not prove the model uses image evidence',
        'Only the projector output dimension, because a 4,096-dimensional tensor guarantees semantic alignment with a 4,096-dimensional language hidden state',
        'Only the tokenizer vocabulary, because image-feature shuffling cannot reveal whether the visual bridge is being used',
      ]),
      answerIndex: 0,
      explanation: 'A projector solves the interface shape problem, but semantic alignment has to be learned. Near text-only performance plus invariance to shuffled image features suggests the visual pathway is not contributing useful information even though tensor dimensions are valid.',
      misconceptionTested: 'A dimensionally compatible projector automatically creates meaningful vision-language alignment without task-relevant multimodal training or evidence-use checks.',
    }),
    Object.freeze({
      id: 'omni-modality-neglect-contradiction-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'modality-presence-vs-causal-evidence-use',
      scenario: 'A support assistant receives a screenshot showing status = FAILED and a spoken user statement saying the job succeeded. On 200 contradiction cases it follows the spoken statement 94% of the time, including cases where the screenshot is the authoritative system state. On text-only and audio-only benchmarks its scores remain high.',
      prompt: 'What failure does this evaluation expose?',
      choices: Object.freeze([
        'Modality neglect or dominance: the system supports both streams but is not using the visual evidence appropriately when modalities disagree',
        'Successful multimodal fusion, because high unimodal scores prove every active modality influences the final answer equally',
        'A temporal-position bug only, because any disagreement between screenshot and speech must come from clock skew',
      ]),
      answerIndex: 0,
      explanation: 'Multimodal capability is not demonstrated by separate unimodal benchmarks. Contradiction tests reveal whether the model actually consults the modality that should control the decision. Consistently following speech over authoritative screenshot state is evidence of modality dominance or neglect.',
      misconceptionTested: 'Supporting multiple input modalities and scoring well on each separately proves the model combines them correctly when evidence conflicts.',
    }),
    Object.freeze({
      id: 'omni-grounding-region-audit-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'fluent-visual-answer-vs-region-grounded-claim',
      scenario: 'A chart-QA model answers 88% of numeric questions correctly, but for 40% of those correct answers its cited bounding box does not overlap the bar, legend, or axis region needed to justify the number. Removing the cited region from the image often leaves the answer unchanged.',
      prompt: 'What should the team conclude?',
      choices: Object.freeze([
        'Answer accuracy is masking weak grounding; region-level evidence and perturbation tests should be treated as separate correctness criteria',
        'Grounding is already validated because a correct numeric answer makes any cited box acceptable',
        'Bounding boxes should be removed from evaluation because visual grounding cannot be tested independently from final-answer accuracy',
      ]),
      answerIndex: 0,
      explanation: 'A correct answer can come from shortcuts, priors, or unrelated visual cues. Wrong cited regions and weak sensitivity to removing the claimed evidence show that the answer is not reliably grounded in the stated source region.',
      misconceptionTested: 'If the final multimodal answer is correct, its claimed visual evidence is necessarily correct and causally relevant.',
    }),
    Object.freeze({
      id: 'omni-multimodal-token-budget-worked',
      level: 'calculation',
      relatedComparison: 'per-modality-token-rate-vs-shared-context-pressure',
      scenario: 'A 10-second request contains 80 text tokens, video sampled at 2 frames per second with 196 visual tokens per frame, and audio encoded at 25 tokens per second. Ignore special tokens and assume all streams are placed into one shared context.',
      prompt: 'How many input tokens come from each modality and what is the total before generation?',
      choices: Object.freeze([
        'Text 80, video 3,920, audio 250; total 4,250 tokens, with video dominating the shared budget',
        'Text 80, video 392, audio 2,500; total 2,972 tokens, because frame count equals seconds rather than sampling rate times seconds',
        'Text 80, video 196, audio 25; total 301 tokens, because modality token rates are charged only once per request',
      ]),
      answerIndex: 0,
      explanation: 'Ten seconds at two frames per second gives 20 frames. At 196 tokens each, video contributes 3,920 tokens. Audio contributes 10 × 25 = 250 tokens, and text contributes 80, for 4,250 total. This is why modality-level accounting matters even when the text prompt is small.',
      misconceptionTested: 'A short multimodal request has a small context footprint because only text-token count meaningfully contributes to the shared model budget.',
    }),
    Object.freeze({
      id: 'omni-fusion-depth-cost-decision',
      level: 'decision',
      relatedComparison: 'early-fusion-depth-vs-cross-attention-selectivity-vs-late-fusion-efficiency',
      scenario: 'A product compares two tasks. Task A classifies an image and an audio clip independently, then combines the two labels. Task B must decide whether a spoken instruction refers to the object being manipulated in a specific video frame, requiring word-level, spatial, and temporal interaction. GPU budget is limited.',
      prompt: 'Which fusion strategy choice is most defensible?',
      choices: Object.freeze([
        'Use a modular late-fusion path for Task A and spend deeper early or selective cross-attention interaction on Task B, because fusion depth should match the evidence interaction required',
        'Use maximum early fusion for both tasks because deeper interaction is always worth its shared-attention cost even when modalities can be solved independently',
        'Use late fusion for both tasks because any separately encoded modality summary preserves all fine-grained word-to-frame relationships automatically',
      ]),
      answerIndex: 0,
      explanation: 'Task A does not require fine-grained token interaction, so late fusion can save compute. Task B does require cross-modal temporal and spatial binding, so deeper fusion or selective cross-attention is justified despite higher cost. One fusion pattern should not be applied blindly to every workload.',
      misconceptionTested: 'The most interactive fusion architecture is universally best, or separately summarized modalities always preserve fine-grained cross-modal relationships.',
    }),
    Object.freeze({
      id: 'omni-first-audio-latency-worked',
      level: 'calculation',
      relatedComparison: 'model-first-packet-latency-vs-end-to-end-interactive-latency',
      scenario: 'A streaming voice assistant spends 45 ms buffering input audio, 70 ms on network transit and gateway work, 95 ms in multimodal reasoning before the Talker can start, 55 ms producing the first codec packet, and 35 ms in safety/playback buffering. Assume these stages are serial for this simplified calculation.',
      prompt: 'What end-to-end first-audio latency does the user experience, and what does that imply for optimization?',
      choices: Object.freeze([
        '300 ms; the product must optimize the whole serial pipeline rather than quoting only the 55 ms Talker packet-generation time',
        '55 ms; first-packet latency is defined only by the audio decoder and excludes upstream and downstream stages',
        '205 ms; network and safety buffering should be excluded because they are not neural-model computation',
      ]),
      answerIndex: 0,
      explanation: 'The serial stages sum to 45 + 70 + 95 + 55 + 35 = 300 ms. User-perceived responsiveness depends on the full input-to-playback path, so optimizing or advertising only model-side packet generation can hide the real latency bottleneck.',
      misconceptionTested: 'A low model-side audio packet latency is equivalent to low end-to-end conversational latency regardless of buffering, networking, reasoning, safety, and playback stages.',
    }),
  ]),
});
