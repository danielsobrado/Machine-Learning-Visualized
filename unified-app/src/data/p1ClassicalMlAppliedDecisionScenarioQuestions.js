export const P1_CLASSICAL_ML_APPLIED_DECISION_SCENARIOS_BY_LESSON = Object.freeze({
  calibration: [
    {
      id: 'calibration-reliability-gap-worked',
      level: 'calculation',
      relatedComparison: 'predicted-probability-vs-observed-frequency',
      scenario: 'A reliability bin contains 200 predictions whose average predicted probability is 0.80. Exactly 120 of those cases are actually positive.',
      prompt: 'What calibration gap does this bin show, and in which direction?',
      choices: ['Observed frequency is 60%, so the model is overconfident by 20 percentage points', 'Observed frequency is 60%, so the model is underconfident by 20 percentage points', 'Observed frequency is 80%, so this bin is perfectly calibrated'],
      answerIndex: 0,
      explanation: 'The observed frequency is 120 / 200 = 0.60. The model predicts 0.80 on average, so predicted probability exceeds observed frequency by 0.20, or 20 percentage points. This bin is therefore overconfident.',
      misconceptionTested: 'High predicted probabilities are trustworthy merely because the classifier separates classes well.',
    },
  ],
  'train-validation-test-split': [
    {
      id: 'split-entity-generalization-worked',
      level: 'decision',
      relatedComparison: 'row-random-vs-grouped-entity-split',
      scenario: 'A churn dataset contains many monthly rows per customer. A random row split scores 94% accuracy because the same customers appear in train and validation. A customer-grouped holdout scores 71%. Production must predict churn for customers not present in training.',
      prompt: 'Which validation result is the more relevant estimate for the stated deployment target?',
      choices: ['The 71% grouped holdout because it preserves the unseen-customer boundary required in production', 'The 94% random-row split because its larger score proves it uses more information efficiently', 'The average of 94% and 71% because combining biased and deployment-matched estimates removes leakage'],
      answerIndex: 0,
      explanation: 'The evaluation split should reproduce the independence boundary that matters at deployment. Randomly splitting repeated rows lets customer-specific information cross the boundary and can inflate performance. The grouped holdout better estimates generalization to unseen customers.',
      misconceptionTested: 'A random row split is valid whenever rows were sampled independently from a table, even when entities repeat across rows.',
    },
  ],
  'bias-variance-tradeoff': [
    {
      id: 'bias-variance-learning-curve-worked',
      level: 'diagnosis',
      relatedComparison: 'small-data-gap-vs-large-data-convergence',
      scenario: 'A model has train/validation error of 4%/18% with 1,000 examples, 7%/11% with 10,000 examples, and 8%/9% with 50,000 examples. Model family and preprocessing stay fixed.',
      prompt: 'What does the learning-curve pattern most strongly suggest?',
      choices: ['Variance was a major problem at small sample sizes because the train-validation gap shrinks substantially as more data arrives', 'Bias is the dominant problem because training error must decrease whenever sample size grows', 'The model is becoming more overfit because training error rises from 4% to 8%'],
      answerIndex: 0,
      explanation: 'The initial 14-point train-validation gap falls to 1 point as the sample grows, while validation error improves sharply. That pattern is consistent with a variance-limited regime becoming more stable with additional data; the slight rise in training error is normal as memorizing a larger sample becomes harder.',
      misconceptionTested: 'Any increase in training error as the dataset grows means generalization is worsening or bias must be the dominant issue.',
    },
  ],
  regularization: [
    {
      id: 'regularization-validation-curve-worked',
      level: 'decision',
      relatedComparison: 'under-regularized-vs-balanced-vs-over-regularized',
      scenario: 'For the same model and split, regularization strength λ=0 gives train loss 0.8 and validation loss 1.7; λ=1 gives 1.0 and 1.2; λ=100 gives 2.3 and 2.4. Lower loss is better.',
      prompt: 'Which setting is the strongest candidate from these validation results?',
      choices: ['λ=1 because it gives the lowest validation loss while reducing the large train-validation gap', 'λ=0 because the lowest training loss must always determine the regularization strength', 'λ=100 because the strongest regularization is safest whenever overfitting is possible'],
      answerIndex: 0,
      explanation: 'λ=1 has the best validation loss at 1.2 and materially narrows the generalization gap. λ=0 is under-regularized for this split, while λ=100 raises both losses substantially and is consistent with excessive constraint. Selection should still be confirmed with the project’s validation protocol.',
      misconceptionTested: 'Regularization should be chosen by minimizing training loss or by always preferring the strongest penalty.',
    },
  ],
  'knn-naive-bayes-svm': [
    {
      id: 'classifier-family-latency-tradeoff-worked',
      level: 'decision',
      relatedComparison: 'validation-quality-vs-serving-latency',
      scenario: 'Three validated text classifiers have F1 / p95 latency of: linear SVM 0.842 / 2 ms, RBF SVM 0.849 / 17 ms, and kNN 0.846 / 45 ms. The production service has a hard p95 latency SLO of 5 ms and no second-stage reranker.',
      prompt: 'Which model is the defensible production choice from the supplied evidence?',
      choices: ['The linear SVM because it satisfies the hard latency SLO with only a small measured F1 trade-off', 'The RBF SVM because the highest offline F1 should override a hard serving constraint', 'kNN because storing the training examples makes its 45 ms latency irrelevant to model selection'],
      answerIndex: 0,
      explanation: 'Only the linear SVM satisfies the stated 5 ms p95 SLO. The RBF SVM gains 0.007 F1 but violates the serving requirement by more than 3×, while kNN is slower still. Model selection must optimize the deployable system rather than a single offline metric.',
      misconceptionTested: 'The classifier with the numerically best offline metric is automatically the best production model regardless of serving constraints.',
    },
  ],
  'time-series-forecasting-track': [
    {
      id: 'tsf-point-in-time-rolling-worked',
      level: 'calculation',
      relatedComparison: 'shifted-history-only-rolling-feature-vs-target-inclusive-leakage',
      scenario: 'A daily-demand model predicts demand for Thursday morning. The three completed daily demands available before the Thursday cutoff are Monday = 90, Tuesday = 110, and Wednesday = 100 units. Thursday actual demand later turns out to be 130. The feature specification says to use the mean of the previous three completed days.',
      prompt: 'What rolling-mean feature value is valid at the Thursday prediction cutoff, and why would using Thursday actual demand in that feature be invalid?',
      choices: ['100 units, because (90 + 110 + 100) / 3 uses only completed history; including Thursday actual demand would cross the prediction-time information boundary', '110 units, because (100 + 100 + 130) / 3 should include the target day whenever the target eventually exists in the training table', '107.5 units, because all four observed values should be averaged so the feature uses as much data as possible'],
      answerIndex: 0,
      explanation: 'The valid feature is (90 + 110 + 100) / 3 = 100. At prediction time, Thursday demand has not happened yet, so it cannot contribute to a deployable feature. A training pipeline that computes an unshifted rolling statistic after the full dataset is known can silently include the target period and produce optimistic backtests that serving can never reproduce.',
      misconceptionTested: 'A rolling statistic is automatically time-safe merely because it is calculated from the target column, even when the current target period is included after the fact.',
    },
    {
      id: 'tsf-rolling-origin-backtest-diagnosis',
      level: 'diagnosis',
      relatedComparison: 'single-cutoff-win-vs-multi-cutoff-rolling-origin-stability',
      scenario: 'A candidate demand model is compared with a seasonal-naive baseline across four rolling-origin cutoffs using the same seven-day horizon. Candidate MAE by cutoff is 7, 9, 18, and 22 units; baseline MAE is 9, 10, 11, and 12. The candidate therefore wins the first two historical launches but degrades sharply in the later regime.',
      prompt: 'What should the team conclude from this backtest pattern before selecting the candidate for production?',
      choices: ['The candidate is temporally unstable and worse overall in the later regime, so the team should investigate drift/window choice and not promote it based on the early cutoff wins', 'The candidate should ship because winning any two cutoffs proves it generalizes better than the seasonal baseline', 'The four cutoffs should be shuffled together and averaged with training rows because chronological instability disappears under random validation'],
      answerIndex: 0,
      explanation: 'The candidate average MAE is 14 units, while the baseline average is 10.5, and the candidate errors worsen from 7 and 9 to 18 and 22. More importantly, the cutoff pattern exposes a regime-dependent failure that one favorable split would hide. Rolling-origin evaluation is valuable precisely because it shows both aggregate performance and temporal stability across historical launch conditions.',
      misconceptionTested: 'A forecasting model is validated once it beats a baseline on one or a few favorable historical cutoffs, even when later rolling-origin results reveal substantial regime instability.',
    },
    {
      id: 'tsf-interval-coverage-worked',
      level: 'calculation',
      relatedComparison: 'nominal-prediction-interval-coverage-vs-empirical-backtest-coverage',
      scenario: 'A forecasting service publishes nominal 80% prediction intervals. On a frozen backtest containing 20 future observations, only 13 actual values fall inside their corresponding intervals. The interval construction method and cutoff protocol are unchanged across those 20 cases.',
      prompt: 'What empirical coverage did the intervals achieve, and what does that say about their calibration on this backtest?',
      choices: ['65% empirical coverage, which is 15 percentage points below the nominal 80% target and indicates undercoverage on this backtest', '80% empirical coverage, because the nominal confidence level defines the observed fraction regardless of backtest outcomes', '35% empirical coverage, because the seven misses should be divided by the 20 forecasts and reported as the interval coverage'],
      answerIndex: 0,
      explanation: 'Empirical coverage is 13 / 20 = 0.65, or 65%. The nominal target is 80%, so this backtest undercovers by 15 percentage points. With only 20 observations the estimate is noisy, but the correct diagnostic is still to compare observed inclusion frequency with the promised interval level rather than assuming the label “80% interval” guarantees calibrated uncertainty.',
      misconceptionTested: 'A prediction interval has its advertised coverage by definition, so there is no need to verify empirical inclusion rates on time-respecting held-out forecasts.',
    },
    {
      id: 'tsf-horizon-release-selection',
      level: 'decision',
      relatedComparison: 'best-one-step-score-vs-deployment-horizon-matched-model-selection',
      scenario: 'A replenishment system places orders four weeks ahead. On identical rolling cutoffs, Model A has MAE 5 at horizon 1 week and MAE 18 at horizon 4 weeks. Model B has MAE 7 at horizon 1 week and MAE 10 at horizon 4 weeks. Both meet latency and cost constraints, and the release metric is four-week MAE because that is when orders become irreversible.',
      prompt: 'Which model should be selected under the stated deployment objective, and what forecasting principle determines that choice?',
      choices: ['Model B, because model selection must match the operational four-week horizon where B has MAE 10 versus A at 18', 'Model A, because the best one-step forecast is automatically the best model for every longer deployment horizon', 'Average the one-week and four-week MAEs for each model because horizon-specific business timing should never influence release criteria'],
      answerIndex: 0,
      explanation: 'The decision is made four weeks ahead, so four-week error is the relevant validated release metric. Model B is worse one week ahead but materially better at the horizon where replenishment becomes committed: MAE 10 versus 18. Forecast performance can change with horizon, especially for recursive or differently trained multi-step strategies, so one-step leadership is not transferable evidence by default.',
      misconceptionTested: 'The model with the strongest shortest-horizon score should always be deployed, even when the real decision is made at a longer horizon where another model performs substantially better.',
    },
  ],
  'recommender-systems-ranking-track': [
    {
      id: 'rec-mf-latent-sgd-worked',
      level: 'calculation',
      relatedComparison: 'static-embeddings-vs-error-driven-latent-factor-learning',
      scenario: 'A matrix-factorization recommender has user vector u = [1, 2] and item vector v = [1, 0.5]. The observed rating is r = 4, so the current prediction is u·v = 2 and the residual e = r - u·v = 2. For this exercise, hold u fixed, ignore regularization, and update only v with learning rate 0.1 using v_new = v + learning_rate * e * u.',
      prompt: 'What item vector results after this one SGD update, and what learning behavior does the step illustrate?',
      choices: ['v becomes [1.2, 0.9], moving the latent factors in the direction that reduces the observed reconstruction error', 'v becomes [0.8, 0.1], because a positive residual requires subtracting the user vector from the item vector', 'v stays [1, 0.5], because matrix-factorization embeddings are fixed lookup vectors once their dimensionality is chosen'],
      answerIndex: 0,
      explanation: 'The update increment is 0.1 * 2 * [1, 2] = [0.2, 0.4], so v_new = [1, 0.5] + [0.2, 0.4] = [1.2, 0.9]. Matrix factorization learns latent user/item coordinates from interaction error; the vectors are parameters optimized to reconstruct observed preferences rather than static semantic labels.',
      misconceptionTested: 'Matrix factorization merely stores fixed user and item embeddings and does not learn their coordinates from reconstruction error.',
    },
    {
      id: 'rec-offpolicy-ips-worked',
      level: 'calculation',
      relatedComparison: 'naive-logged-reward-vs-inverse-propensity-off-policy-estimate',
      scenario: 'A logged recommendation sample has three impressions. A has reward 1, logging propensity 0.50, and target-policy propensity 0.25. B has reward 0, logging propensity 0.25, and target propensity 0.50. C has reward 1, logging propensity 0.25, and target propensity 0.25. Use the simple inverse-propensity estimate mean[r * target_propensity / logging_propensity].',
      prompt: 'What IPS estimate follows from these three logged impressions, and why is it different from simply averaging the observed rewards?',
      choices: ['0.50, because the weighted reward contributions are 0.50, 0, and 1.00 before dividing their 1.50 sum by 3', '0.67, because off-policy evaluation should always use the unweighted logged reward mean 2/3 regardless of how exposure was generated', '1.50, because inverse-propensity evaluation sums weighted rewards but never normalizes by the number of logged impressions'],
      answerIndex: 0,
      explanation: 'The importance ratios are 0.25/0.50 = 0.5, 0.50/0.25 = 2, and 0.25/0.25 = 1. Multiplying by rewards gives 0.5, 0, and 1, so the specified IPS mean is 1.5 / 3 = 0.5. The reweighting corrects for the fact that the logging policy exposed actions at probabilities different from the target policy.',
      misconceptionTested: 'Logged recommendation rewards can be averaged as though items were exposed independently of the policy that chose what users were allowed to see.',
    },
    {
      id: 'rec-exploration-budget-worked',
      level: 'calculation',
      relatedComparison: 'pure-exploitation-vs-randomized-exploration-data-acquisition',
      scenario: 'A recommender serves 20,000 impressions per day. The team reserves 5% for randomized exploration among 50 eligible cold-start or long-tail items, sampling those 50 items uniformly within the exploration bucket. Ignore repeat-user effects and treat the counts as expectations for this planning exercise.',
      prompt: 'How much exploratory traffic is available per day, and how many exploratory impressions should each eligible item receive on average?',
      choices: ['1,000 exploration impressions per day, or about 20 expected exploratory impressions per eligible item', '10,000 exploration impressions per day, or 200 per item, because 5% means one half of traffic is exploratory', '1,000 exploration impressions per day, but all 1,000 should go to the currently most popular item to maximize information about long-tail items'],
      answerIndex: 0,
      explanation: 'Five percent of 20,000 is 1,000 exploratory impressions. Uniform allocation across 50 eligible items gives an expectation of 1,000 / 50 = 20 per item. Controlled exploration deliberately spends some traffic to collect evidence on items that a purely exploitative popularity loop might rarely expose.',
      misconceptionTested: 'A recommender can learn reliably about cold or long-tail items while always exploiting current popularity and never allocating traffic to deliberate exploration.',
    },
    {
      id: 'rec-offline-online-regression-decision',
      level: 'decision',
      relatedComparison: 'offline-ranking-gain-vs-online-user-and-catalog-outcomes',
      scenario: 'Two recommender candidates pass correctness checks. Candidate A improves offline nDCG by 8% with unchanged recall, but a randomized online test shows CTR -2%, conversion -4%, and catalog coverage -20%. Candidate B improves offline nDCG by 3%, while online CTR is +1%, conversion +2%, and catalog coverage +5%. The release policy requires non-negative conversion and catalog coverage guardrails.',
      prompt: 'Which candidate should be preferred under the stated release policy, and what does the comparison teach about recommender evaluation?',
      choices: ['Candidate B, because it satisfies the online business and coverage guardrails even though its offline nDCG gain is smaller', 'Candidate A, because the largest offline nDCG improvement must dominate randomized online outcomes by definition', 'Candidate A, because catalog coverage is unrelated to exposure feedback and therefore cannot be a recommender release guardrail'],
      answerIndex: 0,
      explanation: 'Candidate A wins the offline ranking metric but violates both stated online guardrails, while Candidate B improves the online outcomes the release policy requires. Offline metrics are necessary diagnostics, not substitutes for randomized user-impact evaluation: exposure, novelty, feedback loops, and behavior changes can reverse an apparent offline ranking advantage.',
      misconceptionTested: 'A larger offline ranking metric improvement is sufficient evidence to ship a recommender even when randomized online outcomes and exposure-diversity guardrails regress.',
    },
  ],
});

export function getP1ClassicalMlAppliedDecisionScenariosForLesson(lessonId) {
  return P1_CLASSICAL_ML_APPLIED_DECISION_SCENARIOS_BY_LESSON[lessonId] || [];
}
