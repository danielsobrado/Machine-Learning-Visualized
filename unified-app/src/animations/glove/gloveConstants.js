export const GLOVE_WEIGHTING_DEFAULTS = Object.freeze({
  xMax: 100,
  alpha: 0.75,
  residual: 2,
});

export const GLOVE_COUNT_OPTIONS = Object.freeze([0, 1, 10, 100, 1000, 10000]);

export const GLOVE_RESIDUAL_LIMITS = Object.freeze({
  min: -4,
  max: 4,
  step: 0.25,
});
